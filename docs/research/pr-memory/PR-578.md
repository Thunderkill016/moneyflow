# PR #578 — Public Vietnamese bank-statement regression hardening

- **Scope:** Synthetic regressions modeled on publicly visible Vietcombank, ACB and VietinBank eFAST statement structures; no real statement contents or PII are committed.
- **Risk:** Class 3 import/data-integrity follow-up. No migration, schema/RLS, provider configuration, production data, bank API or bank-system write.
- **Implemented:** Prefer explicit transaction date over effective/accounting date; handle VCB standalone `+/-` direction next to unsigned amount; prevent reference hyphens and `STT/ No` from being misclassified as financial direction/roles; cover observed fee-row and bilingual-header shapes; record evidence levels without enabling bank-specific auto-map.
- **Overlap evidence:** Added `supabase/tests/database/import_overlap_fingerprint.test.sql` proving the same normalized XLSX observation arriving in two different import batches and at different worksheet row positions is classified `duplicate` with reason `fingerprint_transaction_match`; default approval is blocked and only an explicit heuristic-duplicate override may proceed.
- **Observed VietinBank identity evidence:** Two public eFAST statements for the same account cover `31/03/2026` in overlapping windows and preserve `Transaction number` values `1942`, `1943`, and `1944` with matching transaction date/time, amounts and descriptions. Compatibility therefore records `source-stable` at evidence level `observed-but-unverified`, not `confirmed`.
- **Identity boundary:** `sourceExternalId` remains disabled for that public evidence because canonical persistence requires both `evidence=confirmed` and `stability=source-stable`; no public sample is promoted into a provider-guaranteed identity contract.
- **Residual unknowns:** Exact current downloaded-file contract per bank; provider-guaranteed fee semantics; whether VietinBank transaction-number stability is guaranteed across all accounts/export modes/time; overlap cases where the bank materially changes normalized date/amount/description/reference formatting between exports; raw public Google Drive bytes could not be downloaded in the current runtime.
- **Verified:** Previous exact head `8bdd2df9cb4dddd24f6aea2a3b675f9fe738e26f` — CI #3604 PASS, CodeQL #2607 PASS, Secret history scan #2607 PASS. Fresh exact-head verification is required after the compatibility evidence update.
- **Merge:** Owner-controlled; not merged by this verification record.
