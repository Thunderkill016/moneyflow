# PR #578 — Public Vietnamese bank-statement regression hardening

- **Scope:** Synthetic regressions modeled on publicly visible Vietcombank, ACB and VietinBank eFAST statement structures; no real statement contents or PII are committed.
- **Risk:** Class 3 import/data-integrity follow-up. No migration, schema/RLS, provider configuration, production data, bank API or bank-system write.
- **Implemented:** Prefer explicit transaction date over effective/accounting date; handle VCB standalone `+/-` direction next to unsigned amount; prevent reference hyphens and `STT/ No` from being misclassified as financial direction/roles; cover observed fee-row and bilingual-header shapes.
- **Overlap evidence:** Added `supabase/tests/database/import_overlap_fingerprint.test.sql` proving the same normalized XLSX observation arriving in two different import batches and at different worksheet row positions is classified `duplicate` with reason `fingerprint_transaction_match`; default approval is blocked and only an explicit heuristic-duplicate override may proceed.
- **Identity boundary:** Public/official material confirms reference fields and export/history capability but does not guarantee reference uniqueness or stability across downloads. Bank-specific `source_external_id` remains disabled.
- **Residual unknowns:** Exact current downloaded-file contract per bank; provider-guaranteed fee semantics; reference stability; overlap cases where the bank materially changes normalized date/amount/description/reference formatting between exports; raw public Google Drive bytes could not be downloaded in the current runtime.
- **Verified:** Exact head `5d134d0fc3009dc4b7d2c0af6ba2e3525d039927` — CI #3603 PASS including fresh reset + pgTAP (42 files / 789 tests), archive producer/restore round trips, lint, typecheck, unit/static RLS, production build, browser smoke, authenticated ownership, aggregate `verify` and `e2e`; CodeQL #2606 PASS; Secret history scan #2606 PASS. Cross-device UI audit skipped by policy because no UI diff.
- **Merge:** Owner-controlled; not merged by this verification record.
