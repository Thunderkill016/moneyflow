# PR #579 — Source observation boundary

- Date: 2026-09-11
- Change class: Class 3 — import/data-integrity architecture follow-up
- Affected capability or project boundary: source acquisition normalization between evidence-aware source adapters and the existing import/reconciliation candidate pipeline.
- Status impact: PR #578 was squash-merged to `main` at `4d9372110b6446a9ee339912da3be31152ca832d`; this PR carries only the source-observation boundary delta on top of that merged state. It does not promote any bank to provider-confirmed support, enable bank-specific auto-map, or relax source-identity persistence rules.
- Changed: added the transport-neutral `SourceObservation -> CanonicalSourceCandidate` boundary; separated row/sheet locator provenance from provider transaction identity; reused strict date/amount normalization; required `confirmed + source-stable` evidence before `sourceExternalId`; made ambiguous financial semantics fail closed; rejected `removed` lifecycle observations without stable identity; updated the existing bridge to consume the canonical candidate role; added boundary regressions.
- Verified: the pre-rebuild exact head `d285a60df165ed5a7775c4e08637365b6b2717fa` passed CI #3612, CodeQL #2615, Secret history #2615, policy/knowledge, lint/typecheck, unit/static RLS, fresh DB/pgTAP, archive round trips, production build, browser/authenticated ownership, `verify` and `e2e`. Because #578 was squash-merged, #579 was rebuilt from merged `main`; fresh exact-head verification of the clean rebuilt head is required before merge.
- Remaining: fresh exact-head verification of the clean main-based PR; GitHub #576 / Plate THU-46 still need real/privacy-scrubbed VCB/ACB overlap evidence, exact provider file-contract evidence, fee semantics, and broader identity-stability evidence before stronger bank-specific automation claims.
- Production/provider evidence: not applicable — no schema/RLS migration, provider configuration, production-data write, bank-system write, or bank API integration is included.
- Superseded issue, roadmap or claim: none; this refines the acquisition architecture under #576 without replacing the bank-export pilot or #578 evidence boundary.
- Base/rebuild note: the original stacked branch was rebuilt from merged `main` after #578 squash-merge so PR #579 does not replay #578 history.
- Merge: owner-controlled and requires green exact-head machine evidence.
