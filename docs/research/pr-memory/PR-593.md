# PR #593 — Activity 2.0 unified maintenance workstream MVP

- Date: 2026-09-13
- Issue: #592
- Parent intent: #559
- Branch: `feat/592-activity-workstream`
- Change class: product UI / application read-workflow surface; no database migration.
- Affected capability or project boundary: daily transaction maintenance across posted Transactions, transaction Review state, pending Inbox candidates and truthful candidate source/provenance; mutation ownership remains `/transactions` and `/inbox`.
- Status impact: Activity R1 is additive and review-ready in PR #593; `Hoạt động` is discoverable under More while primary `Giao dịch` and `Cần xem` remain intact. No R2 primary-nav promotion or legacy-route retirement is included.
- Changed: added typed Activity item model, additive `/activity` route/workspace, existing-read composition, candidate source/lifecycle presentation, knowledge-aware partial states, unit/browser coverage, additive More discoverability, a fail-closed guard excluding non-authoritative fallback ledger rows when finance reports an error, and explicit review-coverage handling when `transaction_review_feed` is unavailable or malformed.
- Remaining: owner review/merge decision only. R2 primary-nav promotion and R3 retirement/optimization require separate authorization. No merge, production/provider write or production deployment is authorized by this handoff.
- Production/provider evidence: none required or performed; no migration, provider configuration, production data write or deployment intervention is part of R1.
- Superseded issue, roadmap or claim: none. #559 remains parent program intent; #592 is the executable bounded slice. Activity does not supersede `/transactions` or `/inbox` in R1.
- Scope: additive Activity R1 only; no legacy route retirement, DB migration, provider write, merge or production change.
- Baseline: `main@77def2218dfb1a66134bbefe8158c993b960ebf6`; base was rechecked unchanged during final evaluation.
- Decision: Activity is a read/workflow layer, not a new financial truth model. Posted transactions remain ledger facts; pending Inbox candidates remain pre-ledger evidence; `needs_review` is an attribute of a posted transaction rather than a separate Activity item.
- Data flow: compose existing `getFinanceWorkspace()` transaction/review read with the existing `loadInboxForClient()` candidate path, then build typed Activity items. Existing `/transactions` and `/inbox` remain mutation owners and rollback surfaces.
- Source-state boundary: candidate source/lifecycle/match evidence may be displayed when present; posted transactions do not fabricate provider/source state because current transaction feed does not prove it.
- Partial-failure boundary: candidate failure keeps healthy ledger rows visible. Finance failure may still show pending candidate evidence, but Activity excludes non-authoritative transaction fallback rows, prevents those rows from participating in candidate duplicate detection, disables readiness inference, and marks dependent counts unknown. If ledger facts are healthy but review state is unavailable, posted rows remain visible while the `Cần xử lý` count becomes unknown, a warning explains incomplete review coverage, and the attention filter is not presented as complete.
- Research: official Copilot Money review, YNAB approve/match, Actual Budget import and rules documentation support a single maintenance workstream while preserving explicit review, matching and user-owned processing semantics. Official Playwright reporter guidance was also rechecked during evaluation so raw job output/artifacts were used to confirm test execution rather than relying only on green job status.
- Verified: source/runtime head `7907f990d9661c5e6a584d254120cc912fc53452` passed CI #3717 (run `34758071170`), CodeQL #2719 and Secret history scan #2719. CI policy, project knowledge, migration identity, static quality, lint, typecheck, production build, unit/static-RLS and aggregate verify passed; the database job correctly skipped database tests because R1 changes no DB/migration/RLS contract. Raw Browser smoke logs prove `148 passed` for the demo/generic suite and `30 passed, 1 skipped` for the authenticated suite. Activity-specific authenticated tests executed and passed on desktop and 320px phone, including More discoverability without replacing Transactions/Inbox, mixed workstream/no duplication, candidate-read failure, review-state failure, ledger-read failure, light/dark presentation, target sizing and overflow checks. The repository-wide Cross-device UI audit also passed; that global audit route matrix does not include `/activity`, so Activity-specific responsive evidence is attributed to the authenticated Activity specs rather than overclaimed from the global audit.
- Rollback: remove additive `/activity` route/model/presentation and the additive More affordance; `/transactions`, `/inbox`, existing mutations and DB contracts remain unchanged.
