# PR #580 — Privacy-safe bank-export evidence report

- Date: 2026-09-11
- Change class: Class 3 — acquisition/evidence tooling follow-up
- Affected capability or project boundary: THU-46 / GitHub #576 real-bank-export evidence collection around the existing XLS/XLSX pilot parser and reconciliation heuristics.
- Status impact: adds local tooling to produce paste-safe structural/aggregate evidence from one or two bank-export workbooks. It does not promote any bank/layout/reference to confirmed support, enable bank-specific auto-map, or change source identity persistence.
- Changed: added a pure privacy-safe evidence report over `XlsxPilotInspection`; added aggregate two-file overlap using the current client heuristic fingerprint only in memory; added a local npm command that never echoes real paths/filenames; added synthetic privacy/overlap/multiplicity/failure tests; added a runbook forbidding raw statement content in durable project records.
- Verified: pending exact-head CI / CodeQL / secret scan. No local-pass claim because the current runtime cannot clone the GitHub repository.
- Remaining: machine verification; then real/privacy-scrubbed VCB/ACB same-account overlapping exports must still be run locally to obtain actual evidence. Exact provider file contracts, VCB/ACB reference stability, official fee semantics, and broader VietinBank identity stability remain unproven.
- Production/provider evidence: not applicable — local read-only file inspection only; no schema/RLS migration, production data write, provider configuration, bank-system write, bank API integration, or UI change.
- Superseded issue, roadmap or claim: none; this reduces evidence-collection friction under #576 / THU-46 without changing acceptance criteria or replacing the canonical acquisition/reconciliation pipeline.
- Privacy boundary: durable output excludes real filenames/paths, raw rows, amounts, descriptions, account identifiers and per-row fingerprints. Two-file overlap is heuristic reconciliation evidence only and cannot become `sourceExternalId`.
- Merge: owner-controlled; the earlier explicit merge authorization covered #578/#579 and is not assumed to authorize #580.
