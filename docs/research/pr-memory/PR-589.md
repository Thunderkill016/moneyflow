# PR #589 — foreground ledger on default dashboard

- Date: 2026-09-12
- Change class: Class 2 — bounded dashboard UI/flow simplification
- Affected capability or project boundary: signed-in `/dashboard`, progressive disclosure of planning, dashboard client hydration, THU-41 / GitHub #426
- Status impact: current `main` still rendered a full `DashboardPlanningColumn` beside the daily ledger even though compact planning links and the attention strip already preserve planning discoverability and exceptions; this PR removes only that duplicate default-dashboard surface
- Changed: removes `DashboardPlanningColumn` from the signed-in home; keeps all planning routes/features, compact planning navigation, budget/commitment attention behavior, capture dialogs and financial read/mutation semantics unchanged; stops passing `goals` and `incomeTemplates` into the dashboard client boundary and removes demo income-template hydration that only served the removed column; updates the stale goal-dashboard contract test and navigation comments to the new progressive-disclosure contract
- Research basis: MoneyFlow product law requires progressive disclosure; GOV.UK design principles support doing less and simplifying from observed current behavior; Next.js official Server/Client Component guidance supports minimizing code sent across the client boundary. External guidance informs direction only; current MoneyFlow code/tests remain authority
- Verified: implementation-head CI #3654 first failed because the mandatory PR memory did not yet exist. After the mirrors landed, CI #3656 correctly found two remaining issues on head `5e6379779e65edb3003220eb38f7d830352b9d50`: missing exact knowledge-field labels and the stale `insights-goals-card` contract that still required goals to hydrate into the dashboard. Both findings were investigated and fixed rather than retried blindly. Fresh exact-head CI/CodeQL/Secret/browser/UI evidence remains required on the final head
- Remaining: exact-head risk-selected CI; browser smoke and cross-device audit; independent diff/evidence review; same-method dashboard script-transfer evidence where the existing harness/preview permits a truthful comparison; owner merge decision
- Financial/data boundary: no dashboard RPC, schema, RLS, Auth, provider, ledger, transfer, budget, commitment or reconciliation semantics change; no production/provider write
- Rollback: restore the removed planning-column render and client props; all underlying planning routes/data remain intact
- Production/provider evidence: no production or provider write was performed. Existing production deployment remains on merged `main`; this PR is validated through repository/preview evidence only until owner merge and normal deployment
- Superseded issue, roadmap or claim: does not replay #426's stale 2026-08 sidebar-removal recipe and supersedes the old test/comment assumption that planning must appear as full dashboard cards. Current code was revalidated first; planning remains secondary and discoverable through the compact planning nav and More surfaces
