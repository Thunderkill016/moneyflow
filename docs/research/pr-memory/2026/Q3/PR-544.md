# PR #544 — reconcile #536 post-merge production truth

- Date: 2026-09-03
- Change class: evidence-only project-memory reconciliation under active packet #536; no production database/Auth/provider/tenant-data mutation
- Affected capability or project boundary: project knowledge, release-state provenance, #536 runtime/database/provider handoff
- Status impact: updates durable memory after PR #540 merged and production runtime verification completed; #536 remains open for production database parity and Auth/provider acceptance
- Changed: `CURRENT_PROJECT_MEMORY.md` now records merged `main@10c832aaaf27a6bf5406578871708789f4b1b14d`, production Next.js 16.3.4, merged-main database CI, the exact 15-migration production gap, verified backup gate, linked CLI dry-run requirement, and current provider blocker
- Verified: Vercel production deployment `dpl_Ch4Vfpdxw8mbUJ5ynjozZRTGLhYw` is READY from `10c832aa...`; `/api/health` reports that full commit; CI #3260 (`33735497335`) on merged main passed the fresh local Supabase reset, all migrations through `20260825090000`, 39 pgTAP files / 747 tests, archive producer/restore round trips, browser/UI and aggregate gates; live source-identity preflight still reports zero conflicts
- Remaining: capture literal linked `supabase db push --dry-run` showing exactly the 15 expected pending versions; only then obtain explicit owner authorization for the production DB/history write; post-write verify history/contracts/tenant/browser/advisor; separately resolve leaked-password protection/provider-plan acceptance
- Production/provider evidence: production runtime is patched; production Supabase migration history still ends at `20260812043219`; private encrypted logical backup is verified and temporary staging removed; Security Advisor still reports the intentional authenticated SECURITY DEFINER warning class plus leaked-password protection disabled; organization remains Free
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` replaces stale pre-merge #540/runtime claims with current production truth
- Superseded issue, roadmap or claim: PR #540 is not open; production is not on `425af450...`/Next 16.2.11; #536 is not complete merely because #540 merged; MCP `apply_migration` is not equivalent to linked CLI push for the existing timestamped migration files
- Lifecycle impact: advances #536 only; does not close/archive it, clear `PLAN_AUTHORITY.current`, select follow-on work, merge this PR, or authorize database/Auth/provider writes

## Runtime and merged-main evidence

PR #540 merged to `main` as `10c832aaaf27a6bf5406578871708789f4b1b14d`. Vercel built and deployed that exact commit to production and detected Next.js 16.3.4. Live `/api/health` also reports the full merged commit.

Merged-main CI #3260 ran the database gate that was skipped on the pre-merge docs-only selection path: a fresh local Supabase database was started/reset, the complete migration chain through `20260825090000_direct_csv_rule_atomic_ingestion` was applied, and pgTAP completed with 39 files / 747 tests successful. Archive producer and restore round trips also passed.

## Production database handoff

Production remote migration history still ends at `20260812043219_remove_atoryn_from_moneyflow_project`. Exact merged-main source contains 15 later versions from `20260821014500` through `20260825090000`; each file was re-read from `main@10c832aa...` and the source-identity data preflight was re-run against live production with zero conflicts.

The pre-write rollback gate is complete: a private off-repository encrypted logical backup was produced, its encrypted SHA-256 matched the server-side source, private decryption verification matched the expected plaintext checksum, verification plaintext was removed, and temporary production backup staging was dropped.

The remaining pre-write evidence is the literal linked CLI preview `supabase db push --dry-run`, which must list exactly those 15 repository migrations in timestamp order. This environment does not expose a linked Supabase CLI credential/DB-password channel, so the preview has not been fabricated from MCP reads.

Do not substitute MCP `apply_migration`: its migration endpoint currently creates a server-side migration timestamp rather than preserving the existing repository version ID. Do not use migration-history repair to hide that mismatch.

## Provider and permission boundary

Fresh Security Advisor still reports leaked-password protection disabled and the authenticated SECURITY DEFINER warning class. The current live warning set remains evidence-dispositioned as intentional tenant-bound privileged API surface. The Supabase organization remains on Free, so leaked-password protection still requires an eligible provider-plan decision.

Production database/history mutation and Supabase plan/Auth configuration remain separate explicit owner-authorized operations. Generic continuation is not authorization.
