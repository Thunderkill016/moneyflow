# PR #544 — reconcile #536 post-merge production truth

- Date: 2026-09-03
- Change class: evidence-only project-memory reconciliation under active packet #536; no production mutation is performed by this PR
- Affected capability or project boundary: project knowledge, release-state provenance, #536 runtime/database/provider closure handoff
- Status impact: records that runtime remediation and the owner-authorized 15-migration production reconciliation are verified; #536 remains selected only until repository lifecycle authority converges
- Changed: `CURRENT_PROJECT_MEMORY.md` now records 56/56 production migration history, 14/14 durable contract verification, zero source-identity conflicts, the expected 43 authenticated-callable SECURITY DEFINER count, fresh runtime smoke, and the owner-accepted Supabase Free leaked-password limitation
- Verified: Vercel production `/api/health` returns HTTP 200 from `main@10c832aaaf27a6bf5406578871708789f4b1b14d`; no production runtime errors were reported in the post-rollout one-hour window; Supabase remote history ends at `20260825090000`; every durable later acquisition/source-lineage contract passes; the seven new privileged RPCs are tenant-bound/authenticated-only with hardened search path; source-identity conflicts remain zero
- Remaining: repository lifecycle closeout must archive the active #536 packet and change `PLAN_AUTHORITY.current` to `null`; merge of any closeout remains owner-controlled; no follow-on work may be selected before that convergence
- Production/provider evidence: the linked CLI push applied the exact 15 repository versions after verified backup, exact dry-run and explicit owner authorization; Security Advisor still reports the intentional authenticated SECURITY DEFINER warning class plus leaked-password protection disabled; owner explicitly accepts the Free-plan limitation for the current M0 closure and authorizes no paid-plan/Auth workaround
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` replaces pre-write database/provider claims with verified post-write production truth
- Superseded issue, roadmap or claim: production no longer ends at `20260812043219`; the 14 later durable contracts are no longer absent; SECURITY DEFINER count is no longer 36; migration repair is not needed; leaked-password protection remains disabled rather than remediated
- Lifecycle impact: advances #536 only; does not close/archive it, clear `PLAN_AUTHORITY.current`, select follow-on work, or merge this PR

## Production database reconciliation evidence

The production operation followed the packet safety sequence: fresh merged-main database verification, private logical backup with checksum/decryption verification, exact linked migration list, exact `db push --dry-run`, explicit owner authorization, then linked CLI `db push`. No seed, linked reset, MCP migration timestamp or migration-history repair was used.

Fresh production migration history on project `fwpldsdkpzhswpuctbke` contains 56 entries and ends at `20260825090000_direct_csv_rule_atomic_ingestion`. The exact 15 repository timestamps from Aug 21–25 are present with no orphan migration IDs.

A corrected live catalog matrix using `to_regprocedure()` verified all 14 durable contracts: batch approval, manual attachment, deleted-source recovery, changed-source observation, approved-evidence immutability/DELETE restriction, owner-preserving import-batch FK, source lineage/replacement observation, source-aware archive producer/restore, archive updated-at owner guard, lifecycle review and lock ordering, Share Target atomic/rule-aware ingestion, and Direct CSV rule-aware preparation.

The source-identity postflight remains zero-conflict for candidate/candidate and candidate/provenance mappings. PostgreSQL migration logs show the expected DDL/function/grant statements executing on the MoneyFlow project without migration-adjacent ERROR/PANIC/FATAL evidence in the inspected window.

## Privilege and provider evidence

Authenticated-callable SECURITY DEFINER count increased from 36 to the repository-expected 43. The seven new privileged RPCs are owned by `postgres`, use empty `search_path`, grant authenticated execute while denying anon/PUBLIC, contain `auth.uid()` plus explicit authentication/tenant binding, and show no dynamic SQL or role/service-role switching pattern. The Share Target ingestion functions remain SECURITY INVOKER.

Fresh Security Advisor therefore still emits the broad authenticated SECURITY DEFINER warning class, but no new evidence-backed ownership defect was found. Do not weaken the financial API merely to silence the advisor.

Leaked-password protection remains disabled because the Supabase organization is on Free. The owner explicitly chose not to upgrade solely for this control and accepts the provider limitation for current M0 closure. This is a documented limitation, not a claim that the control is enabled.

## Runtime and lifecycle boundary

Fresh Vercel health after the database rollout returned HTTP 200 and exact commit `10c832aaaf27a6bf5406578871708789f4b1b14d`; the production project reported no runtime errors in the selected post-rollout window.

This PR remains evidence-only. `PLAN_AUTHORITY.current` on merged main still points at `docs/plans/active/536-security-runtime-auth-hardening.md`; lifecycle completion must move that authority to `null` in a policy-valid repository change before #536/M0 can be represented as fully closed across all control planes.
