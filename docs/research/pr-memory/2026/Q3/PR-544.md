# PR #544 — complete #536 lifecycle after production verification

- Date: 2026-09-03
- Change class: Class 0/1 repository lifecycle and durable-evidence closeout after completed Class 3 #536 production work; this PR performs no production database/Auth/provider mutation
- Affected capability or project boundary: plan authority, active/completed packet lifecycle, project memory, #536/M0 release truth
- Status impact: completes the current #536 executable slice after runtime, production database and owner provider-decision evidence became truthful; leaves current authority `null` and selects no follow-on work
- Changed: archives #536 as `docs/plans/completed/2026-09-03-536-security-runtime-auth-hardening.md`, removes the active packet, changes `PLAN_AUTHORITY.current` to `null`, and reconciles `CURRENT_PROJECT_MEMORY.md` to the verified post-rollout state
- Verified: production remains on exact `main@10c832aaaf27a6bf5406578871708789f4b1b14d` / Next 16.3.4; Supabase remote history is 56/56 through `20260825090000`; 14/14 durable later contracts pass; source-identity conflicts remain zero; authenticated-callable SECURITY DEFINER count is the expected 43 with the seven new privileged RPCs tenant-bound/authenticated-only; Vercel health is HTTP 200 with no runtime errors in the inspected post-rollout hour
- Remaining: exact-head CI/CodeQL/Secret-history and review state for this lifecycle head must be green before owner merge; after merge re-read merged authority, then close #536/M0 and only later select follow-on work from fresh main
- Production/provider evidence: linked CLI applied the exact 15 repository versions after verified private backup, exact dry-run and explicit owner authorization; fresh Security Advisor still reports the intentionally dispositioned authenticated SECURITY DEFINER class plus leaked-password protection disabled; owner explicitly accepts the Supabase Free leaked-password limitation for this M0 closure and authorizes no paid-plan/Auth workaround
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` now records the completed #536 state, current authority `null`, no follow-on selection, and the continuing Free-plan leaked-password limitation
- Superseded issue, roadmap or claim: production no longer ends at `20260812043219`; later acquisition contracts are no longer absent; SECURITY DEFINER count is no longer 36; migration repair is not needed; #536 no longer requires technical production work; leaked-password protection remains disabled rather than remediated
- Lifecycle impact: completes current slice #536; archives its active packet, changes `PLAN_AUTHORITY.current` from #536 to `null`, updates the current project snapshot, and intentionally selects no follow-on work

## Production database reconciliation evidence

The production operation followed the packet safety sequence: fresh merged-main database verification, private logical backup with checksum/decryption verification, exact linked migration list, exact `db push --dry-run`, explicit owner authorization, then linked CLI `db push`. No seed, linked reset, MCP migration timestamp or migration-history repair was used.

Fresh production migration history on project `fwpldsdkpzhswpuctbke` contains 56 entries and ends at `20260825090000_direct_csv_rule_atomic_ingestion`. The exact 15 repository timestamps from Aug 21–25 are present with no orphan migration IDs.

A corrected live catalog matrix using `to_regprocedure()` verified all 14 durable contracts: batch approval, manual attachment, deleted-source recovery, changed-source observation, approved-evidence immutability/DELETE restriction, owner-preserving import-batch FK, source lineage/replacement observation, source-aware archive producer/restore, archive updated-at owner guard, lifecycle review and lock ordering, Share Target atomic/rule-aware ingestion, and Direct CSV rule-aware preparation.

The source-identity postflight remains zero-conflict for candidate/candidate and candidate/provenance mappings. PostgreSQL migration logs show the expected DDL/function/grant statements executing on the MoneyFlow project without migration-adjacent ERROR/PANIC/FATAL evidence in the inspected window.

## Privilege and provider evidence

Authenticated-callable SECURITY DEFINER count increased from 36 to the repository-expected 43. The seven new privileged RPCs are owned by `postgres`, use empty `search_path`, grant authenticated execute while denying anon/PUBLIC, contain `auth.uid()` plus explicit authentication/tenant binding, and show no dynamic SQL or role/service-role switching pattern. The Share Target ingestion functions remain SECURITY INVOKER.

Fresh Security Advisor therefore still emits the broad authenticated SECURITY DEFINER warning class, but no new evidence-backed ownership defect was found. Do not weaken the financial API merely to silence the advisor.

Leaked-password protection remains disabled because the Supabase organization is on Free. The owner explicitly chose not to upgrade solely for this control and accepts the provider limitation for current M0 closure. This is a documented limitation, not a claim that the control is enabled.

## Runtime and lifecycle result

Fresh Vercel health after the database rollout returned HTTP 200 and exact commit `10c832aaaf27a6bf5406578871708789f4b1b14d`; the production project reported no runtime errors in the selected post-rollout window.

This PR is now the repository lifecycle closeout. It does not select M1/#523 or any other follow-on slice. Its merge remains owner-controlled; only merged-main authority may be used to close #536/M0 and start a later selection transition.
