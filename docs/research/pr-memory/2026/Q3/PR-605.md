# PR #605 — capability layer v0: typed read capabilities with explainable basis

- Date: 2026-09-20
- Change class: Class 1 (bounded server-only subsystem, no route/UI/schema change)
- Affected capability or project boundary: `src/server/capabilities/` (new), `docs/agents/`, capability manifest contract in CI
- Status impact: none → candidate (capability seam exists; no UI/API/MCP consumer yet)
- Changed: `ledger.summary`, `transactions.search`, `reports.financial` read capabilities over existing `getFinanceWorkspace`/`getReportsWorkspace` and `src/lib/reports.ts`; zod input/output contracts; deterministic `basis` per amount; `runCapability` registry with `CapabilityError` codes; generated `docs/agents/capabilities.json` with `capabilities:emit` / `check:capabilities`; router row, `ARCHITECTURE.md` map row and `docs/agents/README.md`
- Verified: lint (one pre-existing warning), typecheck, capability tests 14/14, unit/static tests 1,349/1,349, `check:architecture`, `check:knowledge`, `check:capabilities`, `test:ci-policy` 191/191, `next build`; CI on the PR head
- Remaining: `trust` is always `null` until a standalone ledger-trust loader exists; only exercised against demo fixtures through injected loaders, not an authenticated Supabase workspace; human review and owner merge decision
- Production/provider evidence: none
- Superseded issue, roadmap or claim: none

## Boundaries

- No API route, MCP transport, migration, mutation owner, UI or runtime dependency added.
- Output shapes of `src/lib/reports.ts` and `src/server/finance.ts` unchanged; capabilities wrap them.
- `totalBalance` carries a snapshot basis (no transaction ids) because it is an account-balance snapshot, not a transaction sum.
- `basis.excluded` reasons in use: `transfer`, `outside_range`, `other_kind`; `needs_review` and `other_currency` are reserved enum values.

## Evidence limits

- Golden files are demo-fixture based; authenticated data paths reuse existing loaders but were not executed here.
- Manifest schemas are plain JSON Schema from `z.toJSONSchema`, not an MCP tool-list format.
