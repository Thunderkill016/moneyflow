# PR #708 — Goal↔transaction linkage + debt plans spec packets

- Date: 2026-09-23
- Change class: Class 0 (docs only)
- Affected capability or project boundary: `docs/plans/active/` — two new packets; no runtime surface.
- Status impact: `goal-transaction-linkage.md` specified (recommends `financial_transactions.goal_id` nullable tag as the conservative slice; 3 owner questions); `debt-plans.md` specified (debts as annotated ledger view — `debt_movements` links, outstanding derived never stored; parked at Stage 3).
- Changed: two packet files only.
- Verified: `npm run check:knowledge` pass.
- Remaining: both packets await owner decisions before any implementation.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: advances #432 specification depth; neither packet authorizes implementation.
- Lifecycle impact: future debt/goal work must honor the one-ledger law recorded here — no parallel balances, links to real transactions only.
