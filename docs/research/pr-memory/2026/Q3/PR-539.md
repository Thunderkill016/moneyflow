# PR #539 — select #536 security runtime and auth hardening

- Date: 2026-09-02
- Change class: Class 0 authority/planning change that selects a Class 3 security/auth/provider packet; no runtime or provider mutation in this PR
- Affected capability or project boundary: executable-plan authority, current project memory, release-blocking runtime/auth security planning
- Status impact: selects issue #536 as the single current executable slice if owner-merged; merged `main` remains `current: null` until that merge
- Changed: adds the refreshed `docs/plans/active/536-security-runtime-auth-hardening.md`, changes `PLAN_AUTHORITY.current` from null to the #536 packet with `selectedByPr: 539`, reconciles CURRENT_PROJECT_MEMORY, and records this PR memory
- Verified: fresh-main baseline is merged PR #538 at `aa82d47f70d48e1383140d5daa06be443cb08e5b`; main still pins Next 16.2.11 / eslint-config-next 16.2.12; official Next.js August 2026 release and both Critical GHSA patched ranges were refreshed; live Supabase Security Advisor was re-read and currently reports leaked-password protection disabled plus authenticated-callable `SECURITY DEFINER` WARN findings. Exact-head CI/CodeQL/secret checks for #539 remain required before owner handoff.
- Measured: no runtime performance or production-behavior measurement is claimed by this selector; it is authority/research only
- Remaining: owner merge is required before #536 becomes executable; after merge the implementation must run plan resolution/doctor, patch Next to a vetted 16.3.x release at least 16.3.3, run exact `npm audit --json`, verify auth/browser/financial contracts, triage live Supabase advisor warnings against source/grants/tenant tests, and perform leaked-password protection only through an explicitly authorized reversible provider change
- Production/provider evidence: read-only Supabase project/advisor evidence only; no database mutation, Auth configuration write, Vercel deploy or production deployment was initiated
- Snapshot update: CURRENT_PROJECT_MEMORY now records #538 as merged, `current:null` as the fresh-main baseline, #539 as the selector, current vulnerable Next version, leaked-password status, and the new Supabase `SECURITY DEFINER` advisor warning class
- Superseded issue, roadmap or claim: draft PR #537 remains historical packet-preparation evidence but is superseded as the activation vehicle because it was based on pre-#538 main; #523 remains unselected; #527 remains completed and is not reopened
- Lifecycle impact: selects current slice #536 from a valid `current: null` fresh-main state; does not complete #536, does not select a follow-on slice, and performs no runtime/provider write

## Research refresh

Focused upstream evidence was refreshed on 2026-09-02 before selection:

1. Next.js official blog reports the 2026-08-25 security release and directs the Active LTS 16.x line to 16.3.3 for two Critical vulnerabilities.
2. `vercel/next.js` GHSA-p293-qw3h-jr36 affects Next `>=16.0 <16.3.3` and patches at 16.3.3.
3. `vercel/next.js` GHSA-2xp9-vwfh-vxw4 affects Next `<16.3.3` and patches at 16.3.3.
4. Supabase password-security and Management API documentation confirms leaked-password protection uses HaveIBeenPwned, is available on Pro and above, and is represented by `password_hibp_enabled`.

No exploitability assumption replaces actual repository/provider evidence. MoneyFlow's 16.2.11 dependency is inside the upstream affected ranges even though the current Vercel deployment is not known to satisfy every exploit precondition.

## Live Supabase advisor reconciliation

The fresh advisor read surfaces `authenticated_security_definer_function_executable` warnings for many authenticated-callable financial/planning/reconciliation/import/archive RPCs. Prior MoneyFlow review found explicit `auth.uid()` checks and empty `search_path` on privileged RPCs and did not reproduce a cross-tenant P0.

Therefore #539 does not declare the functions safe merely because prior tests passed, and it does not declare them vulnerable merely because the generic advisor warns. The selected #536 packet requires function-by-function classification against source, grants, ownership predicates, pgTAP and browser tenant tests before changing execution mode or privileges. Bulk conversion to `SECURITY INVOKER` or broad revocation is explicitly rejected without evidence because those RPCs are part of the ledger's financial mutation authority.

## Permission boundary

#539 is a selector only. No Next.js upgrade, lockfile change, database/RPC migration, Supabase Auth mutation, provider configuration change, deployment, or public-beta acceptance is authorized by the unmerged PR itself. After owner merge, the selected packet governs implementation and provider operations still retain their explicit owner-decision boundary.
