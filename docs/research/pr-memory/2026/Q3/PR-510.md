# PR #510 — preserve activated projection through later unchanged main merges

- Date: 2026-08-28
- Change class: Class 3 — focused CI/governance authority repair. No product, schema, RPC, RLS, Auth, provider, financial calculation or transaction-behavior change.
- Affected capability or project boundary: `plan:resolve` and `check:knowledge` freshness after a normal merge that does not touch `docs/plans/active/README.md`.
- Lifecycle impact: authority transition. This recovery repairs the activated #509 projection after #507 exposed its main-continuity gap; it neither selects nor completes an agent-executable slice.
- Status impact: candidate until merged. An unchanged projected board remains valid while its projection commit is an ancestor of the main/base commit; a later board edit still requires its own declared projection.
- Research: not applicable. The defect is reproduced deterministically from exact `main@1a70100` and repository contracts; no external behavior applies.
- Changed: the resolver identifies projection ownership from the commit that last changed the board and requires that commit to be an ancestor of the expected main/base SHA. The board and current memory project PR #510 from #507's merged baseline.
- Verified: a new main-continuity regression was observed red before implementation, then green with all 15 `scripts/plan-authority.test.mjs` tests. `npm run plan:resolve`, `npm run check:knowledge`, `npm run test:ci-policy` (228/228), `npx eslint --fix` on the changed scripts, `npx tsc --noEmit --pretty false`, and `git diff --check` pass; knowledge emits only the existing current-memory soft-size warning.
- Remaining: the first ready-for-review CI head was rejected only for Markdown trailing whitespace in the new packet; it was corrected and a new exact head is pending GitHub gates and review. Docker is unavailable locally, so the selected local database gate cannot run; CI must provide that evidence. No merge, deployment, provider operation or financial mutation is performed by this PR.
- Production/provider evidence: none; this is a deterministic local Git-history contract repair.
- Snapshot update: board/current-memory baseline moves to #507 and both declare PR #510 as their candidate same-PR projection.
- Superseded issue, roadmap or claim: supersedes the false assumption that post-projection continuity ends at the immediate child branch; no product work is promoted.
