# PR #702 — Sticky day headers, import provenance subtitle, diacritic-safe eyebrows

- Date: 2026-09-23
- Change class: Class 2 (bounded ledger UI: sticky positioning + one server-fed subtitle + line-height fixes; contract tests; one Zod enum alignment with an existing migration)
- Affected capability or project boundary: `src/components/transactions/transactions-workspace.tsx` + module CSS; `src/app/transactions/page.tsx` wiring; `src/server/reconciliation-import-evidence.ts` source enum.
- Status impact: candidate → implemented. Day groups are now section landmarks on every viewport; imported rows disclose provenance at the row level.
- Changed: `.dayHeader` is `position: sticky` at `top: var(--mf-ledger-sticky-top)` (64px desktop; `calc(62px + env(safe-area-inset-top))` ≤760px); `.manager` uses `overflow: clip` so the viewport remains the scroll owner; `.row` and `.manager .row :focus` get `scroll-margin-top` past the sticky zone. `importEvidence` prop carries the shared `getReconciliationImportEvidence` map; qualifying rows render `Nguồn nhập · {label}` in `.rowProvenance` (muted, wraps). Server schema enum gains `'agent'` to match `inbox_candidate_source` since `20260920120000`. Uppercase eyebrow/section/day-total labels get `line-height: 1.4`.
- Verified: `npm run test` 1777/1777 incl. `ledger-ux-sticky-provenance.test.ts`; `npm run typecheck` clean; `npm run lint` 0 errors; `check:css-ownership` 0 `!important`; `check:architecture`, `check:knowledge`, `test:ci-policy` 191/191 pass.
- Remaining: visual sticky behavior verified by CSS pins + CI visual lanes, not yet by a hand-held device scroll test.
- Production/provider evidence: none (read path reused; one enum value added to a Zod schema to match an already-live Postgres enum).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: if the shell topbar height changes, update `--mf-ledger-sticky-top` (desktop + mobile) and the `scroll-margin-top` offsets together — the values are measured, not decorative.
