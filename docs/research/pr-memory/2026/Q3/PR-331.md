# PR #331 — Single-agent delivery system

- Date: 2026-08-09
- Change class: Class 3 delivery governance / CI policy
- Affected capability or project boundary: AI delivery workflow, repository-backed memory, task execution gates and evaluation policy
- Status impact: candidate only until merged; does not change MoneyFlow product/provider truth
- Changed: single-agent responsibility contract, non-overlapping memory authority, risk-proportional review budget, scoped `Go` decision gate, AC→task→evidence packet structure, deterministic delivery-contract tests, and a static-RLS comment-scanner false-positive fix discovered by full verification
- Verified: current-main policy and old PR #315/#317 reconciled; focused OpenAI/GitHub first-party research completed; source head `4090b07e1f9a8dbee109a83ede4d576d9cf1c708` passed CI #2132, CodeQL #1228 and Secret history #1228; CI policy suite passed 41/41 including 10 agent-delivery tests; static quality, typecheck, production build, unit/static-RLS and browser smoke passed; database checks were not applicable because no DB contract changed
- Remaining: independent semantic review is still required before `ready_for_review`; the configured Codex review bot reported its usage limit exhausted; after an independent review artifact exists, resolve findings and rerun exact-head checks before owner merge consideration
- Production/provider evidence: not applicable; this PR makes no runtime/database/provider/production change
- Snapshot update: not applicable
- Superseded issue, roadmap or claim: selectively supersedes the delivery-governance intent of stale candidate PRs #315/#317 where this PR lands equivalent current-main rules; it does not claim to port their full task-bootstrap or CI-recovery implementations

## Evidence boundary

The primary AI authored this change, so author self-review is not sufficient Class 3 acceptance. The current conversation's self-evaluation found real defects and improved the diff, but it is intentionally not relabeled as independent review.

The configured Codex PR reviewer could not provide the required independent semantic evaluation because its usage limit is exhausted. Machine-green CI is necessary but does not substitute for an evaluator reading the specification, actual diff and exact evidence. A fresh-context reviewer remains the cheapest acceptable fallback; no paid reviewer was automatically enabled.

## Safety

- branch-only repository governance/tooling;
- no direct `main` write or merge;
- no provider or production write;
- no branch protection/ruleset/CODEOWNERS change;
- no dependency, runtime framework or hidden memory service;
- no historical migration edit; the static-RLS scanner was fixed instead of rewriting production history;
- historical/completed work packets are not mass rewritten.
