# PR #503 — give the product a version that means something

- Date: 2026-08-27
- Change class: Class 1 — one new changelog, one new repository contract with tests, one README link. No application code, schema, migration, RPC, RLS, provider or financial-semantic change; nothing in `src/` is touched.
- Affected capability or project boundary: release versioning and the always-run policy gate.
- Status impact: candidate until merged. On merge the repository can state what a release contains for the first time, and `v0.1.0` becomes taggable.
- Lifecycle impact: authority transition. No slice is completed or promoted — none is registered. It carries the board's move to PR #502's merged commit `f7116494`, the transition this PR is the same-PR projection for.
- The gap, measured: **530 commits, 0 tags, 0 releases**, and `package.json` reading `0.1.0` throughout. The build id added in PR #498 identifies a commit, which is enough to trace one report; it cannot say what changed between two builds, so "it worked yesterday" has nothing to compare against. Firefly III, the closest structural comparator, uses semver, tags every major and minor, and publishes an explicit upgrade guarantee.
- Changed: `CHANGELOG.md` (Keep a Changelog + semver), `scripts/check-release-version.mjs` and `scripts/check-release-version.test.mjs` (9 tests), `check:release-version` in `package.json` with the test registered in `test:ci-policy`, and a README link.
- What was deliberately not done: the history of 530 commits was **not** reconstructed. Writing a retroactive changelog for that many commits would be invention. Per-change provenance already exists in `docs/research/pr-memory/` across **182 records**, each carrying verified evidence and stated limits; the `0.1.0` section describes the state at the tag and says so in the file itself.
- The `0.1.0` entry states what is unfinished as plainly as what works: not public-beta ready, RRB-03/04/06/09 open, restore never exercised on managed Supabase, the support address a personal mailbox with no handover, CSP still carrying `'unsafe-inline'`, and accessibility unscanned on surfaces that need a real account. `0.x` is the honest number — semver defines it as the range where anything may change between minors, which is the actual state.
- The contract also requires the changelog to open with an unreleased section. Without one, every new entry lands under an already-tagged version and silently rewrites what that tag shipped — a quieter failure than drift, and a worse one.
- Verified: `lint`, `typecheck`, `check:release-version`, `check:workflow-pins`, `check:knowledge` exit 0; `npm run test:ci-policy` **226 pass** (+9).
- Proven to fail, not only to pass: setting `package.json` to `0.2.0` while the changelog's newest release stayed `0.1.0` produced `FAILED … package.json says 0.2.0 but the newest CHANGELOG.md release is 0.1.0`, exit 1. It was then restored.
- This record corrects a factual error I published in PR #502. That record's `Remaining` field states the Vercel project builds production on Node 24.x while the repository pins 22, calling it three declarations none of which match what ships. **That is wrong.** The production build log for deployment `dpl_CQCb8JYoU6B94adTRitwpZ2V5XgQ` reads: `Due to "engines": { "node": ">=22 <23" } in your package.json file, the Node.js Version defined in your Project Settings ("24.x") will not apply, Node.js Version "22.x" will be used instead.` `engines` overrides the project setting, production builds on 22.x, and PR #498's pin works exactly as intended. I had read the project's `nodeVersion` field and mistaken a setting for what runs.
- Remaining: no tag or GitHub release exists yet — `v0.1.0` will be created after merge and reported with its real URL, not inferred from a green build. Nothing automates the changelog from PR memory records, so entries are written by hand and can be forgotten; the contract catches version drift, not an empty or dishonest entry. There is no upgrade or downgrade guarantee of the kind Firefly III publishes. The Vercel project setting still reads 24.x and is overridden on every build, which emits a harmless warning until someone changes it in the dashboard.
- Production/provider evidence: the production build log was read through the provider API to settle the Node version question. No provider write, no deployment, no production-data access.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` and the board move to PR #502's baseline `f7116494` and carry the PR #503 projection. No capability row changes.
- Superseded issue, roadmap or claim: supersedes the `Remaining` claim in `PR-502.md` that production builds on Node 24.x and that the three Node declarations disagree; production builds on 22.x and they agree. Also supersedes the implicit claim that a build id alone answers what changed between releases.
