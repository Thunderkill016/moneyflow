# PR #701 — One-tap Inbox confirm for Ready rows + calm inbox-zero state

- Date: 2026-09-23
- Change class: Class 2 (bounded flow addition inside the existing Inbox page; reuses the established post path; contract + e2e tests; no schema/RPC/provider change)
- Affected capability or project boundary: `src/components/inbox/inbox-page.tsx` row actions; `ui-phase8-secondary-safety-contract.test.ts`; `e2e/inbox-exception-first-review.spec.ts`.
- Status impact: candidate → implemented for row-level triage only. Bulk approve, review dialog, rejection and candidate-status persistence semantics unchanged.
- Changed: Ready rows render a one-tap `Xác nhận` that builds the identical deterministic draft and `approvalIdempotencyKey(candidate.id)` post as bulk approve, then calls the same `postOne`; `Cần xem lại` rows keep `Duyệt` → dialog only. `rowConfirmBusy` freezes every row confirm while any ledger write is in flight. Inbox-zero state now reads "Đã xử lý hết" with a check icon. List grid last column widened 5.5rem → 7.5rem for the two-word button; `.rowActions` added to the module CSS (no `!important`).
- Verified: `npm run test` 1774/1774 incl. new contract pins; `npm run typecheck` clean; `npm run lint` 0 errors; `check:css-ownership`, `check:architecture`, `check:knowledge`, `test:ci-policy` 191/191 pass. e2e asserts one-tap confirm, absence on review rows, ledger creation and notice.
- Remaining: row confirm does not auto-open the review dialog afterward — if evidence shows users want to inspect what was written, a follow-up could offer a post-confirm "Xem trong sổ" affordance.
- Production/provider evidence: none (client flow only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: `handleConfirmOne` must keep calling `postOne` — duplicating the write path would break the same-draft/same-key dedup contract the contract test now pins.
