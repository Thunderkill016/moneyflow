# PR #726 — Credit-card lifecycle specification packet

- Date: 2026-09-25
- Change class: docs-only packet (Class 0) — `docs/plans/active/`
  specification; no runtime, schema or policy change.
- Affected capability or project boundary: none today — packet is
  candidate specification for a Stage-3 feature, same posture as
  `debt-plans.md`.
- Superseded issue, roadmap or claim: backlog item "Credit-card
  lifecycle: 6 open questions trong spec".
- Status impact: the credit-card lifecycle now has an honest spec: the
  card stays a ledger liability; statement cycles are a derived dated
  annotation layer; six owner questions are enumerated with
  recommendations.
- Design basis:
  - Incumbent research (Money Lover/YNAB/Actual/Firefly) matches the
    debt-plans synthesis: dated obligation surface over one ledger,
    never a second balance or an amortization engine.
  - `credit_limit_minor` is already schema + archive columns but has no
    write/read path — flagged as Q1 rather than silently widened.
- Changed: `docs/plans/active/credit-card-lifecycle.md` (new).
- Verified: knowledge contract passes; no executable surface changed.
- Remaining: owner must answer Q1–Q6 and confirm the stage gate before
  T2+ tasks open.
- Production/provider evidence: none — documentation only.
