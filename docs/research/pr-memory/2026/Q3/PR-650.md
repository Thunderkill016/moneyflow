# PR #650 — Inbox bulk account assignment

- Date: 2026-09-21
- Change class: Class 2 (bounded UI/flow on existing machinery; no schema change)
- Affected capability or project boundary: `src/lib/inbox/review.ts` (`BulkReviewAction` += `"account"`, new `applyBulkAccount`), `src/lib/inbox/review.test.ts`, `src/components/inbox/inbox-bulk-bar.tsx`, `src/components/inbox/inbox-page.tsx`.
- Status impact: post-`ALL_SURFACES_UPGRADE_PLAN` audit item — the largest Inbox throughput gap found in the 2026-09 project audit (`PROJECT_AUDIT_AND_DIRECTION_2026-09.md`): statement imports land every row "Tài khoản chưa xác định" and the bulk bar had approve/reject/category but no account action, forcing one manual open per row.
- Changed:
  - `applyBulkAccount` assigns the chosen account to every selected **pending** candidate of any kind; for transfer candidates the candidate account is the source leg (the account an export describes). Non-pending/unselected rows never change; no ledger write.
  - `InboxBulkBar` gains radio + account `SelectField` + consequence copy ("với ứng viên chuyển khoản đó là tài khoản chi ra; chưa có giao dịch nào được tạo") + review-dialog detail; `handleBulkApply` mirrors the category path.
  - Plumbing was already complete end to end — `updateInboxCandidateAction` patches `account_id`/`account_name` under RLS, `persistCandidateListForClient` diffs changed ids, demo store accepts the same list. No server action change needed.
- Verified: TDD (applyBulkAccount RED → GREEN); `npm test` pass; lint clean (1 pre-existing warning `quick-add-defaults.test.ts:343`); typecheck, build, check:knowledge, check:architecture, check:css-ownership, check:deployment-env, test:ci-policy pass. Browser verify on dev build 390px: 4 radios, account select populated, dialog shows account + consequence, confirm → toast + unresolved-account flag removed on both selected candidates.
- Remaining: CI lanes must pass on exact head; e2e `inbox-exception-first-review` exercises the bulk path and runs in CI. Authenticated persistence rides the existing tested `applyCandidateListMutationAction` route.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place after the owner declined Options A–D of the September audit.
