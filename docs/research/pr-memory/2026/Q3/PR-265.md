# PR #265 — authenticated deterministic rules

- Date: 2026-08-04
- Change class: Class 3 financial-data/security plus core review workflow
- Affected capability or project boundary: categorization rules, Inbox candidate normalization, tenant-owned rule persistence and `/rules` workspace
- Status impact: candidate — authenticated deterministic rules are implemented on a focused branch, while `main`, production schema and production data remain unchanged
- Changed: add versioned candidate-stage rule contracts; add tenant-owned `inbox_rules` with RLS; add atomic reorder and guarded candidate-application RPCs; add optimistic-version Server Actions; rebuild `/rules` with actual categories and preview; preserve demo-local compatibility
- Financial boundary: rules never post ledger transactions, never change amount/date/account/kind, never rewrite raw source text and never bypass duplicate/transfer/low-confidence review
- Security boundary: authenticated rules, categories and candidates remain viewer scoped; cross-tenant targets are rejected below UI; security-definer functions pin an empty search path and expose narrow grants
- Verified: not yet — draft PR #265 requires exact-head knowledge, architecture, lint, typecheck, unit, production build, fresh migration replay, pgTAP, browser, CodeQL and secret-history evidence
- Remaining: integrate every existing rule consumer; round-trip applied rule provenance through candidate mappings; add browser regressions; independently review final diff; obtain owner merge decision; separately authorize production migration and authenticated smoke
- Production/provider evidence: no production schema, data, provider, secret or configuration write has occurred
- Snapshot update: not applicable — PR #265 is an unmerged candidate and does not change canonical current-project truth
- Superseded issue, roadmap or claim: issue #264 owns roadmap #53 PR D; the browser-local rule stub remains only as demo compatibility and is superseded for authenticated persistence by this candidate
