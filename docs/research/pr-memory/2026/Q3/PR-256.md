# PR #256 — record the MVP release decision

- Date: 2026-08-03
- Change class: documentation-only release-governance record
- Affected capability or project boundary: locked MVP release decision boundary; no product, domain, database or CI behavior
- Status impact: **status-changing** — records the owner's explicit MVP release decision. MoneyFlow is released as MVP at `main@8e08a8a` as of 2026-08-03. This changes release status only; it changes no product, domain, database or CI behavior, and it does not make the product public-beta ready
- Changed: add `docs/release/MVP_RELEASE_DECISION_2026-08-03.md` with the proposed candidate SHA and why, decision-time re-verification, criterion-8 issue classification, an explicit list of what releasing does not include, and a blank owner decision block
- Candidate reasoning: proposes `main@8e08a8a` rather than the ledger's `481a9ee` because `git diff --name-only 481a9ee..8e08a8a` returns no file outside `docs/`; the two are functionally identical, so acceptance evidence carries without re-running acceptance, and either SHA remains a valid owner choice
- Verified: on `8e08a8a` — `check:knowledge`, `test:ci-policy`, `check:architecture`, `check:css-ownership` (1105 `!important` against a 1200 budget, `unauthorizedDocumentSelectors` 0), `lint`, `typecheck`, unit tests 649/649, demo production build, `e2e/expense-path.spec.ts` 4/4 and `e2e/mvp-empty-state-primary-actions.spec.ts` 4/4 on Chromium desktop and mobile
- Recorded verification caveat: the first `typecheck` run failed with `Cannot find module '@vercel/analytics/next'`; this was a stale local `node_modules`, not a defect on `main` — the dependency is declared in `package.json` and the failure cleared after `npm ci`. Recorded rather than silently re-run, because "green after reinstall" and "green" are different claims
- Candidate CI evidence: push-to-main CI **#1240** on `8e08a8a` ran every gate regardless of risk classification — `verify` success, `database` success with the skip step bypassed and pgTAP actually executed, and `e2e` success with Chromium **and WebKit** installed, expense/Auth-CAPTCHA smoke passed and the production cross-device UI audit passed with Playwright evidence uploaded. This was checked rather than assumed and it strengthened the candidate recommendation
- Correction inside this PR: an earlier revision listed `test:db`, WebKit and the full responsive audit as "not claimed". That was wrong — CI #1240 ran all three on the exact candidate. Corrected in the record rather than left standing, because it understated the evidence behind the decision
- Genuinely not claimed: provider state reads or writes, physical-device acceptance and an independent production route smoke
- Criterion 8 at decision time: label searches for `P0`, `priority:P0`, `p0` and `bug:P0` return zero open issues, and each of the six open issues (#254, #174, #172, #72, #53, #40) is classified as post-MVP depth, a public-beta gate, superseded framing or P2. This establishes that no P0 money blocker is known and recorded; it does not establish that none exists, which is why the criterion stays conditional
- Decision recorded: owner selected `8e08a8a` and directed release on 2026-08-03, in direct response to the candidate comparison and recommendation. Transcribed, not inferred; the record says so and says how to correct it if it misstates intent
- Snapshot change: `CURRENT_PROJECT_MEMORY.md` now carries the released SHA, the release decision record link, an explicit "released as MVP, not public-beta ready" qualifier, and a closed locked-MVP-release-closure section replacing the three open actions
- Gate correction during this PR: the first `check:knowledge` run failed because a project-contract marker string had been paraphrased while editing the owner-direction line. The exact wording was restored and the gate re-run. Recorded because the gate did its job and the failure was mine
- Remaining: none for locked MVP. Public-beta gates stay open and separate — provider controls, physical-device evidence, final visual approval and approved staging load. Criterion 8 stays conditional by construction; a P0 found later opens a fix and does not retroactively invalidate the decision
- Production/provider evidence: none. This PR performs no provider read or write and makes no production claim
- Snapshot update: none required until the owner decides; the decision boundary in `CURRENT_PROJECT_MEMORY.md` section 10 is unchanged by this record
- Superseded issue, roadmap or claim: none. This record does not supersede the acceptance ledger; it depends on it
