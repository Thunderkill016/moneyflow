# PR #665 — Close remaining Stage-1 measurement gaps

- Date: 2026-09-22
- Change class: Class 1 (bounded analytics wiring, three components + union extension)
- Affected capability or project boundary: `src/components/add-transaction-dialog.tsx`, `src/components/transfer-dialog.tsx`, `src/components/inbox/capture-share-page.tsx`, `src/lib/safe-analytics.ts`.
- Status impact: closes the second instrumentation gap found in the post-canon audit — the dominant manual-entry paths (add-transaction dialog, transfer dialog) fired no events, so `acquired-vs-retyped share` could not compare manual volume against import/paste commits; `share_received` was declared in the union but had no call site.
- Changed: `manual_transaction_saved` (new event) fires on successful manual saves with `kind` + `keep_open`; `share_received` fires on successful share-target ingest with `candidates`, `paste_count`, `csv_files`. Scalars only.
- Verified: `npm test` 1490/1490; `typecheck` clean; eslint on touched files clean; `check:knowledge` green.
- Remaining: maintenance minutes and trusted-period retention still lack an instrumentation path (require session-duration or reconciliation-cadence signals, not yet designed); correction-opened tracking exists only on quick capture.
- Production/provider evidence: none — events are inert without the Web Analytics runtime.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: together with #664, the Stage-1 scorecard's measurable subset (interventions, corrections, manual-vs-acquired share, review outcomes) is now instrumented; next canon step is collecting real-usage data before choosing an acquisition-path experiment.
