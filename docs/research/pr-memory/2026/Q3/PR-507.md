# PR #507 — one-time quick-capture save-and-continue

- Date: 2026-08-28
- Change class: Class 2 — bounded quick-capture UI/flow change. No schema, RPC, RLS, Auth, provider, financial calculation, money representation or transaction semantics change.
- Affected capability or project boundary: manual quick capture on its dialog and embedded page, specifically repeated entry after an explicit save.
- Lifecycle impact: none. No current agent-executable slice exists; this owner-requested bounded improvement does not promote or complete a registry slice.
- Status impact: candidate until merged. An eligible quick-capture form gains a visible, one-time `Lưu & thêm tiếp` action alongside Save.
- Research: YNAB's official manual-entry guide keeps number-pad amount, type, account/category confirmation and an explicit Save in the flow; Actual Budget's official account-register guide places manual entry in an account context with category selection. Both establish that speed must retain explicit financial context. Applied boundary: this PR reuses the user's already selected account/category, adds no inferred category, auto-posting, native/provider acquisition or AI behavior. Sources: https://support.ynab.com/en_us/how-to-add-transactions-in-ynab-HyDwA_byi?mobile-help=true and https://actualbudget.org/docs/tour/accounts/.
- Changed: `AddTransactionDialog` distinguishes a one-time submitter from the persisted `keepOpen` preference. The secondary action is disabled under the same existing account/category validity boundary, retains the form only for its own successful submission, clears amount/note and restores amount focus. The existing optional checkbox remains the sole persistent repeat mode. The capture footer uses two equal controls where space permits and one column at 360px or less; embedded capture uses three equal controls where space permits. The expense-path browser test covers disabled-before-category, enabled-after-category, one-time save/keep-open/refocus and the unchanged persistent checkbox behavior; the source contract names both paths.
- Verified: TDD browser test was observed red before implementation because `Lưu & thêm tiếp` did not exist, then green. `npm run typecheck`, `npm run lint`, `npm test` (1,226 pass), and demo production `npm run build` passed. Targeted constrained capture audit passed. Full `npm run test:e2e` reached 130 pass; its four failures are the two Turnstile provider-readiness tests in both Chromium projects because no CAPTCHA provider token is available locally. Full `npm run test:ui-audit:pr` reached 540 pass and 141 skip; all Chromium capture/target-size/390px/constrained/text-scale/dark checks passed. Local WebKit produced 54 failures at `page.goto` with `WebKit encountered an internal error` before assertions; a single Android SAFE-09 transaction-geometry failure reran green and that test/directory is outside this diff. `check:migrations`, `test:ci-policy`, `check:architecture`, `check:css-ownership` and `check:code-css-ownership` passed.
- Remaining: `npm run check:knowledge` is currently blocked before lifecycle validation: `origin/main` still carries the #504 baseline with its projected #504 convergence. Open PR #505 is the separate authority-transition recovery that moves the board and project memory to #504's merged commit. #507 must be rebased and rerun after an owner merges #505; it must not forge that transition into this feature PR. GitHub exact-head checks and required review remain outstanding. Provider-backed Turnstile and a functioning local/CI WebKit environment are not claimed by this branch. No merge or production deployment is performed by this PR.
- Production/provider evidence: none. All UI evidence is local demo production-build evidence; no provider or production write is in scope.
- Snapshot update: not applicable. This is unmerged candidate behavior; no current capability/architecture/security/operations truth changes on `main`.
- Superseded issue, roadmap or claim: none. This is deliberately smaller than a capture redesign and does not reopen #426's held planning-column decision.
