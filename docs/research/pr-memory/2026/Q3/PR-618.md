# PR #618 — write-capability spec: agent writes enter as candidates (A4)

- Date: 2026-09-20
- Change class: Class 0 (docs-only spec; underlying domain is Class 3 — financial mutation surface)
- Affected capability or project boundary: `docs/plans/active/617-write-capability-spec.md` (new), `docs/plans/active/megaplan-q4-2026.md` (A4 status), issue #617
- Status impact: spec authored for owner review; **implementation NOT authorized** — gate G5 remains explicit
- Changed: authored the write-capability specification. Core decision — agent writes enter as Inbox **candidates** (`source: "agent"`, `sourceExternalId` = client idempotency key, `oauth_client_id` provenance) converging on the existing candidate/match/approve path; direct-ledger auto-post deferred behind per-client policy + audit + owner flag. Specifies `candidates.propose` contract, per-capability MCP annotations (`readOnlyHint: false`), app-side `client_id` write allowlist (Supabase OAuth scopes are fixed: `openid profile email phone offline_access`), dedicated write rate limit, S1–S8 acceptance criteria.
- Verified: `npm run check:knowledge` pass; all mutation/candidate/provenance facts verified against current `main` (`create_*` RPCs take `p_idempotency_key`; `CandidateSource`/`CandidateProvenance`; MCP tool generation hardcodes `readOnlyHint: true` at `src/server/capabilities/mcp.ts`).
- Remaining: owner review (D1–D3 in packet); implementation phases P-A→P-D only after G5 authorization.
- Production/provider evidence: Supabase OAuth fixed-scope surface confirmed via live discovery response during P2 verification; `client_id` claim name asserted from Supabase convention — flagged in packet as an open question to confirm on a real issued token during implementation spike.
- Superseded issue, roadmap or claim: none — new spec, no prior write-cap spec existed.
- Lifecycle impact: adds active packet 617; megaplan A4 marked spec-authored awaiting owner review.

## Boundaries

- Docs-only PR: no runtime code, no migration, no capability registration.
- Merging this PR approves the spec as the reference design; it does not authorize implementation (G5 is a separate explicit owner decision).

## Evidence limits

- The `client_id` JWT claim name and single-candidate create seam are spec-level assertions pending implementation-spike verification; both are recorded as open questions in the packet.
