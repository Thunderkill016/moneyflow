# PR #638 — Dashboard needs_review attention chip (G3)

- Date: 2026-09-21
- Change class: Class 2 — bounded dashboard UI change (one chip, existing strip)
- Affected capability or project boundary: `src/lib/attention.ts`, `src/components/moneyflow-dashboard.tsx`, `src/lib/attention.test.ts`.
- Status impact: **closed as superseded.** This branch's commits rode along inside the squash-merge of #639 (`3273fb77`) — the docs branch was cut from this feature branch by mistake. The chip code is on main and verified by #639's full CI on that head. Implements G3 of `docs/research/DASHBOARD_UPGRADE_RESEARCH_2026.md`.
- Changed: `buildAttentionItems` accepts `needsReviewCount`; emits a `neutral` chip `"{n} giao dịch cần kiểm tra"` → `/transactions?review=needs_review` (existing filter param) only when count > 0; dashboard counts via shared `getTransactionReviewStatus`; chip appended after budgets/bills/inbox within the 4-item cap.
- Verified: TDD — failing test observed before implementation; `npm test` 1436/1436; typecheck, lint clean; check:knowledge, test:ci-policy, check:css-ownership, check:architecture, build all pass; #639 CI on the squashed head: Browser smoke + Cross-device UI audit + e2e + CodeQL + Gitleaks green.
- Remaining: none — functionality lives on main via #639.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: this PR is superseded by #639's squash, which carried its diff.
- Lifecycle impact: closes G3 of the dashboard upgrade gap list; G1 (per-account balances) remains the recommended next slice.
