# PR #638 — Dashboard needs_review attention chip (G3)

- Date: 2026-09-21
- Change class: Class 2 — bounded dashboard UI change (one chip, existing strip)
- Affected capability or project boundary: `src/lib/attention.ts`, `src/components/moneyflow-dashboard.tsx`, `src/lib/attention.test.ts`.
- Status impact: implements G3 of `docs/research/DASHBOARD_UPGRADE_RESEARCH_2026.md`. Ledger rows with `reviewStatus=needs_review` are now visible from the home screen instead of silently counting into totals.
- Changed: `buildAttentionItems` accepts `needsReviewCount`; emits a `neutral` chip `"{n} giao dịch cần kiểm tra"` → `/transactions?review=needs_review` (existing filter param, no new machinery) only when count > 0; dashboard counts via shared `getTransactionReviewStatus` so demo/auth behave identically; chip appended after budgets/bills/inbox within the 4-item cap.
- Verified: TDD — failing test observed before implementation (`surfaces unreviewed ledger rows`); `npm test` 1436/1436; typecheck, lint clean; check:knowledge, test:ci-policy, check:css-ownership, check:architecture, build all pass on `NEXT_PUBLIC_APP_MODE=demo` local env.
- Remaining: Cross-device UI audit + Browser smoke on CI; owner merge decision; real-device glance is owner-side.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: closes G3 of the dashboard upgrade gap list; G1 (per-account balances) remains the recommended next slice.
