# PR #300 — Phase 3 App Shell ownership

- Date: 2026-08-05
- Change class: presentation/runtime UI ownership migration; no financial-domain, database, auth, RLS, provider, deployment or production-data change
- Affected capability or project boundary: signed-in App Shell identity, viewport/safe-area geometry, focus scroll clearance, mobile navigation reservation, shell Sheet/Toast composition, normal stacking, important shell controls, active navigation IA and shell compatibility retirement
- Status impact: Phase 0, Phase 1 and Phase 2 remain merged; Phase 3 is an active unmerged implementation candidate after the owner instructed **“Triển khai P3”**; Phase 4 remains unauthorized
- Changed: App Shell renders canonical main-branch B3.2 BrandLockup; Capture/More compose Phase 2 Sheet; shell feedback composes ToastRegion; important actions use Button/LinkButton/IconButton while destinations remain links; typed root viewport uses `viewportFit: "cover"`; one shell geometry/layer contract owns mobile reserve and focus clearance; Accounts uses explicit `showPrimaryActionOnMobile`; active `/insights`, signed-in logo guardrail, route padding, broad `dialog[open]` and `body:has()` shell repairs are removed; `MobileShellContract` retains only the Phase 5-owned transaction-dialog dark amount-field remainder
- Verified: source contracts are committed; the UI migration classifier, CSS ownership and architecture checks passed on CI #1693; CodeQL #819 and secret-history scan #819 passed on exact head `21c8643f66c7bb911c84792cd78a531eac20ff3c`; complete lint, typecheck, tests, build and browser evidence remains pending after current fixes
- Remaining: resolve any CI/type/build/browser findings; verify generated viewport meta, public-route regression, 320/360/390 and landscape geometry, Light/Dark/System, text-200, keyboard focus, Sheet Escape/focus return, Accounts phone action and feedback clearance; obtain explicit owner approval before merge
- Production/provider evidence: not applicable — no deployment, provider write, database/auth/RLS change, production-data access or production operation
- Merge-time memory record: update `CURRENT_PROJECT_MEMORY.md` only immediately before an owner-authorized merge; this open candidate is not current product truth
- Superseded issue, roadmap or claim: supersedes private App Shell Brand/dialog/toast implementations, active shell `/insights` inference, the Accounts `body:has()` capability repair and any claim that the entire `MobileShellContract` is removable during Phase 3; the transaction remainder is explicitly deferred to Phase 5
