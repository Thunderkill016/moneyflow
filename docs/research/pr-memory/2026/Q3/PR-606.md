# PR #606 — populate ledger trust in capability layer via standalone loader

- Date: 2026-09-20
- Change class: Class 1 (bounded server-only change; no route/UI/schema/provider change)
- Affected capability or project boundary: `src/server/ledger-trust.ts` (new shared trust-contract owner), `src/server/capabilities/` dep wiring, `npm test` glob
- Status impact: candidate → candidate (capability trust seam live at loader level; still no UI/API/MCP consumer)
- Changed: extracted `ledgerTrustSchema` + `mapLedgerTrust` verbatim from `dashboard.ts` into `src/server/ledger-trust.ts` alongside new `getLedgerTrust()` (demo → null; authenticated → `rpc("ledger_trust_summary")`; errors/malformed payloads logged and withheld as `null`, never fabricated); `CapabilityDeps.loadLedgerTrust`; `ledger.summary` populates `trust` on output and every amount `basis`; `buildSnapshotBasis` gained the optional `trust` it hard-coded; `npm test` now covers `src/server/**/*.test.ts` (P1-1 capability tests were unreachable by the standard gate); regenerated `__golden__/ledger-summary.json` + `docs/agents/capabilities.json`; `docs/agents/README.md`, `ARCHITECTURE.md`, `docs/context/README.md` updated
- Verified: lint (0 errors; 3 pre-existing warnings in unrelated WIP files), typecheck, `npm test` 1366/1366 incl. capability 16/16, `check:architecture`, `check:knowledge`, `check:capabilities`, `test:ci-policy` 191/191, `next build` with CI-equivalent env at `22b1763d`; CI on the PR head
- Remaining: `getLedgerTrust()` exercised through injected deps and the pgTAP-covered SQL contract only, not an authenticated Supabase workspace from this environment; human review and owner merge decision; PR is stacked on unmerged `devin/1789917917-capability-layer-v0` (#605)
- Production/provider evidence: none
- Superseded issue, roadmap or claim: closes the `trust: null` follow-up named in PR #605
- Lifecycle impact: none — no plan/authority state; stacked PR awaiting #605 merge order

## Boundaries

- Dashboard keeps the bundled-RPC trust path (one-call performance contract); `getLedgerTrust` is for consumers outside that bundle — the two share one schema/mapper.
- `trust` stays nullable in the capability output schema; `null` means demo viewer or withheld-on-failure, never a synthesized boundary.
- No new RPC, migration, transport, mutation owner, cache or runtime dependency.

## Evidence limits

- Golden file carries the injected `fixtureLedgerTrust`; authenticated execution path (`requireViewer` → `createClient` → rpc → zod → map) is wiring-verified by typecheck/build, not executed against a live workspace.
- `npm test` glob extension assumes no future `src/server` test needs `import "server-only"`-marked modules resolvable under plain `node --test`.
