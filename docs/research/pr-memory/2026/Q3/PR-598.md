# PR #598 — guard transitive dependency advisory floors

- Date: 2026-09-19
- Change class: Class 1 test-only executable security guard
- Affected capability or project boundary: dependency security regression contract
- Status impact: none
- Changed: require the declared overrides and every installed Hono/js-yaml copy to remain at or above their patched floors
- Verified: repaired-main merge head `9dc870c5` used Node 22.23.2/npm 10.9.8; audit-zero; `verify:prepush` with 1,334 unit tests and production build; 191 CI-policy tests; 62 migration identities; 148 desktop/mobile Playwright cases
- Remaining: exact-head provider checks, CodeQL, Gitleaks and owner review
- Production/provider evidence: none
- Superseded issue, roadmap or claim: the original PR branch attempted to add patched pins, but current `main` already contains Hono 4.13.5 and js-yaml 4.3.2 through later merged dependency work; this PR now adds only the missing permanent guard

## Advisory evidence

- `hono` arrives through `shadcn` -> `@modelcontextprotocol/sdk`; advisories fixed in 4.13.5 covered static-generation path traversal, body-parser memory exhaustion and URL interpretation differences.
- `js-yaml` 4.3.1 arrived through ESLint/cosmiconfig tooling; GHSA-2883-xcg3-v3hh is fixed in 4.3.2.
- Both packages are transitive tooling dependencies. The test checks overrides and all lockfile copies so a later resolution cannot silently reintroduce a vulnerable nested copy.
- No dependency version or lockfile entry differs from current `main` in the final PR.

## Permission boundary

This PR changes a repository test and its bounded provenance record only. It performs no deployment or provider/production-data write.
