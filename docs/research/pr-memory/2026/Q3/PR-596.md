# PR #596 — stable Ghi defaults and immediate correction

- Date: 2026-09-14
- Change class: Class 3 financial-entry behavior; owner review required.
- Affected capability or project boundary: shared Ghi transaction capture defaults and immediate post-save correction across Dashboard, Transactions and Capture Quick; no schema/provider/production configuration change.
- Status impact: ready for owner review on PR #596 only. `PRINCIPLES.md` remains product law; current `main` remains released truth until owner-approved merge.
- Changed: added a pure stable-ledger preset helper and counterexample tests; Ghi may preselect an exact account/category pair only when 2 of the 3 most recent eligible same-kind ledger transactions agree; existing browser-local preset remains fallback; primary capture surfaces pass transaction history; successful single-save capture exposes `Sửa` through the existing `EditTransactionDialog` / `useTransactions.updateTransaction` path.
- Baseline: `main@a7e45d30ba0d1f9e6763cd51210543007154735b` (merged PR #594). Main was rechecked during evaluation and had not moved.
- Research: YNAB official categorization guidance was rechecked on 2026-09-14 and explicitly updates an established default when 2 of the 3 most recent transactions support a different category; older backdated transactions do not change that default because only the three most recent count. Actual Budget official rules documentation was rechecked as a deterministic, inspectable automation reference. Apple HIG guidance was checked for reduced input plus clear recovery. MoneyFlow adapts only the stability/recovery patterns; no payee model, new rule engine, AI inference or platform-specific architecture is adopted.
- Decision: ledger evidence outranks browser-local history only when the exact `accountId + categoryId` pair is stable. The pure helper requires normalized `reviewStatus === "reviewed"`, excludes transfers/opposite-kind/split rows and invalid current account/category references, and follows MoneyFlow's canonical ledger ordering: `occurredOn` descending, then creation timestamp (`occurredAt`) descending, then id descending. Missing review metadata is not accepted by the helper itself; legacy compatibility normalization remains owned by the existing transaction workspace/hook rather than duplicated here. Fewer than three eligible rows or a 1/1/1 split yields no ledger default. No amount, merchant or fuzzy inference is used.
- Authority boundary: this PR does not authorize provider work, schema/migration changes, AI/ML, new financial recommendations, new persistence, cross-device preference sync or production writes. Merge remains a human-owner decision.
- Financial/security impact: capture defaults read already-authorized ledger rows, but posted amounts and mutation semantics are unchanged. Integer VND, transfer neutrality, tenant isolation/RLS, idempotency and the existing mutation owner remain unchanged. No second financial truth or mutation path is introduced.
- Production/provider evidence: none performed or authorized. No provider/deployment/configuration writes are in scope.
- Superseded issue, roadmap or claim: none. Issue #595 owns this bounded implementation slice.
- Evaluation findings: evaluator corrected two semantic issues before handoff: ordering now matches the existing ledger (`occurredOn` before creation time), preventing newly-entered backdated rows from hijacking recency; helper eligibility now requires reviewed status rather than merely excluding `needs_review`. CI #3738 found packet trailing whitespace and CI #3741 found a missing required PR-memory `Verified` field; both were documentation-contract repairs without product-scope expansion.
- Verified: content head `e1cd796435be0da389c83a839d3639e417867710` passed CI #3742 (run `34776438370`), including diff hygiene, migration identity, project knowledge, CI classification/retry, deployment configuration, CSS ownership/debt budgets, architecture boundaries, lint, typecheck, unit tests/static RLS, production build, Browser smoke (expense/auth CAPTCHA and authenticated ownership), Cross-device UI audit, and the final `e2e` aggregate. Database reset/pgTAP was correctly classified as not required. The same content head passed CodeQL #2742 (run `34776438357`) and Secret history scan #2742 (run `34776438354`). This ready-for-review bookkeeping commit is documentation-only and must retain green exact-head checks before the PR is marked ready. Local clone/test execution was unavailable because the execution container could not resolve GitHub; no local green claim is made.
- Remaining: verify the final bookkeeping head, then owner review/merge decision. Production behavior remains unverified until an owner-approved merge/deploy. Do not merge automatically.
- Rollback: revert PR #596. The local quick-add preference format is unchanged and no database migration/backfill exists, so rollback restores the previous local-preset-only capture behavior without data conversion.
