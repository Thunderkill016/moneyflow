# PR #642 — Transactions review queue KPI + date presets

- Date: 2026-09-21
- Change class: Class 2 (bounded UI on one existing surface; new pure date module)
- Affected capability or project boundary: `src/lib/date-presets.ts`, `src/components/transactions/transactions-workspace.tsx`, `transactions-workspace.module.css`.
- Status impact: T1+T2 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — the ledger page now shows the needs_review queue it can already filter, and one-tap date ranges replace slow manual entry on mobile.
- Changed: new `datePresetRange`/`activeDatePreset` (Monday-start weeks, UTC calendar math, server-resolved `today` only). A "Cần kiểm tra · N" toggle chip on its own toolbar row counts the ledger-wide queue and toggles `?review=needs_review` — kept out of the summary strip after the UI audit showed a fifth cell squeezing money columns into overflow on rich seeded values. Preset chips sit above the manual date inputs, write the same `from`/`to` state, press-state via `activeDatePreset`, re-tap clears.
- Verified: TDD — date-presets tests failed on missing module, pass 5/5 after. `npm test` 1446/1446; tsc, lint, build, check:knowledge/architecture/css-ownership/deployment-env clean. Browser-verified on dev demo at 390/1024/1366px incl. a seeded `+12.345.678.900 ₫` income: presets render one segmented row, "Tháng này"→01/09→21/09, "Tuần này"→today (Monday), re-tap clears, review chip sets `?review=needs_review`, zero money overflow. First-head CI failed on the five-cell variant (`financial-value-overflowed`, P1) — fixed by moving the toggle to the toolbar.
- Remaining: CI on exact head; owner merge decision.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes Transactions T1/T2; next in plan order is dashboard D2 (month-shape bars) / D4 (prior-month compare).
