# PR #591 — surface ledger trust on Home

- Date: 2026-09-13
- Change class: Class 3 — financial/data trust presentation plus additive authenticated read-model migration
- Affected capability or project boundary: signed-in `/dashboard`, `get_dashboard_bundle`, existing `ledger_trust_summary()`, GitHub #590 / parent program #559
- Status impact: MoneyFlow already computes a deterministic tenant-scoped trusted-through boundary, and production now contains that standalone function, but Home does not expose it. This PR wires the existing trust contract into the single bounded dashboard read and presents a compact trusted/limited/blocked state with at most one next maintenance action.
- Changed: additively extends `get_dashboard_bundle` to include the existing `ledger_trust_summary()` output; keeps the browser/server dashboard path at exactly one `.rpc()` call; parses the new field as optional so old bundle payloads remain valid during application-before-migration skew; keeps demo and authenticated fallback trust state null rather than fabricating reconciliation truth; adds deterministic Vietnamese presentation mapping and a compact Home trust strip; adds tenant-isolation/coverage-boundary pgTAP and an authenticated mobile browser proof.
- Research basis: current official YNAB approval/reconciliation guidance supports explicit review and reconciliation as trust-maintenance loops; Actual Budget import/rules guidance supports dedupe plus explainable user-owned automation; Copilot Money surfaces a bounded `To Review` queue on the daily dashboard. Local Money Lover/MISA product framing reinforces keeping the Vietnamese daily path simple. MoneyFlow adopts the workflow pattern only: visible trust/maintenance state plus one next action, not competitor budgeting models, AI mutation or provider sync.
- Architecture finding: the initial #590 draft proposed a second `ledger_trust_summary()` call. Current `src/lib/dashboard-performance-contract.test.ts` and the historical one-bundle performance work reject that shape by requiring exactly one dashboard RPC and no direct table read. #590 and its work packet were revised before implementation; trust is now composed inside `get_dashboard_bundle` instead of adding network fan-out.
- Verified: pending exact-head CI. Local repository execution is not claimed because the current agent sandbox cannot clone GitHub over outbound DNS. Repository/source review confirms the one-RPC guard remains in place, the standalone trust semantics are unchanged, and the new browser fixture explicitly asserts one served `get_dashboard_bundle` request and no `ledger_trust_summary` RPC. Exact-head CI/database/browser/UI-audit/CodeQL/secret evidence remains required before review.
- Performance evidence: no performance win is claimed. The selected architecture preserves the existing one-network-RPC dashboard contract; the database does additional trust computation inside that RPC. Exact runtime impact has not yet been measured, so this PR makes no latency/LCP claim.
- Remaining: pin the new migration identity, run/fix exact-head policy/type/unit/build/database/browser/UI-audit/CodeQL/secret gates, update this record with final evidence, then owner review. Merge and production migration remain owner decisions.
- Financial/data boundary: no change to `ledger_trust_summary()` semantics, integer-VND rules, transfer neutrality, mutation RPCs or tenant identity. The Home copy explicitly limits its claim to reconciled known MoneyFlow ledger state and never asserts external source completeness.
- Rollback: revert the additive bundle migration and Home presentation in a new change; keep the standalone trust function. Optional application parsing keeps the prior dashboard bundle shape usable during rollback/deployment skew.
- Production/provider evidence: no production migration, provider configuration or production-data write is performed by this PR. The new bundle migration is repository-only until separately authorized after merge.
- Superseded issue, roadmap or claim: supersedes only #590's first implementation sketch that would have added a second dashboard RPC. It does not execute the broad #559 redesign, add bank sync, or revive withdrawn release gates.
