# PR #591 — surface ledger trust on Home

- Date: 2026-09-13
- Change class: Class 3 — financial/data trust presentation plus additive authenticated read-model migration
- Affected capability or project boundary: signed-in `/dashboard`, `get_dashboard_bundle`, existing `ledger_trust_summary()`, GitHub #590 / parent program #559
- Status impact: MoneyFlow already computes a deterministic tenant-scoped trusted-through boundary. This PR wires that existing contract into the single bounded dashboard read and presents a compact trusted/limited/blocked state with at most one next maintenance action; it does not broaden the truth boundary to external bank/provider completeness.
- Changed: additively extends `get_dashboard_bundle` with the existing `ledger_trust_summary()` output; keeps the authenticated Home path at one `.rpc()` call; parses the new field as optional so an application-before-migration deployment keeps the prior dashboard usable; keeps demo/auth fallback trust null; adds deterministic Vietnamese trust copy and one existing maintenance CTA; adds tenant-isolation pgTAP plus authenticated 320px phone and desktop browser evidence in light/dark mode.
- Research basis: official YNAB approval/reconciliation guidance supports explicit review and reconciliation as trust-maintenance loops; Actual Budget import/rules guidance supports dedupe plus explainable user-owned automation; Copilot Money makes bounded `To Review` work visible on the daily path. Local Money Lover/MISA framing reinforces a simple Vietnamese daily-money path. MoneyFlow adopts only the workflow pattern: visible trust/maintenance state plus one next action, not competitor budgeting models, AI mutation or provider sync. Playwright official emulation guidance was also used to harden the browser proof: the narrow viewport is set before navigation and dark mode is graded through the real `prefers-color-scheme` emulation.
- Architecture finding: the first #590 sketch proposed a second `ledger_trust_summary()` server RPC. Current `src/lib/dashboard-performance-contract.test.ts` rejects that shape by requiring exactly one dashboard RPC and no direct table read. The issue/work packet were revised before implementation; trust is composed inside PostgreSQL `get_dashboard_bundle` instead of adding network fan-out.
- Evaluation findings: CI first caught work-packet trailing whitespace, then the missing migration-identity entry and two manually mistyped historical migration filenames; all were corrected without editing applied migration SQL. A SAFE-03 source contract initially expected a literal `safeInteger` call even though the implementation deliberately strengthened the boundary to `safeCount` (`safeInteger` plus non-negative enforcement); the test was updated to lock the stronger semantic guard. Evaluator review also found that demo-only cross-device audit could not prove an auth-only trust surface, so explicit authenticated 320px phone and 1280px desktop light/dark coverage was added. Finally, source review caught a test-harness false failure before CI: `signIn()` already lands on `/dashboard`, so a second `page.goto('/dashboard')` would manufacture a second RPC; the trust tests now grade the first post-login dashboard render.
- Verified: pre-final evidence on head `5c82d0360db199256127fe2001cff895de0c446c` showed CI #3681 with policy, migration identity, project-knowledge, unit/static RLS, production build, presentation ownership, deployment/CSS/architecture, lint/typecheck, fresh local database reset + pgTAP, archive producer/restore, and aggregate `verify` green; CodeQL #2684 and Secret history scan #2684 were also green. That head was intentionally superseded before acceptance to strengthen the mobile proof so the 320px viewport is established before navigation, following Playwright guidance. Final exact-head CI therefore remains required after this evidence update.
- Browser evidence contract: the strict authenticated Supabase double seeds one deterministic limited-trust payload; phone and desktop tests assert truthful wording, external-source limitation, one existing CTA, >=44px CTA height, no horizontal overflow, light and dark presentation, exactly one served `/rest/v1/rpc/get_dashboard_bundle`, and zero network calls to `ledger_trust_summary`. The double cannot prove PostgreSQL/RLS; those remain pgTAP responsibilities.
- Performance evidence: no performance win is claimed. The selected architecture preserves one dashboard network RPC, but the database now performs the trust computation inside that call. Existing indexes cover the Inbox and needs-review predicates and current reconciliation paths; no speculative new index was added. Exact database latency/LCP impact has not been benchmarked, so this PR makes no unchanged-latency claim.
- Remaining: obtain one final exact-head green CI/CodeQL/Secret run after these evidence records, inspect the authenticated-browser and cross-device steps, then hand off to owner review. Merge and any production migration remain separate owner decisions.
- Financial/data boundary: no change to `ledger_trust_summary()` semantics, integer-VND rules, transfer neutrality, mutation RPCs or tenant identity. Home copy limits its claim to reconciled known MoneyFlow ledger state and never asserts external source completeness.
- Rollback: revert the additive bundle migration and Home presentation in a new change; keep the standalone trust function. Optional application parsing keeps the prior dashboard bundle shape usable during rollback/deployment skew.
- Production/provider evidence: no production migration, provider configuration or production-data write is performed by this PR. The new bundle migration remains repository-only until separately authorized after merge.
- Superseded issue, roadmap or claim: supersedes only #590's first two-RPC implementation sketch. It does not execute broad #559 redesign, add bank sync, add AI categorization, or revive withdrawn release gates.
