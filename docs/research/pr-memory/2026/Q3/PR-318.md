# PR #318 — Phase 9 public and authentication cleanup

- Date: 2026-08-07
- Change class: Class 2 — public UI/authentication presentation cleanup with preserved authentication semantics
- Affected capability or project boundary: Landing, Login, Register, Forgot Password, Update Password presentation ownership, public theme ownership and retired public CSS generations
- Status impact: candidate — active unmerged Phase 9 work; current product behavior remains `main` until owner review and merge
- Changed: keeps Landing Login/Register actions reachable on phones; restores 44px header targets; removes stale public green/dark fallback declarations from Auth presentation; deletes three unreferenced Landing/Auth generation stylesheets; shrinks CSS ownership allowlisting; adds public/auth source and browser contracts
- Preserved: Guided Story structure, Fresh Blue public identity, public light-only behavior, truthful claims/media, auth actions, OAuth, CAPTCHA, validation, redirect, recovery and database/provider boundaries
- Verified: first ready-for-review run reached real Class 2 jobs but the policy shard rejected missing packet/PR-memory governance markers; that run is not final evidence. Refreshed exact-head CI, browser/cross-device, CodeQL and secret-history remain pending.
- Remaining: observe refreshed exact-head gates, fix any real regression at its owner, then obtain owner merge decision; P10 and P11 remain separate dependent phases
- Production/provider evidence: none; no merge, deployment, provider setting, schema/RLS, production data or destructive production action is included
- Snapshot update: not applicable — unmerged candidate does not change current implementation truth
- Superseded issue, roadmap or claim: retires obsolete public green/dark CSS generations as runtime authority; does not supersede the selected Guided Story/Fresh Blue direction

## Authorization and safety

- Owner authorization: explicit “Thôi làm xong kế hoạch này đi đã” on 2026-08-07 authorized completion of the remaining P9–P11 migration work through controlled branches/PRs.
- Branch: `agent/ui-phase-9-public-auth` from `main@8b97566a9bb70228ea5593d545660900aa626efb`.
- Packet: `docs/plans/active/ui-phase-9-public-auth.md`.
- Pull request: #318; opened Draft for initial classification and moved Ready only to run cumulative Class 2 verification.
- Merge authorization: not delegated to the agent; owner decision remains required.
- No auth action, database, RLS, provider, production-data, branch-protection or manual deployment write is included.

## Repository findings

1. Current Landing and Auth already render through local CSS Modules and `public-brand-theme.module.css`; the selected runtime public identity is Fresh Blue/light-only.
2. `landing-refresh.css`, `landing-dark-mode-guardrails.css` and `auth-refresh.css` remained as old green generations but were not imported by the current root layout.
3. `auth-form.module.css` still carried green raw variables and public dark-mode selectors even though current public semantic roles override them.
4. Landing hid its Login action below 680px and reduced the Register target to 42px, leaving the narrow public entry contract inconsistent with the 44px MoneyFlow target.
5. Auth fields already expose email/current-password/new-password autocomplete purposes and do not intentionally block paste.

## Research interpretation

- W3C WCAG 2.2 Accessible Authentication identifies password-manager support and copy/paste as mechanisms that reduce authentication cognitive burden; Phase 9 protects autocomplete and absence of paste blocking.
- Next.js official CSS guidance supports locally scoped CSS Modules for component presentation; Phase 9 deletes stale globals rather than adding another override layer.
- Playwright device emulation is responsive/browser evidence, not physical-device evidence; P11 keeps Android/iOS acceptance separate.

## Evaluation record

- The three retired public generation files are physically deleted and the CSS ownership contract now fails if they reappear.
- Auth presentation consumes semantic `--auth-*` roles and contains no public dark selector or retired green palette branch.
- Landing keeps both entry paths visible in responsive source and restores the 44px header target.
- Focused source tests lock Guided Story, public-light, autocomplete and no-paste-blocking behavior.
- Focused Playwright coverage checks public entry geometry and Login/Register/Forgot Password semantics.
- Initial cumulative CI surfaced governance drift rather than a product regression; `docs/plans/active/ui-phase-9-public-auth.md` was missing required `## Research` and `## Evaluation` headings and this PR memory used a non-applicable snapshot phrase incorrectly. Both are corrected on the refreshed head.

## Evidence interpretation

Final Phase 9 evidence must include the exact cumulative PR head and selected policy/static/unit/build/browser/cross-device/security gates. A successful emulator matrix will not be reported as physical Android/iOS acceptance.