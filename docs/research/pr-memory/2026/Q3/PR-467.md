# PR #467 — docs: reconcile the board and memory baseline after PR #464

- Date: 2026-08-26
- Change class: Class 0 — documentation/lifecycle recovery. No code, schema, RLS, provider, CI or financial-semantics change.
- Affected capability or project boundary: `docs/plans/active/README.md` and `docs/research/CURRENT_PROJECT_MEMORY.md` header baselines and merged-state wording.
- Status impact: candidate until merged. It changes no capability truth; it makes the recorded baseline match the merged one.
- Lifecycle impact: recovery of stale lifecycle state. It does not complete a registered slice and does not promote one — `plan:resolve` still reports `current slice: none` after the change, and selecting the next #432 P2 slice remains an owner decision.
- Why: PR #464 merged as `3975007738a3cf383c11e73b9e6d9fdfccfb2f59` without carrying its own post-merge convergence, so both authorities still declared the PR #462 baseline `86ef47a1` with `Post-merge projection: PR #464`. The consequence was not cosmetic: `plan:resolve` stayed in `post-merge-projection` mode, and `check:knowledge` **failed on any branch cut from current main** because the declared baseline no longer matched the merge-base. It still passed when run on `main` itself, which is why the drift went unnoticed until a feature branch hit it.
- How it was found: while recording PR #466, the knowledge gate failed on that branch. Verifying the same gate on `main` showed it passing, which isolated the cause to branch merge-base comparison rather than to the parser change.
- Changed: both baselines set to the merged commit and the `Post-merge projection` lines removed; the `#463 / PR #464` board entry marked done with its merge SHA; three memory sentences that described #462 and #464 as post-merge candidates rewritten as merged truth; the `#432 P2` precondition corrected, since it read "after PR #464 merges" and that has happened; the NOW note no longer calls #464 candidate-only; and closed PR #431 recorded under OWNER DECISION as resolved on 2026-08-26, superseded by merged #432/#433 authority with its branch retained on origin.
- Kept separate on purpose: this was found while delivering the #465 parser defect fix (PR #466) and was blocking it. `AGENTS.md` sanctions a separate lifecycle-closeout PR as recovery-only for stale state, so it was not folded into the product fix. Mixing a board reconciliation into a parser change would have made both harder to review and to revert.
- Verified: `check:knowledge` passes; `plan:resolve` READY with `mode: declared-base` and `board baseline` matching `expected`; `test:ci-policy` 197 pass; `git diff --check` clean. No application, database or browser gate applies to a documentation-only change.
- Remaining: no successor #432 P2 slice is registered, and this PR deliberately does not register one. The warning `no current agent-executable slice is registered; only owner/external lanes may proceed` persists by design.
- Production/provider evidence: not applicable. No provider write, deployment or production-data access.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` is changed here, and that is the point of the PR — its recorded baseline was wrong. No capability, architecture, security or verification truth changes.
- Superseded issue, roadmap or claim: supersedes the stale `86ef47a1` baseline declaration and the pending-projection wording for #462 and #464. Does not supersede #432, #465, #403, #417, #418, #426 or any RRB gate.
