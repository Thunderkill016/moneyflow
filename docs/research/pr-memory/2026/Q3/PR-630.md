# PR #630 — megaplan C5: #559 parked with preserved exploration

- Date: 2026-09-21
- Change class: Class 0 (docs bookkeeping only)
- Affected capability or project boundary: `docs/plans/active/megaplan-q4-2026.md` C5 row; remote branch `archive/559-territory-exploration` (created outside this PR, at checkpoint commit `818b213f`); comment on issue #559.
- Status impact: #559 territory decision recorded as parked. The three-territory exploration checkpoint — previously local-reflog only and at risk of GC loss — is now durable on the remote archive branch. Resume path is explicit: owner picks a territory, a bounded packet is selected, and the Phase 0 failure postmortem (P0–P11, PRs #319–#340) runs before any runtime work.
- Changed: megaplan C5 row updated `owner decide` → `parked` with archive pointer.
- Verified: docs-only diff; policy/knowledge lanes cover the exact head.
- Remaining: none for bookkeeping; territory choice remains an owner decision.
- Production/provider evidence: not applicable.
- Superseded issue, roadmap or claim: the stale "(local reflog)" recovery note — reflog-only recovery is no longer the only option.
- Lifecycle impact: none.

## Boundaries

- The archived exploration includes an unauthorized landing rewrite; it is preserved as evidence only and must not merge as-is.

## Evidence limits

- Archive branch existence verified via `git push` output; content is the checkpoint commit itself, not a new review of the territories.
