# PR #381 — feat: refine manual capture and accounts

- Date: 2026-08-14
- Change class: Class 2 UI/browser delivery (the path classifier also protects account
  boundaries conservatively); no approval-boundary write is implied by the diff.
- Affected capability or project boundary: Manual Capture presentation, demo Account
  balance/register presentation, local account hierarchy and their existing financial,
  accessibility, runtime-mode and overlay contracts.
- Status impact: candidate only. Slice 2 remains the one active bounded packet; this draft
  does not start Phase E/F, replace Slice 1, alter Trust-program authority or make a
  production claim.
- Changed: 17 intentional files — active Slice 2 lifecycle/packet and PR memory; local
  Capture and Accounts components/CSS; transaction-store demo boundary; account-register
  reconciliation helper; and focused contract/unit tests. The owner's untracked physical
  phone-evidence file is excluded.
- Verified: the independent exact-head review found and this amendment fixes a P1 demo
  first-financial-paint defect: Accounts and account detail now withhold snapshot balances,
  totals and register entries behind an explicit reconciliation state until browser-ledger
  truth is ready. Focused regression tests prove a stored-ledger difference, exact income /
  expense / both transfer-leg reconciliation, initial list/detail withholding, and unchanged
  authenticated server ownership. Live demo expense, income and transfer proof including
  transfer neutrality; Accounts list/detail/create/edit/archive/restore/reconciliation-path
  proof; browser 390×844 and targeted 390×568 proof; unchanged Harness V2 capture at
  1440×900 and 390×844; `check:migrations`, `check:knowledge`, `test:ci-policy`,
  `check:architecture`, typecheck and focused tests passed. Sequential wrapper-contract
  suites pass 38/38 both on untouched `main@dd735700` and Slice 2, so the earlier
  aggregate-only wrapper behavior is local parallel contention, not a Slice regression.
- Remaining: final exact-head Class 2/provider checks and owner review/merge are required. Local
  `test:e2e` could not start while the owner demo server held port 3315. Local
  `test:ui-audit:pr` finished non-green with WebKit internal `page.goto` errors before
  assertions; the clean-worktree follow-up was blocked by that temporary worktree's external
  `node_modules` symlink, so neither result is classified as pre-existing or a Slice 2
  product regression. Aggregate `npm test` was invoked once, but terminal transport lost
  the final summary after the large parallel output; it is not reported as green.
- Production/provider evidence: none. No provider, production, database, Auth, CI workflow
  or Design Harness write occurred.
- Snapshot update: active registry, current memory and Trust parent continue to identify
  Slice 2 as active; this draft makes no future-phase authority change.
- Superseded issue, roadmap or claim: none.
