# PR #540 — patch runtime dependency tree

- Date: 2026-09-02
- Change class: Class 3 security/runtime dependency change under active packet #536; no database or provider mutation in this PR
- Affected capability or project boundary: Next.js runtime, image/native dependency chain, browser-target tooling, share-capture lifecycle, authentication/runtime regression surface, release security
- Status impact: advances #536 Phase B/C repository-side dependency remediation; #536 remains the active current slice because Supabase advisor/Auth-provider work is not closed here
- Changed: upgrades `next` 16.2.11 -> 16.3.4, aligns `eslint-config-next` 16.2.12 -> 16.3.4, preserves React / React DOM 19.2.4, updates `sharp` 0.35.0 -> 0.35.4, pins `browserslist` 4.28.8 and `qs` 6.16.0, regenerates `package-lock.json` with Node 22 / npm 10 semantics, hardens dependency-security floors, and makes Share Target ingestion Strict-Mode-safe by consuming its one-shot ref only after the scheduled frame actually starts
- Verified: Node 22.23.2 / npm 10.9.8 bootstrap resolved active #536 and `agent:doctor` reported `ready: true`; after the final `qs` remediation a clean exact-tree `npm ci` and `npm audit --audit-level=low` both succeeded; the previously failing shared-candidate Playwright regression passed after the Strict Mode lifecycle fix; final exact-head policy/static/unit/build/browser/UI/CodeQL/Gitleaks gates remain required after this memory commit
- Measured: the pre-fix browser smoke on the patched Next tree passed 134/136 cases and failed only the same Share Target redirect case on desktop and mobile; the failure trace stayed on the capture loading state, while the targeted regression passed after the lifecycle fix; no production performance improvement is claimed
- Remaining: finish exact-head repository/browser/UI/security gates; continue evidence classification of current Supabase `authenticated_security_definer_function_executable` WARN findings without bulk privilege rewrites; leaked-password protection remains disabled and any Auth config write requires explicit owner authorization plus rollback evidence; production remains unpatched until owner merge/deployment
- Production/provider evidence: read-only Supabase Security Advisor was refreshed again on 2026-09-02 and still reports the authenticated `SECURITY DEFINER` warning class plus `auth_leaked_password_protection`; live catalog evidence shows the warned set is not executable by `anon` or `PUBLIC`, is executable by `authenticated`, owned by `postgres`, hardened with empty `search_path`, and references `auth.uid()`; no database schema/grant/function mutation, Auth setting change, tenant ledger write, Vercel deployment or production deployment was performed
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` records the final dependency candidate including `qs` 6.16.0, the fresh-advisory chronology, the Share Target Strict Mode regression/fix, narrowed read-only Supabase evidence, and the still-open provider/deployment gaps without claiming production patched or #536 complete
- Superseded issue, roadmap or claim: Dependabot PR #524 is not used as the security fix because its earlier Next target did not meet the packet's August patched floor; the earlier branch statement that the exact tree had permanently reached zero vulnerabilities is superseded by a later fresh audit that surfaced newly published `qs` advisories and was remediated in this PR; #527 remains completed and is not reopened
- Lifecycle impact: advances active slice #536 through runtime dependency remediation and regression repair only; does not complete or archive #536, does not set `PLAN_AUTHORITY.current` to null, does not select follow-on work, and performs no provider write or deployment

## Dependency advisory evidence

The candidate moves Next.js to 16.3.4, above the packet's minimum patched 16.x floor of 16.3.3 for the 2026-08-25 Critical advisories. React 19.2.4 remains unchanged because this vetted Next patch does not require a React movement.

After the first Next/Sharp lock regeneration, exact `npm audit --json` exposed one High result for `browserslist <=4.28.6`, via GHSA-c83g-rgw3-j3cx and GHSA-73wf-gq98-2v4g. The PR pins stable 4.28.8 rather than using broad `npm audit fix --force`; at that point the exact tree returned zero findings.

A later fresh audit on the same 2026-09-02 workstream surfaced a newly available Moderate `qs` result at 6.15.3: GHSA-x5fp-wj9c-mxmx and GHSA-4mjr-xmp4-gh2g. Both existing parent ranges accepted the patched 6.16.0 release, so the PR adds a minimal exact override instead of broad modernization. A Node 22 runner then regenerated the lock, restored the exact shipped tree with `npm ci`, and `npm audit --audit-level=low` completed successfully with no remaining audit finding. The permanent dependency guard now requires every resolved `qs` copy to be at least 6.16.0.

Upstream references refreshed during implementation:

1. Next.js security release / advisories: https://nextjs.org/blog and the linked GitHub security advisories
2. Browserslist GHSA-c83g-rgw3-j3cx and GHSA-73wf-gq98-2v4g
3. qs GHSA-x5fp-wj9c-mxmx and GHSA-4mjr-xmp4-gh2g; stable patched release 6.16.0

## Browser regression found by the patch

The first full browser smoke after the dependency patch passed 134/136 tests. Both failures were the same Share Target path at desktop/mobile widths: `/capture/share` remained on the Vietnamese loading state instead of ingesting the staged payload and redirecting to `/inbox`.

`CaptureSharePage` had marked its one-shot `ranRef` before scheduling work in `requestAnimationFrame`, while effect cleanup cancelled the frame. React development Strict Mode can run effect setup -> cleanup -> setup; that sequence could cancel the first frame after the ref was consumed and make the second setup exit immediately. The fix leaves the ref unconsumed until the frame callback actually starts, so a cancelled development probe cannot strand ingestion. The exact previously failing shared-candidate Playwright case passed on a GitHub-hosted Chromium runner after this change. Assertions were not weakened.

## Lock-generation and preflight evidence

The conversation sandbox did not expose a usable repository checkout, so temporary branch-only GitHub Actions runners were used for exact Node 22 lock generation and targeted browser reproduction. Each temporary workflow was removed from the branch after producing its evidence; no `tmp-536-*` workflow belongs in the final PR diff.

Bootstrap evidence included `npm run plan:resolve` -> active #536 and `npm run agent:doctor -- --json` -> `ready: true`. Temporary tooling installation was followed by fresh `npm ci`, so audit evidence applies to the shipped tree rather than the bootstrap tools.

## Live Supabase advisor reconciliation

Read-only provider evidence was refreshed after dependency work. Security Advisor continues to report authenticated-callable `SECURITY DEFINER` warnings across ledger/planning/reconciliation/import/archive RPC surfaces and separately reports leaked-password protection disabled.

Live catalog reconciliation of the warned function set found no `anon` or `PUBLIC` execute grant, retained intended `authenticated` execute, `postgres` ownership, empty `search_path`, and an `auth.uid()` reference in every warned body. The identity-parameter case `reconciliation_snapshot_for_user(p_user_id, ...)` explicitly rejects a caller when `p_user_id is distinct from auth.uid()`. Representative mutation functions inspected live derive tenant identity from `auth.uid()` and bind effects to that identity. Repository `cross_tenant_rpc.test.sql` and `browser_role_privileges.test.sql` provide additional ownership/role regression coverage.

This narrows the lint class to intentional privileged authenticated RPC surfaces rather than accidental anonymous/public exposure, but it is not a blanket waiver. Each surface still needs evidence-based disposition before suppression or privilege change. No `SECURITY DEFINER` function was converted, moved or revoked here.

Supabase remediation reference: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable
Leaked-password reference: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

## Permission boundary

Repository dependency changes, regression repair and read-only provider inspection are within active #536. Merge and deployment remain owner decisions. Any future Supabase Auth write, including enabling leaked-password protection, must first record the previous value/rollback and receive explicit owner authorization. This PR grants no such authorization and performs no production write.
