# PR #727 — Design leftovers: reports link target, capture width, dark sweep

- Date: 2026-09-25
- Change class: presentation-only CSS (Class 1) — two CSS modules; no
  runtime logic, schema, or contract change.
- Affected capability or project boundary: `/reports` footnote prose and
  `/capture` chooser layout. CSS-module ownership preserved; no global
  overrides added.
- Superseded issue, roadmap or claim: the three remaining findings from
  the PR-719 UI audit — reports inline "Tùy chọn xuất" link measured
  ~96×16px; `/capture` stayed a 640px strip on desktop; whole-app
  dark-mode visual sweep left open.
- Status impact: the inline link now boxes at 108×44px at both 1280px
  and 393px viewports (Playwright `boundingBox` evidence), clearing the
  44px target audit; `/capture` widens to 760px and renders its options
  two-up at ≥720px while phones keep the single-column stack. The
  dark-mode sweep is closed with evidence: dashboards/capture/reports/
  accounts render coherently; remaining raw whites are intentional
  inverse-brand and provider-mark constants.
- Design basis: padding + matching negative margin expands the tap area
  without reflowing prose (WCAG 2.2 exempts inline links, but the audit
  flag was real); 760px joins the existing settings/detail width
  cluster rather than inventing a new breakpoint.
- Changed: `src/components/reports-page.module.css`,
  `src/components/inbox/capture-page.module.css`.
- Verified: css-ownership clean, lint/typecheck clean, capture-page and
  reports-trend contract tests pass; browser-verified desktop + mobile +
  dark theme.
- Remaining: none for these findings; the `minimum-target-size`
  responsive audit lane re-verifies `/reports` in CI.
- Production/provider evidence: none — local browser evidence only.
