# PR #447 — chore(governance): converge completed slices in the same PR

- Date: 2026-08-23
- Change class: Class 3 — task-selection/lifecycle governance policy; no product runtime or financial semantics change.
- Affected capability or project boundary: fail-closed plan selection, current-slice completion lifecycle, Current Work Board freshness, active→completed packet transition, current project memory convergence, PR memory, and CI classification of the governance guard.
- Lifecycle impact: completes current slice — one-time recovery of already-merged #442, while installing same-PR convergence so later current-slice PRs do not require routine cleanup PRs.
- Status impact: candidate governance hardening. If merged, #442/#445 truth is reconciled and future current-slice completion is mechanically required to converge board + packet + memory in the same implementation PR before owner handoff.
- Changed: reconciles `main@e0b30350…` after #445; removes #442 from NOW/current registry and archives its packet; records #445 exact merge/evidence; adds `scripts/lifecycle-projection.mjs` + adversarial tests; wires it into `check:knowledge`; makes lifecycle-policy files select every CI gate; blocks an activated projection that still retains current work; updates AGENTS/plan/memory/PR-template rules from dedicated-closeout-by-default to same-PR convergence; carries `Post-merge projection: PR #447` in board and current memory.
- Selection boundary: a completing PR projection must leave zero current agent-executable slices. While unmerged, projection keeps task selection NOT READY; after its exact squash merge it becomes lifecycle truth. Follow-on work remains NEXT only and requires fresh-main resolution plus its own bounded issue/spec/packet.
- Research: current official GitHub docs confirm squash merge consolidates a PR into one base-branch commit; pull-request workflows normally test the synthetic merge ref; and the merge REST API can bind a merge to an expected head SHA. The repository therefore keeps exact-PR projection activation and same-head acceptance instead of adding a post-merge bot/direct-main write.
- Verified: pending final exact-head Class 3 CI after the systemic guard changes. Earlier docs-only/draft runs are historical diagnostics only and cannot be used as final evidence.
- Remaining: run the new lifecycle and existing authority/policy tests, inspect full exact-head CI/database/browser/UI/CodeQL/secret-history evidence selected by the governance classifier, fix root cause of any failure, then hand off to owner. Merge remains the owner decision.
- Production/provider evidence: not applicable. No provider, deployment, production-data, schema, RLS or financial mutation is in scope.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records merged #445 truth, PR #447 projection, and the new same-PR lifecycle rule; no next product child is promoted.
- Superseded issue, roadmap or claim: supersedes the repeated pattern “merge feature → open a second lifecycle-closeout PR”. Dedicated reconciliation remains recovery-only for legacy/stale state or exceptional merge races.
- Rollback: revert PR #447. The repository returns to #444 fail-closed authority plus the older dedicated-closeout lifecycle, and task selection will fail closed on any stale board rather than guessing.
- Untrusted external instructions copied into project memory: none.
