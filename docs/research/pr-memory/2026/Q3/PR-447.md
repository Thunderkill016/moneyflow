# PR #447 — docs(governance): reconcile PR #445 merge truth

- Date: 2026-08-23
- Change class: Class 0 — documentation/governance lifecycle reconciliation; no runtime or financial semantics change.
- Affected capability or project boundary: fail-closed plan selection after merged #445; Current Work Board freshness, #442 packet lifecycle, current project memory and PR #445 delivery provenance.
- Status impact: candidate reconciliation. If merged, repository authority/memory will describe already-shipped #445 truth and no merged #442 artifact will remain current/active.
- Changed: advances board baseline to `main@e0b30350c1e819237ce769a9d5af40cc2d0324c0`; removes #442 from NOW/current agent registry; archives its completed packet; records #445 exact merge and final-head verification evidence; updates current memory from pre-#445 candidate truth to shipped #445 capability; carries `Post-merge projection: PR #447` so this dedicated closeout can remain board-fresh after its own squash merge.
- Selection boundary: PR #447 deliberately leaves no current agent-executable product/governance child. Source lifecycle → ledger/reconciliation policy remains NEXT only and may not be started from this unmerged projection. A fresh-main resolver pass after merge is required before a new bounded child can be created/promoted.
- Research: official GitHub documentation confirms squash merge consolidates a PR into one base-branch commit and closing keywords such as `Closes #446` close the linked issue after merge. These mechanics support the repository's existing explicit projection/closeout protocol; repository code/tests remain the authority for plan selection behavior.
- Verification contract: draft CI is not acceptance because draft-only runs may skip policy/static shards. Before owner handoff, the final exact head must have the risk-selected ready-for-review CI green; provider check identities remain live GitHub evidence rather than self-referential future run IDs embedded in this commit. `plan-selection` is expected to remain NOT READY while the projection is unmerged, while authority/knowledge validation must remain internally consistent.
- Remaining: no implementation work is authorized. Owner handoff occurs only after exact-head ready-for-review checks are inspected; merge remains the owner decision.
- Production/provider evidence: not applicable. No provider, deployment, production-data, schema, RLS or financial mutation is in scope.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` sections 3–12 reconcile merged #445 capability, execution state, inventory, gaps and next allowed action.
- Superseded issue, roadmap or claim: supersedes only stale post-merge prose that still named #442 as NOW/current and `99257178…` as current main baseline. It does not supersede #432 or select the next P1 child.
- Rollback: revert PR #447; plan selection will intentionally fail closed again on stale board state rather than silently choose work.
- Untrusted external instructions copied into project memory: none.
