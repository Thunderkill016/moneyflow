# PR #447 — feat(agent): replace dispatcher with an event-sourced capability harness

- Date: 2026-08-23
- Change class: Class 3 — agent execution/security/task-selection governance boundary; no MoneyFlow financial product semantics change.
- Affected capability or project boundary: repository-local agent execution, GitHub command admission, fresh-main worktree isolation, child Git/GitHub permission guard, local run persistence/replay safety, plan/lifecycle convergence and CI classification of agent-governance code.
- Lifecycle impact: completes current slice — one-time recovery of already-merged #442, while installing same-PR convergence so later current-slice PRs do not require routine cleanup PRs. PR #447's harness-v2 packet is bounded implementation inside this already-started recovery/governance PR and is not a newly promoted product child.
- Status impact: candidate agent-governance hardening. If merged, the old monolithic dispatcher is replaced by the provider-neutral harness, #442/#445 truth is reconciled, and future current-slice completion is mechanically required to converge board + packet + memory in the same implementation PR before owner handoff.
- Changed: replaces `scripts/agent-dispatcher/*` with `scripts/agent-harness/`; `npm run agent:dispatch` and `agent:dispatch:watch` now use the v2 CLI; introduces a named capability context, append-only JSONL run journals, v1 dispatcher-state migration, source/workspace/permission/Codex providers, provider capability negotiation, holder-owned agent run handles, serial watch polling, token stripping and the full preserved Git/GitHub security matrix. Also reconciles `main@e0b30350…` after #445 and installs the same-PR lifecycle projection guard.
- Selection boundary: a completing PR projection must leave zero current agent-executable slices. While unmerged, projection keeps task selection NOT READY; after its exact squash merge it becomes lifecycle truth. Harness provider registration does not grant product/provider/production authority; follow-on product work remains NEXT only and requires fresh-main resolution plus its own bounded issue/spec/packet.
- Research: official DeepSeek Harness architecture, core/tools, session, subagent/workflow, sandbox/approval and capability-seam documentation established the patterns adopted here: thin coordinator, Service Definition → Provider → Consumer seams, append-only reconstructable state, fail-loud capability negotiation, guarded execution boundaries and holder-owned run cleanup. DeepSeek Harness/Cordis itself, dynamic self-modification, runtime plugin installation and unrestricted agent swarms were explicitly not adopted because the upstream project is developer preview and MoneyFlow already has stricter repository/financial authority boundaries.
- Verified: pending final exact-head Class 3 CI after harness-v2 implementation, legacy-dispatcher removal and durable-doc updates. A diagnostic pre-removal head proved unit/static RLS, static quality and production build green; it is historical only. Earlier docs-only/draft runs are also not final evidence.
- Remaining: run/inspect the final lifecycle, harness, security, authority and CI-policy tests plus exact-head database/browser/UI/CodeQL/secret-history gates; fix root cause of any failure; then hand off to owner. Merge remains the owner decision.
- Production/provider evidence: not applicable. No provider, deployment, production-data, schema, RLS or financial mutation is in scope.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` carries PR #447's post-merge projection for both the harness-v2 replacement and same-PR lifecycle rule; no next product child is promoted.
- Superseded issue, roadmap or claim: supersedes both “the local Codex dispatcher is the repository agent runtime” and the repeated pattern “merge feature → open a second lifecycle-closeout PR”. Dedicated lifecycle reconciliation remains recovery-only for legacy/stale state or exceptional merge races.
- Rollback: revert PR #447. The code entrypoint returns to the v1 dispatcher only if its deleted files are restored by that revert; legacy `.agent-dispatcher/state.json` is deliberately not deleted by migration. Plan selection returns to #444 fail-closed authority plus the older dedicated-closeout lifecycle and will fail closed on stale board state rather than guessing.
- Untrusted external instructions copied into project memory: none.