# PR #679 — Obligations-aware remainder line on the dashboard statement

- Date: 2026-09-22
- Change class: Class 2 (bounded UI — one quiet line on the dashboard statement over a new tested domain module)
- Affected capability or project boundary: `src/lib/planning/committed-remainder.ts` (new pure derivation + label builder + `gaps` refusal contract), `src/lib/planning/committed-remainder.test.ts` (18 cases), `src/components/moneyflow-dashboard.tsx` (computes over already-loaded commitments/accounts and passes a finished sentence), `src/components/dashboard/dashboard-overview-sections.tsx` + `statement.tsx` + `statement.module.css` (renders the line under the standing-figure caption, linking to /commitments), `src/lib/dashboard-phase4-contract.test.ts` (wiring contract).
- Status impact: adds an honest obligations-aware annotation to Home — `currentBalance − unpaid declared commitments this month`, labeled as exactly that. Canon Stage 2 territory ("no fake safe-to-spend without the data") served by refusing to render: `complete=false` and no line whenever an input cannot support the claim (missing/invalid amount, undateable unpaid occurrence, non-VND or unparseable account currency, unsafe balance, malformed month). Zero unpaid commitments this month renders nothing rather than "sau 0 khoản" implying coverage.
- Changed: `buildCommittedRemainder` returns `{remainder|null, obligationTotal, obligationCount, expectedIncome, expectedIncomeCount, expectedIncomeIncluded, complete, gaps}`; negative remainder is kept and labeled `Thiếu X ₫ cho N khoản…`. Expected income templates are modeled and disclosed (`chưa gồm`/`kể cả … thu dự kiến`) but the dashboard does not hydrate income-template rows into its client boundary — the THU-41/#426 contract (`insights-goals-card.test.ts`) keeps the full planning surface out, so the shipped line is the plain obligations remainder. Demo runs the same derivation over demo seeds + hydrated occurrences; the seeded USD fixture account means demo exercises the withhold path by design.
- Verified: committed-remainder.test 18/18, dashboard-phase4-contract 9/9, full suite 1614/1614, typecheck, lint (1 pre-existing warning), build 51 routes, check:knowledge, ci-policy 191/191, check:architecture, check:css-ownership.
- Remaining: remainder excludes expected income on the dashboard by deliberate boundary choice — a consumer holding template rows can opt into `includeExpectedIncome`. Demo never shows the number while the USD fixture exists (refusal path is the demo-visible behavior). No browser-rendered evidence of the line yet — delegated to CI lanes.
- Production/provider evidence: none (read-only derivation, no migration; production verification remains an owner decision post-merge).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: if users read the remainder as spendable anyway, revisit copy; if income folding or a `chưa đủ dữ liệu` visible state is wanted, the module already carries both seams (`expectedIncome*`, `gaps`).
