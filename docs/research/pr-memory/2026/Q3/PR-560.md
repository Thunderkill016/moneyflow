# PR #560 — MoneyFlow web redesign foundation selector

- Date: 2026-09-10
- Status: selector candidate; executable only after owner merge
- Base: `main@ad0461514acaec1c9a8a100ba92592c83ece46b2`
- Change class: Class 0 authority/planning change selecting a Class 2 multi-flow redesign research/specification packet; no runtime, schema, provider or production mutation in this PR.
- Affected capability or project boundary: executable plan authority, design system v3 specification, information architecture, visual identity territories, failure postmortem, current project memory.
- Status impact: authority transition candidate from `PLAN_AUTHORITY.current: null` to bounded issue #559 through `selectedByPr: 560`; runtime implementation remains blocked before owner merge plus fresh authority/doctor resolution.
- Changed: adds the #559 foundation packet, projects `PLAN_AUTHORITY.current` to it via `selectedByPr: 560`, records historical failure postmortem (F1–F10 + P0–P11 retrospective in `docs/research/DESIGN_FAILURE_POSTMORTEM_2026.md`), full 47-route inventory, Design System v3 specification, 3 visual territories, and reconciles current project memory.
- Verified: selector starts from exact post-MON-63 `main@ad0461514acaec1c9a8a100ba92592c83ece46b2`; merged-main authority before this selector is `current: null`; `node scripts/plan-authority.mjs` resolves candidate status for PR #560; `node scripts/lifecycle-projection.mjs` validates authority transition; knowledge contracts pass.
- Repository reconnaissance: forensic analysis of merged PRs (#319, #336, #337, #339, #340, #321) identified 10 recurring failure modes: presentation ownership gaps (unowned classes in onboarding), token/runtime mismatches, cascade layer conflicts, runtime-mode false greens (demo mode masking auth in CI), AppShell geometry hydration shifts, sync/async ledger persistence gaps, mobile primary action hijacking, and brand/token authority fragmentation. PR #319 had to delete 9,660 lines of accumulated global CSS across 6 override stylesheets.
- External research boundary: W3C WCAG 2.2 AA (24x24px target floor, >=44px mobile touch targets, focus visibility), WAI-ARIA APG (semantic tables vs interactive grids, focus containment in dialogs), MDN (CSS container queries, prefers-reduced-motion), and modern fintech patterns (Wise, Linear, Copilot, YNAB). These sources inform workflow and accessibility principles but do not establish MoneyFlow's visual identity or Vietnamese banking semantics.
- Architecture decision: maintain strict presentation authority with exactly 2 root CSS files (`document-theme.css` and `legacy.css`), 27 scoped CSS Modules, zero unowned classes, double-sided token proof, and strict mode assertions distinguishing demo from authenticated tests.
- Visual territories: established three distinct design directions for owner decision: (1) "Kỷ Hà Đương Đại" (Swiss Grid / architectural precision ledger), (2) "Dòng Chảy Xanh" (tactile flow / modern fintech porcelain), and (3) "Bản Ghi Bản Lĩnh" (editorial craft ledger).
- Migration strategy: 11 vertical slices (Slices 0 to 10) with immediate legacy retirement per slice; no new global override layers.
- Remaining: latest exact selector head must pass project knowledge/lifecycle, CI, CodeQL, Secret History, mergeability and thread checks. Only explicit owner merge may activate #559. After merge, owner selects visual territory, then implementation proceeds in bounded vertical slices starting with Slice 1 (Tokens & Shared Primitives).
- Production/provider evidence: read-only documentation evidence only. PR #560 performs no Supabase DB/Auth/provider write, CSS/runtime mutation, Vercel/WAF mutation, environment-secret change or production-data write.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` is updated in this PR with merge-stable #560 candidate/active projection semantics, design foundation baseline, and visual territory selection boundaries.
- Superseded issue, roadmap or claim: supersedes the assumption that UI redesign can begin with screen rewrites or global stylesheet additions without establishing design authority, route inventory, and presentation ownership guardrails.
- Lifecycle impact: authority transition from `PLAN_AUTHORITY.current: null` to `docs/plans/active/559-web-redesign-foundation.md` if #560 is owner-merged; this selector does not complete #559.
- Production writes: none.
- Merge: explicit owner authorization required.

## Focused source record

1. MoneyFlow `docs/research/DESIGN_FAILURE_POSTMORTEM_2026.md` — forensic postmortem of failures F1–F10 across PRs #319, #321, #336, #337, #339, #340.
2. MoneyFlow `docs/plans/active/559-web-redesign-foundation.md` — bounded foundation packet with 47-route inventory, Design System v3 specification, and visual territories.
3. MoneyFlow `src/app/document-theme.css` and `src/app/legacy.css` — current presentation root authority.
4. MoneyFlow `scripts/check-code-css-ownership.mjs` — presentation ownership gate.
5. W3C WCAG 2.2 AA and WAI-ARIA Authoring Practices Guide — accessibility standards.
6. MDN Web Docs — CSS Container Queries and prefers-reduced-motion specifications.

## Permission boundary

#560 is a selector and foundation specification only. It may change planning authority if owner-merged. It authorizes no runtime UI code change, screen rewrite, component redesign, new global CSS stylesheet, schema/RLS change, financial behavior, or production-data write.

If owner merges #560, runtime implementation begins only in subsequent vertical slices (Slice 1: Tokens & Primitives) after explicit owner selection of visual territory.
