# PR #646 — Reports six-month category trend strip (R1)

- Date: 2026-09-21
- Change class: Class 2 (bounded UI on one surface + additive capability output field)
- Affected capability or project boundary: `src/lib/reports.ts`, `src/server/reports.ts`, `src/components/reports-page.tsx` + module CSS, `src/server/capabilities/reports-financial.ts`, regenerated `docs/agents/capabilities.json` and `__golden__/reports-financial.json`.
- Status impact: R1 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — implemented. R2 verified already shipped (export route + primary action existed before this PR; no change needed).
- Changed: each `report.categories` entry gains `trend` — six `YYYY-MM` months ending at the viewed month, amounts via a shared `expenseCategoryLines()` so strips and share totals cannot diverge on split rows. `categoryTrendWindowStart` extends the loader bound (`min(previousStart, trendStart)`) — same query/columns, no second path. Rows render as calm bars scaled to the row's own max; zero months are baseline stubs; aria-label carries all six amounts.
- Verified: TDD — 4 new tests (keys/labels, split distribution, out-of-window exclusion, year/leap bounds); `npm test` 1460/1460; lint/typecheck/build/css-ownership/architecture/capabilities/deployment-env/knowledge/ci-policy pass. Browser-verified demo at 390px + 1366px — strips render, zero stubs honest (demo samples span current month only), no money overflow.
- Remaining: exact-head CI; owner merge decision.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes R1; R2 confirmed pre-existing. Next in plan order: S1 (backup-reminder chip), then X1/X2.
