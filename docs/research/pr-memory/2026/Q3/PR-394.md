# PR #394 — test: prove WCAG 2.2 accessible authentication mechanisms

- Date: 2026-08-15
- Change class: Class 2 browser/accessibility proof over current authentication UI; no provider/database/deployment/production mutation.
- Affected capability or project boundary: Release Readiness blocker RRB-07 — scoped WCAG 2.2 SC 3.3.8 Accessible Authentication (Minimum) evidence for MoneyFlow-owned login/re-auth/recovery UI.
- Status impact: candidate — RRB-07 remains BLOCKED until this current-main proof itself runs and passes exact-head delivery.
- Changed: one focused `e2e/audit/accessible-authentication.audit.spec.ts` proves email/password purpose semantics, real clipboard paste, OAuth alternative visibility and recovery/update password-manager mechanisms; `playwright.audit.config.ts` now routes this spec into the existing `chromium-keyboard-desktop` PR audit project. No runtime UI change is made unless evidence exposes a defect.
- Research: W3C WCAG 2.2 SC 3.3.8 and its current Understanding document were used as primary authority. Password recall is a cognitive-function test, while password-manager autofill/copy-paste can provide a mechanism; OAuth may be an alternative; recovery/change steps remain inside the authentication boundary. Account creation is supporting evidence rather than the core conformance target.
- Evidence boundary: the claim is scoped to MoneyFlow-owned browser mechanisms. The repository does not certify real Google OAuth or every Cloudflare Turnstile provider-managed challenge. `AuthTurnstile` uses `appearance: interaction-only`, but that source setting alone is not treated as provider accessibility proof.
- Verified: base is exact `main@7d47ca2f088a190a7422758f41bc844bcb07b6bb` after #392; existing `AuthForm` uses `type=email` + `autocomplete=email`, `AuthPasswordField` uses `current-password` or `new-password`, and no paste-blocking handler was found. Existing `auth-and-amount.audit.spec.ts` already pins password reveal/autocomplete semantics but not clipboard paste/recovery/OAuth mapping. Ready-state CI #2502 selected the correct risk gates but stopped deterministically at the knowledge contract because this record initially used the wrong verification field name; that metadata defect was fixed without weakening the checker. Corrected CI #2503, CodeQL #1576 and Secret history #1576 all passed on head `1e24ebbf974a06b3345af940868bf5f72393d958`, and browser smoke/UI-audit/e2e aggregators were green. However, inspection of UI-audit artifact `ui-audit-evidence-31863334221-1` (digest `sha256:3362c834fb8aebbbd3a17a585899f5640a3a6566dbb5dacdeeff321f1bafa66a`) showed overall `692` executions / `551` expected / `0` unexpected / `0` flaky but **did not contain `accessible-authentication.audit.spec.ts` at all**. Root cause: every audit project uses an explicit `testMatch` allowlist, and the new spec was not assigned to any project. Therefore the prior green UI-audit was not RRB-07 evidence. The config now assigns the spec to `chromium-keyboard-desktop`, a project already included in `test:ui-audit:pr`; viewport/browser expansion is intentionally avoided because the scoped SC 3.3.8 mechanisms are not responsive-layout claims.
- Remaining: the new routed head must run policy/static/UI-audit/browser evidence from scratch and the resulting UI-audit artifact must explicitly contain the Accessible Authentication tests with no unexpected/flaky result. Then freeze one final evidence head, record provenance, rerun exact-head CI/CodeQL/Secret history, and recheck base/reviews/threads before merge.
- Production/provider evidence: not claimed. Real provider challenge/OAuth behavior remains provider/runtime evidence.
- Snapshot update: not applicable while RRB-07 proof is candidate. Lifecycle closeout after merge owns Current Work/current-memory transition.
- Superseded issue, roadmap or claim: none yet. A successful merge will supersede the audit claim that current main lacks explicit Accessible Authentication proof.
