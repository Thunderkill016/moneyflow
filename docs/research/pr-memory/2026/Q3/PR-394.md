# PR #394 — test: prove WCAG 2.2 accessible authentication mechanisms

- Date: 2026-08-15
- Change class: Class 2 browser/accessibility proof over current authentication UI; no provider/database/deployment/production mutation.
- Affected capability or project boundary: Release Readiness blocker RRB-07 — scoped WCAG 2.2 SC 3.3.8 Accessible Authentication (Minimum) evidence for MoneyFlow-owned login/re-auth/recovery UI.
- Status impact: candidate — RRB-07 remains BLOCKED until this current-main proof passes exact-head delivery and merges.
- Changed: one focused `e2e/audit/accessible-authentication.audit.spec.ts` proves email/password purpose semantics, real clipboard paste, OAuth alternative visibility and recovery/update password-manager mechanisms; no runtime UI change is made unless evidence exposes a defect.
- Research: W3C WCAG 2.2 SC 3.3.8 and its current Understanding document were used as primary authority. Password recall is a cognitive-function test, while password-manager autofill/copy-paste can provide a mechanism; OAuth may be an alternative; recovery/change steps remain inside the authentication boundary. Account creation is supporting evidence rather than the core conformance target.
- Evidence boundary: the claim is scoped to MoneyFlow-owned browser mechanisms. The repository does not certify real Google OAuth or every Cloudflare Turnstile provider-managed challenge. `AuthTurnstile` uses `appearance: interaction-only`, but that source setting alone is not treated as provider accessibility proof.
- Verified before CI: base is exact `main@7d47ca2f088a190a7422758f41bc844bcb07b6bb` after #392; existing `AuthForm` uses `type=email` + `autocomplete=email`, `AuthPasswordField` uses `current-password` or `new-password`, and no paste-blocking handler was found. Existing `auth-and-amount.audit.spec.ts` already pins password reveal and autocomplete semantics but not clipboard paste/recovery/OAuth mapping.
- Remaining: run policy/static/UI-audit/browser evidence; classify any failure without weakening security or the SC 3.3.8 contract. Once stable, mark non-draft for full risk-selected checks, then require exact-head CI/CodeQL/Secret history and clean review/thread/base state before merge.
- Production/provider evidence: not claimed. Real provider challenge/OAuth behavior remains provider/runtime evidence.
- Snapshot update: not applicable while RRB-07 proof is candidate. Lifecycle closeout after merge owns Current Work/current-memory transition.
- Superseded issue, roadmap or claim: none yet. A successful merge will supersede the audit claim that current main lacks explicit Accessible Authentication proof.
