# PR #599 — restore required security scans

- Date: 2026-09-20
- Change class: Class 3 CI/security policy repair
- Affected capability or project boundary: required GitHub security workflows
- Status impact: none
- Changed: retain checkout's read-only credential for the existing private-repository all-ref fetch; run the pinned CodeQL queries without unavailable SARIF/database upload; add offline regression guards and align the risk policy with the repository's actual provider capability
- Verified: 191 CI-policy tests; typecheck; workflow-pin and knowledge contracts; 1,334 unit tests and production build; exact Gitleaks 8.30.1 scan over 3,837 commits; 148 demo browser tests; 30 authenticated browser passes with one configured skip; 595 UI-audit passes with 141 configured skips
- Remaining: fresh exact-head CI and local CodeQL evidence after the provider finding, owner review and merge decision
- Production/provider evidence: with explicit owner approval, 1,986 GitHub Actions artifacts already marked expired (29,058,215,460 bytes) were permanently deleted; both unexpired artifacts were retained and the provider API reports two artifacts total
- Superseded issue, roadmap or claim: none

## Failure evidence

- PR #598 and the latest scheduled `main` runs both reproduced the failures, establishing that its dependency-guard diff did not cause them.
- Gitleaks failed before scanning because `persist-credentials: false` removed authentication before the explicit private-repository fetch.
- CodeQL scanned and exported SARIF. After metadata read access was added, GitHub returned the underlying error: code scanning is unavailable because this private repository does not have GitHub Code Security.
- Browser smoke completed both test commands on PR #598, then artifact upload failed because Actions storage quota was exhausted.

## Boundaries

- Required check identities, triggers, scan commands and fail-closed behavior are unchanged.
- Gitleaks remains `contents: read`; CodeQL uses read-only `actions`, `contents` and `packages` access, with `upload: never` and database upload disabled.
- The owner explicitly selected real local CodeQL analysis instead of changing repository visibility or purchasing/enabling GitHub Code Security.
- No branch protection, ruleset, secret, deployment, application, database or production-data state changes are included.
- Local database verification was unavailable because Docker is absent; fail-safe provider CI selects the real database gate.
