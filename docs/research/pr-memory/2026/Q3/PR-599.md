# PR #599 — restore required security scans

- Date: 2026-09-20
- Change class: Class 3 CI/security policy repair
- Affected capability or project boundary: required GitHub security workflows
- Status impact: none
- Changed: retain checkout's read-only credential for the existing all-ref fetch; grant CodeQL its required metadata and SARIF-upload permissions; add offline regression guards and align the risk policy with provider capability
- Verified: 191 CI-policy tests; typecheck; workflow-pin, knowledge, migration and architecture contracts; npm audit with zero vulnerabilities; 1,334 unit tests and production build; exact Gitleaks 8.30.1 scan over 3,837 commits; 148 demo browser tests; 30 authenticated browser passes with one configured skip; 595 UI-audit passes with 141 configured skips. On head `e770aee56361bb260cdfee1e5b790bb6fc3f2727`, CodeQL `35462743313`, Gitleaks `35462743314` and full CI `35462743409` passed; provider CodeQL recorded zero results across 87 rules.
- Remaining: owner review and merge decision after the final bookkeeping head repeats exact-head checks
- Production/provider evidence: with explicit owner approval, 1,986 GitHub Actions artifacts already marked expired (29,058,215,460 bytes) were permanently deleted; both unexpired artifacts were retained. The owner subsequently approved changing the repository from private to public after official CodeQL terms ruled out automated private-repository analysis without a paid entitlement; GitHub now reports public visibility with secret scanning and push protection enabled. The accepted CI run uploaded all three evidence artifacts, and the API reports five unexpired artifacts totaling 44,686,909 bytes.
- Superseded issue, roadmap or claim: none

## Failure evidence

- PR #598 and the latest scheduled `main` runs both reproduced the failures, establishing that its dependency-guard diff did not cause them.
- Gitleaks failed before scanning because `persist-credentials: false` removed authentication before the explicit private-repository fetch.
- CodeQL scanned and exported SARIF. After metadata read access was added, GitHub returned the underlying error: code scanning was unavailable while the repository was private without GitHub Code Security.
- Browser smoke completed both test commands on PR #598, then artifact upload failed because Actions storage quota was exhausted.

## Boundaries

- Required check identities, triggers, scan commands and fail-closed behavior are unchanged.
- Gitleaks remains `contents: read`; CodeQL uses read-only `actions`, `contents` and `packages` access plus `security-events: write` solely for SARIF upload.
- After the audit found that CodeQL's license excludes automated analysis of private repositories without a paid entitlement, the owner explicitly selected public repository visibility rather than purchasing GitHub Code Security or replacing the scanner.
- No branch protection, ruleset, secret, deployment, application, database or production-data state changes are included. Repository visibility changed from private to public as a separately approved provider operation.
- Local database verification was unavailable because Docker is absent; fail-safe provider CI selects the real database gate.
