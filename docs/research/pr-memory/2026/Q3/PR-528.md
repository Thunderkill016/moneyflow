# PR #528 — select #527 production load performance

- Date: 2026-09-02
- Change class: Class 0 planning/authority change that selects a future bounded performance implementation slice; no runtime behavior changes in this PR.
- Affected capability or project boundary: current task selection, canonical `/` and authenticated `/dashboard` performance evidence, and performance work-packet authority.
- Lifecycle impact: selects current slice. If merged, #527 becomes the single `current agent-executable` slice; this PR does not implement or complete it.
- Status impact: candidate selection; production/runtime behavior is unchanged by this PR.
- Research: fresh Vercel production project/deployment read-back and direct public-origin fetch; current MoneyFlow source; completed #403/#415/#419/#483 performance evidence; current Vercel/Next.js performance guidance. Historical evidence is used as attribution provenance only and is not substituted for the required fresh baseline.
- Changed: added `docs/plans/active/527-production-load-performance.md`; reconciled the board to merged `main@d6ff88bff08d9efc3bfca828ce861b15b4cc0620`; selects #527 after owner merge and keeps #523 as candidate NEXT. No runtime, schema, RLS, Auth, provider, deployment or financial-semantic file changed.
- Verified: production `/` is HTTP 200, `x-vercel-cache: HIT`, `x-nextjs-prerender: 1`; anonymous `/` skips Supabase auth when no auth cookie exists. Current code still places authenticated dashboard factual sections under a top-level client boundary and a shared authenticated client shell. Completed #403 evidence found client-JS/main-thread render delay as the remaining measured bottleneck and PR #483 recorded the loading-boundary attribution experiment as negative. Exact-head repository CI on this planning branch remains authoritative.
- Remaining: owner merge of this planning PR; then fresh-main `npm run plan:resolve` and `npm run agent:doctor -- --json`; fresh current-main analyzer/Lighthouse baseline; measured mechanism selection; implementation, before/after evidence, browser/UI/a11y gates, independent evaluation and exact-head CI in a later implementation PR.
- Production/provider evidence: read-only production evidence only. No Vercel/Supabase/project configuration or production data was mutated, and no production load test was run.
- Snapshot update: not applicable — current runtime/financial capability truth has not changed, so `CURRENT_PROJECT_MEMORY.md` is intentionally untouched in this planning-only selection PR.
- Superseded issue, roadmap or claim: the board's zero-current state is superseded only if this exact planning PR is owner-merged; #527 then outranks candidate #523 until completion/stop. #403 remains completed historical evidence rather than active work. The owner-reported Vercel score 39 is not claimed as independently reproduced.