# PR #615 — record prod OAuth verification (Track 0 done)

- Date: 2026-09-20
- Change class: Class 0 (docs-only)
- Affected capability or project boundary: `docs/plans/active/megaplan-q4-2026.md`
- Status impact: recorded; no runtime change
- Changed: marked megaplan Track 0 complete — 0.4 Site URL reverted to `https://mfvn.vercel.app`; 0.5 prod OAuth smoke done via the existing `mcp-inspector` client (client-side localhost redirect URI works against any resource domain — no second registration needed); A1 marked done; noted legacy `www.moneyflow.app` serving a ~3-week-old build as owner-ops cleanup
- Verified: `npm run check:knowledge` locally; prod endpoints via curl — `/api/mcp` 401 + `WWW-Authenticate`, `/.well-known/oauth-protected-resource` JSON advertising Supabase issuer, `/oauth/consent` 200 on `mfvn.vercel.app`; owner completed the interactive flow (authorize → consent → approve → token → real data)
- Remaining: legacy domain removal/redirect (owner ops); megaplan gates G1–G5 pending owner decisions
- Production/provider evidence: production OAuth flow verified end-to-end on `https://mfvn.vercel.app` 2026-09-20 (owner-run, MCP Inspector as client)
- Superseded issue, roadmap or claim: completes megaplan Track 0; also records the prod verification evidence that post-dates merged PR #612 (whose record stays immutable)
- Lifecycle impact: megaplan Track 0 closed; no packet state change

## Boundaries

- Prod OAuth verification used the same pre-registered public client + PKCE; no production client registered for a real end-user agent yet — by design (register when a real client exists).
- The stale `www.moneyflow.app` deployment is documented as a known risk, not fixed by this PR.

## Evidence limits

- The interactive consent on prod was executed by the owner in their browser; this record captures the endpoint-level verification (`curl` 401/metadata/200) plus the owner's reported completion.
