# PR #335 — reconciliation import evidence

- Date: 2026-08-10
- Change class: Class 3 — financial trust / authenticated read / user-flow change
- Affected capability or project boundary: account reconciliation, import provenance visibility, deterministic matching assistance
- Status impact: candidate only — current merged reconciliation remains PRs #261/#263 until this PR is merged
- Changed: reuse existing reconciliation and import provenance to show source evidence on imported ledger rows and flag pending account legs whose signed impact exactly equals the current nonzero statement difference
- Preserved: current four reconciliation RPCs, exact-zero completion, statement/session history, transfer-leg independence, split-leg atomicity, transaction review separation, integer money, RLS and no direct balance writes
- Verified: implementation/static/unit/build gates are being evaluated on exact head; final browser/security evidence remains subject to the latest head
- Remaining: finish exact-head gates, address findings and obtain owner merge decision
- Production/provider evidence: none; no production schema/data/Auth/provider write is authorized or performed
- Snapshot update: not applicable; this unmerged candidate does not change `CURRENT_PROJECT_MEMORY.md` merged/provider truth
- Superseded issue, roadmap or claim: historical PR #222 remains evidence only and is not restored; #261/#263 are current implementation authority

## Reuse-first record

- Existing MoneyFlow implementation reviewed: merged reconciliation contract #261 and workspace #263.
- Historical reconciliation implementation reviewed: PR #222; useful laws were compared with current #261, stale branch not reused.
- Existing import primitives reused: PR #183 `transaction_import_provenance`, Inbox atomic approval and tenant RLS.
- Existing transaction/account semantics reused: `account-register`, current reconciliation account-leg feed/RPCs and transaction review state.
- New runtime code is limited to read-only provenance enrichment, one pure exact-difference candidate helper, presentation and regression coverage.

## Financial/security boundary

- Exact amount equality is a review hint only and never changes `pending`/`cleared`/`reconciled` state.
- Completion still requires the existing server-authoritative exact-zero RPC contract.
- Provenance is queried through authenticated own-row RLS; no service role or caller-provided user ID is introduced.
- Imported transactions remain ordinary ledger transactions after Inbox approval; there is no duplicate imported ledger model.
- No migration, backfill, parser change, bank aggregation, AI/LLM/API dependency or direct balance adjustment is included.

## Candidate implementation

- `src/lib/reconciliation-import-evidence.ts`: serializable evidence contract, source labels and pure exact-difference candidate helper.
- `src/server/reconciliation-import-evidence.ts`: bounded transaction-ID chunk reads from existing `transaction_import_provenance`; failure is non-blocking and discards provenance rather than fabricating it.
- reconciliation route: loads current reconciliation state and import evidence in parallel.
- reconciliation workspace: shows source evidence and explicit `Kiểm tra giao dịch này` hint; existing state button remains the only way to mark a row cleared.
- focused unit tests cover zero/unsafe difference, later dates, cleared/reconciled exclusion and multiple equal candidates.
- Playwright extends the existing demo reconciliation flow to prove a hint appears before manual clearing and disappears only after the user clears the row.
- `src/lib/rls-migrations.test.ts` also carries an ancillary static-test harness fix: it lexically removes comments/literals before scanning `SECURITY DEFINER`, preventing an already-merged SQL comment from being counted as executable SQL. No migration/runtime behavior changes.

## Research boundary

Current first-party QuickBooks and Xero reconciliation workflows were used only as product-pattern checks: they retain human review/selection and zero-difference completion. Their bank-feed, invoice/accounting and AI-specific features are not adopted.

## Verification state

- Draft CI shells are not acceptance evidence because heavy shards are skipped while draft.
- Full Class 3 CI is running after marking the PR ready solely to activate heavy gates; ready state is not merge authorization.
- An earlier full run exposed two non-product blockers: packet trailing whitespace and the latent static `SECURITY DEFINER` comment false-positive. Both were corrected without weakening policy or editing historical migrations.
- On the post-scanner head, production build, static quality, lint, typecheck and unit/static-RLS have passed; project-knowledge/browser aggregation must be rerun on the final head after this memory-contract correction.
- Database schema is unchanged. CI correctly classifies database reset/pgTAP as not required; skipped DB tests are not claimed as executed.

## Merge/production boundary

- PR #335 is not authorized to merge automatically.
- A future owner-approved merge still does not authorize a production database/provider write; none is needed for this application-only slice.
- Post-merge production verification, if requested, should use a safe authenticated account and inspect source evidence without completing or altering real financial data unless separately approved.
