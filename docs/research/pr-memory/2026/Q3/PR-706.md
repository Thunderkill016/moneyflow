# PR #706 — Consented demo→account carryover via Inbox review

- Date: 2026-09-23
- Change class: Class 3 (crosses auth boundary; writes tenant data as pending candidates only)
- Affected capability or project boundary: `inbox` — new `carryDemoLedgerAction` server action; `client-inbox-core` gains `carryover` offer state + `carryDemoLedgerForClient`/`declineDemoCarryover`; Inbox page renders the consent card.
- Status impact: packet `demo-ledger-migration.md` specified → implemented. The five owner questions were resolved with conservative defaults under the owner's product-development delegation (2026-09-23 session): real trial ledger; batch label `moneyflow-demo-ledger` + `source:'manual'` (no enum migration); transfers/splits excluded with disclosed count; non-ledger stores out of scope; fixture rows excluded.
- Changed: new `src/lib/demo-ledger-carryover.ts` (raw localStorage reader bypassing the fixture fallback, classifier, candidate mapper, marker, `demo-tx-` prefix); `src/app/actions/inbox.ts` (`carryDemoLedgerAction` with provenance-prefix enforcement → `demo-tx-%` dedupe → empty-`transaction_feed` guard → labelled batch → pending candidates); `src/hooks/client-inbox-core.ts` (offer computation + accept/decline); `src/components/inbox/inbox-page.tsx` + module CSS (consent card); packet updated.
- Verified: `typecheck`, `lint`, `test` 1816/1816 (incl. 11 new unit + 8 new contract pins), `check:css-ownership`, `check:architecture`, `test:ci-policy` 191/191, `build` — all green on the branch head.
- Remaining: no e2e auth-boundary consent/decline spec yet (flagged in packet handoff); no browser evidence of the consent card; RLS/tenant behavior inherits existing `requireAuthedClient` + `user_id` insert path.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: advances #432 (candidate/provenance convergence for the demo source).
- Lifecycle impact: demo rows can never post directly to the ledger — the contract test pins `inbox_candidates`-only writes, the empty-target ordering, and both consent paths; future demo-data migrations must keep that shape.
