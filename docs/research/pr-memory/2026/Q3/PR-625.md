# PR #625 — A5 invocation/audit spec: no Activity surface, table deferred, D5 completion scoped

- Date: 2026-09-21
- Change class: Class 0 (docs-only spec packet; no code authorized)
- Affected capability or project boundary: `docs/plans/active/624-capability-invocation-audit.md` (new), megaplan A5 row
- Status impact: megaplan A5 spec authored; issue #624 tracks; implementation (P-A minimal log) remains separately authorized
- Changed: authored the invocation/audit decision record. Decisions: (1) capability invocations do NOT surface in `/activity` — it is a financial workstream, and agent action already shows via Inbox provenance "đề xuất bởi `<client>`"; (2) `capability_invocations` table deferred — no consumer exists and it touches hash-pinned archive/restore + account-deletion contracts; named triggers recorded; (3) D5 minimal structured log is only partially shipped (REST errors only, MCP none) — P-A scopes one JSON line per `runCapability` execution with `capabilityId`/`viewerId`/`clientId`/`transport`/`status`/`errorCode`/`durationMs`, never input args (they can carry financial data).
- Verified: `check:knowledge` green locally; docs-only change.
- Remaining: owner review of decision points D1–D3; P-A authorization; CI policy contracts on the PR head.
- Production/provider evidence: not applicable (docs-only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: none — decision record only; no code, schema or UI change.

## Boundaries

- No implementation authorized by this packet; P-A (log completion) and P-B (table) are separate work.
- Spec explicitly forbids logging input arguments — `candidates.propose` args carry financial data.

## Evidence limits

- Decision quality rests on cited current code (`route.ts:86`, MCP route) and merged packets (P2 D5, #592) — no external research was needed for this scope.
