# PR #444 — chore(governance): make plan authority resolution fail closed

- Date: 2026-08-22
- Change class: Class 3 — repository governance / agent task-selection policy.
- Affected capability or project boundary: project authority discovery before any product/engineering implementation; Current Work freshness, master-plan supersession and standard agent doctor/knowledge entrypoints.
- Status impact: candidate governance change until merge; also reconciles already-merged #441 truth that was stale in the board/current memory. If accepted, future agents must resolve master/current plan authority and board freshness before selecting `NOW`/`NEXT`.
- Changed: added `docs/plans/PLAN_AUTHORITY.json`; added fail-closed `plan:resolve`; cross-checks exactly one registry master, at most one current executable slice, graph↔registry identity, master/predecessor existence, current/base SHA and master first-parent Git history; supports only explicit numbered post-merge lifecycle projections rather than trusting any merge that touched the board; allows a deliberate future replacement master only when `introducedByPr` equals the current PR and keeps it `candidate` until merged history proves it; made standard doctor readiness depend on authority resolution; made `check:knowledge` run authority resolution; classified authority-policy files as full-gate Class 3; removed hard-coded master-plan discovery from AGENTS/README; reconciled post-#441 board/current memory and retired active #440 into completed history while pausing #442 behind #443.
- Design evidence: repository history establishes #420 introduced `PRODUCT_DEVELOPMENT_PLAN.md`, open #431 never became authority, and merged PR #433 installed #432 as the master product program before #435/#437/#439/#441 executed its P1 sequence. Official Git `git log` documentation supports first-parent integration history and single-path `--follow`; GitHub merge documentation supports squash-merge provenance. Actual merge commit `a35d6f96960e889cf988d9d37d4320a8f674cd85` contains subject `docs(product): align MoneyFlow around low-maintenance acquisition (#433)`.
- Pre-ready / adversarial evaluation: reproduced the original defect on `main@6123d263…`: board/current memory remained on pre-#441 truth. The first strict-baseline design exposed a self-merge problem because a branch cannot know its future squash SHA. A first attempted exception (`HEAD is latest board commit`) was then rejected before final acceptance because it would have blessed the original #441 failure too: #441 itself edited candidate board state, so after merge it was also the latest board commit while still stale. The resolver now requires an explicit `Post-merge projection: PR #N` whose number matches the exact squash commit; ordinary implementation merges remain fail-closed until lifecycle reconciliation. A separate evaluator check found that a resolver demanding the introducing PR always be in merged history would make future master replacement impossible; the current PR may therefore declare itself as candidate master, but only under its own PR number and only becomes active after merge history proves it. Another finding was that authority files initially did not select the full policy gate set; `workflowOrPolicyMatchers` now owns those paths and tests require full verify/database/browser/UI/CodeQL selection.
- Historical verification: on earlier head `376e0c8aeaab5e2be42057e15e9bea2f471eb6b0`, CI #2796 had already passed diff hygiene, migration identity, project knowledge, CI classification/retry and production build before the projection/future-master review changed the branch. On head `68387a8ef47f0d1cbaadb7b51f96e7f98829ff35`, CI #2802 proved the resolver itself resolved #432 as active via PR #433 with #420 as superseded and current slice #443; static quality, lint/typecheck, production build, unit/static RLS, fresh Supabase/pgTAP and archive producer/restore all passed. That CI failed only because this PR-memory record used `Verified in current source:` instead of the required `Verified:` field, so browser/UI aggregate shards were skipped. CodeQL #1860 and Secret history #1860 passed on that head. All of those heads are historical after this record correction and cannot certify the final head.
- Verified: deterministic resolver/unit counterexamples cover normal resolution, stale baseline, ordinary board-editing merge remaining stale, explicit post-merge projection, copied/old projection rejection, duplicate master, duplicate current slice, no-current warning, graph/registry disagreement, missing master/predecessor paths, wrong introduction PR, valid current-PR candidate master and wrong-PR candidate rejection; doctor tests require authority in the standard report and `ready=false` on stale authority; classifier tests require every gate for authority-policy changes. Final exact-head provider evidence is pending after the PR-memory schema correction.
- Remaining: require non-draft exact-head CI with real project-knowledge/authority resolution, policy/classification, lint/typecheck/unit/build, database, browser/auth smoke, cross-device UI audit, aggregate verify/e2e, CodeQL and Secret history; inspect failures without weakening the resolver; perform a fresh-context adversarial review of bypass paths and post-merge behavior before owner handoff.
- Production/provider evidence: not applicable. This PR changes repository governance/tooling/docs only and performs no provider, deployment, production-data, Auth, schema or financial mutation.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` is reconciled from merged #439/#440-candidate state to merged #441 truth and records #443/#442 only as unmerged candidate execution state.
- Superseded issue, roadmap or claim: `docs/plans/PRODUCT_DEVELOPMENT_PLAN.md` from #420 is recorded as predecessor history after merged #433/#432 authority; open PR #431 remains candidate only; the stale claim that #440 is current `NOW` is superseded by merged #441. This PR does not supersede #432 or promote #442 to shipped behavior.
- Rollback: revert PR #444. Product/runtime behavior is unaffected; the repository would return to manual plan/board discovery and the stale-authority failure mode would be reopened.
- Untrusted external instructions copied into project memory: none.
