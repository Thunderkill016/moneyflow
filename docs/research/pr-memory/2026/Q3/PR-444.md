# PR #444 — chore(governance): make plan authority resolution fail closed

- Date: 2026-08-22
- Change class: Class 3 — repository governance / agent task-selection policy.
- Affected capability or project boundary: project authority discovery before any product/engineering implementation; Current Work freshness, master-plan supersession, lifecycle projection and standard agent doctor/knowledge entrypoints.
- Status impact: candidate governance change until merge. If accepted, future agents must resolve a merged master/current authority and board freshness before selecting `NOW`/`NEXT`; no acquisition child is auto-promoted by this closeout.
- Changed: added `docs/plans/PLAN_AUTHORITY.json`; added `scripts/plan-authority.mjs` for structural/history validation and `scripts/plan-selection.mjs` for the stricter task-selection decision; cross-checks exactly one registry master, at most one current executable slice, graph↔registry identity, master/predecessor existence, current/base SHA and master first-parent Git history; future replacement masters may validate only under their own current PR number and remain non-authoritative `candidate` until merged history proves them; every unactivated post-merge board projection is validation-only regardless of whether a GitHub PR event is available, and `plan:resolve`/doctor stay NOT READY until the exact merged commit activates that projection; standard doctor readiness consumes selection-ready authority; `check:knowledge` validates authority shape/history; authority/selection policy files select full Class 3 CI gates; hard-coded #432 discovery was removed from agent entrypoints; post-#441 board/current memory was reconciled; #440/#441 history is completed; #443 is projected into completed lifecycle on merge; #442 remains NEXT candidate rather than silently becoming current.
- Design evidence: repository history establishes #420 introduced `PRODUCT_DEVELOPMENT_PLAN.md`, open #431 never became authority, and merged PR #433 installed #432 as the master product program before #435/#437/#439/#441 executed its P1 sequence. Official Git `git log` documentation supports first-parent integration history and single-path `--follow`; GitHub merge documentation supports squash-merge provenance. Actual merge commit `a35d6f96960e889cf988d9d37d4320a8f674cd85` contains subject `docs(product): align MoneyFlow around low-maintenance acquisition (#433)`.
- Pre-ready / adversarial evaluation: reproduced the original defect on `main@6123d263…`: board/current memory remained on pre-#441 truth. A strict `board baseline == HEAD` design would invalidate its own reconciliation merge. A first attempted exception (`HEAD is latest board commit`) was rejected because it would also have blessed the original #441 stale-board failure. The design moved to explicit numbered lifecycle projections. Later review found three more privilege/continuation problems: (1) a valid unmerged replacement master could make resolver validation `ok=true` and therefore make doctor READY before merge; (2) a post-merge projection could be mistaken for current execution state before merge; (3) checking only the current GitHub PR number left local feature checkouts with no PR event able to treat an unmerged projection as selectable. Validation and task-selection readiness are now separate: only merged `active` masters are selectable, and any projection remains blocked until `baselineMode=post-merge-projection` is proven from the exact merged commit. A narrow lifecycle rule avoids deadlock: the already-started reconciliation PR may finish acceptance defects/evaluation in its recorded scope while projected `NOW`/`NEXT` remains unusable for follow-on work. Another finding was that authority files initially did not select the full policy gate set; classifier ownership now covers authority, selection and doctor files. CI itself also caught a PR-memory schema defect when `Verified in current source:` did not match the executable `Verified:` field; the checker was preserved and the record corrected.
- Historical verification: on head `376e0c8aeaab5e2be42057e15e9bea2f471eb6b0`, CI #2796 had passed diff hygiene, migration identity, project knowledge, CI classification/retry and production build before later adversarial findings changed the branch. On head `68387a8ef47f0d1cbaadb7b51f96e7f98829ff35`, CI #2802 proved the authority resolver resolved #432 as active via PR #433 with #420 superseded; static quality, lint/typecheck, production build, unit/static RLS, fresh Supabase/pgTAP and archive producer/restore passed. That CI failed only on the PR-memory field name, so browser/UI aggregate shards skipped. CodeQL #1860 and Secret history #1860 passed on that head. On head `0adc049bfe7aba7e2a8c4cc1f9f25194d2ad7ea6`, policy/knowledge, lint/typecheck, unit/static RLS, production build, aggregate verify, CodeQL #1876 and Secret history #1876 were green before the local-checkout projection bypass was found; that head is also historical. All earlier heads are invalid as final acceptance after later source changes.
- Verified: deterministic authority tests cover normal resolution, stale baseline, ordinary board-editing merge remaining stale, explicit/copy-stale projection behavior, duplicate master, duplicate current slice, no-current warning, graph/registry disagreement, missing master/predecessor paths, wrong introduction PR, valid current-PR candidate master and wrong-PR candidate rejection. Selection tests separately require merged `active` master status, reject candidate master task selection, reject any unactivated post-merge projection without depending on PR-event context, and accept that projection only after exact merged activation. Doctor tests require the same selection result and `ready=false` on stale/candidate authority; classifier tests require every gate for authority/selection policy changes. Final exact-head provider evidence is pending after the local-checkout projection hardening and lifecycle wording update.
- Remaining: require non-draft exact-head CI with real authority/knowledge validation, policy/classification, lint/typecheck/unit/build, fresh Supabase/pgTAP, archive producer/restore, browser/auth smoke, cross-device UI audit, aggregate verify/e2e, CodeQL and Secret history; inspect any failure without weakening the resolver/selection boundary; then hand off the exact head for owner merge decision.
- Production/provider evidence: not applicable. This PR changes repository governance/tooling/docs only and performs no provider, deployment, production-data, Auth, schema or financial mutation.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` is a post-merge projection that preserves #441 as the runtime/financial baseline, records fail-closed plan selection as governance truth if #444 merges, leaves no current agent-executable child, and keeps #442 as a fresh-main NEXT candidate requiring explicit promotion.
- Superseded issue, roadmap or claim: `docs/plans/PRODUCT_DEVELOPMENT_PLAN.md` from #420 is predecessor history after merged #433/#432 authority; open PR #431 remains candidate only; the stale claim that #440 is current `NOW` is superseded by #441; the broader claim that “newest plan/open PR/valid candidate graph can authorize work” is superseded by merged-history selection readiness. This PR does not supersede #432 or promote #442 to shipped/current behavior.
- Rollback: revert PR #444. Product/runtime behavior is unaffected; the repository would return to manual plan/board discovery and the stale-authority failure mode would reopen.
- Untrusted external instructions copied into project memory: none.
