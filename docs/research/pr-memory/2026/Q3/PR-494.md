# PR #494 — docs(rrb-08): record the owner's real-phone observation

- Date: 2026-08-27
- Change class: Class 0 — documentation only. No code, workflow, schema, migration, RPC, RLS, provider or dependency change.
- Affected capability or project boundary: `docs/plans/active/rrb-08-physical-device-proof.md`, plus the board entry for `#398`.
- Status impact: none. It records evidence supplied by the owner; it does not change capability and it does not close a gate.
- Lifecycle impact: completes current slice. This PR carries its own post-merge convergence: the board projection names PR #494, no successor slice is promoted, and projected current memory is updated here. `plan:resolve` intentionally reports NOT READY while this is unmerged.
- Why it matters beyond bookkeeping: this is the **first current real-phone observation** in the program. Every previous entry recorded that none existed and that browser emulation could not substitute. It is also the first evidence of any kind in this session that did not come from inference — the eight bank SMS behind the parser fix, the demo judgement and the budgeting method were all reasoned, not observed.
- Changed: the runbook's `Current evidence state` now records an owner observation dated 2026-08-27 — origin `https://mfvn.vercel.app` confirmed correct by the owner, POCO X8 Pro, Android, Chrome, authenticated with a real account, owner verdict PASS with no defects reported. The `Next action` section becomes an owner decision rather than a request for evidence.
- Recorded verbatim, with gaps left as gaps: OS version, browser version, network context and time were not supplied and are marked `unstated` rather than estimated. The owner gave one summary verdict rather than six checkpoint notes, so the six PASS lines are that summary applied to each step and the record says so. A physical-device record that guesses is worth less than one with holes in it.
- Coverage stated so it is not overread: one Android device on Chrome. There is no iOS or Safari observation, which matters because `webkit-iphone` was the only CI project to behave differently across this session — a `safety-review-states` audit failure appeared there twice and passed everywhere else.
- A correction to an earlier PR of mine, recorded rather than left standing: the owner stated that **demo mode is used only for AI/agent testing, not by real users**. PR #487 argued for its demo fix on the grounds that the demo is "the first thing a stranger sees". That justification was wrong. The change itself remains worthwhile for a different reason — the cross-device audit renders demo mode, so a demo whose statement contradicted its own category panel made those gates measure fiction — but the reason given at the time was not the real one.
- What this PR deliberately does not do: it does not close `#398`, does not mark RRB-08 complete, and does not declare public-beta readiness. Whether the observation satisfies the gate is the owner's declaration. The runbook and the board both now say so explicitly, and the board's owner-action row for `#398` changes from "go and test" to "decide whether this is enough".
- Verified: `check:knowledge` passes, including the new local record validation added in PR #493 — this record was checked on the machine that wrote it before being pushed. `test:ci-policy` and diff hygiene apply; no application, database or browser gate is selected by a documentation-only change.
- Limitation, not a pass: I did not witness the device session. Everything here is the owner's report transcribed, and its accuracy is theirs. No screenshot, log or recording accompanies it.
- Remaining: the unstated fields above, and an iOS/Safari observation, are the smallest additions if the owner wants the gap closed rather than accepted. `RRB-02` hosted restore, `RRB-04`, `RRB-05`, `RRB-06` and `RRB-09` are untouched and still open.
- Production/provider evidence: the owner exercised the deployed production origin on a real device. No provider write, deployment or production-data access was performed by me.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` is updated here as an explicit post-merge projection, together with the board baseline moving to `cac5f157` (PR #493).
- Superseded issue, roadmap or claim: supersedes the statement that no current real-phone observation exists. Does not supersede `#398` itself, `#432`, `#426` or any other RRB gate.
