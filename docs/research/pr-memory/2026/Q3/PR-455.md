# PR #455 — feat(inbox): apply deterministic rules to shared candidates

- Date: 2026-08-25
- Change class: Class 3 — authenticated Share source ingestion now carries optional rule evidence through a database wrapper, while preserving pending-candidate and no-ledger boundaries.
- Affected capability or project boundary: PWA Share Target text/CSV acquisition, deterministic candidate-stage rules, authenticated Supabase RPC and demo/browser-local candidate persistence.
- Lifecycle impact: current slice #454 remains active. No completion projection is made while exact-head provider verification is pending.
- Status impact: candidate only until merge. A saved matching rule may normalize a future Share candidate and record its exact revision; it never approves, posts or writes a financial transaction.
- Changed: adds pure Share rule-evidence selection; authenticated payload validation plus a SECURITY INVOKER wrapper that calls existing atomic Share ingestion then the authoritative tenant/rule/version/match validator in the same transaction; preserves demo evidence locally; adds unit, pgTAP and browser regression coverage.
- Verified: focused unit tests passed (including transfer/category guards); `check:knowledge`, `test:ci-policy`, `check:migrations`, `check:rls`, and `NEXT_PUBLIC_APP_MODE=demo npm run verify:prepush` passed locally (1,109 tests, lint zero errors with five pre-existing outside-diff warnings). The focused deterministic-rules browser suite passed 12/12 on Chromium desktop/mobile. Full UI audit recorded 503 pass and 141 configured skips in Chromium-family projects.
- Remaining: exact-head provider CI, a real PostgreSQL pgTAP run, review and owner merge decision. Local `supabase test db` and advisor cannot connect because Docker/PostgreSQL is absent. Full local UI audit has 54 WebKit failures at `page.goto` with `WebKit encountered an internal error`, reproduced by one minimal unrelated navigation before application assertions.
- Production/provider evidence: no deployment or production write occurred. Applying the new migration to production requires a separate owner-approved production-schema action and post-merge read-back.
- Snapshot update: board and current memory name PR #455 only as draft candidate evidence; #454 remains the current active slice.
- Superseded issue, roadmap or claim: closes #454 only when the PR is merged; it does not supersede the atomic source guarantee from PR #451.
- Rollback: revert PR #455. Existing Share ingestion and Rules workspace remain available; no ledger data, raw source, or historical candidate is rewritten.
- Untrusted external instructions copied into project memory: none.
