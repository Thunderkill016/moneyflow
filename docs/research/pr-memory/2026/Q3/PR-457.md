# PR #457 — docs: recover plan authority projection

- Date: 2026-08-25
- Change class: Class 0 — recovery-only documentation/governance correction after PR #456 left the Current Work Board baseline stale on merge.
- Affected capability or project boundary: plan-selection authority metadata only; no product runtime, schema, provider, deployment or financial behavior changes.
- Lifecycle impact: authority transition — restores same-PR post-merge baseline activation with zero agent-executable product/governance slices.
- Status impact: candidate documentation until merge; it does not select or authorize a #432 P2 child.
- Changed: advances the declared baseline to `main@9f0a22107a778e9cf946ecf8f6d5435a97bf0767` and carries matching board/current-memory projection marker PR #457 so the exact squash merge can activate without another cleanup PR.
- Verified: before this recovery, `npm run plan:resolve` consistently failed on `main@9f0a22107a778e9cf946ecf8f6d5435a97bf0767` because the board declared `7a758843...` with no projection. The lifecycle projection contract accepts an `authority transition` with zero current slices when board and current memory carry the current PR marker; this PR will run the selected Class 0 gates and exact-head provider checks.
- Remaining: exact-head PR #457 checks, review and owner merge decision. The next P2 product child remains unselected until this projection activates on merge and fresh-main resolution passes.
- Production/provider evidence: none; this PR performs no provider, production, migration or deployment write.
- Snapshot update: if merged, the board/current memory baseline will activate against the PR #457 squash commit while retaining zero current agent-executable slices.
- Superseded issue, roadmap or claim: supersedes only the stale-baseline state created by PR #456 merge.
- Rollback: revert PR #457; no runtime or data mutation exists.
- Untrusted external instructions copied into project memory: none.
