# PR #661 — Transactions: replace window.confirm with review dialogs

- Date: 2026-09-22
- Change class: Class 1/2 (bounded UI flow; pinned contract + e2e updated deliberately)
- Affected capability or project boundary: `src/components/transactions/transactions-workspace.tsx`, `src/lib/ui-phase5-transactions-contract.test.ts`, `e2e/transaction-review-bulk-correction.spec.ts`.
- Status impact: removes the last two `window.confirm` call sites in the app, completing the convention that consequential actions use reviewable in-app dialogs (account archive, category archive, rule delete already do). Native confirms were unstyled, focus-hostile on mobile, and bypassed the app's own review-dialog evidence.
- Changed:
  - Bulk recategorize → "Đổi danh mục?" SecondaryReviewDialog (selected count, target category, consequence: amount/date/account untouched).
  - Delete → "Xóa giao dịch?" SecondaryReviewDialog (note, amount, destructive intent, keeps the 8-second undo promise copy).
  - Failed mutations close the dialog before the error notice so the toast is never occluded behind the modal top layer (same defect class fixed in PR #660).
  - Contract pin updated from asserting `window.confirm` presence to asserting its absence + both dialog labels; e2e spec clicks the in-app confirm instead of auto-accepting native.
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning); typecheck; build; check:knowledge, check:architecture, check:deployment-env. Browser (dev build, demo): bulk dialog → confirm → "Đã đổi danh mục cho 3 giao dịch."; delete dialog shows note/amount/undo copy; zero native dialogs fired.
- Remaining: e2e shard must confirm the updated spec passes on exact head (locator assumptions verified locally, not yet in CI).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
