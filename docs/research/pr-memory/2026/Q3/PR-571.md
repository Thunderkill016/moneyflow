# PR #571 — prove RPC default ACL and reduce snapshot privilege

- Date: 2026-09-10
- Base: owner-merged #567 selector `main@2309fca6fb1d3ce03732679d4ae33c5e09668c8f`.
- Change class: Class 3 database/security implementation and lifecycle closeout for `docs/plans/active/567-rpc-least-privilege-proof.md`.
- Affected capability or project boundary: postgres function default EXECUTE policy, authenticated privileged RPC inventory, reconciliation snapshot read authority, and #567 execution authority.
- Status impact: completes current slice #567 in this PR by archiving its packet and projecting `PLAN_AUTHORITY.current` from #567 to `null`; the projection becomes merged project truth only after owner merge.
- Changed: establishes a global postgres function-default ACL override so future postgres-owned functions do not inherit PostgreSQL's hard-wired PUBLIC EXECUTE; tests both global and public-schema default ACL state; changes only `reconciliation_snapshot_for_user(uuid,uuid,date)` to SECURITY INVOKER; updates the reviewed authenticated SECURITY DEFINER inventory from 43 to 42; grants one pg_temp lifecycle-test helper explicitly because deny-by-default intentionally removes its previous implicit PUBLIC execution path; archives #567 and clears current executable authority.
- Verified: exact-head CI run #3540 at `359d56cb3600e780c1ead4d584a5260ae6dfa2c9` passed production build, static quality, unit/static-RLS, fresh local reset, all 41 database files / 784 pgTAP tests, archive producer and restore round trips. CodeQL #2545 and Secret History #2545 passed. The only failing shard was lifecycle taxonomy because this record still said implementation continued after the branch had already projected `current -> null`; this record corrects that metadata without changing code or database behavior.
- Remaining: run exact-head CI/CodeQL/Secret History after this lifecycle-record correction, independently review the final diff, update Plate handoff, and obtain explicit owner merge authorization. Production deployment/database verification of #571 happens only after owner merge through the normal path.
- Production/provider evidence: production database was queried read-only for catalog/default-ACL evidence only; production Vercel after selector #568 was READY at `2309fca6fb1d3ce03732679d4ae33c5e09668c8f` and `/api/health` returned 200 for that commit. No migration, provider setting, user data or production configuration was written.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` is reconciled and compacted in this PR to record #568 as merged, #567/#571 as the current completion handoff, `PLAN_AUTHORITY.current -> null`, and #570 as an unselected follow-on rather than executable work.
- Superseded issue, roadmap or claim: corrects the prior assumption that a schema-scoped default-privilege REVOKE can remove PostgreSQL's hard-wired PUBLIC function EXECUTE. It does not widen into #174, #570, provider work or #559 UI work.
- Lifecycle impact: completes current slice #567 by archiving its packet and returning `PLAN_AUTHORITY.current` to null; final exact-head validation and explicit owner merge remain required.

## Permission boundary

PR #571 may change only the bounded S1/S2 database/test surface and directly required lifecycle/memory artifacts for #567. No unrelated SECURITY DEFINER conversion, provider/Auth/WAF/UI work, direct production database write, financial-semantics change or merge is authorized.
