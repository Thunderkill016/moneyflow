# PR #571 — prove RPC default ACL and reduce snapshot privilege

- Date: 2026-09-10
- Base: owner-merged #567 selector `main@2309fca6fb1d3ce03732679d4ae33c5e09668c8f`.
- Change class: Class 3 database/security implementation under `docs/plans/active/567-rpc-least-privilege-proof.md`.
- Affected capability or project boundary: postgres function default EXECUTE policy, authenticated privileged RPC inventory, and reconciliation snapshot read authority.
- Status impact: implementation candidate for current slice #567; `PLAN_AUTHORITY.current` remains #567 and no completion is claimed yet.
- Changed: establishes a global postgres function-default ACL override so future postgres-owned functions do not inherit PostgreSQL's hard-wired PUBLIC EXECUTE; tests both global and public-schema default ACL state; changes only `reconciliation_snapshot_for_user(uuid,uuid,date)` to SECURITY INVOKER; updates the reviewed authenticated SECURITY DEFINER inventory from 43 to 42; grants one pg_temp lifecycle-test helper explicitly because deny-by-default intentionally removes its previous implicit PUBLIC execution path.
- Verified: read-only production catalog confirmed `acldefault('f', postgres) = {=X/postgres,postgres=X/postgres}` and showed the earlier public-schema default row was additive rather than a global override. On implementation head before the test-helper correction, fresh local reset applied both new migrations; reconciliation suites, browser-role privileges, security catalog, security-definer contract, cross-tenant RPC and all preceding pgTAP suites passed. The sole database failure was the pg_temp test helper losing implicit EXECUTE, now corrected with an explicit test-only grant. Exact final-head CI remains required.
- Remaining: exact-head migration/knowledge gates, full database + archive round trips, static/unit/build/browser policy-selected checks, CodeQL, Secret History, evaluator pass, then same-PR lifecycle convergence if all evidence remains green.
- Production/provider evidence: production database was queried read-only for catalog/default-ACL evidence only. No migration, provider setting, user data or production configuration was written.
- Snapshot update: current production behavior is unchanged by this open PR. #567 remains the sole executable slice until an owner-merged lifecycle closeout returns authority to null.
- Superseded issue, roadmap or claim: corrects the prior assumption that a schema-scoped default-privilege REVOKE can remove PostgreSQL's hard-wired PUBLIC function EXECUTE. It does not widen into #174, #570, provider work or #559 UI work.
- Lifecycle impact: implementation continues current slice #567 with no authority transition; completion remains pending exact-head evidence and lifecycle closeout.

## Permission boundary

PR #571 may change only the bounded S1/S2 database/test surface and directly required lifecycle/memory artifacts for #567. No unrelated SECURITY DEFINER conversion, provider/Auth/WAF/UI work, direct production database write, financial-semantics change or merge is authorized.
