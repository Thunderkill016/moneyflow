# PR #707 — Optimistic concurrency on update_money_transaction

- Date: 2026-09-23
- Change class: Class 3 (migration on the money write path)
- Affected capability or project boundary: `transactions` update path — `update_money_transaction` RPC signature, `transaction_feed` view, `use-transactions` hook.
- Status impact: `docs/operations/multi-device-write-semantics.md` "if this ever needs to change" increment → implemented for `update_money_transaction` only. Other update RPCs keep LWW (out of scope, recorded in packet).
- Changed: migration `20260923160000` (drop 8-arg signature; 9-arg with `p_expected_updated_at timestamptz default null`; stale → `stale_write` under the `for update` lock; identical grants); feed view + `feedSchema` + `mapTransactionFeedRow` expose `updated_at`/`updatedAt`; `updateTransactionAction` forwards `p_expected_updated_at` and maps `stale_write` to a reload message; `use-transactions` attaches the version on single + bulk edits; `optimistic-transactions.test.ts` pin updated to tolerate the wrapped argument; packet `optimistic-concurrency-update-transaction.md` added; pgTAP signature assertion updated + `optimistic_concurrency_update.test.sql` (stale rejected, row untouched, fresh accepted, null → LWW, feed exposes version); migration identity baseline registered; hook contract assertions made format-tolerant.
- Verified: `typecheck`, `lint`, `test` 1824/1824 local, `check:architecture`, `test:ci-policy` 191/191, `build`, `check:knowledge` — all green. CI: every lane pass including `database` 2m19s (pgTAP stale_write suite ran). Post-merge-with-main rerun: database lane hit GHCR `toomanyrequests` on the postgres image pull (runner-pool quota, infra flake — retried, not a code failure).
- Remaining: real two-device stale-write rehearsal requires a live DB (preview branch or post-merge staging check); other write RPCs unchanged.
- Production/provider evidence: none — migration applies on merge.
- Superseded issue, roadmap or claim: advances #432 multi-device correctness; updates nothing in the ops doc's "current truth" section until merged.
- Lifecycle impact: `stale_write` is now part of the write contract for this RPC; future update RPCs needing a precondition should copy this pattern (optional defaulted param, check under lock, honest client message).
