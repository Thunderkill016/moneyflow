# PR #700 — Compact VND on nowrap figures; error doors land on /dashboard

- Date: 2026-09-23
- Change class: Class 1 (display-only money formatting on identified nowrap surfaces + three error-boundary href changes; contract tests; no schema/RPC/provider change)
- Affected capability or project boundary: `formatMoney`'s existing `compact` flag applied to dashboard chips/captions/chart labels; `moneyDisplayAriaLabel` semantics; `RouteError` + two route error boundaries.
- Status impact: candidate → implemented. `/insights` is a redirect shim only; no live UI href may target it (sweep test enforces).
- Changed: attention chips (`src/lib/attention.ts`), dashboard category bar rows + prior-period compare caption, reports net-worth delta/balance-scale/trend-average/peak labels render compact VND. `moneyDisplayAriaLabel` always announces exact đồng regardless of `compact`. Error doors default and both explicit boundaries now use `/dashboard`. New `compact-vnd-surfaces.test.ts` pins compact-on-nowrap vs exact-on-ledger/totals; `route-error.test.ts` adds the href sweep.
- Verified: `npm run test` 1783/1783; `npm run typecheck` clean; `npm run lint` 0 errors; `check:css-ownership`, `check:architecture`, `check:knowledge`, `test:ci-policy` 191/191 pass.
- Remaining: compact stays a whitelist decision per surface — the contract test fails if a new compact call site appears without review, and if exact-figure surfaces (ledger rows, totals, inbox, export) ever adopt it.
- Production/provider evidence: none (display layer only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: any future compact adoption must update `compact-vnd-surfaces.test.ts` expectations deliberately; aria must stay exact — never pass `compact` into `moneyDisplayAriaLabel` paths.
