# PR #416 — docs: close out PR #415 and keep #403 open

- Date: 2026-08-17
- Change class: Class 0 — documentation and lifecycle reconciliation only. No runtime, test, schema, RLS, Auth, provider, CI or production change.
- Affected capability or project boundary: project lifecycle truth. Records the merged canonical performance-measurement harness as a verification capability, and records what it does **not** establish. Touches no financial, security or runtime boundary.
- Status impact: durable. This is the closeout that moves merged PR #415 from candidate to recorded truth, so `CURRENT_PROJECT_MEMORY.md` changes with it.
- Changed: filled merge SHA `c9a21781daed50df1d7e19cdab512deeaf36f6cd` into `docs/plans/active/README.md` and `docs/research/pr-memory/2026/Q3/PR-415.md`; set that record's status from candidate to merged and its snapshot field from "not applicable" to the applied update; advanced the board's current main baseline to the merge SHA; reconciled `CURRENT_PROJECT_MEMORY.md` with a performance-measurement capability row, the post-merge open-PR truth, and the agent-side next action for #403.
- Verified: `npm run check:knowledge`, `npm run test:ci-policy`, `npm run check:migrations`, `npm run lint`, `npm run typecheck`, `npm run test` and `git diff --check` on this branch. Exact-head provider checks on this PR's own head are authoritative and are the only green that counts for it. No browser, database or provider evidence is applicable to a documentation-only diff, and none is claimed.
- Remaining: #403 stays open in `NOW`. Its owner-set first step is an attribution experiment for the bimodal `/dashboard` FCP — enough samples per side, one runner, loading boundary on versus off, recording the paint element if the harness can capture it, reported as a mode split rather than a median. Follow that signal only if it proves real; otherwise go directly at the measured bottleneck, dashboard hydration / client-JS / main-thread cost (766 → 814 ms JS bootup against 311.6 KB transferred script, server response only 5–15 ms), targeting a measurable cost reduction rather than another intuition-led optimization. RRB-08 remains open and owner-owned, and now explicitly requires a re-test of the post-#414 build, so no earlier dated phone evidence can close it.
- Production/provider evidence: not applicable. No provider write, deployment, production-data access or production verification is part of this branch.
- Snapshot update: applied. `CURRENT_PROJECT_MEMORY.md` gains a performance-measurement capability row that records both the harness and its limit — script-transfer bytes stable to 0 B, LCP median drifting a few hundred ms across runs of behaviourally identical code, LCP missing the 2.5 s budget on both canonical routes — plus the merged status of #415, that #403 remains open, and the post-#414 condition on RRB-08.
- Superseded issue, roadmap or claim: supersedes the pre-merge wording that described #415 as a candidate and its snapshot update as pending. Does **not** supersede #403, which is untouched and open. Records verbatim, and does not soften, that **#415 produced no demonstrated cold-load performance improvement**: measurement infrastructure is not restated as a performance success in the board, this record, PR-415's record or the snapshot. The −99 ms `/dashboard` LCP reading once taken for a win is retracted as noise.
