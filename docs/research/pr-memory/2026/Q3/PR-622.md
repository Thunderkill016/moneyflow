# PR #622 — expand read capabilities: accounts.list, budgets.status, goals.status, activity.candidates (A3)

- Date: 2026-09-20
- Change class: Class 1 (bounded executable change inside the existing capability boundary)
- Affected capability or project boundary: `src/server/capabilities/` (4 new modules + tests + goldens, `types.ts` deps/re-exports, `manifest.ts` registration), generated `docs/agents/capabilities.json`, `docs/agents/README.md`, megaplan A3 status
- Status impact: megaplan A3 implemented — the registry now covers accounts, budgets, goals and pending-Inbox reads alongside ledger/reports/transactions and `candidates.propose`
- Changed: `accounts.list` (accounts + balances, `includeArchived` opt-in, per-account currency preserved — non-VND accounts never stamped VND); `budgets.status` (`month: YYYY-MM`, per-category limit/spent/remaining + monthIncome); `goals.status` (goal progress, totals, reserve picture — `null` when underivable); `activity.candidates` (pending candidates only, server ordering preserved). All four reuse existing workspace loaders (`getAccountsWorkspace`/`getBudgetsWorkspace`/`getGoalsWorkspace`/`listInboxFromServer`) via injectable deps; loader `dataError`/failure maps to `internal`, never an empty-looking success. All money is `Minor` + snapshot `basis`. No transport code — REST and MCP pick the capabilities up from the registry.
- Verified: `npm test` 1416/1416 (16 new tests incl. deterministic goldens); typecheck; lint 0 errors (1 pre-existing warning); `check:knowledge`, `check:architecture`, `check:capabilities` (manifest regenerated), `check:migrations`, `test:ci-policy` 191/191; `npm run build` clean.
- Remaining: CI provider lanes (database/browser smoke/CodeQL) on the exact PR head; production smoke of the new endpoints after deploy.
- Production/provider evidence: none yet — registry unit tests only; no live authenticated call against the new ids yet.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: additive — four new read capabilities in the shared registry; no schema, migration, RLS or UI change.

## Boundaries

- Read-only: all four are `authorization: "read"`, `sideEffects: "none"`.
- `activity.candidates` exposes pending items only — review stays in the Inbox UI; nothing here approves.
- `budgets.status` month input is `YYYY-MM` validated by regex; the loader's future-month guard still applies.
- Demo viewers are rejected upstream by the transport auth seam (401); loader demo branches are unreachable from capabilities.

## Evidence limits

- Loader behavior (RLS, month resolution, reserve derivation) is asserted through injected fixtures, not a live database — `test:db` unavailable locally (no Docker); CI database lane is the provider check.
- Golden fixtures pin determinism of shape, not correctness against production data.
