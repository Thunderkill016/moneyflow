# PR #589 — foreground ledger on default dashboard

- Date: 2026-09-12
- Change class: Class 2 — bounded dashboard UI/flow simplification
- Affected capability or project boundary: signed-in `/dashboard`, progressive disclosure of planning, dashboard client hydration, THU-41 / GitHub #426
- Status impact: current `main` still rendered a full `DashboardPlanningColumn` beside the daily ledger even though compact planning links and the attention strip already preserve planning discoverability and exceptions; this PR removes only that duplicate default-dashboard surface
- Changed: removes `DashboardPlanningColumn` from the signed-in home; keeps all planning routes/features, compact planning navigation, budget/commitment attention behavior, capture dialogs and financial read/mutation semantics unchanged; stops passing `goals` and `incomeTemplates` into the dashboard client boundary and removes demo income-template hydration that only served the removed column; updates the stale goal-dashboard contract test, mobile UI-audit contract and navigation comments to the new progressive-disclosure contract
- Research basis: MoneyFlow product law requires progressive disclosure; GOV.UK design principles support doing less and simplifying from observed current behavior; Next.js official rendering guidance supports minimizing unnecessary client-side JavaScript. External guidance informs direction only; current MoneyFlow code/tests remain authority
- Verified: implementation-head CI #3654 first failed because mandatory PR memory did not yet exist. CI #3656 then correctly found missing knowledge-field labels plus a stale goal-dashboard contract; both were fixed. On head `59a75ae3c5fd0f6523c445ab60190a88be686c66`, CI #3662 passed unit/static RLS, policy, production build, CSS ownership, architecture boundary, lint, typecheck, Browser smoke, Cross-device UI audit, aggregate `verify` and aggregate `e2e`; CodeQL #2665 and Secret history scan #2665 also passed. The first UI-audit attempt had failed because the audit still required the three planning panels THU-41 intentionally removes; the contract was changed to assert the four compact planning links remain visible, correctly routed, at least 44px, within the phone viewport, with no horizontal overflow, while the historical full panels remain absent. A transient edit accidentally overwrote later audit coverage; that coverage was restored before the final passing run. Source review confirms `attentionItems` still derive from live budgets, commitments and inbox count, while `DashboardHeaderSections` still renders all `PLANNING_LINKS`. Evaluator diff/evidence review found no new semantic blocker; independent owner/human review remains separate from this agent evaluation
- Performance evidence: using the existing `performance.mobile.auth.spec.ts` harness on comparable authenticated browser-smoke artifacts, `/dashboard` script transferred bytes moved from 294,407 B before THU-41 to 280,617 B after THU-41: -13,790 B (-4.684%). Both before and after were stable across three samples with a 0 B script-byte range. The control `/` route was effectively unchanged at 187,167 B → 187,170 B (+3 B), supporting a dashboard-specific reduction rather than harness drift. These values were re-read directly from each run's `performance/lighthouse-summary.json`, not copied from an intermediate summary. Baseline provenance was checked: from the #584 browser-running merge through the current pre-THU-41 base there were no `src/**` changes affecting the dashboard runtime
- Remaining: obtain fresh exact-head CI/CodeQL/Secret risk-selected evidence after this final evidence correction, then independent owner/human review and owner merge decision. No merge is authorized by this memory update
- Financial/data boundary: no dashboard RPC, schema, RLS, Auth, provider, ledger, transfer, budget, commitment or reconciliation semantics change; no production/provider write
- Rollback: restore the removed planning-column render and client props; all underlying planning routes/data remain intact
- Production/provider evidence: no production or provider write was performed. Existing production deployment remains on merged `main`; this PR is validated through repository/preview evidence only until owner merge and normal deployment
- Superseded issue, roadmap or claim: does not replay #426's stale 2026-08 sidebar-removal recipe and supersedes the old test/comment assumption that planning must appear as full dashboard cards. Current code was revalidated first; planning remains secondary and discoverable through the compact planning nav and More surfaces
