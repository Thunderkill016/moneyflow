# PR #553 — select MON-63 acquisition UX

- Date: 2026-09-09
- Status: selector candidate; executable only after owner merge
- Base: `main@05323e2cb45609a85b3e7e3f2a4af94679149c31`
- Change class: Class 0 authority/planning change selecting a Class 3 acquisition/data-integrity/user-flow packet; no runtime, schema, provider or production mutation in this PR.
- Affected capability or project boundary: executable plan authority, M1 Vietnam Acquisition Depth, import mapping/retry/history/review UX, current project memory.
- Status impact: if owner-merged, this PR changes repository executable authority from `current: null` to MON-63. It does not implement MON-63 or select any later follow-on.
- Changed: adds `docs/plans/active/mon-63-mapping-presets-batch-retry-review.md`, sets candidate `PLAN_AUTHORITY.current` to that packet with `selectedByPr: 553`, reconciles `CURRENT_PROJECT_MEMORY.md` after merged/deployed #552, and records this PR memory.
- Verified: selector branch starts from exact fresh `main@05323e2cb45609a85b3e7e3f2a4af94679149c31`; selector-base authority was re-read as `current: null`. PR #552 is merged and Vercel production is READY on the exact merge commit; `/api/health` returned 200 with that full commit and the post-deploy runtime-error query found no errors. MON-62 is Done in Linear.
- Repository reconnaissance: `import-batch-store.ts` already owns batch metadata with `parsed | committed | cancelled` and intentionally does not retain raw file content; authenticated/history surfaces already exist. `import-preview-page.tsx` currently creates candidates before marking a batch committed, so metadata completion can fail after candidate creation and the current UI warns the user to inspect Inbox before retry. MON-63 therefore targets idempotent/state-aware recovery and history enrichment rather than a second batch subsystem.
- Evaluation finding 1 — preset semantics: the first selector draft allowed a deterministic structural header signature to read too much like authority for automatic preset reuse. Fresh code review of `parse-csv.ts` shows generic parsing still has header/position heuristics plus date/amount/direction semantics that can differ even when normalized headers are identical. The packet was hardened so structural equality may prefill column choices only; authoritative auto-application must bind to the relevant parser/adapter semantic contract and fail closed on equal-header/different-semantics counterexamples.
- Evaluation finding 2 — merge-stable durable memory: the first selector snapshot truthfully described pre-merge `main` as `current: null`, but that wording would become false immediately if #553 merged because the same PR changes `PLAN_AUTHORITY.current` to MON-63. `CURRENT_PROJECT_MEMORY.md` is now merge-stable: it records the selector base separately from the PR projection and gives conditional next actions for pre-merge vs post-merge states. This directly prevents the stale-memory window observed after #552.
- Evaluation finding 3 — project-knowledge heading contract: exact head `db4d4a8a74f6518c03ada509f020585fa8b51972` failed CI #3395 Project Knowledge even though plan-authority resolution and lifecycle transition were valid. The merge-stable rewrite had renamed required heading `## 9. Open pull-request memory`. The required heading was restored with a nested `### PR #553 — MON-63 selector projection` while preserving the merge-stable semantics. The failed head is non-acceptance evidence and is not reused as green proof.
- Historical branch evidence: stale `feat/p2-direct-csv-mapping-presets` is 53 commits behind current main and contains a versioned normalized-header-shape/local preset prototype; stale `fix/direct-import-retry-idempotency` is 265 commits behind and documents stable idempotency-key reuse for unchanged intent plus the limitation that mounted-attempt retry is not durable page/cross-device recovery. These branches are evidence only: reuse principles after fresh-main revalidation, not stale code, storage assumptions or authority.
- Research refresh: Actual Budget documents stable imported IDs, fallback reconciliation, dry-run and added/updated/errors outcomes. YNAB's August 2026 file-import guide documents explicit CSV field mapping, inflow/outflow swap and remembered settings for an account; its current approval/matching guide documents bulk actions and automatically approved matched imports.
- Research limits: those products are workflow references only. They do not prove Vietnamese bank export layouts, source identity, storage choices or MoneyFlow mutation semantics. Header equality is not source or semantic evidence. No evidence justifies raw-statement server retention or a background queue for this slice.
- Architecture decision: mapping presets require a deterministic, versioned, privacy-safe eligibility key and must fail closed on mismatch. Filename, row contents, raw account number, mutable MoneyFlow account mapping and guessed bank identity are forbidden preset identity substitutes. Structural-only generic presets may prefill mapping but cannot suppress uncertainty or claim bank/source identity; any behavior that changes financial interpretation must be bound to the relevant parser/adapter semantic version and evidence.
- Recovery decision: preview→Inbox completion must become idempotent or reconcilable after uncertain responses. A blind retry is forbidden if candidate creation may already have succeeded. Durable outcome metadata should explain added/skipped/changed/review-needed results without storing raw statement payloads. Stable retry identity is reused for unchanged intent; fuzzy fingerprints remain advisory.
- Review decision: reuse the existing Ready/Needs-attention classifier and explicit approval authority. MON-63 may group deterministic rows for explicit bulk confirmation but must not create auto-posting or a second review taxonomy.
- Privacy decision: history/analytics may store counts, status, parser/mapping version and safe reason categories only; no raw rows, descriptions, amounts, source IDs, account identifiers or bank credentials enter generic telemetry/memory.
- Measured: selector claims no runtime improvement. The implementation packet requires before/after evidence on repeated mapping/review interventions and retry outcomes, with duplicate/correction safety preserved.
- Remaining: fresh exact-head selector policy/CI, CodeQL and Secret-history gates after all three evaluation corrections; final mergeability/review/thread recheck; then Ready handoff for explicit owner merge. After merge, fresh `plan:resolve` and `agent:doctor` are required before implementation.
- Production/provider evidence: #552 deployment evidence is read-only basis for this selector. PR #553 performs no Supabase DB/Auth/provider write, Vercel production mutation, external bank access or production financial-data write.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records #552 as merged/deployed/Done and describes #553's MON-63 authority transition in merge-stable conditional terms rather than a pre-merge-only snapshot.
- Superseded issue, roadmap or claim: supersedes stale memory that #552 remains owner-unmerged and that production baseline is `0bf9335c...`; it does not claim MON-63 is already executable before selector merge and does not enable target-bank presets/live sync.
- Lifecycle impact: authority transition from `PLAN_AUTHORITY.current: null` to the MON-63 packet if #553 is owner-merged; this selector does not complete the slice. A later completing MON-63 implementation PR must archive the packet, return `current → null`, reconcile memory and leave follow-on work unselected.
- Production writes: none.
- Merge: explicit owner authorization required; selector PR #553 must not be auto-merged from prior authorization for #552.

## Focused source record

1. Actual Budget API Reference — `https://actualbudget.org/docs/api/reference/` — stable `imported_id`, fallback reconciliation, dry-run and added/updated/errors outcomes; workflow reference only.
2. Actual Budget Importing Transactions — `https://actualbudget.org/docs/transactions/importing/` — file import, stable-ID-first duplicate avoidance and fallback matching; not a storage/provider blueprint.
3. YNAB File-Based Import — `https://support.ynab.com/en_us/file-based-import-a-guide-Bkj4Sszyo` — explicit CSV mapping, inflow/outflow swap and remembered account settings; not proof of Vietnamese bank layouts or semantic equivalence.
4. YNAB Approving and Matching Transactions — `https://support.ynab.com/en_us/approving-and-matching-transactions-a-guide-ByYNZaQ1i` — matching/approval and bulk-action workflow reference; MoneyFlow keeps its own readiness and approval authority.

## Permission boundary

#553 is a selector only. It may change planning authority if owner-merged. It authorizes no runtime code, migration/RPC, Supabase Auth/provider mutation, bank credential flow, scraping, provider API integration, Vercel production mutation, production-data write, raw-statement server retention or bank-specific preset/auto-map claim. Implementation remains blocked until selector owner merge plus fresh repository authority resolution/doctor.
