# PR #553 — select MON-63 acquisition UX

- Date: 2026-09-09
- Status: selector candidate; executable only after owner merge
- Base: `main@05323e2cb45609a85b3e7e3f2a4af94679149c31`
- Change class: Class 0 authority/planning change selecting a Class 3 acquisition/data-integrity/user-flow packet; no runtime, schema, provider or production mutation in this PR.
- Affected capability or project boundary: executable plan authority, M1 Vietnam Acquisition Depth, import mapping/retry/history/review UX, current project memory.
- Status impact: if owner-merged, this PR changes executable authority from `current: null` to MON-63. It does not implement MON-63 or select later work.
- Changed: adds the MON-63 packet, projects `PLAN_AUTHORITY.current` to it with `selectedByPr: 553`, reconciles current memory and records selector provenance/evaluation.
- Verified: selector branch starts from exact `main@05323e2cb45609a85b3e7e3f2a4af94679149c31`; selector-base authority was `current: null`; #552 is merged/deployed/Done. Final selector acceptance is latest-head only; Ready-state evidence is recorded in PR checks/conversation rather than by post-green evidence commits.
- Repository reconnaissance: import batches/history already exist and raw files are not retained. `import-preview-page.tsx` creates candidates before finalizing batch metadata, so partial completion makes blind retry unsafe. Fresh-main review also confirmed current Direct CSV already has an active device-local remembered-mapping seam.
- Evaluation finding 1 — preset semantics: equal normalized headers can still encode incompatible date/amount/direction semantics. Structural evidence may prefill mapping but cannot silently elevate confidence, suppress uncertainty or become source/bank identity.
- Evaluation finding 2 — merge-stable durable memory: current memory separates selector-base truth from #553 projection so the same snapshot stays truthful across owner merge.
- Evaluation finding 3 — project-knowledge heading contract: CI #3395 correctly rejected a renamed required `## 9. Open pull-request memory` heading. The heading was restored; that failed head is non-acceptance evidence.
- Evaluation finding 4 — selector base is not implementation base: pre-merge `05323e2...` is selector evidence only. Runtime implementation must resolve and record fresh main after owner merge of #553.
- Evaluation finding 5 — live Direct CSV preset seam: fresh-main review found `src/lib/inbox/direct-csv-mapping-preset.ts` already ships v1 normalized-header mapping persistence and `direct-csv-import-page.tsx` actively reads it from `localStorage`, requires explicit user application and reminds the user to verify dry-run. The selector was corrected from stale-branch-only framing: MON-63 hardens/generalizes this live capability; the stale preset branch is provenance only.
- Historical branch evidence: stale `feat/p2-direct-csv-mapping-presets` is not a code source because its preset helper is already incorporated in current main. Stale `fix/direct-import-retry-idempotency` remains principle evidence for stable idempotency-key reuse and the limitation of mounted-attempt-only recovery.
- Research refresh: Actual Budget documents stable imported IDs, fallback reconciliation, dry-run and outcome counts. YNAB documents explicit CSV mapping, inflow/outflow swap, remembered settings and matched/bulk review patterns.
- Research limits: competitor behavior does not prove Vietnamese bank layouts, source identity, storage or mutation semantics. Header equality is not source or semantic evidence. No evidence justifies raw-statement retention or a background queue.
- Architecture decision: current Direct CSV structural preset remains explicit/user-applied and dry-run reviewed. Any broader persistence requires deterministic versioning, privacy-safe eligibility and parser/adapter semantic binding when financial interpretation can change.
- Recovery decision: preview→Inbox completion must become idempotent or reconcilable after uncertain responses. Stable retry identity is reused for unchanged intent; fuzzy fingerprints remain advisory.
- Review decision: reuse existing Ready/Needs-attention classifier and explicit approval authority; no auto-posting and no second taxonomy.
- Privacy decision: history/analytics may store counts/status/version/reason categories only; no raw rows, descriptions, amounts, source IDs, account identifiers or credentials.
- Measured: selector claims no runtime improvement. Implementation must show fewer repeated mapping/review interventions without duplicate/correction regression.
- Remaining: lifecycle-conditional — latest exact head must independently pass required CI/CodeQL/Secret-history and mergeability/thread checks; once Ready and green, only explicit owner merge may activate MON-63; after merge, fresh `plan:resolve`, `agent:doctor` and recorded post-selector-merge main SHA are required before runtime work.
- Production/provider evidence: #552 deployment evidence is read-only basis. #553 performs no Supabase DB/Auth/provider write, Vercel production mutation, bank access or production financial-data write.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records #552 as merged/deployed and #553 in merge-stable conditional terms, including the current live Direct CSV preset seam.
- Superseded issue, roadmap or claim: supersedes stale claims that #552 is unmerged, that `05323e2...` is an implementation base, or that Direct CSV mapping presets exist only in a stale branch. It does not claim MON-63 is executable before selector merge.
- Lifecycle impact: owner merge transitions `PLAN_AUTHORITY.current: null` to MON-63. The later completing implementation PR must archive the packet, return `current → null`, reconcile memory and leave follow-on work unselected.
- Production writes: none.
- Merge: explicit owner authorization required.

## Focused source record

1. Actual Budget API Reference — stable `imported_id`, fallback reconciliation, dry-run and added/updated/errors outcomes; workflow reference only.
2. Actual Budget Importing Transactions — stable-ID-first duplicate avoidance and fallback matching; not a storage/provider blueprint.
3. YNAB File-Based Import — explicit mapping, inflow/outflow swap and remembered settings; not proof of Vietnamese layouts.
4. YNAB Approving and Matching Transactions — matching/approval and bulk-action workflow reference; MoneyFlow keeps its own readiness/approval authority.

## Permission boundary

#553 is a selector only. It may change planning authority if owner-merged. It authorizes no runtime code, migration/RPC, Supabase Auth/provider mutation, bank credential flow, scraping, provider API integration, Vercel production mutation, production-data write, raw-statement retention or bank-specific preset/auto-map claim. Implementation remains blocked until selector merge plus fresh authority/doctor and exact post-selector-merge main baseline resolution.
