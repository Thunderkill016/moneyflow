# PR #532 — defer non-critical landing and dashboard work

- Date: 2026-09-02
- Change class: Class 2 bounded performance/UI-loading behavior
- Affected capability or project boundary: public landing critical resources; authenticated dashboard capture-dialog loading; performance regression guard
- Status impact: none — current #527 remains active; this PR applies only the smallest critical-path deferrals supported by current production/source evidence
- Changed: below-fold landing story screenshots no longer use `priority`; dashboard AddTransactionDialog and TransferDialog remain `next/dynamic` but mount only while open; performance contract tests pin both behaviors
- Verified: production baseline before code showed `/` prerendered while explicitly preloading `/landing/moneyflow-quick-capture.svg`; Vercel build used Next.js 16.2.11 and classified `/` static and `/dashboard` dynamic. Exact-head run at `4e6b1d5...` passed unit/static/build/CodeQL/secret scan; policy stopped browser shards because the PR-memory snapshot sentinel was malformed, not because of runtime behavior.
- Remaining: rerun exact-head CI after correcting snapshot metadata; inspect browser/UI behavior and any available after-state request evidence; run same-methodology quantitative performance evidence where accessible; do not claim the owner-reported score 39 is resolved without that evidence
- Production/provider evidence: read-only Vercel production HTML/build logs only; no provider configuration/deployment write initiated by this PR
- Snapshot update: not applicable
- Superseded issue, roadmap or claim: none; #403 remains historical provenance, #523 remains unselected, and score 39 remains owner-observed rather than independently reproduced
- Lifecycle impact: continues current slice #527; does not complete it and does not select follow-on work

## Phase A evidence boundary

Current production root HTML explicitly contained an image preload for `/landing/moneyflow-quick-capture.svg`. Source places that screenshot in the story section after the hero and marks only index 0 `priority`; the other story images already lazy-load. Removing that priority removes unnecessary eager transfer without changing visible layout or content.

`MoneyFlowDashboard` already dynamic-imported the AddTransactionDialog and TransferDialog with `ssr: false`, but rendered both unconditionally with `open={false}` during initial hydration. Official Next guidance for deferred modals requires the dynamic component itself to be conditionally rendered so its JavaScript is requested on first open. The change preserves the same handlers, account/category inputs, mutation functions and transfer handoff.

No fresh Lighthouse score, LCP/TBT or analyzer module-byte table is claimed yet. The connected Vercel tools expose build/request evidence but not the Speed Insights metric query; the local execution container cannot resolve `github.com` for a fresh checkout. Historical #403 figures therefore remain provenance only.

## Safety boundary

No finance calculation, optimistic mutation logic, ledger data path, integer-VND behavior, transfer neutrality, schema/RLS/Auth, provider settings, Analytics/Speed Insights, font strategy, loading boundary or design semantics change. If browser/UI checks show that conditional unmount changes dialog behavior, revert that mechanism rather than weakening the checks.
