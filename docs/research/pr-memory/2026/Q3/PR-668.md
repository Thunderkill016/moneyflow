# PR #668 — Near-tier duplicate/transfer flags within ±2 days

- Date: 2026-09-22
- Change class: Class 1 (bounded code in one subsystem — inbox detection + presentation labels)
- Affected capability or project boundary: `src/lib/inbox/detect.ts` (near-tier duplicate + transfer matching), `src/lib/inbox/candidate-store.ts` (optional `duplicateDayDiff`/`transferDayDiff`/`nearMatch` fields + guard), `src/lib/inbox/review.ts` (tier-aware Explain text), `src/components/inbox/inbox-page.tsx` (badge labels + analytics props).
- Status impact: implements the bounded flag-only experiment specified in `docs/research/STAGE1_MATCHING_GAP_ANALYSIS_2026-09.md` (PR #667). Stage-1 scorecard item "duplicate and transfer matching measured" now has both the widened heuristic and the measurement (`flagged`/`near_match` props on `candidate_approved`/`candidate_rejected`).
- Changed: near tier matches same account+amount+normalized-description with date distance 1–2 days, flag-only (no auto-merge/approve/mutation); empty descriptions excluded so the loose key cannot degrade to amount+date noise; exact fingerprint always wins; transfer pairing window widened to ±2 days with account-difference dominance and same-account exclusion preserved; Explain/badges report real basis (`lệch N ngày`) instead of claiming same fingerprint/day.
- Verified: detect.test 13/13, inbox tests 40/40, typecheck, lint (1 pre-existing warning), full suite 1492/1492, check:knowledge, ci-policy 191/191, build 51 routes.
- Remaining: kill/keep rule needs real usage data (near-flag rejection vs approval rates); durable `match_status`/`match_reason`/`match_confidence` columns still unwritten — deliberate, this experiment stays render-time; ±2 window sizing is a hypothesis pending real import data.
- Production/provider evidence: none (render-time heuristic; browser smoke delegated to CI lanes).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: if near-flag rejection burden is high, narrow or remove the tier per the spec note; if it holds, consider whether durable match fields should be populated — that is a separate, larger decision.
