# PR #609 — P2 capability transports work packet

- Date: 2026-09-20
- Change class: Class 0 delivery mechanics for a Class 3 spec (docs-only; no runtime change)
- Affected capability or project boundary: `docs/plans/active/p2-capability-transports.md` (new packet), issue #608 (new backlog entry)
- Status impact: candidate → specified (packet awaits owner decision on D1–D6; implementation not authorized)
- Changed: added the P2 packet covering recon (cookie-only auth seam, redirect semantics, registry dispatch, existing limiter), research (MCP auth + Streamable HTTP specs, Supabase OAuth 2.1 server beta, official TS SDK), rejected alternatives (PATs, MCP-only, service-role), staged plan P2a Bearer+dispatch → P2b stateless MCP+RFC9728 → P2c OAuth+consent, six owner decision points with defaults
- Verified: no runtime diff — CI static gates on the PR head
- Remaining: owner resolves D1–D6 and explicitly approves implementation; P2c additionally needs owner enabling the Supabase OAuth beta in project settings
- Production/provider evidence: none
- Superseded issue, roadmap or claim: none; records the transport half of the P2 follow-up named in PR #605
- Lifecycle impact: adds an active packet — per repo rules a packet's presence does not authorize work; issue #608 tracks the backlog item

## Boundaries

- Packet is a spec artifact: no code, config or provider state changed.
- PAT minting is documented as rejected (RLS bypass) — future proposals should not re-open it without new evidence.
- OAuth `scopes` do not gate data access; per-client narrowing would rely on `client_id`-aware policy (deferred).

## Evidence limits

- Supabase OAuth 2.1 server is beta; provider behavior verified only from documentation, not a live project.
- MCP spec has a newer 2026-07-28 revision; the packet requires pinning the conformance target at implementation time.
