# PR #662 — Budgets/reports/capture: honest error states + prerequisites

- Date: 2026-09-22
- Change class: Class 1 (bounded UI honesty fixes; no domain/RPC changes)
- Affected capability or project boundary: `src/components/planning/budgets-page.tsx`, `src/components/planning/budget-dialog.tsx`, `src/components/reports-page.tsx`, `src/components/inbox/capture-quick-page.tsx`, `src/components/inbox/capture-paste-page.tsx`.
- Status impact: third dead-end/honesty audit pass (budgets, reports, capture-quick, capture-paste), applying the same defect classes fixed in #660 — fabricated zero summaries under dataError, toasts occluded behind open modal dialogs, silently disabled/dead actions with no explanation.
- Changed:
  - `/budgets` "Xóa hạn mức" failure closes `PlanningReviewDialog` before the error notice (toast was invisible under the top-layer modal).
  - `/budgets` + `/reports` under `dataError` no longer render "0 ₫" summaries or "Chưa có…" claims; error `EmptyState` + "Về Tổng quan" (pinned EmptyState slices preserved).
  - `/budgets` empty state routes to `/categories` when no expense category exists; a hint line explains the disabled "Thêm ngân sách" when every category is budgeted; `BudgetDialog` warns when the editing budget's category is archived (previously an empty select + dead submit).
  - `/capture/quick` not-ready state links to whichever prerequisite is missing (`/accounts` vs `/categories`).
  - `/capture/paste` removes a dead `setNotice` immediately before `router.push("/inbox")` — AppShell unmounts, so it never painted.
  - `/reports` trend-empty copy "Chưa có giao dịch" contradicted the transfer-inclusive transaction meta — now "Chưa có khoản thu hoặc chi".
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning); typecheck; build; check:knowledge, check:architecture, check:css-ownership, check:deployment-env; pinned suites green (52 tests: phase7-planning, mvp-empty-state-release-gate, budget-month-history, planning-pages, phase8-secondary-safety, r7-reports, reports-trend, ghi-chi-dialog).
- Remaining: none from this audit's confirmed list. e2e pins `Chưa có ngân sách cho` heading on an older demo month — preserved in the non-error branch; CI e2e shard is the final check.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
