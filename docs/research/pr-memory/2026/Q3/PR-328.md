# PR #328 — Audit service-role read-only hardening

- Date: 2026-08-09
- Change class: Class 3 database/security least-privilege migration
- Affected capability or project boundary: MoneyFlow Trust Provider Sync / financial mutation audit effective privileges
- Status impact: none until merge and production application; this PR is a candidate forward migration for the live audit ACL mismatch
- Changed: adds a forward migration that revokes all table privileges from `service_role` on `public.financial_mutation_audit_events` then restores SELECT only; adds pgTAP coverage for DML and PostgreSQL 17 table-management privilege denial; updates the active Provider Sync packet
- Verified: repository/current-memory/config/audit migrations/tests reconciled; live production ACL read shows `service_role=arwdDxtm/postgres`; official Supabase and PostgreSQL privilege behavior reviewed; exact-head CI/database/CodeQL/secret evidence pending
- Remaining: run exact-head Class 3 gates; independent diff review; owner merge decision; after merge a separate explicit owner checkpoint is required before applying the ACL migration to production; Edge v5 remains blocked afterward until live ACL evidence is clean
- Production/provider evidence: read-only inspection only for this PR; production audit table currently grants effective SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER to `service_role`; no production ACL/DDL, Edge, provider-config or user-data write has been performed by PR #328
- Snapshot update: not applicable
- Superseded issue, roadmap or claim: none; this candidate addresses the open Provider Sync audit least-privilege blocker recorded by #327

## Evidence boundary

Supabase documents that older projects may carry automatic Data API table grants while newer defaults are opt-in; changing default privileges affects future objects and does not repair existing objects. PostgreSQL documents that effective access is the sum of direct, PUBLIC and inherited grants. Production inspection found a direct table grant and no alternate role-membership path that would restore the revoked audit-table privileges.

## Safety

- no historical migration is edited;
- no project-wide default privilege is changed;
- authenticated own-row audit SELECT remains unchanged;
- trigger-owned audit writes remain unchanged;
- production writes require a later explicit owner checkpoint after candidate merge/review.
