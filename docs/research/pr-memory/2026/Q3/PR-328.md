# PR #328 — Audit service-role read-only hardening

- Date: 2026-08-09
- Change class: Class 3 database/security least-privilege migration
- Affected capability or project boundary: MoneyFlow Trust Provider Sync / financial mutation audit effective privileges
- Status impact: none until merge and production application; this PR is a reviewed candidate forward migration for the live audit ACL mismatch
- Changed: adds a forward migration that revokes all table privileges from `service_role` on `public.financial_mutation_audit_events` then restores SELECT only; adds pgTAP coverage for DML and PostgreSQL 17 table-management privilege denial; updates the active Provider Sync packet
- Verified: exact-head source candidate `da02ede839bbcec4f9a5512384af1d90231dc52e`; CI #2111 policy/knowledge/classification passed; fresh Supabase reset applied `20260809010648_financial_audit_service_role_read_only.sql`; 26 pgTAP files / 481 tests passed; CodeQL #1210 and Secret history #1210 passed; browser/cross-device work was intentionally not selected because no app/UI path changed; independent diff review found the table-local REVOKE ALL → GRANT SELECT correction preserves authenticated own-row reads and trigger-owned audit writes
- Remaining: final exact-head protected checks after this evidence-only docs update; owner merge decision; after merge a separate explicit owner checkpoint is required before applying the ACL migration to production; live ACL read-back/log observation is required before the Edge checkpoint
- Production/provider evidence: read-only inspection only for PR #328 so far; production audit table currently grants effective SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER to `service_role`; no production ACL/DDL, Edge, provider-config or user-data write has been performed by PR #328
- Snapshot update: not applicable
- Superseded issue, roadmap or claim: none; this candidate addresses the open Provider Sync audit least-privilege blocker recorded by #327

## Evidence boundary

Supabase documents that older projects may carry automatic Data API table grants while newer defaults are opt-in; changing default privileges affects future objects and does not repair existing objects. PostgreSQL documents that effective access is the sum of direct, PUBLIC and inherited grants. Production inspection found a direct table grant and no alternate role-membership path that would restore the revoked audit-table privileges.

The clean-reset evidence proves the forward migration composes with the full repository migration chain and pgTAP contract. It does **not** prove the production ACL is already fixed; only a later owner-approved provider write plus live effective-privilege read-back can establish that.

## Safety

- no historical migration is edited;
- no project-wide default privilege is changed;
- authenticated own-row audit SELECT remains unchanged;
- trigger-owned audit writes remain unchanged;
- production writes require a later explicit owner checkpoint after candidate merge/review.
