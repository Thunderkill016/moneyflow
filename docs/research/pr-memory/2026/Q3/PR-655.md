# PR #655 — First-run guidance on rules/imports empty states

- Date: 2026-09-22
- Change class: Class 1 (bounded copy/conditional-render change; shared `EmptyState` contract preserved — one primary + one secondary)
- Affected capability or project boundary: `src/components/inbox/rules-page.tsx`, `src/components/inbox/imports-page.tsx`, `src/lib/ui-phase8-secondary-safety-contract.test.ts`.
- Status impact: closes two first-run gaps found during the overnight upgrade sweep — `/rules` previously dropped its only forward action when the viewer had zero active categories (silent dead end), and `/imports` never told a first-run user what file formats to prepare.
- Changed:
  - Rules empty state: when `availableCategories.length === 0` the primary action becomes "Tạo danh mục" → `/categories` with copy explaining a rule needs a category target. With categories present the description teaches by example (chứa "HIGHLANDS" → Ăn uống) and restates that rules only normalize candidates, never post transactions.
  - Imports empty state: description now names accepted inputs (CSV UTF-8, .xlsx/.xls, PDF text-layer, ≤10MB).
  - Contract test pins the no-category guidance path and the format copy.
- Verified: focused contract test 6/6; `npm run typecheck` clean; browser verify on dev build (390px, demo, cleared rule/import stores): `/rules` shows teach-by-example copy + "Thêm quy tắc" + "Về Inbox"; `/imports` shows format line + "Tải sao kê" + "Dán nội dung".
- Remaining: full `npm test` + build run on CI lanes; the no-active-categories branch is unreachable in seeded demo (exercised via code review only).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
