# PR #578 — public VCB/ACB/VietinBank XLSX pilot hardening

- Date: 2026-09-11
- Change class: Class 3 — import/data-integrity follow-up
- Affected capability or project boundary: XLS/XLSX acquisition pilot, generic transaction-date/debit/credit role selection, overlap duplicate handling and privacy-safe public-format regression coverage
- Status impact: continues GitHub #576 / Plate THU-46 after PR #577; does not enable a bank-specific parser or provider-confirmed source identity
- Changed: added synthetic regressions modeled on publicly indexed Vietcombank, ACB and VietinBank eFAST statement structures; prefer explicit transaction date; recognize safe bilingual debit/credit/description roles; support exact standalone VCB direction signs; add database overlap fallback verification; record observed VietinBank overlap-reference stability without relaxing source-ID persistence guards
- Verified evidence: public VietinBank eFAST statements for the same account overlap on 31/03/2026 and preserve transaction numbers 1942/1943/1944 with matching date/time, amount and description; this is `observed-but-unverified`, not a provider guarantee. No downloaded real statement or customer financial row is committed.
- Safety boundary: canonical `sourceExternalId` still requires `confirmed + source-stable`, so the observed VietinBank reference is not persisted as source identity; bank-specific auto-map remains off.
- Remaining: exact current downloaded-file contracts; provider-guaranteed fee semantics; guaranteed transaction-number stability beyond the observed pair; normalization-changing overlap cases; fresh exact-head CI/CodeQL/secret-scan after the compatibility evidence update; owner review and merge decision
- Production/provider evidence: not applicable — no migration, schema/RLS, provider configuration, production-data or bank-system write is included
- Superseded claim: the blanket statement “VietinBank transaction reference stability unknown” is narrowed to “source-stable observed across one public overlapping pair, still unconfirmed as provider contract”
