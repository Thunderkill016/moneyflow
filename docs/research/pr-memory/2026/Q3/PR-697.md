# PR #697 — RRB-02 hosted restore evidence + multi-device write semantics note

- Date: 2026-09-23
- Change class: Class 0 (documentation/evidence records only; no runtime change)
- Affected capability or project boundary: `docs/plans/active/public-beta-trust.md` evidence log and gates table; `docs/operations/beta-owner-action-pack.md` RRB-02 disposition drafts + checklist; new `docs/operations/multi-device-write-semantics.md`.
- Status impact: RRB-02 hosted restore is no longer "unexecuted" — a bounded proof ran on the live Supabase project; the formal close remains an owner disposition. Multi-device last-write-wins is now documented current truth instead of an assumption.
- Changed: recorded the 2026-09-23 hosted rehearsal in the trust packet (disposable auth tenants A→B, export→delete→restore round-trip, per-table counts matched, zero residue) and updated the action pack's RRB-02 draft disposition to a "closed" draft citing the evidence. Added an ops note describing LWW semantics (update RPCs lock rows `for update` but take no version precondition), existing mitigations (idempotency keys, soft delete, audit events, advisory locks), and the smallest honest future increment (`expected_updated_at` check) explicitly marked Class 3 / not implemented.
- Verified: `npm run check:knowledge` pass; `npm run test:ci-policy` 191/191 pass. Rehearsal evidence: `export_user_archive` gen `20260922120000` → `restore_user_archive` restored_row_counts matched tenant_row_counts; a direct `monthly_budgets` INSERT was refused `42501` under `role authenticated` (hosted grants enforce RPC-only writes); `auth.users` returned to 5 with zero rehearsal residue.
- Remaining: owner formal disposition for RRB-02; RRB-03 (destructive recent-auth edges) still needs authorize-or-accept; backup schedule/retention console read-back still open.
- Production/provider evidence: the rehearsal itself is the hosted evidence (disposable tenants, RLS-isolated, fully cleaned up).
- Superseded issue, roadmap or claim: the packet/action-pack lines stating hosted restore was unexecuted.
- Lifecycle impact: if restore semantics change (e.g., a version precondition lands), update `multi-device-write-semantics.md` in the same diff.
