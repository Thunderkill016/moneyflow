# PR #588 — deterministic ledger trusted-through boundary

- Date: 2026-09-11
- Change class: Class 3 — read-only financial trust derivation / database contract
- Affected capability or project boundary: P2 maintenance measurement, reconciliation-derived ledger trust, known review backlog, tenant-scoped read model
- Status impact: implements GitHub #586 / Plate THU-54 as the smallest decision-changing slice under THU-44; does not claim source completeness and does not deploy production state
- Changed: adds `public.ledger_trust_summary()` as a stable SECURITY INVOKER authenticated-only function; derives a base boundary from latest clean completed reconciliations across all active accounts; reduces trust before the earliest pending Inbox item, `needs_review` transaction, or unreconciled active-account leg; excludes archived accounts; exposes only bounded dates/reasons/counts plus `known_ledger_state_only`; adds 27 pgTAP assertions covering tenant isolation and counterexamples
- Verified: pending fresh exact-head CI/database/CodeQL/secret-scan on PR #588
- Remaining: machine verification; independent review; owner merge decision; any production migration rollout requires separate explicit owner authorization; THU-44 still separately lacks truthful intervention/100 and active-maintenance-minute instrumentation
- Production/provider evidence: no production write performed; contract was designed against read-only hosted Supabase schema/ACL inspection and existing completed-reconciliation snapshot semantics
- Superseded issue, roadmap or claim: supersedes the weaker idea that backlog absence plus reconciliation completion alone is enough for a trusted period; backdated known ledger facts with unreconciled account legs now explicitly reduce the trust boundary
- Privacy boundary: no descriptions, amounts, account IDs, source IDs or raw payloads are returned or copied into analytics storage; output is aggregate trust metadata only
- Semantic boundary: `trusted_through` means MoneyFlow's known ledger state is trusted through that date; it never means every external provider/source event has been observed, imported or synced
