# PR #428 — chore: remove one unused dependency and one dead module

- Date: 2026-08-18
- Change class: Class 1 — removes `@radix-ui/react-slot` from `package.json` and deletes `src/lib/supabase/client.ts`. No runtime behaviour, schema, RLS, Auth, provider, CI or product change.
- Affected capability or project boundary: dependency surface and dead code. Nothing a user can observe changes.
- Status impact: candidate. Closes no gate.
- Changed: dropped an unused dependency and an unimported browser Supabase client factory. `@radix-ui/react-dropdown-menu` is the only Radix package actually used, and the app performs its Supabase work server-side, so the browser factory was a leftover rather than a gap.
- The measurement matters more than the change: I went looking on the hypothesis that the client bundle was full of waste, and across the entire codebase there was exactly one unused dependency and one dead module. Root main files stay at **446.1 KB** after both removals, confirming neither reached the bundle. **This is hygiene, not a performance improvement, and no improvement is claimed.**
- Tooling caution worth recording: `depcheck` reported five unused dependencies and four were false positives. `shadcn`, `tailwindcss`, `@tailwindcss/postcss` and `tw-animate-css` are used through CSS `@import` or `postcss.config.mjs` rather than JavaScript imports. Acting on the tool's output would have deleted `shadcn` and broken the build; each was verified by hand first.
- Verified: `build`, `typecheck`, `lint`, `check:architecture`, `check:css-ownership`, `check:code-css-ownership`, `check:knowledge`, `test:ci-policy`, `check:migrations` and 1093 unit tests pass. Exact-head provider checks are authoritative.
- Remaining: the real client-weight finding is that `rootMainFiles` is 446.1 KB of framework runtime shipped on every route, and the gap between `/` (195.8 KB transferred) and `/dashboard` (311.6 KB) is roughly 116 KB of dashboard client components. Reducing either is architectural work with modest payoff, not a simple win, and an earlier hypothesis that splitting the app shell would be the large lever is **not supported** by this measurement.
- Production/provider evidence: not applicable. No provider write, deployment, production-data access or production verification.
- Snapshot update: not applicable while Status impact remains candidate.
- Superseded issue, roadmap or claim: supersedes my own working hypothesis that the codebase carried significant dead weight. It did not.
