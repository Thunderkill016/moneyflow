# PR #430 — docs: remove a vendor-tooling doc and stop citing absent evidence

- Date: 2026-08-18
- Change class: Class 0 — documentation only. No runtime, test, schema, RLS, Auth, provider, CI or product change.
- Affected capability or project boundary: documentation accuracy. Nothing a user can observe changes.
- Status impact: candidate. Closes no gate and changes no release state.
- Changed: deleted `docs/CLAUDE_SKILLS.md`, which had no inbound reference from any document or any code and whose own opening lines describe it as "vendor/tool-support materials, not MoneyFlow product, project-state". Corrected `docs/performance-budgets.md`, which cited `logs/lighthouse-landing-q8.json` and `logs/lighthouse-insights-q8.json` as the evidence behind its Pass 3 table; those files are git-ignored and exist only on the machine that produced them, so the citation resolved to nothing for anyone else. The table is the durable record and the text now says so.
- Bundled deliberately: two pull requests for a 51-line file and a one-line citation would cost more process than the changes are worth, which is the tax this project has been reducing.
- What checking first prevented, worth recording because it contradicted my expectation twice: `rate-limit.md` and `performance-load-acceptance.md` appeared orphaned by a documentation grep but are both live — one is referenced from code and the other is the execution appendix of `performance-budgets.md` through a relative path the search missed. And three pairs previously suspected of duplication — `design-system.md`/`CURRENT_DESIGN_SYSTEM.md`, `UX_PRINCIPLES.md`/`product/PRINCIPLES.md`, `COMPETITOR_AND_OSS_RESEARCH.md`/`PRODUCT_COMPETITIVE_MEMORY.md` — share zero headings and are detail-plus-summary pairs rather than copies. That duplication suspicion is withdrawn.
- Verified: `check:knowledge`, `test:ci-policy`, `lint`, 1093 unit tests and `git diff --check` pass. Exact-head provider checks are authoritative.
- Remaining: after today's archive of sixteen superseded documents and this removal, no further stale documentation was found. The remaining 344 markdown files are 130 policy-mandated PR memory records, 60 completed packets, and live referenced material.
- Production/provider evidence: not applicable. No provider write, deployment, production-data access or production verification.
- Snapshot update: not applicable while Status impact remains candidate.
- Superseded issue, roadmap or claim: retires the claim in `performance-budgets.md` that its Pass 3 figures are backed by retrievable JSON artifacts, and withdraws my earlier suspicion that the documentation set carried duplicate authority files.
