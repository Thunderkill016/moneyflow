# PR #453 — feat(inbox): save confirmed review as rule

- Date: 2026-08-24
- Change class: Class 3 — a reviewed financial-candidate classification preference crosses the Inbox/domain boundary, while leaving ledger mutation and rule persistence architecture unchanged.
- Affected capability or project boundary: explicit Inbox review, deterministic candidate-stage rules, demo/browser-local persistence, authenticated rule-action reuse, and #432 P2 low-maintenance ingestion.
- Lifecycle impact: advances current slice #452; it does not complete, archive, project or select a following slice.
- Status impact: candidate only until merge. A user may explicitly save a non-transfer reviewed merchant/category confirmation as a future candidate-stage rule; the reviewed candidate remains pending and the rule cannot create a ledger transaction.
- Changed: adds a pure reviewed-rule seed helper that rejects transfers, unknown merchants and mismatched categories; adds a review-dialog confirmation/action that reuses `saveRuleForClient`; passes demo/auth mode from Inbox; adds unit and browser regression coverage; registers the Class 3 packet and #452 on Current Work.
- Verified: the seed test was first observed failing because the helper did not exist, then passed. Fresh targeted ESLint and typecheck passed. Focused Inbox domain and UI contract tests passed. The demo review-save browser regression passed in Chromium desktop and mobile; it proves the saved rule exists and the current candidate remains pending. `check:knowledge`, `test:ci-policy` and diff hygiene passed before the final follow-up commit. Production build compiled successfully before the final follow-up commit but its terminal completion line was not captured, so it is not recorded as a final local build pass. Full unit/test, e2e and responsive-audit commands were launched locally but their terminal sessions exceeded the tool capture window; exact-head provider CI remains the authoritative full evidence.
- Remaining: exact-head PR #453 CI, review, full build/browser/UI audit as selected by CI, and authenticated-environment rule action/RLS behavior. Merge remains owner-authorized only.
- Production/provider evidence: not applicable. No provider connection, credential, deployment, production-data write, migration, RPC or RLS policy change is in scope.
- Snapshot update: this branch registers #452 as the one active Class 3 P2 child. It carries no post-merge completion projection.
- Superseded issue, roadmap or claim: supersedes no shipped behavior; it removes only the need to navigate away and re-enter a confirmed rule after review.
- Rollback: revert PR #453. Existing Inbox approval and Rules workspace remain available; any rule already saved can be edited, disabled or deleted through `/rules`.
- Untrusted external instructions copied into project memory: none.
