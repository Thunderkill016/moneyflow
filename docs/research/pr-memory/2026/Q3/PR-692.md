# PR #692 — Emit goal `created_at` in the dashboard bundle for pace counting

- Date: 2026-09-23
- Change class: Class 3 (data boundary — `create or replace` of an authenticated RPC projection; no schema-shape, grant, RLS or semantic change)
- Affected capability or project boundary: `supabase/migrations/20260923120000_dashboard_bundle_goal_created_at.sql` (`get_dashboard_bundle` goals projection gains `created_at`), `supabase/tests/database/dashboard_read_bundle.test.sql` (plan 17→19), `supabase/migration-identity.json` baseline.
- Status impact: partial → implemented. PR #691's dashboard pace chip counted only overdue goals on the primary one-RPC path because bundle rows arrived with `createdAt: null` and `goalPaceStartDate` found no anchor (the page passes `allocations: []`); behind-pace is now provable there, matching `/goals` and the multi-query fallback which read via `getGoalsWorkspace` (already selecting `created_at`).
- Changed: `get_dashboard_bundle` re-created byte-identical to the newest prior body (20260922120000) plus `'created_at', goal.created_at` inside the goals `jsonb_build_object`; `stable`, security invoker, `search_path=''` and the revoke/grant block preserved verbatim. Raw `timestamptz` is emitted — the same implicit jsonb cast `transactions[].created_at` already uses — so `vietnamDay` keeps owning the Asia/Ho_Chi_Minh day conversion; a `::date` cast in SQL would have baked in the session timezone. Additive-safe: `goalSchema.created_at` is `nullish`, so app deploys run ahead of or behind the migration without parse failure.
- Design decisions: no `mapGoalRow`/`dashboardBundleSchema`/`page.tsx` change was needed — the parse contract already tolerated the field; the gap was purely the SQL projection. pgTAP seeds a tenant-B goal and pins both presence/type (`? 'created_at'` + `jsonb_typeof = 'string'`) and value parity against `to_jsonb(savings_goals.created_at)`, which survives any session DateStyle.
- Verified: `npm run typecheck` clean, `npm run lint` 0 errors (1 pre-existing warning in `quick-add-defaults.test.ts`, untouched), `npm run test` 1751/1751, `npm run build` green, `check:knowledge` pass, `test:ci-policy` 191/191, `check:migrations` pass (68 pinned, baseline regenerated via `--write`). Function body diffed against the 20260922120000 definition — the only delta is the added field line.
- Remaining: `test:db` not runnable locally (no Docker/Podman — `npx supabase status` fails at container inspect); pgTAP `dashboard_read_bundle.test.sql` (plan 19) and the `database` CI lane carry the live-PostgreSQL evidence. `backup_reminder_state.test.sql` and `financial_read_plan_regressions.test.sql` assertions on the same function remain satisfied (tenant predicates, `-62` bound, `p_recent_limit > 20` all verbatim).
- Production/provider evidence: none at open; migration applies on deploy, owner decision post-merge. Affected production verification: pace chip on a behind-pace goal.
- Superseded issue, roadmap or claim: resolves the "Remaining" item recorded in `PR-691.md`.
- Lifecycle impact: if the bundle goals projection changes again, keep `created_at` — dropping it silently reverts behind-pace counting to overdue-only with no parse error, because the field is `nullish` on read.
