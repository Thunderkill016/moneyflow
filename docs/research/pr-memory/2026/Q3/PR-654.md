# PR #654 — Batch account picker on paste preview

- Date: 2026-09-22
- Change class: Class 1 (bounded UI/helper change in one subsystem; candidate `accountId`/`account` plumbing already existed end to end)
- Affected capability or project boundary: `src/lib/inbox/parse-text.ts` (`toCreateCandidateInputs` optional `{ account }`), `src/lib/inbox/parse-text.test.ts`, `src/components/inbox/capture-paste-page.tsx`, `src/app/capture/paste/page.tsx` (`getFinanceWorkspace` for accounts, same as #652).
- Status impact: closes the remaining third of the account-resolution gap — `/capture/paste` was the last acquire path that never asked which account a batch belongs to, so pasted rows landed "Tài khoản chưa xác định" even though the user knew the source. Sibling halves: #650 (Inbox bulk assign), #652 (statement-import preview).
- Changed:
  - Preview phase gains an opt-in "Các mục này thuộc tài khoản" `SelectField` — default "Chọn sau trong Inbox"; the parser's no-bank-inference stance is preserved.
  - Chosen account stamps `accountId`/`account` on every generated candidate input; the candidate/provenance/Inbox review path is unchanged, no ledger writes from paste.
- Verified: TDD (`toCreateCandidateInputs` RED → GREEN); `npm test` 1487/1487; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:css-ownership, check:architecture, check:deployment-env, test:ci-policy. Browser verify on dev build (390px, demo): paste 3 lines → select lists 4 demo accounts with `(USD)` suffix → commit with MB Bank → Inbox row shows "MB Bank".
- Remaining: CI lanes on exact head; share-target capture (`/capture/share`, `/api/share-target`) is a separate surface that still lacks a batch account question — flagged as possible follow-up, not bundled here.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
