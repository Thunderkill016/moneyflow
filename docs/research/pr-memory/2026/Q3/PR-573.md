# PR #573 — #570 production runner and execution evidence

- Date: 2026-09-11
- Base: owner-merged `main@ab02529b954c59dfe7335776ee9ec47c8cc18f9c` after selector PR #572.
- Change class: Class 3 operational tooling + lifecycle evidence; production remains untouched until owner merge and explicit execution.
- Affected capability or project boundary: #570 hosted Supabase migration reconciliation, #567 production security verification, MON-63 durable maintenance-measurement truth, and one temporary production migration workflow.
- Status impact: #570 is active authority. PR #573 provides a version-preserving Supabase CLI execution path but remains unmerged, so first production write is still blocked.
- Changed: adds `.github/workflows/production-supabase-migrations.yml`, reconciles `CURRENT_PROJECT_MEMORY.md`, updates the active #570 packet, and records execution/preflight evidence.
- Verified: hosted migration history still ends at `20260825090000`; all four selected versions are absent. Exact `main@ab02529b...` migration-directory inspection proves the repository has only those four files after the hosted tail. Production PostgreSQL is 17.6; target import/measurement objects are absent; reconciliation snapshot remains SECURITY DEFINER; postgres global future-function ACL override is absent; tenant-own RLS remains enabled on the affected import tables; no blocked sessions or non-idle transaction older than 30 seconds were present at capture time.
- Production/provider evidence: read-only hosted Supabase inspection confirms the selected four-version gap and pre-rollout catalog/RLS/lock state. Supabase project is ACTIVE_HEALTHY. No production migration, migration-history mutation, provider-setting change, user-data write, runtime change or UI change has occurred in #570.
- Canonical file identity: `20260909090000_import_batch_atomic_commit.sql`=`9968c56e70e1eb7427bec8a146de9705d8071985`; `20260909120000_import_batch_measurement.sql`=`745588452f68f7ad42c0eac4b11b02acb846b5b2`; `20260910181500_function_default_acl_hardening.sql`=`b25bb94bdc73932926f1fac206db753f2716c6bb`; `20260910182000_reconciliation_snapshot_security_invoker.sql`=`0b9898e316057e88834a66b74044e7bccd96e4fe`.
- Research: official Supabase guidance refreshed 2026-09-11 recommends CI/CD/GitHub Actions for production migrations and continues to use `migration list` -> `db push --dry-run` -> `db push`; remote reset and production seed are forbidden. Current stable Supabase CLI is 2.117.0. MCP issue #241 confirms `apply_migration` cannot preserve an existing caller-supplied migration version.
- Execution design: PR #573 pins `supabase/setup-cli` action identity and CLI 2.117.0; workflow is owner-triggered only, checks current #570 authority, verifies all four canonical Git blobs, verifies a SHA-256 fingerprint of the intended production project ref, scopes production secrets only to remote Supabase steps, compares local/remote history, requires the exact four-version dry-run, serializes runs, and refuses a second apply after convergence.
- Trigger boundary: `workflow_dispatch` defaults to preflight and is restricted to `github.actor == github.repository_owner`; apply additionally requires exact confirmation. An exact OWNER-authored command on GitHub issue #570 may also trigger preflight/apply after merge; non-owner comments cannot run the job. The workflow has only `contents: read` repository permission.
- Safety: no automatic deploy on PR/push; no `migration repair`, manual history insertion, remote reset, seed, direct SQL patch, MCP timestamp synthesis, provider/Auth/WAF change, user-data backfill or UI work. The temporary runner must be removed after #570 production verification.
- Remaining: exact-head CI/CodeQL/Secret History and workflow review; explicit owner merge; then first run preflight-only. Apply is allowed only if authority, project fingerprint, canonical blobs, migration list and dry-run all pass. After apply, run bounded S3/S4 hosted verification and remove the temporary runner during lifecycle closeout.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` is reconciled to active #570 authority, the four-version production gap, PR #573 temporary runner, and the owner-merge/preflight gate.
- Superseded issue, roadmap or claim: “#570 has no version-preserving execution path” is superseded at branch-design level by PR #573, but remains true for merged production until PR #573 is owner-merged. “MCP `apply_migration` is equivalent to `db push`” remains false for existing canonical timestamped migrations.
- Lifecycle impact: implementation/operations continue under current slice #570; this enabler PR does not complete the slice and does not project `PLAN_AUTHORITY.current` to null.

## Permission boundary

PR #573 is the separately reviewed temporary executor/evidence enabler for the already-selected #570 slice. It has no production execution force before explicit owner merge. After merge, preflight-only is the first allowed run; apply remains conditional on every fail-closed gate. Repository merge remains an explicit owner decision.