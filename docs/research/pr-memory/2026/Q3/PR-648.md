# PR #648 — X1/X2 offline dashboard cache + install surface

- Date: 2026-09-21
- Change class: Class 3 (new persistence surface — authenticated HTML in CacheStorage; security boundary on wipe points)
- Affected capability or project boundary: `public/sw.js` (fetch handler), `src/lib/pwa.ts` + `src/lib/pwa.test.ts`, `src/lib/service-worker-offline.test.ts` (vm harness), `src/components/layout/app-shell.tsx`, `src/components/offline-cache-reset.tsx`, `src/app/(auth)/layout.tsx`, `src/components/delete-account-page.tsx`, `src/app/settings/install/{page,loading}.tsx`, `src/components/install-app-page.tsx`, `src/components/settings-hub-page.tsx`, `src/components/icons.tsx`, `scripts/verify-offline-pwa.mjs`. Work packet: `docs/plans/active/offline-pwa-polish.md`.
- Status impact: X1/X2 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — implemented, browser-verified locally, pending CI + owner review.
- Changed:
  - SW fetch handler: network-first for navigations; caches **only** `/dashboard` into `moneyflow-offline-v1`; uncached routes offline get a calm notice linking to the saved overview. Push handlers untouched (22/22 focused tests incl. existing push-security tests).
  - Registration moved into `app-shell` — unconditional, no longer gated behind push opt-in.
  - Wipe points: `OfflineCacheReset` in `(auth)/layout.tsx` clears the versioned cache whenever the login surface renders (covers every sign-out path); both `performDeletion` flows wipe it explicitly.
  - `/settings/install`: deferred `beforeinstallprompt` flow with honest states — promptable, installed/standalone, iOS manual guidance, unavailable (guidance copy, no fake button). Settings-hub row + `download` icon.
- Verified: `npm test` 1484/1484; lint (1 pre-existing warning in `quick-add-defaults.test.ts`), typecheck, build, check:knowledge, check:architecture, check:deployment-env, check:css-ownership, test:ci-policy. Browser verify on production build: SW registered scope `/`, `moneyflow-offline-v1` holds `/dashboard`, hub row + install page render, offline `/dashboard` serves full cached copy, offline uncached route serves notice.
- Remaining: `beforeinstallprompt` promptable path is unit-tested via the install-state store — the real prompt cannot fire headless. First-load lifecycle: registering page is not controlled until next visit, so the cache populates on the second `/dashboard` load (verified). CI lanes pending on exact head. Owner review + merge decision required (Class 3).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: implements X1/X2. Roadmap items from `ALL_SURFACES_UPGRADE_PLAN_2026.md` now exhausted through X2; next item requires owner direction.
- Trap note: a stale `next start` process serving an older build produced false-negative browser evidence (404 on the new route, no SW registration). Verification must confirm the served build is fresh (`lsof`/`ss` the port, check BUILD_ID) before trusting results.
