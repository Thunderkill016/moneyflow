# PR #370 — feat: evolutionary refresh of the shipped UI (shell, Overview, Transactions)

**Merged** at `main@75ade3bc4ce9d17a099a38df704c8c82542447ee` on 2026-08-14, owner-accepted
for Slice 1. The packet is archived at
`docs/plans/completed/2026-08-14-current-ui-evolutionary-refresh-slice-1.md`.

Two cautions for anyone reading the merge commit rather than this record: the squash body
carries **intermediate metric claims from earlier commits on the branch**, which are
superseded — the authoritative four-state table is below — and the findings recorded here
are the history of the work, kept as written.

- Date: 2026-08-13
- Change class: Class 2 (UI presentation across shell + Overview + Transactions), confirmed by `agent:doctor`
- Affected capability or project boundary: AppShell chrome and mobile navigation; Overview hierarchy; Transactions evidence workspace
- Status impact: Historical #370 delivery: `current-ui-evolutionary-refresh.md` was active and owner-accepted for Slice 1. It is now **accepted, merged and archived** at `main@75ade3b`. **Phase E candidate exploration is paused** with every territory rejected and none selected; Phase F is not started; PBT-AC15 remains open
- Changed: sidebar recedes onto the page canvas at 228px with a marker-based active state; topbar 64px; mobile nav becomes a floating 70px bar with its inset folded into the shared reserve; Overview leads with the financial statement instead of the greeting; Transactions summary and manager share one surface and secondary filters move into a latching disclosure; one new per-theme token `--mf-border-subtle`
- Verified: unit 1057/1057, `test:ci-policy` 119, typecheck, lint 0 errors, architecture, both CSS-ownership gates, knowledge, migrations, registry guard. **Evidence types:** Design Harness V2 artifacts for `/dashboard` and `/transactions` at 1440×900 and 390×844; **390×568 is targeted browser measurement only — not a harness artifact and not physical-device evidence.** No physical-device validation is claimed

## Authoritative metric table (measured on the exact implementation head)

| Viewport and state | `main@55ee401` | this branch | delta |
|---|---|---|---|
| mobile 390 — no secondary filter | 791px | **271px** | −520px |
| mobile 390 — secondary filter active/open | 851px | **641px** | −210px |
| desktop 1440 — no secondary filter | 273px | **155px** | −118px |
| desktop 1440 — secondary filter active/open | 333px | **369px** | **+36px (regression)** |

Toolbar height, `/transactions`, demo runtime, measured in Chromium on the exact
implementation head against a dev server on both branches with the same data. The
filtered state uses `?review=needs_review`. Three of the four states improve; the
desktop filtered state is 36px worse than main because the disclosure adds its own
44px summary row above the same grids. That regression is kept visible rather than
omitted.
- Review findings: an independent evaluator ran the app and found five real defects, three of them blocking; all fixed and re-measured
- Remaining: Desktop-with-filter remains a knowingly accepted +36px regression; Overview below the statement is still a stacked panel set. The owner's blank phone-evidence file was removed from the delivery and remains untracked. No further UI work is implied by this record
- Production/provider evidence: none, and none claimed. No database, migration, Supabase, Auth, provider, financial-calculation, transfer-semantics, planning, reports, import, backup, route or nav-destination change
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` — recorded Phase E paused with every candidate rejected and no territory selected. Slice 1 is now merged/archived, not active UI work. No capability, financial or architectural truth changed; the update was to lifecycle narrative only
- Snapshot rationale: `not applicable` was wrong here and CI caught it after it passed locally, because the local diff base did not include the new record. The fourth time this exact gap has bitten; the fix is to update the snapshot, never to reword the status impact to dodge the rule
- Superseded issue, roadmap or claim: supersedes the old shell/Overview/Transactions geometry pinned by six contract tests. Does **not** supersede Fresh Blue, which remains the palette

## Why this exists

The owner rejected every Phase E territory. Rather than propose another direction, this
improves what actually ships. It is explicitly not Brand v3, not Design System v3, and it
does not complete Phase E.

## What the evaluator caught, and why it mattered

It ran the app rather than reading the CSS, which is the only reason three of these were
found:

- **The "one workspace" claim was false.** Summary and manager are grid siblings, so the
  18px workspace gap reopened the seam they were meant to close — leaving a card with no
  bottom edge, visible in light and dark on the flagship screen. Fixed by tying a negative
  offset to the gap variable so the two cannot drift.
- **The disclosure closed itself under the user's hands.** `open` derived straight from
  the filter state auto-*closed* when a filter was cleared, taking focus off the control
  being operated. It now latches: seeded from the initial filter state — the browser fires
  no toggle for a panel already open on mount — and carried by `onToggle` thereafter. All
  four cases verified in the browser.
- **I shipped a new 40px control while the commit message asserted no new sub-44px
  targets.** The disclosure summary is now 44px, and the claim was wrong when written.
- **The headline number was quoted selectively.** The mobile figure held only with no
  filter active; opened, the panel was taller than what it replaced. Two-up filter grids
  fixed that, and every document now carries one authoritative four-state table measured
  on the exact head — including the desktop filtered state, which remains a real
  regression.
- **Dark mode inverted the mechanism it claimed.** `--mf-surface-muted` is brighter than
  the manager surface in dark, so the filter band advanced instead of receding. Changed to
  `--mf-canvas`, which sits below the card in both themes; measured luminance 0.954 vs
  1.000 light and 0.0057 vs 0.0092 dark.

## Two regressions I caught myself, by measuring

Sidebar nav items had dropped to 42px, below the 44px floor, and the mobile planning strip
left a destination offscreen. Neither was visible by eye in a screenshot.

## Not claimed

- No physical-device evidence. Harness V2 encodes 1440 and 390×844; 390×568 is browser
  measurement, because this packet may not modify the harness.
- Local `npm run build` fails on `/accounts` identically on clean main, and four
  `auth-captcha` e2e specs fail on clean main without Turnstile keys. CI is the authority.
- "Materially better" is the owner's judgement, not a claim this PR can make.

## Related

- Archived packet:
  `docs/plans/completed/2026-08-14-current-ui-evolutionary-refresh-slice-1.md`
- Rejected-candidate provenance: GitHub PR #369 (closed, unmerged and retitled
  `docs: rejected/obsolete Phase E creative-territory candidates`). It has no repository
  PR-memory file because it never merged.
