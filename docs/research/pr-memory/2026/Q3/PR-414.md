# PR #414 — refactor: make Capture a compact keyboard-first sheet

- Date: 2026-08-16
- Change class: Class 2 bounded presentation/responsive redesign; no schema, RLS, Auth/provider, deployment or production-data mutation.
- Affected capability or project boundary: high-frequency manual Expense/Income capture presentation during RRB-08 physical-device follow-up; financial mutation and learned-preset semantics remain outside this presentation-only change.
- Trigger: direct owner real-phone screenshots after #412. Capture 3 learned-preset logic is functionally fast, but the sheet still presents repeated instructions, oversized type controls, a heavy category card, clipped horizontal category suggestions, redundant Cancel + close affordances and unnecessary common-path height.
- Status impact: candidate only. #413 is owner-authorized current work. RRB-08 remains open and final acceptance remains owner-observed on a physical phone after merge/deployment.
- Changed: default modal title resolves to `Ghi khoản chi` / `Ghi khoản thu`; amount retains an accessible label without a visible explanatory row; type switching is compact but touch-safe; selected category/account appears once; only two alternate categories plus `Khác` are exposed in the first decision surface; full account/category controls remain on demand; optional date/note/keep-open remain secondary and still reachable; modal footer removes redundant visible Cancel while embedded capture keeps explicit cancel; constrained-height spacing is compact without deleting the secondary-detail entry point.
- Preserved: #412 coherent learned presets, first-use explicit category requirement, integer-VND parsing, explicit Save, idempotency, optional note rules, keep-open semantics, trusted transfer handoff and transfer neutrality.
- Research rationale: Apple HIG Entering Data recommends pre-gathering known data and minimizing decisions; Designing for iOS recommends prioritizing primary tasks with secondary actions discoverable through minimal interaction; Apple Sheets (2026-03-24) recommends brief scoped sheet tasks; current Android Material 3 ModalBottomSheet/window-inset guidance keeps critical controls outside IME/system obstruction. External patterns inform presentation only; MoneyFlow code/tests remain authority.
- CSS discipline: Capture-specific presentation stays in `capture-fast-path.module.css`, reuses existing `--space-*`, radius, typography, motion and semantic color tokens plus shared `targetSize` primitives. No CSS `!important` or `:global()` is introduced. The candidate no longer overrides the shared dialog max-height with a new arbitrary percentage; established responsive breakpoints are reused.
- Verification contract: source contracts reject horizontal `overflow-x:auto` in Capture 4; dedicated 390×568 audit requires concise title, selected context, exactly two quick category correction buttons, `Khác`, closed full/secondary disclosures and explicit Save reachable; normal browser smoke, cross-device UI audit and e2e remain required.
- Verified: ready-state CI reached full static/build/unit/browser execution. CI #2594 on head `78797f…` passed policy, production build, unit/static RLS, static quality, Browser smoke, CodeQL #1661 and Secret history #1661. A partial prior UI-audit artifact exposed two presentation-contract defects before final UI completion: the full `Khác` panel was rendered by its author `display:grid` even while the native `<details>` was closed, squeezing full-category buttons into the third quick-action grid column and producing sub-44px widths; and the constrained-height rule hid the closed optional-detail summary entirely, blocking a rich-state note flow. The implementation now defaults `.morePanel` to `display:none` and enables it only under `.moreDisclosure[open]`, while keeping the compact optional-detail summary reachable at constrained height. Exact-head UI/browser/e2e must rerun after these fixes before candidate acceptance.
- Production/provider evidence: not applicable; no provider or production write/read-back is claimed.
- Snapshot update: not applicable while PR #414 remains candidate. Current Work Board + active packet own execution state; durable `CURRENT_PROJECT_MEMORY.md` changes only after merge alters current capability truth.
- Superseded issue, roadmap or claim: #411/#412 remain shipped authority for Capture 3 learned-preset behavior; #413/#414 supersede only the heavy Capture 3 presentation as the current candidate UX. #403 performance work remains NEXT and held behind this one-at-a-time capture follow-up.
- Remaining: run corrected exact-head gates, inspect phone-width/constrained artifacts, then hand the merged/deployed build back for owner physical-device observation.
