# PR #309 — Phase 8 secondary and safety ownership

- Date: 2026-08-06
- Change class: Class 3 — cross-cutting authenticated UI/safety migration with preserved financial and provider boundaries
- Affected capability or project boundary: Reports, Categories, Inbox individual/bulk review, Rules, import history/preview/direct CSV, Settings hub, appearance, notifications, transaction/Inbox export, privacy preferences, account deletion and deletion-result receipt
- Status impact: verified implementation candidate with owner-authorized merge once the final exact-head governance refresh passes; current product behavior remains `main` until that squash merge completes
- Changed: introduced shared secondary/safety workspace/header/summary/section/review owners; migrated active secondary and data-rights surfaces to local CSS/primitives; made report export scope explicit; exposed category icon/color identity; added reviewed category hide, Inbox bulk, rule delete, import metadata/commit/cancel/direct-ledger and account-deletion flows; added demo candidate-based approval retry identity; disabled inactive parser-sharing consent; added account-deletion cleanup receipt; kept Rules creation reachable on mobile after the first rule exists
- Preserved: report formulas/range repair/transfer exclusion, flat category domain and history, authenticated atomic Inbox approval, candidate-only rules, review-first default import path, Timeline Phase 5 ownership, local file export generation, notification opt-in, theme preference and server-first account deletion
- Verified: implementation head `6bc575b2b8f6c0949f19c011f0327a7413f84f97` passed CI #1936 (`31150496664`) including policy/static/unit/build/browser/cross-device/e2e, CodeQL #1047 and secret-history #1047; this governance reconciliation changes the head and therefore still requires one refreshed exact-head run before merge
- Remaining: pass selected exact-head gates on the final governance head, squash merge under the owner's authorization, reconcile issue #72 with the final automated evidence and observe the automatic Vercel deployment without claiming physical-device/provider evidence that was not run
- Production/provider evidence: no manual deployment, provider setting, production data, schema, migration, RLS or new RPC operation is included; provider-backed recent re-auth remains a separate hardening boundary; an owner-authorized merge is expected to auto-trigger Vercel
- Snapshot update: records P8 as post-merge UI-system truth while preserving physical-device, provider recent-auth and complete archive/restore gaps
- Superseded issue, roadmap or claim: supersedes the initial suspicion that authenticated Inbox approval was non-atomic; repository inspection proved the existing authenticated action/RPC path is atomic and the actual uncovered retry gap was demo/browser transaction identity

## Authorization and safety

- Owner authorization: explicit `làm đi` on 2026-08-06 authorized bounded Phase 8 implementation; the owner later explicitly authorized merge once exact-head gates are complete.
- Branch: `feat/ui-phase-8-secondary-safety` from the Phase 7-era main lineage and kept current against the reviewed PR base.
- Packet: `docs/plans/active/ui-phase-8-secondary-safety.md`.
- Pull request: #309, created draft while implementation was in progress and later marked ready for cumulative verification.
- Merge authorization: granted by the owner on 2026-08-07, conditioned on complete exact-head gates and an unchanged mergeable head.
- No schema, migration, RLS, provider, production-data or manual deployment write is included.

## Product and data truth

- Reports export the resolved report/transaction range and are not a complete restorable account backup.
- Category color/icon aid recognition while kind and hidden/active state remain textual.
- Authenticated Inbox approval continues through the existing atomic approval action; demo approval now links the browser ledger row to the candidate identity so a failed candidate-state write can be retried without creating a duplicate.
- Rules remain deterministic candidate normalization evaluated in priority order; they do not auto-post transactions.
- Import history distinguishes authenticated server batch/candidate storage from browser draft storage and demo-local storage.
- The default import path remains parse → preview → Inbox → approval → ledger. Direct CSV import is explicitly advanced, retains dry-run/dedupe/transfer-skip behavior and requires a final review.
- Transaction/Inbox CSV/JSON export is not represented as an all-domain archive or restore mechanism.
- The parser-improvement field remains readable for compatibility but the unavailable capability cannot store `true` consent.
- Authenticated account deletion remains server-first, then best-effort local cleanup. Cleanup verification and partial local failure are surfaced in a public noindex receipt. Recent password/OAuth re-auth is not claimed without a separately specified provider-backed implementation.

## Evaluation record

Findings are corrected in the owning boundary:

1. Repository inspection overturned the initial non-atomic-authenticated-Inbox concern. The existing authenticated approval path already owns the atomic mutation and server-side pre-check; no redundant RPC/schema change was added.
2. Demo transaction creation previously used a random ledger ID independent of the candidate. Candidate identity is now the demo idempotency link, and retry checks the browser ledger before posting.
3. Reports already had custom ranges, repaired/clamped windows and previous-period comparison. P8 migrates ownership and accessibility rather than rewriting report math.
4. Timeline was already migrated in Phase 5 and remains verification-only.
5. Import copy that mixed authenticated server synchronization with a broad `local-first` claim was replaced by mode-specific storage language.
6. Parser-improvement UI looked like effective consent despite no remote processing path. The capability is disabled and the domain normalizes historical `true` values to false.
7. Account-deletion query parameters were previously not rendered. A dedicated receipt distinguishes server cleanup verified/unverified and local cleanup complete/partial.
8. Direct CSV import already had dry-run/dedupe/result depth, but the final ledger write did not have a separate review step. It now does and clearly states there is no batch undo.
9. Provider-backed recent password/OAuth verification remains outside this phase; typed confirmation is not represented as recent-auth proof.
10. The recovered exact-head CI run exposed a real mobile Rules regression rather than a flaky selector: after one rule existed, the empty-state CTA disappeared while AppShell hid its primary action below 760px. `showPrimaryActionOnMobile` is now enabled only when a visible rule list replaces the empty-state CTA, preserving one primary add action and allowing creation of a second rule on mobile.

## Intermediate evidence

Early draft implementation head `f18693f97432cda320534a01373b840bd194aa12`:

- CI classifier #1872 / run `31092981166`: success;
- secret-history scan #988: success;
- expensive draft jobs were skipped by policy and are not claimed as verification.

First ready-for-review cumulative head `69030e093f3d8a0c6a67998558b701e70214556c`:

- classifier selected full verification and passed;
- policy correctly rejected missing standard governance markers, which later updates restored;
- other implementation jobs are evaluated independently and must not be inferred from the policy failure.

Recovered implementation head `09d68b54983c919c657a1a532de57615bc765498`:

- GitHub Actions incident recovery eventually produced CI #1935;
- policy/static/unit/build/database/verify passed;
- browser smoke passed 83/84 tests and exposed the mobile Rules add-action defect; the failure was treated as product evidence rather than hidden with a timeout/selector workaround.

Corrected implementation head `6bc575b2b8f6c0949f19c011f0327a7413f84f97`:

- CI #1936 / run `31150496664`: success;
- Browser smoke: success, including the previously failing mobile Rules flow;
- Cross-device UI audit: success across the selected Chromium/WebKit matrix;
- stable `e2e` aggregator: success;
- CodeQL #1047: success;
- secret-history scan #1047: success.

The final governance-only reconciliation commit intentionally changes the PR head after this implementation evidence, so repository policy still requires refreshed exact-head checks before merge. Final run IDs belong in the PR metadata to avoid a self-referential evidence-commit loop.

## Evidence interpretation

The implementation is merge-ready only when the final cumulative head passes policy, project-memory, static quality, complete unit/static-RLS, production build, browser smoke, cross-device Chromium/WebKit and security workflows. Physical Android/iOS acceptance and provider-backed recent re-auth remain explicit limitations even after automated evidence passes.