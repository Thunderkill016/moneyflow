# PR #673 — Payee as a first-class ledger field

- Date: 2026-09-22
- Change class: Class 3 (financial/data — schema migration + archive contract generation)
- Affected capability or project boundary: `supabase/migrations/20260922120000_transaction_payee.sql` (column + RPCs + feed/bundle + archive `*_pre_payee` wrappers + reconciled-guard exclusion), `src/lib/archive/payee-archive-validator.ts` + `payee-archive-ingress.ts` (third archive generation `20260922120000`), `src/lib/transactions/contracts.ts`, `src/server/finance.ts`, `src/app/actions/{transactions,inbox-approval,archive}.ts`, `src/components/{add,edit}-transaction-dialog.tsx` (+ `payeeSuggestions` datalist in `src/lib/quick-add-defaults.ts`), workspace subtitles, `transactions.search` capability schema/haystack/golden, `docs/agents/capabilities.json`.
- Status impact: closes the structural gap where Inbox `merchant` was flattened into `note` at approval — payee provenance was destroyed, blocking payee-grouped reports, payee→category memory, and honest "Số này từ đâu?" surfaces. Payee is first-class in every top comparator (Actual, Monarch, Firefly, Money Lover).
- Changed: `financial_transactions.payee text not null default ''` + ≤200 check; `create/update_money_transaction`, `create_split_expense`, `approve_inbox_candidate` gain `p_payee` appended last (old positional callers bind unchanged; batch approval falls back to persisted `candidate.merchant`); recurring commitment/template names become payees; feed + dashboard bundle project it; `guard_reconciled_transaction_mutation` excludes `payee` as descriptive metadata (money fields still locked); archive generation `20260922120000` strips/delegates/post-applies via the proven source-lineage wrapper pattern — generations 0416/0945 unchanged.
- Verified: `npm test` 1533/1533 (archive 7/7 incl. all-three-generations ingress regression, `transactions.search matches payee text`), typecheck, lint, build 51 routes, knowledge, migrations 65 pinned, ci-policy 191/191, architecture, css-ownership, rls 9/9, capabilities manifest. `test:db` + `test:e2e` not runnable locally (no Docker/Playwright browsers) — CI gates.
- Remaining: pgTAP `transaction_payee.test.sql` (17 assertions) and dialog e2e are CI-only evidence at PR time. Payee is write-path only — no payee grouping/report surface exists yet; that is the follow-on this unlocks. Archive restore applies payee post-delegation; the audit trigger diffs only kind/note/occurred_on so no audit noise.
- Production/provider evidence: none (schema ships via CI migration gate; no provider writes).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: payee→category suggestion memory and payee-grouped reports are the natural next slices now that provenance survives commit; if `payee` stays unused in captures, revisit whether Inbox review friction is worth the field.
