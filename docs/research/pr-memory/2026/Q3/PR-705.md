# PR #705 — Stop marking proceed/reversible actions as destructive

- Date: 2026-09-23
- Change class: Class 1 (two `intent` prop changes; no behavior/schema change)
- Affected capability or project boundary: `direct-csv-import` actions row; `accounts` card action row.
- Status impact: candidate → implemented. Destructive visuals are reserved for discard/delete again.
- Changed: CSV import "Xem lại ghi N giao dịch" → `primary` (forward action, matches import-preview); accounts "Lưu trữ" row button → `quiet` (reversible via archive surface; archive dialog confirm unchanged).
- Verified: `npm run typecheck` clean; `npm run check:knowledge` pass.
- Remaining: no browser evidence of the new button tones yet — CI visual lanes will exercise them.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: future proceed-vs-discard button pairs should follow the import-preview convention (primary + destructive); reversible actions must not use `destructive`.
