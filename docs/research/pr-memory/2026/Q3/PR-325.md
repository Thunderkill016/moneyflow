# PR #325 — MoneyFlow Trust provider-drift reconciliation

**Status:** active PR memory  
**PR:** #325  
**Branch:** `agent/moneyflow-trust-reconcile-p1`  
**Base:** `main@fd984a18201f1663d3d8c622d51c41dfd650c816`  
**Scope:** docs/current-memory + read-only provider reconciliation

## Outcome

Correct repository authority after #324 merged. The initial post-merge interpretation treated Vercel `READY` as if Phase 1 Secure were deployed end-to-end. Live Supabase inspection disproved that assumption before #325 merged.

PR #325 now records the split provider truth and creates the Provider Sync prerequisite without performing any production write.

## Current truth recorded

- canonical short program name: **MoneyFlow Trust**;
- conceptual sequence: **Secure → Recover → Prove → Improve → Release**;
- operational prerequisite: **Provider Sync → Secure acceptance → Recover**;
- #316 closed superseded and retained only as historical evidence;
- #324 merged as `fd984a18201f1663d3d8c622d51c41dfd650c816`;
- Vercel `dpl_8Eak3CqtjepuqY4mnq5UTLHwfeq9` is READY for the exact Next.js commit;
- ordinary unauthenticated delete-account/login routing is verified on that Vercel deployment;
- explicit one-hour Vercel runtime-error check found no runtime errors;
- MoneyFlow Supabase project `fwpldsdkpzhswpuctbke` is ACTIVE_HEALTHY;
- production `delete-account` is still Edge Function **version 5** and its source lacks the merged recent-auth claim gate;
- production v5 tenant inventory is older than current main;
- production database migration history is behind confirmed merged MoneyFlow migrations for FK indexes, reconciliation, authenticated rules and financial mutation audit;
- production catalog lacks `account_reconciliations`, `account_reconciliation_events`, `inbox_rules` and `financial_mutation_audit_events`;
- production contains `transaction_import_provenance`, while the deployed purge RPC does not explicitly delete/verify it and provenance references candidate/transaction rows with `ON DELETE RESTRICT`;
- no destructive production request was executed;
- P1 state is corrected to **`merged`**, not `deployed`;
- P2 Recover implementation is blocked until provider alignment + Secure acceptance.

## Corrected inference

The first #325 draft said P1 was `deployed` because the exact #324 commit was Vercel READY. That was too broad. Vercel deploys the Next.js application; Supabase database migrations and Edge Functions require their own provider lifecycle. The branch was corrected before acceptance rather than preserving a convenient but false progress state.

## Changed authority files

- `docs/research/CURRENT_PROJECT_MEMORY.md`
- `docs/plans/active/account-deletion-recent-auth.md`
- `docs/plans/active/public-beta-trust.md`
- `docs/plans/active/moneyflow-trust-provider-sync.md`
- this PR memory

## Provider Sync finding

Confirmed missing merged migration files currently include:

- `20260802060004_cover_foreign_key_indexes.sql`;
- `20260803142000_account_reconciliation_current_main.sql`;
- `20260803144500_account_reconciliation_ci_hardening.sql`;
- `20260803153000_account_reconciliation_workspace_read_model.sql`;
- `20260804110000_authenticated_deterministic_rules.sql`;
- `20260804160000_financial_mutation_audit.sql`;
- `20260804160100_financial_read_plan_indexes.sql`;
- `20260804160200_financial_audit_service_role_inspection.sql`;
- `20260804160300_financial_audit_request_id_token.sql`.

This set is **confirmed but not yet claimed exhaustive**. Provider Sync requires a complete local/current-main vs remote migration comparison before owner write approval.

## Boundaries

No runtime code, UI, schema, migration, RLS, provider configuration, Edge deployment, production data or financial semantics were changed by #325.

Provider actions used during discovery were read-only:

- list/read Supabase Edge Function metadata/source;
- query migration history, catalog tables, function definitions and FK definitions;
- read Vercel deployment/routes/runtime errors.

## Verification

Exact-head project-knowledge/policy/CodeQL/secret checks must pass after the provider-drift correction before #325 can become an owner merge checkpoint.

The PR must not claim:

- P1 provider deployment;
- production recent-auth enforcement;
- production reconciliation/rules/audit schema alignment;
- provider-backed password/Google step-up acceptance.

## Remaining risk and next allowed action

Highest-priority next work is Provider Sync planning:

1. mechanically complete current-main-vs-remote migration inventory;
2. review every missing migration and rollout dependency;
3. fresh replay + full pgTAP/security evidence;
4. return to owner with the exact migration set/order and rollback/forward-fix strategy;
5. make **no provider write** until explicit owner approval.
