# PR #664 — Measure Stage-1 intervention and correction burden

- Date: 2026-09-22
- Change class: Class 1 (bounded code in inbox subsystem + analytics union extension)
- Affected capability or project boundary: `src/components/inbox/inbox-page.tsx` (event firing), `src/lib/inbox/review.ts` (`draftWasEdited` helper), `src/lib/safe-analytics.ts` (new `candidate_field_assigned` event name), `src/lib/inbox/review.test.ts` (contract tests).
- Status impact: first canon-derived work after #663 — closes the instrumentation gap where `candidate_approved`/`candidate_rejected` were declared in the event union but never fired, making the Stage-1 "intervention and correction burden measured" gate requirement unmeasurable.
- Changed: `candidate_approved` fires per candidate on single + bulk approve with `kind`, `source`, `edited`, `bulk`; `candidate_rejected` fires with `kind`/`source`/`possible_duplicate` (single) and `count`/`bulk` (bulk); `candidate_field_assigned` fires on bulk account/category assignment with `field` + `count`. `draftWasEdited` compares the submitted draft against the candidate-derived draft; `allowHeuristicDuplicate` acceptance deliberately does not count as an edit. Privacy-safe scalars only — no merchant/note/raw text.
- Verified: `tsx --test review.test.ts` 15/15; `npm test` 1490/1490; `typecheck`, `lint` (1 pre-existing warning), `build` 51/51; `check:knowledge`; `test:ci-policy` 191/191.
- Remaining: coverage gaps stay unmeasured — manual retype rate, maintenance minutes and trusted-period retention still have no instrumentation path; bulk approve does not record per-candidate failure reasons (only counted).
- Production/provider evidence: none — events are inert without a Web Analytics runtime; demo mode fires them harmlessly.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: enables the Stage-1 scorecard's intervention/correction metrics; the next canon step is deciding the smallest acquisition-path experiment this data can evaluate.
