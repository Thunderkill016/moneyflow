# PR #380 — fix(tooling): harden Codex dispatcher boundaries

- Date: 2026-08-14
- Change class: Class 1
- Affected capability or project boundary: owner-started local Codex dispatcher and
  its isolated GitHub-to-Codex execution boundary.
- Status impact: candidate; the open draft does not change product or production
  capability.
- Changed: stable body-marker identity, Markdown-context rejection, fresh-main
  validation before worktree creation, per-command Git/GitHub CLI guard, token-variable
  stripping and child-environment propagation, deterministic regression tests, and the
  active packet/registry.
- Verified: focused dispatcher tests 20/20, typecheck, migration identity, knowledge,
  CI-policy invocation, lint with zero errors (three existing warnings), and diff
  hygiene. The new child-process test was observed failing before `defaultRun` forwarded
  the environment and passing after the fix.
- Remaining: `npm test` is not green because existing code-CSS ownership, dead-CSS
  scanner and deployment-environment guard tests fail outside this diff. The required
  post-hardening authenticated smoke and exact-head provider checks remain pending.
- Production/provider evidence: none. No product runtime, schema/RLS, Auth, CI workflow,
  provider or production change.
- Snapshot update: not applicable; an open PR is candidate evidence only.
- Superseded issue, roadmap or claim: hardens the merged dispatcher from #377 in response
  to issue #379; #377 remains historical delivery evidence.
