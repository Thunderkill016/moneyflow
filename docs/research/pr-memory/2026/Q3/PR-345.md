# PR #345 — test: add authenticated financial truth harness scenario

- Date: 2026-08-12
- Change class: Class 3 (cross-cutting verification of financial semantics; test-only executable slice)
- Affected capability or project boundary: authenticated browser harness and dashboard financial-truth verification
- Status impact: candidate only — no user-facing capability or production financial behavior changes
- Changed: extended `e2e/auth/harness.ts` so synthetic accounts/categories/balances can vary without changing existing defaults; added a canonical two-account income/expense/transfer fixture and `e2e/auth/financial-truth.mobile.auth.spec.ts`; added the Class 3 work packet `docs/plans/active/financial-truth-harness-v1.md`
- Verified: source-level reconnaissance and focused external research complete; exact CI/browser evidence pending on the draft PR
- Remaining: exact PR CI, authenticated-phone contract execution, independent diff evaluation and owner merge decision
- Production/provider evidence: none — no application production code, provider setting, migration, database row or real financial data was changed or requested
- Snapshot update: not applicable — candidate verification tooling only; current implementation/product capability truth is unchanged
- Superseded issue, roadmap or claim: none; this extends the authenticated ownership harness from #336 and preserves the financial/domain authority already encoded in current code/tests

## What this slice proves when green

The canonical authenticated fixture contains two VND accounts, one income, one
expense and one internal transfer. The browser contract drives the real login
and `/dashboard`, requires the healthy `get_dashboard_bundle` path to be served,
and checks independent literal outcomes:

- total balance `2,700,000` VND;
- period income `2,000,000` VND;
- period expense `300,000` VND;
- period net `1,700,000` VND;
- transfer `500,000` VND remains visible with transfer semantics but does not
  enter income, expense or net.

Expected totals are not calculated by importing the production dashboard
summarizer or production money formatter.

## Evidence boundary

This is an application/browser composition harness. It proves the real
authenticated Next.js/Supabase-client route, server mapping, dashboard
composition and rendered semantic outcome against a strict synthetic HTTP
double. It does **not** prove PostgreSQL, RLS, tenant isolation, SQL/RPC bodies,
provider settings or production behavior; those remain owned by pgTAP/provider
and production evidence.

The strict double still returns `501` for unimplemented requests and records a
miss. The browser contract requires the miss ledger to stay empty, so a missing
server query cannot silently degrade into empty financial data.

## Research decision

The repository already owns an authenticated harness, CI classifier and
repository-backed agent workflow. Research supported strengthening deterministic
environment/outcome feedback, not building another `mf` CLI or agent platform.
The selected slice therefore extends the existing harness and adds no dependency,
runtime or management layer.

## Related

- Work packet: `docs/plans/active/financial-truth-harness-v1.md`
- Existing authenticated harness origin: PR #336
- Presentation false-green precedent: PR #337
- Current base at branch creation: `bc26a6fa6181c8e0e90b2fdd44f0b69088a03c17`