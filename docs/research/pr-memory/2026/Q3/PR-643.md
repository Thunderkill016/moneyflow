# PR #643 — Dashboard statement month-shape strip + prior-window compare

- Date: 2026-09-21
- Change class: Class 2 (bounded UI on the existing statement; new pure domain module)
- Affected capability or project boundary: `src/lib/dashboard-month.ts`, `src/lib/dashboard-transaction-window.ts`, `src/components/dashboard/statement.tsx` + `statement.module.css`, `dashboard-overview-sections.tsx`, `moneyflow-dashboard.tsx`.
- Status impact: D2+D4 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — the statement now shows when in the month spending happened and the factual prior-window total, without a second data path.
- Changed: new `monthStatementDetail()` lifts `reportRange(today, "month")` + `buildFinancialReport()` verbatim — shape is `report.trend` daily buckets (expense only), compare is `report.previous.expense` plus a prior-window row count so an unrecorded prior month stays silent instead of printing 0. `dashboardTransactionStart` additionally covers the monthly `previousStart`, keeping the loaded window bounded under ~2 months for the RPC, fallback and demo paths alike. Statement renders a 26px baseline strip (8% minimum mark for non-zero days, `role="img"` + aria-label + per-day titles) and a muted "Kỳ trước cùng N ngày: chi X" caption — factual only, no verdict.
- Verified: TDD — `dashboard-month.test.ts` failed on missing module, passes 5/5 after; window tests re-pinned (`2026-07-26`→`2026-06-05`, `2026-08-02`→`2026-07-20`); phase4 contract pins the lift-from-reports invariant. `npm test` 1452/1452; tsc, lint, build, check:knowledge/architecture/css-ownership/deployment-env, test:ci-policy clean. Browser-verified production build at 390/1280px: strip renders 21 marks, compare reads "Kỳ trước cùng 21 ngày: chi 45.000 ₫", zero `data-money` overflow, and both stay silent correctly when the seeded rows vanish.
- Remaining: CI on exact head; owner merge decision.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes dashboard D2/D4; next in plan order is the Capture V2 benchmark run (C1), then P1, R1/R2, S1, X1/X2.
