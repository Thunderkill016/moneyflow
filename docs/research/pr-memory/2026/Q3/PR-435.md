# PR #435 — feat(acquisition): make Direct CSV provenance-backed and batch-atomic

- Date: 2026-08-21
- Change class: Class 3 — financial import/mutation boundary.
- Parent authority: #432 P1 Acquisition Foundation; bounded child issue #434.
- Base: `main@a35d6f96960e889cf988d9d37d4320a8f674cd85`.
- Affected capability or project boundary: authenticated Direct CSV acquisition, import provenance, heuristic duplicate override and the atomic ledger/provenance/approval boundary for one persisted import batch. Demo-local import, transfers, provider connectivity and reconciliation ownership remain outside this slice.
- Status impact: candidate until merge. If accepted, authenticated Direct CSV stops bypassing the persisted acquisition/provenance contract and gains an all-or-nothing ledger/provenance/approval commit boundary. It does not add bank/e-wallet/provider/native capability and does not alter public-beta readiness.
- Changed: adds `approve_inbox_candidates_batch(uuid,jsonb)` around the existing single-candidate approval contract; persists Direct CSV batch/candidate evidence before financial commit; carries source row, raw snippet, parser/mapping version and confidence into persisted candidates; moves authenticated Direct CSV off ordinary per-row `addTransaction`; keeps demo browser-local; preserves transfer review in Inbox; preserves the explicit `Bỏ qua dòng trùng` choice by passing `allowHeuristicDuplicates: !skipDuplicates`; pins the new migration identity; updates UI consequence copy to state batch rollback before commit and per-transaction lifecycle after a successful commit.
- Verified: non-draft CI #2733 on head `709f1f48303d629d16505e1bc54f30238e012838` proved production build, lint/typecheck/static quality, fresh Supabase reset + pgTAP/database, CodeQL #1795 and Secret history #1795 while correctly exposing stale migration identity and two stale unit/document contracts. Those failures were remediated without weakening the migration checker. On exact head `fc1858665b28d183f1d491878adca0d5621dc79b`, CI #2737 has already passed diff hygiene and migration identity (`42 migrations pinned`) and the policy shard correctly found this PR-memory schema incomplete; that governance failure is being fixed by this commit. Secret history #1799 passed on `fc1858665…`; CodeQL #1799 and remaining CI #2737 shards were still running when this memory record was updated. Final handoff still requires a new exact-head run after this commit.
- Remaining: exact-head Class 3 revalidation after this PR-memory fix, including project knowledge + CI classification/retry, unit/static RLS, build, fresh database/pgTAP, aggregate verify/e2e as selected, CodeQL and secret history; independent review of partial-commit rollback, retry ambiguity, duplicate override, tenant isolation and copy; then owner merge decision. No merge is authorized by this memory record.
- Production/provider evidence: not applicable. This slice performs no production/provider write, bank/e-wallet connection, deployment, payment initiation, physical-device verification or production-data mutation.
- Snapshot update: not applicable while this PR remains candidate; `docs/research/CURRENT_PROJECT_MEMORY.md` is intentionally unchanged until the financial behavior is merged and exact-head verified.
- Superseded issue, roadmap or claim: if merged, supersedes only the implementation claim that authenticated Direct CSV may post directly through ordinary per-row ledger creation. It does not supersede #432, close RRB-08 or any release/provider/legal gate, and does not claim bank/provider connectivity exists.
- Financial invariants: integer VND; transfer neutrality unchanged; transfer rows remain on reviewed Inbox path; account/category ownership and category-kind/archive validation remain server-side; exact source identity remains stronger than heuristics; reconciliation remains separate from import evidence.
- Security boundary: new `approve_inbox_candidates_batch(uuid,jsonb)` is SECURITY DEFINER, derives `auth.uid()`, pins empty `search_path`, denies anon/PUBLIC, grants authenticated only, verifies batch/candidate ownership and delegates financial mutation to existing `approve_inbox_candidate`.
- Provenance: Direct CSV carries source row, raw snippet, parser/mapping version and confidence into persisted candidates. No source external ID is fabricated; the database remains the canonical candidate-fingerprint authority.
- Atomicity design: import batch + candidate evidence are persisted before financial commit. The batch approval RPC is one PostgreSQL transaction; any item failure rolls back all transaction/entry/provenance/candidate-approval mutations in that RPC. Parsed/pending evidence may remain after failure for review/recovery.
- Retry design: already-approved candidates return their linked transaction on replay. Exact source-ID duplicates cannot be overridden. Heuristic duplicates require explicit user override; Direct CSV’s existing `Bỏ qua dòng trùng` control owns that choice. Ambiguous post-commit response must not instruct a blind retry.
- Research/adoption evidence: Actual Budget official API/docs distinguish raw add from import/reconciliation, use stable `imported_id` where available, support dry-run and fallback matching. Firefly III Data Importer’s 2026 changelog records duplicate-detection failures from unstable/session-scoped external IDs. These inform contract shape only; no external code is copied.
- Rollback: revert PR #435. Existing Inbox and ordinary transaction paths remain intact; no provider/production state is changed.
- Untrusted external instructions copied into project memory: none.
