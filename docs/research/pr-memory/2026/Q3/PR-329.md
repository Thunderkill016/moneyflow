# PR #329 — Production audit ACL alignment evidence

- Date: 2026-08-09
- Change class: documentation/evidence reconciliation after approved Class 3 provider write
- Affected capability or project boundary: MoneyFlow Trust / Provider Sync / financial mutation audit effective privileges
- Status impact: Provider Sync database migration/schema/ACL boundary advances from partial to provider aligned; overall Provider Sync remains evaluating because production delete-account Edge v5 is stale
- Changed: updates the active Provider Sync packet, MoneyFlow Trust parent and current project memory to record the live SELECT-only audit ACL and the remaining Edge blocker
- Verified: #328 merged as 1618f817c6a96810160f6261029dd038eb8b41ea after CI #2113, CodeQL #1212, Secret #1212 and 26 files / 481 pgTAP tests; owner explicitly approved the scoped production ACL write with go; live post-write history/ACL/RLS read-back matches the merged contract
- Remaining: exact-head checks for this docs-only evidence PR; owner merge decision; then prepare a separate Edge deployment checkpoint; no Edge write is authorized by this PR
- Production/provider evidence: exact migration 20260809010648 exists once with no stray same-name generated row; service_role SELECT=true and INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN=false; authenticated SELECT=true; RLS=true; Postgres migration apply committed without a new ACL-specific permission-error cluster
- Snapshot update: current project memory updated because live provider truth changed materially
- Superseded issue, roadmap or claim: supersedes the current-memory/parent claim that audit effective least privilege is still an open Provider Sync blocker; does not supersede the stale Edge v5 blocker

## Evidence boundary

The consumed owner `go` authorized only the merged audit ACL production migration. It did not authorize Edge deployment, provider configuration changes, financial-row mutation or destructive real-user deletion. Immediate API logs contained no relevant post-write destructive-flow request, so this evidence is not a provider-backed recent-auth acceptance result.

## Safety

- no historical migration file was edited;
- no real financial or audit row was inserted, updated or deleted for verification;
- RLS and authenticated own-row SELECT remain intact;
- exact migration identity was normalized only after guarded proof of a single successful provider-generated history row;
- Edge v5 remains unchanged and requires a separate explicit owner checkpoint.
