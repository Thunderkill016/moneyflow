# PR #533 — defer non-critical dashboard and landing work

- Date: 2026-09-02
- Change class: Class 2 bounded UI/performance runtime change
- Affected capability or project boundary: public landing critical transfer and authenticated dashboard initial client loading
- Status impact: implements the first bounded mechanism under current #527; does not complete #527 yet
- Changed: closed dashboard capture/transfer dialogs are conditionally rendered so their `next/dynamic` chunks are requested only when opened; below-fold landing story image no longer uses eager `priority`
- Verified: fresh `main@0585caea055797cf3c0bfe45494946629ae5a7d0` push-CI resolved master #432 and current #527 as active; exact-head PR CI/CodeQL/secret scan and preview before/after evidence remain pending after this record lands
- Remaining: prove capture/transfer still work in browser gates; record same-methodology performance evidence; keep or revert each mechanism based on measured underlying cost; consider MoreSheet deferral only if shared shell attribution remains material
- Production/provider evidence: owner-observed Vercel performance/load score 39 is the field baseline observation; no provider write is performed and no claim that 39 is fixed is allowed without later field evidence
- Snapshot update: current project memory remains authoritative for product/runtime truth; this PR record captures the bounded implementation and verification state
- Superseded issue, roadmap or claim: none; #523 remains candidate while #527 is current
- Lifecycle impact: continues current slice #527; no current→null transition yet

## Attribution

Current code already used `next/dynamic` for `AddTransactionDialog` and `TransferDialog`, but rendered both components even while `open=false`. Current official Next.js guidance states that a dynamically imported modal should also be conditionally rendered so its JavaScript is requested when the modal is first opened. The landing's first story screenshot sits below the hero but was marked `priority`, bypassing default lazy loading.

## Safety boundary

No financial calculations, ledger mutation semantics, optimistic transaction store behavior, server data fetching, schema/RLS/Auth, provider configuration or production deployment behavior are changed. The dashboard still owns the same dialog state and handlers; only closed-state component mounting changes.
