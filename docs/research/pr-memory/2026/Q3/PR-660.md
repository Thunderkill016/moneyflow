# PR #660 — Ledger surfaces: honest error states + archive guard + disabled-action hints

- Date: 2026-09-22
- Change class: Class 1 (bounded UI honesty fixes; no domain/RPC changes)
- Affected capability or project boundary: `src/components/accounts/accounts-workspace.tsx` (+ module.css), `src/components/accounts/account-archive-dialog.tsx`, `src/components/categories-page.tsx`, `src/components/transactions/transactions-workspace.tsx` (+ module.css).
- Status impact: applies the dead-end/honesty audit (second subagent pass, now covering /accounts + /categories + /transactions). Fixes four confirmed findings: fabricated zero summaries under dataError, occluded failure toasts behind open review dialogs, an archive action the RPC always refuses without warning, and silently disabled header actions.
- Changed:
  - dataError no longer renders "0 ₫" / "0 giao dịch" summaries or a "Chưa có…" empty claim; the shared EmptyState names the failure and offers "Về Tổng quan" (mirrors /commitments; pinned EmptyState occurrence counts preserved via conditional content).
  - AccountArchiveDialog gains `isLastActive`; when true it shows an error alert explaining the at-least-one-active-account rule and disables confirm. A refused archive also closes the dialog so the notice reaches the user.
  - `/categories` archive failure now closes `SecondaryReviewDialog` for the same reason.
  - Disabled "Chuyển tiền" / "Chia khoản chi" / "Chuyển tiền ví" now print their precondition as a hint under the action row.
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning); typecheck; build 51/51; check:knowledge, check:architecture, check:css-ownership, check:deployment-env, test:ci-policy 191/191; pinned contract suites (ui-phase6-accounts, ui-phase5-transactions, mvp-empty-state-release-gate) green.
- Remaining: `/transactions` still uses `window.confirm` for bulk-recategorize and delete — migrating to SecondaryReviewDialog needs pinned-test + e2e updates, deferred as its own PR. Categories filter-empty state remains unreachable (not fixed).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
