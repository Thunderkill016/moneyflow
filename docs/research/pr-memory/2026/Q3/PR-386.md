# PR #386 — deps: deliver js-yaml 4.3.1 security backport

- Date: 2026-08-15
- Change class: Class 1 dependency/security maintenance; `branch_write` only.
- Affected capability or project boundary: indirect development/tooling dependency resolution for `js-yaml`; no MoneyFlow product/runtime, financial, database, Auth, provider, production, UI or CI-workflow semantics change.
- Status impact: candidate — if this PR passes the full ready-state ruleset and merges, it completes the final implementation item in open-work reconciliation; the canonical board/memory remain unchanged until the immediate post-merge lifecycle closeout.
- Changed: `package-lock.json` resolves `js-yaml` from 4.3.0 to 4.3.1 using the same three-line version/resolved/integrity delta verified in Dependabot #320, plus this truthful PR-memory record.
- Research: upstream `js-yaml` 4.3.1 (2026-07-31) records a security backport removing quadratic complexity from `!!omap` duplicate-key detection. GitHub's reviewed advisories document algorithmic-complexity denial-of-service defects in older 4.x releases. No new dependency or provider is adopted.
- Verified: draft head `fd3e7a4e0c2c5cf8a72f1f31125092a31568621c` passed CI #2462, CodeQL #1540 and Secret history #1540. Ready-state head `e05add269931f6d609b298d46f07efbbacb8549c` passed CodeQL #1542, Secret history #1542, unit/static/build/database-not-required gates, but full CI #2465 failed deterministically because the PR-memory record violated the canonical knowledge fields. Head `583e530935c158bacb78b9dfa519cb48667a32ae` fixed those fields but CI #2466 exposed the remaining status/snapshot semantic mismatch. Both failures are preserved as deterministic policy findings; neither is treated as a clean retry.
- Remaining: this corrected exact head must pass all five active required checks (`database`, `verify`, `e2e`, `Gitleaks all refs`, `Analyze JavaScript and TypeScript`); then recheck current main/base, PR head, reviews and unresolved threads, and use protected squash merge with expected-head protection.
- Production/provider evidence: none required; no provider/production write occurs.
- Snapshot update: not applicable — current project truth remains on the pre-merge state in this delivery PR; the post-merge lifecycle closeout owns the snapshot transition.
- Superseded issue, roadmap or claim: fresh PR #386 replaces Dependabot PR #320 as the candidate MoneyFlow delivery for the same `js-yaml` 4.3.1 lockfile patch; #320 should be closed only after #386 merges.