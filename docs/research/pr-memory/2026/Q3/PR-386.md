# PR #386 — deps: deliver js-yaml 4.3.1 security backport

- Date: 2026-08-15
- Change class: Class 1 dependency/security maintenance; `branch_write` only.
- Affected capability or project boundary: indirect development/tooling dependency resolution for `js-yaml`; no MoneyFlow product/runtime, financial, database, Auth, provider, production, UI or CI-workflow semantics change.
- Status impact: finishes the final implementation item in open-work reconciliation once full ready-for-review required checks pass and the PR is safely merged; Release Readiness Audit v1 remains next.
- Changed: `package-lock.json` resolves `js-yaml` from 4.3.0 to 4.3.1 using the same three-line version/resolved/integrity delta verified in Dependabot #320, plus this truthful PR-memory record.
- Research: upstream `js-yaml` 4.3.1 (2026-07-31) records a security backport removing quadratic complexity from `!!omap` duplicate-key detection. GitHub's reviewed advisories document algorithmic-complexity denial-of-service defects in older 4.x releases. No new dependency or provider is adopted.
- Draft-head evidence: `fd3e7a4e0c2c5cf8a72f1f31125092a31568621c` passed CI #2462, CodeQL #1540 and Secret history #1540. Merge was correctly rejected because the active ruleset still expected the non-draft `database` / `verify` / `e2e` checks.
- Ready-state verification required: this state commit must trigger and pass all five active required checks (`database`, `verify`, `e2e`, `Gitleaks all refs`, `Analyze JavaScript and TypeScript`) on the same current head. Final diff must remain the expected lockfile dependency delta plus this memory record.
- Remaining: after full exact-head required checks pass, recheck current main/base, PR head, reviews and unresolved threads, then use protected squash merge with expected-head protection.
- Production/provider evidence: none required; no provider/production write occurs.
- Snapshot update: not applicable.
- Superseded issue, roadmap or claim: fresh PR #386 replaces Dependabot PR #320 as MoneyFlow lifecycle authority for the same `js-yaml` 4.3.1 lockfile patch; #320 should be closed only after #386 merges.