# PR #391 — test: prove authenticated mixed-ledger financial truth

- Date: 2026-08-15
- Change class: Class 2 authenticated browser/user-flow proof over financial semantics; no database/provider/production mutation.
- Affected capability or project boundary: Release Readiness blocker RRB-01 — current authenticated rendered financial truth for a deterministic mixed ledger.
- Status impact: candidate — RRB-01 remains BLOCKED until this proof passes final exact-head delivery and merges.
- Changed: existing authenticated harness gains optional account/category/balance seed overrides plus one coherent two-account mixed-ledger fixture; a new authenticated Playwright spec proves dashboard totals, transfer neutrality and per-account rendered balances.
- Research: current Playwright guidance was checked for user-visible behavior, test isolation, resilient locators and web-first assertions. Historical PR #345 was used only as provenance and ported against current main instead of revived.
- Verified: branch base is exact `main@530834596e906fa5166579a1f0926eb75259bfd8` after #389. Ready-state head `48116b219a1f0dacca379b4014b658715706f318` passed CodeQL #1565 and Secret history #1565. CI #2491 / run `31862093324` selected `fullVerify=true`, `browserSmoke=true`, `database=false`, `uiAudit=false`; policy, static quality, lint, typecheck, unit/static-RLS, production build, presentation ownership, `verify`, Browser smoke and `e2e` aggregators all passed. The authenticated suite ran 21 tests with 21 passes; `financial-truth.mobile.auth.spec.ts` passed first-run and proved the literal 2,700,000 VND aggregate balance, 2,000,000 income, 300,000 expense, 1,700,000 net, 500,000 transfer neutrality, 1,200,000 cash balance and 1,500,000 bank balance through authenticated rendered routes. Database checks were intentionally not selected, so this PR does not claim fresh pgTAP/RLS proof.
- Retry finding: the separate pre-existing demo `global-pfm-ux.spec.ts` mobile scenario timed out on its first attempt in CI #2491 and passed on retry #1; Browser smoke/e2e aggregators passed. This is preserved as a retry-pass finding, not described as a clean no-flake run, and is outside the RRB-01 changed-path scope.
- Independent review: unavailable. An attempted reviewer request was rejected by GitHub because the requested reviewer is not a repository collaborator; no review submission is claimed.
- Remaining: this evidence update changes the PR head, so final exact-head CI/CodeQL/Secret history must pass again. Recheck base/head/diff/reviews/threads before expected-head squash merge.
- Production/provider evidence: not applicable to this browser proof. PostgreSQL/RLS/provider/production claims remain owned by their existing evidence layers.
- Snapshot update: not applicable while RRB-01 proof remains candidate. Lifecycle closeout after a successful merge owns Current Work/current-memory transition.
- Superseded issue, roadmap or claim: none yet. A successful merged proof will supersede the audit claim that current main lacks authenticated mixed-ledger rendered financial-truth evidence.
