# PR #427 — refactor: simplify the default daily path

- Date: 2026-08-18
- Change class: Class 2 bounded presentation/client-surface simplification; CI selects full verify, Browser smoke, Cross-device UI audit and CodeQL, while database work is not selected because no schema/SQL/RLS path changes.
- Affected capability or project boundary: signed-in App Shell capture presentation and default `/dashboard` composition only. Transaction/transfer semantics, planning routes, the one-bounded dashboard RPC, request-private caching semantics, Auth, schema/RLS, provider/deployment and production data stay outside this slice.
- Status impact: candidate only. #426 is current agent work; #403 is paused rather than closed; RRB-08/#398 remains an independent owner physical-device gate.
- Changed: desktop sidebar stops rendering the `PRIMARY_NAV` capture action while the topbar `Ghi chi tiêu` remains; mobile centered `Ghi` is preserved; unreachable shell `CaptureSheet`/`CAPTURE_OPTIONS` client code is removed; default dashboard stops rendering `DashboardPlanningColumn`; compact planning links and all planning routes remain; income templates/goals are no longer serialized into the client dashboard because that removed column was their only dashboard consumer; budgets/commitments remain because attention signals still use them; the existing app-shell contract now locks the simplified shell behavior.
- Research rationale: repository authority came first — one primary action per viewport, daily-ledger core, progressive disclosure and smallest coherent slice. Existing #419 visual evidence showed the duplicate desktop actions; #403 measured dashboard client-JS/main-thread cost (~311.6 KB script, ~766–814 ms bootup, ~14–15 ms server response). Supporting primary research only: GOV.UK/GDS `Do less`, `Design with data`, `Do the hard work to make it simple`; Hick/Hyman choice-information results used narrowly for unnecessary simultaneous choices; Next.js App Router guidance to keep client boundaries limited to interactive surface.
- Verified: exact-head `94ff1a8d86344e9407ff96d7d8b2c1410c13d2c4` classification succeeded and selected full verify + Browser smoke + Cross-device UI audit + CodeQL with no database shard required. Secret history passed. The first policy run correctly failed because this memory record lacked required fields/headings in the active packet; the checker was not weakened and this commit fixes those records. Runtime/build/browser/UI/performance evidence must rerun on the new exact head before recommendation.
- Remaining: rerun exact-head CI after this documentation correction; verify build/static/unit/browser/UI/CodeQL; inspect visual artifacts for one desktop capture primary, unchanged mobile `Ghi`, and discoverable planning links; compare same-method `/dashboard` script transferred bytes against the ~311.6 KB baseline; run an independent evaluation; update the packet with measured truth. No LCP/TBT win may be claimed inside the known noise floor.
- Production/provider evidence: not applicable. No provider write/read-back, deployment, production-data access, production load test or production verification is part of this candidate.
- Snapshot update: not applicable while PR #427 remains candidate. `CURRENT_PROJECT_MEMORY.md` is intentionally unchanged; Current Work Board + active packet + this PR memory own candidate execution state until merge changes durable truth.
- Superseded issue, roadmap or claim: #426 temporarily supersedes #403 as the one-at-a-time agent `NOW` item but does not close or complete #403. It supersedes only the duplicated default-path presentation; it does not supersede Capture 4 behavior, planning routes, RRB-08, #417 or #418.
