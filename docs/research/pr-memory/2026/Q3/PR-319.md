# PR #319 — UI migration Phase 10 legacy retirement

- Date: 2026-08-07
- Change class: Class 2 — cross-route presentation cleanup and deterministic CSS ownership verification
- Affected capability or project boundary: global compatibility CSS, legacy selector reachability, root presentation contracts, target-size repair and semantic token ownership
- Status impact: candidate — unmerged P10 work stacked on unmerged P9 #318; current product truth remains `main`
- Changed: ports the stronger historical dead-CSS reachability scanner and its fixtures onto current P9 lineage; creates a P10 execution packet; no historical CSS deletion list is accepted yet
- Preserved: current financial/auth behavior, Fresh Blue/B3.2, public light-only, workspace theme behavior, `document-theme.css` semantic authority, current target-size safety until measured replacement proof exists
- Verified: pending cumulative exact-head CI/browser/security evidence after target-size ownership recovery
- Remaining: obtain final exact-head gates and owner review; P11 remains dependent
- Production/provider evidence: none; no merge, deployment, provider setting, schema/RLS, production-data or branch/ruleset write is included
- Snapshot update: not applicable — candidate branch does not change merged implementation truth
- Superseded issue, roadmap or claim: historical PR #171 deletion candidates are evidence only; this PR replaces them with a current-lineage reachability decision

## Safety and lineage

- Branch: `agent/ui-phase-10-legacy-retirement`.
- Pull request: #319, initially Draft while the scanner is expected to reveal current unreachable selectors.
- Base PR view targets `main` only so protected CI can evaluate the cumulative P9+P10 candidate; #318 remains a dependency and merge ordering is owner-controlled.
- The scanner/test blobs came from stale PR #171, but no old CSS deletion blob was copied.
- Merge, deployment and production verification remain owner-only.

## Current evidence

- P8 exact static evidence: 1,096 `!important` declarations.
- P9 static evidence after retiring three stale public generations: 694 declarations, a reduction of 402.
- Six files remained imported through `src/app/legacy.css` at P10 start; the current P10 boundary now keeps only the foundation `globals.css` import.
- The ported scanner intentionally included a repository-level zero-unreachable-selector assertion so the first P10 run could produce a current inventory rather than trust historical deletion candidates.

## CI recovery evidence — 2026-08-08

- Exact failed P10 head `41b485ac217deff6de151209c6e02addaf7725a2` failed CI run `31195074203`, job `92921682650`, step `Production cross-device UI audit`.
- The uploaded `ui-audit-evidence-31195074203-1` artifact contained 146 Playwright failure contexts. The failures were not independent: the dominant minimum-target offenders repeated across routes (`Đăng ký` 120 contexts, mobile brand 80, secondary breadcrumb 60, planning breadcrumb 24).
- That evidence proved the first attempted deletion of `MinimumTargetSizeContract` violated P10-AC4 and the packet stop condition: important current controls still depended on the safety contract. The contract was restored temporarily to re-establish the 44px product floor while the failures were mapped to direct owners.
- The same artifact exposed one separate 200% text regression on `/capture/quick`: date and note inputs extended outside the 320px viewport. The current `TransactionForm` grid already owned `min-width: 0`, but the replaced input elements retained intrinsic width under enlarged text. The local owner now constrains text-field wrappers to `max-width: 100%` and text-field inputs to `width: 0; min-width: 0; max-width: 100%`, matching the proven containment pattern already used by the account dialog without reopening retired global CSS.
- Exact recovery head `bb98a2a380db83207d7eece8eca74a97e6006ef1` then failed CI run `31217024337`, static-quality job `92992954853`, step `CSS ownership and debt budgets`: the temporary contract restoration reintroduced 18 `!important` declarations against P10's final budget of `0`.
- The policy was not weakened and no exception was added. The audit artifact was reduced to 15 unique target-size controls and each was reassigned to a current owner: app-shell demo/mobile brand, secondary and planning breadcrumbs, dashboard attention/section links, auth demo/forgot/back links, landing skip/footer links, and onboarding actions.
- Onboarding now uses the shared Button `targetSize="important"` contract for its actions, while the owning CSS modules use explicit 44px minimum geometry for route-specific links. No new `!important` declaration is introduced.
- With those direct owners in place, `MinimumTargetSizeContract`, its root mount, and its CSS repair layer can be retired again consistently with P10-AC4; the static contract test returns to asserting absence and leaves final product-wide authority to the measured browser audit.
- The final recovery head must be judged only by exact-head CI/browser/security evidence; no merge is authorized here.

## Adoption decision

DTCG/token generation is deferred in P10. The current CSS semantic token authority has a real owner and no proven current interoperability consumer requires a generated second source. Reopen only through a later adoption decision tied to a concrete design-tool/platform consumer.
