# PR #636 — Capture V2 Slice 0 benchmark harness

- Date: 2026-09-21
- Change class: Class 1 (bounded tooling — `public/` static page + contract test + packet; no product runtime, schema, or loader changes)
- Affected capability or project boundary: `public/capture-bench.html` (new evidence tool), `src/lib/capture-bench-tool-contract.test.ts` (new), `docs/plans/active/capture-v2-benchmark-harness.md` (new packet).
- Status impact: Slice 0 of `capture-v2-low-maintenance-acquisition.md` now has a runnable measurement tool. TTLT/taps/correction evidence for H1–H5 can be collected without product changes. Authorizes nothing beyond measurement.
- Changed: two-tab TTLT runner observing demo-mode `storage` events on `moneyflow-demo-transactions-v1` (ledger leg) and `moneyflow-inbox-candidates-v1` (candidate leg); 7 synthetic tasks (A1–A4 amount-first `/capture/quick` incl. transfer-neutrality + #596-defaults probes; D1–D3 description `/capture/paste`); kind/amount verification vs task expectation; 60 s same-id rewrite watch flags post-save corrections; report emits per-task timings + per-group medians with the tab-switch-latency caveat.
- Verified: contract test 4/4; `npm test` 1434/1434; lint (1 pre-existing `_reviewStatus` warning); typecheck; build; check:knowledge / test:ci-policy / check:css-ownership / check:architecture all pass. Playwright end-to-end: second-tab localStorage write stopped the timer (0.8 s), `expense 45.000₫` matched `đúng`, report + medians rendered. `check:deployment-env` needs `NEXT_PUBLIC_APP_MODE=demo` locally (`.env.local` gap, not a repo defect).
- Remaining: real benchmark runs (esp. physical phones per RRB-08 pattern) are owner-side; tab-switch latency means numbers are relative-comparison only; true first-time cohort needs a fresh-profile run.
- Production/provider evidence: none; tool is same-origin only (`/api/health` for commit identity).
- Superseded issue, roadmap or claim: none — implements Slice 0 tooling already specified; does not supersede any claim.
- Lifecycle impact: adds measurement tooling only; no Capture V2 product change authorized or implemented.
