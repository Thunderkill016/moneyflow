# PR #545 — select #523 Vietnam bank-export compatibility

- Date: 2026-09-03
- Change class: Class 0 authority/planning change that selects a Class 3 acquisition/import packet; no runtime, database, provider or production mutation in this PR
- Affected capability or project boundary: executable-plan authority, M1 Vietnam Acquisition Depth, current project memory, bank-export compatibility research/fixture contract
- Status impact: owner has selected M1 product direction and MON-50/MON-61 are In Progress; if owner-merged, this PR changes repository executable authority from `current: null` to #523. MON-62 and MON-63 remain unselected.
- Changed: adds `docs/plans/active/523-vietnam-bank-export-compatibility.md`, sets `PLAN_AUTHORITY.current` to that packet with `selectedByPr: 545`, reconciles `CURRENT_PROJECT_MEMORY.md` after completed M0/#544, and records this PR memory
- Verified: branch starts from exact merged `main@43020263333317ee8be8c7a8adea7ee502e7585d`; that main has `PLAN_AUTHORITY.current: null`, M0 #536 closed, production 56/56 migrations, 14/14 later acquisition contracts and Next 16.3.4 production health
- Repository reconnaissance: current direct import is CSV-only with map/dry-run; generic CSV/XLSX parsers reuse one matrix-normalization path; authenticated direct commit uses existing provenance/all-or-nothing approval; generic parser output has no provider stable transaction-reference field; preview fingerprints are not persisted source identity; DB source-lineage can use explicit source IDs only when source evidence actually supplies them
- Research refresh: three first-party bank sources were reviewed. Vietcombank public Digibank material confirms account history/statement workflows; ACB first-party guidance explicitly documents Excel transaction-history export for a supported flow; current VietinBank first-party card guidance documents statement delivery/reading and iPay access. The reviewed public material does not establish one complete current consumer-account export header/reference/status contract across all three banks.
- Research limits: no exact VCB/VietinBank consumer-export schema, stable transaction reference, fee-row convention, overlap behavior or lifecycle/status mapping is claimed without stronger first-party or privacy-safe real-export structural evidence. ACB store-management Excel structure cannot be generalized to every ACB personal-account export.
- Architecture decision: provider-specific work must remain an adapter into the shared candidate/provenance/matching/ledger/reconciliation path. A row index, display reference, generated hash or MoneyFlow heuristic fingerprint must never be promoted to `source_external_id` unless source evidence proves stable provider identity.
- Privacy decision: repository fixtures for Phase A must be synthetic and structural; no real account holder, account number, transaction reference, bank credential or customer export is committed.
- Measured: no production behavior or performance measurement is claimed by this selector; it changes planning authority only
- Remaining: exact-head policy/CI/CodeQL/Secret-history checks; explicit owner merge; then fresh merged `plan:resolve` + `agent:doctor` before implementation. Phase A implementation must build the evidence-tagged VCB/ACB/VietinBank compatibility matrix, synthetic fixtures, normalization tests and minimal user guidance without DB/provider writes unless scope is explicitly reclassified.
- Production/provider evidence: none added by this PR; no Supabase DB/Auth write, Vercel deploy/config change, external bank access or production data mutation was initiated
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` now records M0/#544 as fully merged/closed, M1 owner selection, #545 selector-pending state, current generic acquisition boundary and bank-specific evidence gaps
- Superseded issue, roadmap or claim: prior memory that #544 remained open and #536 closure was pending is superseded; generic `sample-bank.*` fixtures do not prove target-bank compatibility; M1 being chosen does not itself bypass the repository selector/owner-merge gate
- Lifecycle impact: authority transition from `current: null` to current slice #523 if PR #545 is owner-merged; does not complete #523, does not select MON-62/MON-63, and performs no runtime/provider write

## Focused source record

1. Vietcombank VCB Digibank user guide — `https://digibankm5.vietcombank.com.vn/get_file/ibomni/html/hdsdib/hdsd.pdf` — proves account transaction-history/statement workflows, not a complete current downloadable-file schema.
2. ACB ONE store-management guide — `https://acb.com.vn/acbwebsite/files/ACB_HDSD_Quanlycuahang.pdf` — proves Excel transaction-history download for that supported flow; it is not universal evidence for all personal-account exports.
3. VietinBank current card user guide — `https://www.vietinbank.vn/assets/1149f1b0-3f81-436a-9041-87430eaa02e4` — proves statement delivery/reading and iPay access for current card products; it does not establish the target consumer-account export file schema.

## Permission boundary

#545 is a selector only. No parser runtime implementation, dependency change, database/RPC migration, Supabase Auth/provider mutation, Vercel production mutation, bank credential flow, browser automation, production data write or public compatibility claim is authorized by the unmerged PR itself. Merge remains an explicit owner decision. After merge, implementation remains branch-scoped and any later provider/production write retains its separate owner-authorization boundary.
