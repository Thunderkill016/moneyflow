# PR #291 — budget month history and exact drill-down

- Date: 2026-08-05
- Change class: Class 3 financial budget read/write correctness
- Affected capability or project boundary: budget month normalization, selected/previous month reads, monthly comparison, budget mutation month binding, transaction drill-down and responsive budget route UI
- Status impact: candidate — adds the budget history/drill-down Wave 1 depth slice on current main
- Changed: add canonical `/budgets?month=YYYY-MM`; visibly repair invalid/future input; load selected and immediately previous month; distinguish missing comparison data from numeric zero; add previous/next navigation and month form; keep create/edit/delete bound to the effective selected month; add exact selected-month expense/category drill-down; keep deterministic demo current/previous data and truthful older empty states
- Financial boundary: selected month changes which budget rows are displayed and mutated; integer VND, transfer neutrality, ledger facts, category spend authority and existing budget RPC ownership remain unchanged
- Security boundary: authenticated reads retain `requireViewer`, explicit `.eq("user_id", viewer.id)` plus RLS; writes continue through existing ownership-safe RPCs; no grant, migration, secret, provider or deployment configuration change
- Privacy boundary: no new financial payload field, telemetry, production data access or private identifier is introduced
- Performance boundary: the authenticated loader performs one bounded two-month `budget_progress` read plus the existing category read; no full-ledger client filtering, cache or dependency is added
- Verified: pending exact-head policy/static/unit/build, database classification, browser/cross-device gates, CodeQL, secret-history scan and independent review after this mandatory memory commit
- Remaining: fix any current-main integration finding; prove month controls and full VND values across the audit matrix; confirm exact transaction filter wiring and no unresolved review thread; merge only the final verified SHA; run production verification separately
- Production/provider evidence: none; this PR performs no production schema/data/provider write or authenticated production smoke
- Snapshot update: update current capability memory only after merge if this slice changes the audited budget-depth status
- Superseded issue, roadmap or claim: closes issue #290; does not authorize rollover, copy-last-month, forecast, recurring matching or broader planning redesign
