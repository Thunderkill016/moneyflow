# PR #716 — Kind filter + folded search inside the account register

- Date: 2026-09-25
- Change class: doctor projects Class 3 off the `account-detail` path
  keyword (`auth-and-account-deletion` heuristic); actual scope is a
  bounded presentational filter — no schema, no migration, no server
  action, no RLS surface. CI lanes per doctor: verify + browser smoke +
  cross-device UI audit + codeql; database false.
- Affected capability or project boundary: `/accounts/[accountId]`
  register (`src/components/account-detail-page.tsx` + CSS module) and
  the register domain module `src/lib/account-register.ts`.
- Status impact: the "no search/filter inside an account's register"
  gap from the account-surface comparison is closed. Remaining
  deferred gaps (credit-card lifecycle, per-account trends/export,
  institution logos, sync/forecast) are untouched and each still needs
  its own spec/owner decision.
- Changed:
  - `filterAccountRegisterEntries(entries, { kind, query })` in
    `src/lib/account-register.ts` — narrows by `transaction.kind`
    (`all | income | expense | transfer`) and a diacritic-folded query
    built with `normalizeSearchText` over note, payee, category, split
    category lines and the transfer counterparty name.
  - Toolbar row inside the register panel, only rendered when the
    register has entries: labeled search field + segmented kind buttons
    (`Tất cả / Khoản chi / Khoản thu / Chuyển tiền`, `aria-pressed`,
    44px targets), same recipe and token set as the transactions
    toolbar (`searchControl`, `kindFilter`, `kindButton`).
  - Meta line `Đang hiển thị N/M giao dịch` + `Xoá lọc` appears only
    while a filter is active.
  - **Summary invariance**: `displaySummary`, the four cards, the
    heading `N giao dịch · biến động …` and every `Biến động ngày`
    header always reflect the full register — `filteredEntries` narrows
    only the visible rows, while day totals come from
    `accountRegisterDailyImpacts(displayEntries)`, so a filter can
    never bend any labelled total.
  - Transfer neutrality preserved — kind filter `Chuyển tiền` shows
    only transfer legs, which keep their neutral subtitles
    (`Nhận từ…/Chuyển đến… · không tính thu nhập/chi tiêu`); no row is
    ever reclassified.
  - Filtered-empty state (`Không có biến động nào khớp bộ lọc đang
    dùng` + `Xoá bộ lọc`) is distinct from the true empty-register
    EmptyState, which is untouched.
  - Client state only — no URL params, no persistence, intentionally
    unlike `/transactions` presets (register filters are ephemeral
    lookups, not saved workflows).
- Verified:
  - `account-register.test.ts` +3 tests: kind narrowing; folded query
    reaching note (`ca phe` → `Cà phê`), payee, split categories and
    transfer counterparty; kind+query composition — file 9/9;
    `npm test` 1879/1879; `typecheck` clean; `lint` 0 errors (one
    pre-existing unrelated warning); `check:knowledge`,
    `check:architecture`, `check:capabilities`, `check:migrations`,
    `test:ci-policy` 191/191, `check:dead-css`, `check:css-ownership`,
    `check:code-css-ownership` (0 new unowned); `npm run build` 52
    routes.
  - Browser (demo, :3111, Playwright): `Khoản chi` → 2/3 rows + meta
    `Đang hiển thị 2/3 giao dịch`; `Chuyển tiền` on a seeded transfer
    → 1 row with `Chuyển đến Ví Momo · không tính chi tiêu`; query
    `an uong` → `Ăn uống` row (fold end-to-end); query `momo` reaches
    the counterparty; impossible query → filtered-empty + `Xoá bộ lọc`
    restores all 3; summary cards byte-identical before/after
    filtering; mobile 390px — no page h-scroll, 44px segments, meta
    line visible; zero console/page errors.
- Review round 2 (director via ChatGPT, exact head `cab9b159`): one
  High + two Medium findings, all resolved and re-verified:
  - High — `groupEntries(filteredEntries)` let a `Biến động ngày`
    header sum only visible rows (e.g. −50.000 shown for a day whose
    real movement was +950.000). Fixed by computing day headers from
    `accountRegisterDailyImpacts(displayEntries)`; the grouping helper
    no longer carries a `dailyImpact` field. Pinned by a mixed-kind
    same-day test.
  - Medium — payee was searchable but never rendered, producing
    invisible matches. Fixed by prefixing the row subtitle with
    `payee ·` when present.
  - Medium — the search input's `aria-label` overrode its visible
    `<label>` (WCAG 2.5.3). Fixed by dropping the override; the
    kind-filter `div` gained `role="group"` so its `aria-label` is
    valid.
  - Hardening in the same round: `key={accountId}` on
    `AccountDetailPage` in `src/app/accounts/[accountId]/page.tsx` —
    client-side `/accounts/a → /accounts/b` navigation reuses the
    mounted component, so without a key a stale filter would leak
    across accounts and make the new register look filtered-empty.
- Remaining: CI must pass on the new exact head after round 2.
  Browser re-verify (demo, :3101): day header `+ 950.000 ₫`
  byte-identical before/after `Khoản chi` filter; payee search shows
  `Highlands Coffee · Ăn uống`; accessible name resolves via the
  visible label; navigating to another account resets the filter; zero
  console errors. Local gates after round 2: `npm test` 1880/1880,
  typecheck, lint 0 errors, build, css-ownership, dead-css,
  ci-policy 191/191. Merge is the owner's call.
- Production/provider evidence: none — not deployed, no schema.
- Superseded issue, roadmap or claim: closes account gap "register
  controls are narrow" from the account-surface comparison.
- Lifecycle impact: if register filters later need persistence or URL
  sync, reuse `saved-transaction-filters` conventions (viewer-scoped
  keys, validated reads) rather than inventing a second store.
