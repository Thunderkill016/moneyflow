# PR #670 — trend labels follow span-based buckets; custom-range audit spec repaired

- Date: 2026-09-22
- Change class: Class 1 (bounded code in one subsystem — reports presentation label + e2e spec)
- Affected capability or project boundary: `src/lib/reports.ts` (new `reportTrendGranularity(range)`, `trendBuckets` delegates to it), `src/components/reports-page.tsx` (trend unit labels), `e2e/audit/reports-custom-range.audit.spec.ts` (selectors + relative window), `src/lib/reports.test.ts`, `src/lib/reports-trend-contract.test.ts`.
- Status impact: closes two leftover seams of the custom report range shipped in PR #289 — the feature itself (URL `period=custom&from&to`, normalization, equal-length comparison, notices, drill-down, CSV export, demo + auth loaders) was already complete on main.
- Changed: exported `reportTrendGranularity` so the daily/monthly span rule has one source; section copy and the "TB/{ngày,tháng} có chi" stat now name the bucket shape instead of keying on `period === "year"` (fixes ~30× mislabelled per-day average on year and >62-day custom windows); audit spec ported to current DOM selectors (`[data-slot="report-metrics"]`, `#report-period-title`, `a[href]`) and a `todayInVietnam`-derived window since the demo fixture now dates rows relative to today; all strings pinned by `reports-custom-range-audit-contract.test.ts` preserved.
- Verified: typecheck, lint (1 pre-existing warning), full suite 1495/1495 (baseline 1492 + 3 new), check:knowledge, ci-policy 191/191, build 51 routes, check:architecture, check:css-ownership. Local build required `NEXT_PUBLIC_APP_MODE=demo` inline and a hardlinked `node_modules` copy because the worktree symlink points outside the Turbopack filesystem root.
- Remaining: browser lanes (`test:e2e`, `test:ui-audit`) not run locally — the audit spec change mirrors `e2e/reports-custom-range.spec.ts`, which already passes in the standard e2e lane; exact-head CI is the evidence.
- Production/provider evidence: none (presentation label + test-only change; no schema, provider or production-data operation).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: none; if the audit lane still fails on device-specific rendering, fix the spec against the captured DOM rather than weakening assertions.
