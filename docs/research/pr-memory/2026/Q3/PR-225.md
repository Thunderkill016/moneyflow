# PR #225 — reference-led landing and authentication candidate

- **Status:** open draft candidate; evaluating
- **Status impact:** candidate only; no current product truth changes
- **Branch:** `design/public-entry-reference-led-v2`
- **Base:** `main`
- **Owner decision:** not selected or approved

## Scope

- Study official landing and authentication surfaces from Money Lover, Monefy, Spendee, Lunch Money, Copilot Money and Monarch.
- Replace the current landing collage/icon-card structure with one hero product screen and task-based product sections.
- Remove the authentication proof rail and keep one focused form surface.
- Add an accessible password show/hide control.
- Preserve all existing authentication actions and product boundaries.

## Decision boundary

This PR is an implementation candidate for browser review. It must not be cited as evidence that the owner selected its copy, hierarchy, layout, colors or visual direction. CI, screenshots, preview deployment and mergeability prove implementation state only.

Production, provider configuration, database, RLS, financial calculations and signed-in product UI are not changed.

## Research

- `docs/research/PUBLIC_ENTRY_REFERENCE_STUDY_2026-08.md`
- `docs/plans/active/public-entry-reference-led-v2.md`

## Verification

Executable candidate head `72a0f15f00811013fbf4eb054dc42b14fcc19a4a` passed:

- CI run `30750648519`;
- CodeQL run `30750648487`;
- Secret history scan `30750648486`.

A noindex review deployment is READY:

- deployment `dpl_Cu1t5jGgNwayaPTVVwDRHqzxHwJj`;
- landing route returned HTTP 200;
- review forms are intentionally non-submitting and labelled as preview-only.

The Vercel review bundle mirrors the copy and hierarchy for visual review. PR #225 remains the source of truth for the real Next.js and authentication implementation.

Documentation-only commits after the executable head record evidence and do not alter the candidate UI/runtime behavior. Required PR checks must still settle on the final review head before any later transition.

## Next allowed action

The owner reviews the preview and accepts, rejects or requests another iteration. Do not merge, promote or record a selected direction before explicit owner approval.
