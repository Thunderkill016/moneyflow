# PR #651 — Devin CLI subagent profiles (reviewer, gates-runner)

- Date: 2026-09-21
- Change class: Class 0 (tooling config only — no runtime code)
- Affected capability or project boundary: `.devin/agents/reviewer.md`, `.devin/agents/gates-runner.md` — new Devin CLI custom subagent profiles.
- Status impact: none (agent tooling).
- Changed: added two project-scoped subagent profiles in Devin frontmatter format (`allowed-tools`, unpinned model → default subagent model). `reviewer` does independent read-only diff/packet evaluation with file:line evidence and a Ready/Not-ready verdict; `gates-runner` executes the AGENTS.md verification list and reports per-gate output without edits. Mirrors the intent of `.claude/agents/evaluator.md` for the Devin transport.
- Verified: files committed; profiles discovered by the CLI. Note: a session must be reloaded (or the agents directory rescanned) before new profiles are selectable — first `run_subagent` with profile `gates-runner` failed to start in-session; expected to resolve next session.
- Remaining: real invocation smoke in a future session; model pinning (currently default subagent model — cheap, intended).
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: enables cheaper parallel research/review per the owner's 2026-09-21 direction to use subagents for speed.
