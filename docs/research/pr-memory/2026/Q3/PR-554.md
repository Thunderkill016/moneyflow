# PR #554 — MON-63 retry/preset/history hardening slice

- Date: 2026-09-09
- Status: implementation slice under active MON-63 authority
- Base: `main@a34ac54dfe33175dc96c05348522463aa8cbe1d0`
- Change class: Class 3 acquisition/data-integrity and user-flow implementation with additive Supabase migration/RPC, client retry behavior, mapping-preset versioning and import-history recovery UX.
- Affected capability or project boundary: generic statement import preview→Inbox commit integrity, Direct CSV remembered mapping eligibility, import batch history/recovery, migration identity guard.
- Status impact: none; MON-63 remains the active executable packet after this PR because this change lands a validated implementation slice rather than selecting or completing plan authority.
- Changed: adds a tenant-safe atomic `commit_import_batch_candidates` RPC with stable batch/intent retry identity and exact replay behavior; routes authenticated batch-bound preview commits through that transaction; upgrades Direct CSV remembered mapping to semantic-contract v2 using parser and mapping versions; prevents cross-device history from promising resume when the local draft is absent; enriches history with parser/mapping provenance; repairs one inherited truncated migration-identity digest after confirming the migration blob matches main.
- Verified: fresh local Supabase reset and pgTAP passed replay, rollback, tenant-isolation and no-ledger-mutation coverage on the implementation branch; CodeQL and secret-history scan passed on head `8676e7d6ad7251ca2fbcbc26a496a583d8b650af`; the following CI run exposed a TypeScript narrowing defect in stored preset parsing and the missing PR-memory record, both addressed by subsequent commits before merge handoff.
- Remaining: MON-63 authority remains active for follow-up work that is not claimed complete by this PR, including any additional privacy-safe measurement, durable preset-application evidence, broader browser acceptance or same-slice lifecycle closeout chosen for a later implementation PR. No follow-on plan is selected here.
- Production/provider evidence: no production Supabase, Auth, bank-provider, Vercel production or financial-data mutation was performed from this PR workflow. Database verification used fresh local Supabase/pgTAP CI. Runtime code keeps browser clients under existing RLS authority and does not introduce service-role bypass.
- Snapshot update: not applicable; Status impact is none and current project authority/truth remains MON-63 active.
- Superseded issue, roadmap or claim: supersedes the unsafe generic preview behavior where candidate creation and batch completion could diverge across separate requests, the v1 header-only preset eligibility contract, and history UI that could imply resumability when a local draft was unavailable. It does not supersede the MON-63 packet or claim the full slice complete.
- Lifecycle impact: none; this PR does not transition `PLAN_AUTHORITY.current`, complete MON-63, or select follow-on work.
- Production writes: none.
- Merge: explicit owner authorization received in chat on 2026-09-09.

## Focused evidence record

1. Current repository code/tests are the acceptance authority for atomic import behavior, preset semantics and history recovery.
2. Supabase guidance supports own-row RLS plus `SECURITY INVOKER` for user-scoped database functions instead of privilege bypass.
3. PostgreSQL row locking is used to serialize same-batch writers before commit/replay decisions.
4. GitHub required CI/CodeQL/secret-history checks are treated as exact-head merge evidence.
