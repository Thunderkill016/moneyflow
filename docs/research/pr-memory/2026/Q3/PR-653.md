# PR #653 — Reconcile completion review + post-window disclosure

- Date: 2026-09-21
- Change class: Class 2 (bounded UI on existing reconcile machinery; no domain/RPC/migration change)
- Affected capability or project boundary: `src/components/account-reconciliation-page.tsx`, `account-reconciliation-page.module.css`, `e2e/account-reconciliation-workspace.spec.ts`.
- Status impact: reconcile workflow-depth item from the 2026-09 audit. Domain/session semantics unchanged (`difference === 0` to complete, reopen rules, immutability trigger).
- Changed:
  - "Hoàn tất đối soát" now opens `SecondaryReviewDialog` (statement date, statement balance, count of cleared entries that will lock) with copy disclosing that reconciled rows are immutable until the period is reopened — previously one click locked rows with no confirm, the only destructive-ish action in the flow without the app's review pattern.
  - Post-window transactions were a bare count; now a `<details>` lists each excluded row (note/date/signed amount, read-only).
  - e2e spec gained the dialog step + lock-count assertion.
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:css-ownership, check:architecture, check:deployment-env, test:ci-policy 191/191. Browser verify demo 390px: dialog shows "3 mục đã khớp" + consequence → confirm → "1 kỳ" history; details lists excluded rows.
- Remaining: CI lanes on exact head; e2e lane is the real proof of the new dialog path.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: overnight queue item 1/4 (reconcile depth). Remaining audit candidates in this surface: entry-list filter, difference-resolution subtotals, bulk marking, "missing transaction" escape hatch — all noted, not started.
