# PR #616 — G1 closeout: archive 5 merged packets

- Date: 2026-09-20
- Change class: Class 0 (docs-only)
- Affected capability or project boundary: `docs/plans/completed/` (+5 archived packets), `docs/plans/active/megaplan-q4-2026.md`
- Status impact: recorded; no runtime change
- Changed: archived `590-home-ledger-trust`, `592-activity-workstream`, `ghi-5-stable-ledger-defaults`, `financial-wellbeing-product-strategy`, `repair-security-workflows` — all five already merged (#591 `77def221`, #593 `20c4ae88`, #596 `f7a5ae07`, #594 `a7e45d30`, #599 `bfbb1787`); delivery records updated with merge dates/squash/CI; megaplan Track C + B2 + G1 marked done with residual follow-ups noted (#602, #597, activity R2/R3 auth boundaries, #590 prod-flow unverified)
- Verified: `npm run check:knowledge` pass; merge commits verified via `gh pr view … mergeCommit`
- Remaining: none for bookkeeping — follow-ups tracked as open issues/PRs (#602, #597) and megaplan notes
- Production/provider evidence: none — bookkeeping only; #590's honest "production flow verified: no" flag preserved in megaplan
- Superseded issue, roadmap or claim: resolves megaplan gate G1 — the "pending review" state was stale bookkeeping, not undecided work
- Lifecycle impact: five packets move to `docs/plans/completed/`; `docs/plans/active/` now holds only genuinely active items

## Boundaries

- Archival is bookkeeping: no acceptance criteria re-evaluated; each packet's own recorded evidence stands as its truth.
- #592's R2 (nav promotion) and R3 (retirement) remain separate authorization boundaries — archival does not authorize them.

## Evidence limits

- Merge/CI facts verified via GitHub metadata; whether each merged feature is exercised in production remains as each packet recorded (e.g. #590 explicitly unverified on prod).
