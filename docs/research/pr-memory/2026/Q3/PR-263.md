# PR #263 — account reconciliation workspace

- Date: 2026-08-03
- Change class: Class 3 financial/data/security plus core user flow
- Affected capability or project boundary: account register reconciliation UI, companion account-leg read model, authenticated reconciliation Server Actions and browser-local demo parity
- Status impact: candidate — the user-facing reconciliation workspace is implemented on a focused branch; current `main`, production schema and production data remain unchanged until separate owner decisions
- Changed: add `/accounts/[accountId]/reconcile`; add start, pending/cleared, exact-zero complete and latest-only reopen interactions; add stable completed-history presentation; add a security-invoker companion entry feed grouped by logical account leg; keep `transaction_feed` unchanged; add demo-local reconciliation state and permanent unit/database/browser regressions
- Financial boundary: no direct balance overwrite, no invented adjustment transaction, no amount/date/account/category edit inside reconciliation, integer money only, transfer legs account-independent, split rows atomic and transaction review orthogonal
- Security boundary: authenticated reads remain viewer/RLS scoped; writes call only the four ownership-safe RPCs merged in PR #261; missing production schema disables the feature calmly
- Verified: candidate head `576aed261c90b8253b52cae5e01e8138c66551b1` passed fresh migration replay and full pgTAP in CI #1317; CodeQL #457 and secret-history scan #457 passed; application verify stopped at the knowledge contract before install/typecheck/tests/build and must rerun
- Remaining: independent evaluation findings; lint/typecheck/unit/build; desktop/mobile browser evidence; final exact-head database retention; CodeQL and secret scan on any changed head; owner merge decision; separately authorized production migration and authenticated production smoke
- Production/provider evidence: no production schema, data, provider or configuration write is part of this branch
- Current project memory remains unchanged in this candidate; it will be reconciled only when the candidate reaches a truthful status transition that requires a canonical snapshot change
- Superseded issue, roadmap or claim: PR #222 is closed as historical provenance; issue #260 is completed; issue #262 owns the UI slice
