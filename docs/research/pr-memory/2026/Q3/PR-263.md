# PR #263 — account reconciliation workspace

- Date: 2026-08-04
- Change class: Class 3 financial/data/security plus core user flow and shared visual-token change
- Affected capability or project boundary: account register reconciliation UI, companion account-leg read model, authenticated reconciliation Server Actions, browser-local demo parity, and the shared MoneyFlow semantic color authority
- Status impact: candidate — implementation changed after the previously verified head; exact-head verification is required again before the owner-authorized merge may proceed
- Changed: add `/accounts/[accountId]/reconcile`; add start, pending/cleared, exact-zero complete and latest-only reopen interactions; add stable completed-history presentation; add a security-invoker companion entry feed grouped by logical account leg; keep `transaction_feed` unchanged; add demo-local reconciliation state and permanent unit/database/browser regressions
- Visual-system refresh: adopt the approved MoneyFlow brand-blue ramp, harmonized cool supporting tokens, true green/red/yellow/blue functional roles, accessible light/dark mappings, and reconciliation cards/badges/buttons that consume only shared `--mf-*` semantic tokens
- Financial presentation rule: signed positive values use true green, signed negative values use true red, transfers use indigo, zero reconciliation uses the success treatment, and nonzero difference never implies permission to complete
- Financial boundary: no direct balance overwrite, no invented adjustment transaction, no amount/date/account/category edit inside reconciliation, integer money only, transfer legs account-independent, split rows atomic and transaction review orthogonal
- Security boundary: authenticated reads remain viewer/RLS scoped; writes call only the four ownership-safe RPCs merged in PR #261; missing production schema disables the feature calmly
- Previous evidence: implementation head `30d99769735249fe9264f3b5b38d8587813a9f99` passed CI #1329, CodeQL #469 and secret-history scan #469 before the visual-system refresh
- New candidate changes: `63a369bb868693d2358565d1cf38a1ef21906879` updates the semantic theme authority; `d0247e2a3d7a1423d928aa5b59df27a4347b2744` applies those roles to the reconciliation workspace
- Evidence boundary: the previous runtime/database/browser evidence remains historical only; it is not exact-head evidence for the refreshed candidate
- Remaining: exact-head diff hygiene, knowledge, CSS ownership, architecture, lint, typecheck, unit/static RLS, production build, database replay/pgTAP, desktop/mobile browser coverage, CodeQL and secret-history scan
- Production/provider evidence: no production schema, data, provider or configuration write is part of this branch
- Snapshot update: not applicable — PR #263 remains open, so canonical current-project truth remains unchanged until a merge commit exists
- Superseded issue, roadmap or claim: PR #222 is closed as historical provenance; issue #260 is completed; issue #262 owns this UI slice
