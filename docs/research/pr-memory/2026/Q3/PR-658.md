# PR #658 — Paid-date evidence on commitment cards + income-template prerequisites

- Date: 2026-09-22
- Change class: Class 1 (bounded; one extra column in an existing select, optional domain field, copy/conditional-render changes)
- Affected capability or project boundary: `src/server/commitments.ts` (occurrence select + `mapCommitmentRow` optional `paidOn`), `src/lib/planning/commitments.ts` (`RecurringCommitment.paidOn`), `src/components/planning/commitments-page.tsx` (dated paid label), `src/components/planning/income-templates-page.tsx` (prerequisite guidance), `src/lib/planning-pages.test.ts`.
- Status impact: audit findings #6 and the income-templates sibling of #3. Paid cards previously asserted "Đã có giao dịch chi" with no inspectable evidence; `commitment_occurrences.paid_at` existed since the original migration but was never selected.
- Changed:
  - Occurrence select carries `paid_at` → `paidOn` (VN calendar date via `todayInVietnam`); paid cards render "Đã ghi ngày DD/MM", falling back to the undated label when unknown.
  - Demo seed (`demo-rent`) carries a plausible `paidOn` (its due date) so the evidence is visible in demo; locally-stored demo occurrences keep the id-only map and fall back honestly.
  - `/income-templates` empty state names the missing prerequisite (account → `/accounts`, income category → `/categories`) instead of a silent dead end.
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:architecture, check:css-ownership, check:deployment-env, test:ci-policy. Browser (dev build, demo 390px): paid card renders "Đã ghi ngày 05/09".
- Remaining: `paidOn` could later link to the posted transaction once a non-looping deep target exists (`?open=` deliberately resolves recurring rows to a notice today); overdue-goal pace wording and `goalProgress` rounding edge remain from the audit.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
