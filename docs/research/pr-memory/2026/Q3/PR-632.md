# PR #632 — jev-eval: TypeSafe System One evaluation harness

- Date: 2026-09-21
- Change class: Class 0/1 (repo tooling only; no product path, schema or runtime change)
- Affected capability or project boundary: `scripts/jev-eval.mjs` (new).
- Status impact: none — standalone evaluation tool. Lets the owner exercise the Jev API (TypeSafe "System One" typed-decision model, released ~2026-09-18) against MoneyFlow-shaped synthetic decisions without writing integration code.
- Changed: new script posts `{state, model, questions}` to `https://api.typesafe.ai/v1/systemone` with `Authorization: Bearer $TYPESAFE_API_KEY`; prints typed answers (choice/score/noul) with probabilities, confidence, latency and usage. Two built-in synthetic scenarios (candidate triage, duplicate suspicion); ad-hoc `--state`/`--questions`/`--model`/`--raw` supported.
- Verified: `node --check` clean; missing-key path exits 2 with the console URL; request/response shapes verified against `docs.typesafe.ai` quickstart. No live call — no key was available in-session.
- Remaining: owner supplies `TYPESAFE_API_KEY` (console.typesafe.ai/keys) and runs `node scripts/jev-eval.mjs`.
- Production/provider evidence: none — the tool is local-only and does not touch production.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: none.

## Boundaries

- Evaluation harness only; not imported anywhere; adding Jev to any product path remains unauthorized (external AI on financial data requires a researched spec per product law).
- Built-in scenarios use synthetic fixtures; the ad-hoc mode places responsibility on the caller.

## Evidence limits

- The live API response format is verified from official documentation only — a real call may differ; first run should use `--raw` once to confirm.
