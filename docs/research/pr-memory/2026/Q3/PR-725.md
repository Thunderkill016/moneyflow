# PR #725 — Stored account icon/color on list, detail and dialog

- Date: 2026-09-25
- Change class: feature slice with one migration extending two existing
  RPCs (appended defaulted params, backward-compatible). Owner's
  standing autonomous merge grant applies.
- Affected capability or project boundary: account management write
  path (`create/update_financial_account`) + account identity
  presentation on `/accounts`, `/accounts/[id]` and the dialog.
- Superseded issue, roadmap or claim: backlog item "Accounts: identity
  (màu/icon riêng)".
- Status impact: accounts can carry a user-picked icon and tone that
  survives backup/restore; the columns the schema and archive contract
  already carried finally have a write and render path.
- Design basis:
  - Mirrors merged #718 (categories): stored-wins → kind fallback,
    literal-union + guard so css-ownership keeps tracking emission, zod
    enums at the write boundary, palette mirrored into the RPC
    allowlists (convention already exists for currency codes).
  - RPC change convention: drop old signature, recreate with appended
    defaulted params, re-grant execute to authenticated — positional
    pgTAP callers keep working.
  - `mapAccountRow` moved to `src/lib/accounts.ts` — the mapper is pure
    and belongs in the testable domain layer; `server/accounts`
    re-exports so the three callers are unchanged.
  - `null` identity stores `null` and falls back to kind — the app never
    invents a persona for an account the user did not style.
- Changed:
  - `supabase/migrations/20260926120000_account_identity.sql`: both RPCs
    gain `p_icon`/`p_color` with allowlist checks + re-grants.
  - `src/lib/accounts.ts`: palette/icon constants, guards, kind
    defaults, `AccountSummary`/`SaveAccountInput` fields, moved mapper.
  - `src/server/accounts.ts`, `src/server/reports.ts`,
    `src/app/actions/accounts.ts`: select `icon,color`; zod enums; pass
    `p_icon/p_color`; re-export `mapAccountRow`.
  - `account-dialog.tsx`: icon + color selects (icon re-seeds from kind
    only while still a kind default).
  - `accounts-workspace.tsx` + `account-detail-page.tsx` + both CSS
    modules: stored-wins icon/tone; 8-tone palette classes.
  - `src/lib/account-identity.test.ts` (11) +
    `supabase/tests/database/account_identity_rpc.test.sql` (9).
- Verified: unit 1937/1937 incl. allowlist-parity pins; typecheck; lint
  (1 pre-existing warning elsewhere); css-ownership; architecture;
  knowledge; ci-policy 191/191; build 52/52; demo-browser evidence on
  list + detail + dialog. pgTAP runs on CI `database` lane.
- Remaining: authenticated save round-trip is pgTAP-covered, not
  browser-verified locally (no auth env) — stated for the reviewer.
- Production/provider evidence: none — read/write of presentation
  fields only; no provider or production-data write touched.
