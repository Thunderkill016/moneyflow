# PR #603 — throttle public POST routes, add HSTS, fix local deployment-env check

- Date: 2026-09-20
- Change class: Class 3 (server route handlers are request boundaries per `scripts/agent-policy.mjs`)
- Affected capability or project boundary: public API routes (`/api/client-error`, `/api/share-target`), shared security headers, deployment-env guard, dependency manifest
- Status impact: none
- Changed: per-client sliding-window throttle on both public POST routes (30/min and 20/min); additive `allow`, `maxKeys` bound and `clientKeyFromHeaders` in `src/lib/rate-limit.ts`; production-only `Strict-Transport-Security`; `check:deployment-env --force` loads `.env.local` outside Vercel without overriding process values; `shadcn` moved to `devDependencies`
- Verified: lint and typecheck; 1,349 unit/static tests; knowledge and architecture contracts; CI on the PR head
- Remaining: limiter is process-local per serverless instance; HSTS `preload` is a directive only until the domain is submitted; human review and owner merge decision
- Production/provider evidence: none
- Superseded issue, roadmap or claim: none

## Boundaries

- No schema, migration, RLS, financial calculation or UI change.
- `client-error` keeps its always-`204` contract when throttled; `share-target` returns `429` with `Retry-After`.
- Existing server-action limiter semantics (`check`/`peek`) are unchanged.
- Vercel deployment validation is unchanged; the `.env.local` loader runs only for forced local validation.

## Evidence limits

- Throttle thresholds were chosen from expected legitimate usage, not measured production traffic.
- Behind a proxy that does not set `x-forwarded-for`, all clients share one key.
