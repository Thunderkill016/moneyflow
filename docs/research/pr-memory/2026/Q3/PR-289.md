# PR #289 — arbitrary report date range on current main

- Date: 2026-08-04
- Change class: Class 3 financial report/read correctness
- Affected capability or project boundary: report range normalization, equal-length comparison windows, trend bucketing, report loader, report UI and CSV export
- Status impact: candidate — ports the custom date-range feature from obsolete PR #257 onto current main
- Changed: add `period=custom&from=YYYY-MM-DD&to=YYYY-MM-DD`; validate real calendar dates; repair reversed input; clamp future and overlong ranges; compare with the immediately preceding equal-length window; switch long trends to monthly buckets; keep page and CSV export on the same resolved range; surface invalid/swapped/clamped notices
- Current-main reconciliation: nine feature files are ported byte-for-byte; `src/server/reports.ts` is manually merged to preserve the current explicit `.eq("user_id", viewer.id)` tenant predicate alongside RLS
- Financial boundary: report calculations and read windows change; ledger mutation authority, stored amounts, transfers, reconciliation, budgets and transaction facts do not change
- Security boundary: authenticated report reads remain owner-scoped by `requireViewer`, explicit tenant predicate and RLS; no grant, RPC, secret, provider or deployment configuration changes
- Privacy boundary: no new financial payload field, telemetry, production data access or private identifier is introduced
- Performance boundary: custom windows are capped at 1,096 days and long trend output is monthly; no cache or runtime dependency is added
- Verified: pending exact-head policy/static/unit/build, applicable database and browser gates, CodeQL, secret-history scan and independent review after this mandatory memory commit
- Remaining: inspect any correctness or integration failure against current main; confirm no unresolved review thread; merge only the verified final head; run production verification separately
- Production/provider evidence: none; this PR performs no production schema/data/provider write or authenticated production smoke
- Snapshot update: not applicable while PR #289 remains unmerged
- Superseded issue, roadmap or claim: supersedes stale/conflicted PR #257; internal refresh PR #288 is no longer needed
