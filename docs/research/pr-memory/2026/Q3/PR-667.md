# PR #667 — Stage-1 matching gap analysis vs Actual Budget

- Date: 2026-09-22
- Change class: Class 0 (docs-only research note)
- Affected capability or project boundary: `docs/research/STAGE1_MATCHING_GAP_ANALYSIS_2026-09.md` (new).
- Status impact: evidence artifact for the Stage-1 scorecard item "duplicate and transfer matching measured" — documents that MoneyFlow's fingerprint/transfer pairing are same-day-only while the schema's `match_status`/`match_reason`/`match_confidence` columns exist but are never written.
- Changed: distills Actual Budget's three-step matching (exact imported_id → fuzzy payee ±7d → fuzzy amount/date), reimport-deleted and update-dates policies, transfer-payee linking, merge precedence; proposes a bounded flag-only ±2-day experiment with keep/change/kill criteria — flagging only, never auto-merge; explicit do-not-copy list.
- Verified: docs-only; knowledge gate in CI.
- Remaining: the proposed fuzzy experiment requires owner authorization; DeepWiki summary is not a code read; fuzzy-window sizing needs real import data.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: gives the owner a specified, measurable experiment option for Stage 1 without opening an implementation.
