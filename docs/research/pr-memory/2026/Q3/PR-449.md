# PR #449 — feat(acquisition): bind source lifecycle to reviewed clearing

- Date: 2026-08-23
- Change class: Class 3 — financial ingestion/reconciliation boundary.
- Affected capability or project boundary: authenticated Inbox source identity/lifecycle review, account-leg clearing state, financial mutation audit, source provenance/replay safety, and #432 P1 delivery lifecycle.
- Lifecycle impact: completes current slice — PR #449 is the bounded #448 current agent-executable slice and must converge board + packet + current memory before owner handoff.
- Status impact: candidate P1 acquisition hardening. If merged, reviewed source lifecycle becomes durable clearing evidence without becoming ledger or statement-reconciliation truth.
- Changed: source identity resolution now retains the latest approved lifecycle baseline; same-ID lifecycle-only changes become explicit `source_external_id_lifecycle_changed`; reviewed `posted` evidence may move one exactly matching one-leg income/expense account leg from `pending` to `cleared`; `pending`, `removed`, mismatched, split/transfer, already-cleared and reconciled cases preserve stronger MoneyFlow truth. Existing same-ID changed and explicit predecessor UI actions route through the same reviewed lifecycle policy. A follow-up migration preserves reconciliation account→transaction lock ordering before source helpers can lock target rows.
- Research: current Plaid transaction-state/sync documentation establishes that pending→posted may replace an ID, details can change, pending can disappear, and posted records can later be modified/removed. Open Banking UK distinguishes Pending from Booked and separately models mutability; TrueLayer exposes pending records separately. These sources support treating upstream posted/booked state as clearing evidence, not as immutable ledger fact or MoneyFlow statement reconciliation.
- Verified: draft-triggered CI #2915, CodeQL #1972 and Secret History #1972 are diagnostic only because all substantive CI jobs were skipped while PR #449 was draft. Final acceptance requires a non-draft exact-head Class 3 run with policy/static/unit/build/database/browser/UI/e2e plus CodeQL and Secret History actually executed.
- Remaining: converge this PR's board/current-memory/packet projection; mark ready; inspect and fix exact-head Class 3 findings without weakening financial/security policy; hand off to owner when green. Merge remains owner-authorized only.
- Production/provider evidence: not applicable. No provider connection, credential, deployment, production-data write or provider configuration is in scope.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` carries PR #449 post-merge projection; #448 leaves active execution and no next product child is pre-promoted.
- Superseded issue, roadmap or claim: supersedes the temporary #445 rule that source lifecycle is evidence-only in every circumstance. Lifecycle remains source evidence, but a reviewed exact `posted` observation may now establish only MoneyFlow `cleared`; it still cannot establish `reconciled` or overwrite ledger truth.
- Rollback: revert PR #449. Source lifecycle returns to #445 observation-only behavior; account reconciliation and canonical source provenance remain intact.
- Untrusted external instructions copied into project memory: none.