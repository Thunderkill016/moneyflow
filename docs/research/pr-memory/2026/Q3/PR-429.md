# PR #429 — docs: reconcile the board with what actually shipped

- Date: 2026-08-18
- Change class: Class 0 — documentation only. No runtime, test, schema, RLS, Auth, provider, CI or product change.
- Affected capability or project boundary: the Current Work Board's accuracy. Nothing a user can observe changes.
- Status impact: candidate. Closes no gate and changes no release state.
- Changed: advanced the board baseline from `c9a21781` (#415) to `6d4c360b` (#428), recorded what landed in #416 and #419–#428, and rewrote the #403 entry to carry the measurement that should govern whether it resumes.
- Why: the board was eight pull requests behind while presenting itself as current, which is worse than having no board — a reader would have taken a superseded baseline as truth.
- The substantive part is the #403 stop sign: client weight was measured directly, and `rootMainFiles` is 446 KB of framework runtime shipped on every route while the gap between `/` (195.8 KB transferred) and `/dashboard` (311.6 KB) is roughly 116 KB of dashboard client components. The framework floor dominates and cannot be removed, so no simple high-value LCP win exists there. My own earlier hypothesis that splitting the `app-shell` client boundary would be the large lever is **not supported** by that measurement, and the entry says so. Next actor moves from implementer to owner because resuming is now a deliberate architectural choice.
- Verified: `check:knowledge`, `test:ci-policy`, `check:migrations`, `lint`, the active-packet registry validator and `git diff --check` pass. Exact-head provider checks are authoritative.
- Remaining: #403 stays in `NOW` pending an owner decision on whether it belongs there; the FCP attribution experiment remains the only cheap open question and needs one owner-triggered dispatch run. RRB-08 (#398) is unchanged and still owner-owned.
- Production/provider evidence: not applicable. No provider write, deployment, production-data access or production verification.
- Snapshot update: not applicable while Status impact remains candidate.
- Superseded issue, roadmap or claim: supersedes the board's stale `c9a21781` baseline and the implication that #403 has an obvious next implementation step.
