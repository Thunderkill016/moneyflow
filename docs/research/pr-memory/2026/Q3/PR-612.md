# PR #612 — OAuth 2.1 consent screen + local OAuth server config (P2c)

- Date: 2026-09-20
- Change class: Class 3 (security boundary — user consent for third-party OAuth access)
- Affected capability or project boundary: `src/app/oauth/consent/page.tsx` + `actions.ts` (new authorization path), `supabase/config.toml` (`[auth.oauth_server]`), `docs/agents/README.md`, packet `p2-capability-transports.md`
- Status impact: implemented (P2c); stacked on unmerged #611 → #610; completes the P2 transport program pending owner smoke + merges
- Changed: consent page requires signed-in non-demo viewer (unauthenticated users round-trip `/login?next=/oauth/consent?authorization_id=…`), loads details via `supabase.auth.oauth.getAuthorizationDetails`, short-circuits to Supabase's ready redirect when consent was already granted (`redirect_url` response), otherwise renders client name, scope list and destination host; Approve/Deny forms post to server actions that validate the authorization id, re-check the viewer, call `approveAuthorization`/`denyAuthorization` with `skipBrowserRedirect: true` and follow the returned `redirect_url`; `[auth.oauth_server]` enabled locally with `authorization_url_path = "/oauth/consent"` and `allow_dynamic_registration = false`; consent copy states plainly that approval grants read access to the user's own data
- Verified: typecheck, `npm test` 1383/1383 (incl. new `oauth-consent-contract.test.ts`), eslint clean on new files, `check:knowledge`/`check:architecture`/`check:capabilities`/`check:css-ownership` pass, `next build` route table includes `/oauth/consent`
- Remaining: end-to-end OAuth smoke (authorize → consent → callback → token exchange → `/api/mcp`) requires a registered client against the enabled Supabase project — owner-run; CI on stacked head pending; owner merge order #610 → #611 → #612
- Production/provider evidence: owner confirmed OAuth 2.1 beta enabled in the Supabase project; no OAuth client registered or flow exercised yet
- Superseded issue, roadmap or claim: implements stage P2c of issue #608 / packet `p2-capability-transports.md`
- Lifecycle impact: none — packet remains active until owner merges the stack

## Boundaries

- The app never mints authorization codes or tokens — it only records the consent decision; Supabase issues credentials. Consent decision is explicit; no auto-approve path exists.
- `redirect_url` returned by approve/deny comes from Supabase's validated client registration — the app does not accept arbitrary redirect targets from the browser.
- Demo viewers can never reach the consent decision — `viewer.isDemo` redirects to `/login` like an unauthenticated user.
- `authorization_id` travels through the `next` login param and hidden form inputs; it is an opaque request handle, not a credential.
- Client registration stays manual (`allow_dynamic_registration = false`) — no open DCR attack surface.

## Evidence limits

- Page/action verified via contract test + build route table, not a real browser OAuth session.
- `client_id` claim on issued tokens asserted from Supabase documentation, not observed from a minted token.
- Local `[auth.oauth_server]` support assumes the repo's Supabase CLI version accepts the block; `supabase start` not run locally.
