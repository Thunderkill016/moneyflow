# PR #508 — close out the merged #505 authority projection

- Date: 2026-08-28
- Change class: Class 0 — recovery-only documentation and authority-state reconciliation. No product code, schema, RPC, RLS, Auth, provider, financial calculation or transaction behavior changes.
- Affected capability or project boundary: Current Work Board and current project memory baseline/projection metadata only.
- Lifecycle impact: authority transition. This recovery-only closeout replaces PR #505's activated same-PR projection with its exact squash-merged `main` baseline; it neither selects nor completes an agent-executable slice.
- Status impact: shipped project truth now records PR #505 as the current `main` baseline and presents the remaining #426 decision accurately.
- Research: not applicable. This is a deterministic reconciliation from the merged #505 commit, its same-PR board/memory projection, and the plan-authority contract; no external product or provider claim is introduced.
- Changed: the board baseline and project-memory runtime baseline move from PR #504 to PR #505, their activated `Post-merge projection` markers are removed, and owner-facing #426 text records the merged desktop capture slice and its still-separate dashboard-planning decision.
- Verified: `git diff --check`, `npm run plan:resolve`, and `npm run check:knowledge` passed before adding this record. The resolver reported the declared board baseline equal to `23f1e2f9861fa428ab0ec498a496faf2bdc6e8bb`; project knowledge emitted only its existing soft-size warning for current memory. The lifecycle projection parser validates the recovery transition with no replacement projection.
- Remaining: GitHub exact-head documentation/policy checks and required review remain outstanding. No merge, deployment, provider operation or financial mutation is performed by this PR.
- Production/provider evidence: none; no provider or production system is in scope.
- Snapshot update: `docs/plans/active/README.md` and `docs/research/CURRENT_PROJECT_MEMORY.md` now reflect the merged #505 baseline rather than candidate projection state.
- Superseded issue, roadmap or claim: none. This does not promote a new current slice or alter the master program.
