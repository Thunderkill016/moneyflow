# PR #531 — retire Markdown board authority

- Date: 2026-09-02
- Change class: Class 3 governance / CI-policy boundary
- Affected capability or project boundary: executable plan authority, lifecycle validation, task selection and project-knowledge routing
- Status impact: replacement for closed unmerged draft PR #530; recover merged PR #528 intent with #527 as the current executable slice while removing duplicated Markdown board/SHA-baseline authority
- Changed: `PLAN_AUTHORITY.json` schema v2 owns master + zero/one current packet; current selection is activated only when the selecting PR appears in merged manifest first-parent history; resolver/doctor/lifecycle use manifest authority; the former Current Work Board is a retired compatibility pointer; guidance/tests/current memory are aligned
- Verified: predecessor head `1c386a5493801a7cacd7dda6b3fea4e568ca64a6` passed CI #3173, CodeQL #2208 and Secret history scan #2208 before replacement PR metadata was required; exact-head verification for PR #531 is required after rebinding `selectedByPr`
- Remaining: exact-head CI/CodeQL/secret scan on PR #531; no runtime #527 performance implementation belongs in this recovery PR; after merge, fresh-main authority must resolve #527 active before runtime edits
- Production/provider evidence: none required or claimed; repository governance only, no provider/deployment write and no production behavior change
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` tracks #531 as the active recovery PR
- Superseded issue, roadmap or claim: the Markdown Current Work Board, hand-maintained Current main baseline SHA and Post-merge projection marker are no longer executable authority; GitHub Issues/PRs track human work and `PLAN_AUTHORITY.json` selects executable work
- Lifecycle impact: authority transition — recover the already-owner-merged #528 selection by representing #527 as manifest current authority; does not complete #527 and does not select #523

## Root cause

PR #528 passed pull-request checks and was owner-merged, but push CI #3162 failed because the Markdown board still declared a pre-merge baseline. The resolver therefore treated merged main as stale and returned no current slice. The recovery removes duplicated state rather than adding another reconciliation marker.

PR #530 carried the same recovery and reached an exact-head green state, but remained Draft because the connected GitHub ready-for-review mutation fails on an upstream GraphQL schema field. GitHub REST correctly refused to merge a Draft PR. PR #530 was therefore closed unmerged and replaced by this non-draft PR; `current.selectedByPr` is rebound to #531 so merged manifest history remains truthful.

## Safety boundary

No application runtime, financial semantics, schema/RLS/Auth, provider configuration, production deployment or performance implementation changes. Rollback is one focused revert restoring the prior plan-authority implementation; partial rollback of only manifest or scripts is not safe.
