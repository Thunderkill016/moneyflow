# PR #302 — add exact-head CI monitoring fast path

- Date: 2026-08-06
- Change class: Class 1 developer-experience and CI observability tooling with no application runtime behavior change
- Affected capability or project boundary: contributor and agent GitHub Actions monitoring, exact-head verdict validation, failed-run log extraction and CI operations documentation
- Status impact: approved candidate — adds an exact-head CI watch command on PR #302; the owner explicitly authorized merge after successful exact-head verification, and the branch was rebased onto merged Phase 4 before final verification
- Changed: add `scripts/watch-pr-ci.mjs`; expose `npm run ci:status` and `npm run ci:watch`; validate the PR head SHA before and after monitoring; delegate low-noise watching to `gh pr checks --watch --fail-fast`; list only workflow runs for the recorded head SHA; print only failed-step logs; add helper unit coverage and `docs/operations/ci-observability.md`
- Verified: design is grounded in official GitHub CLI documentation for PR checks, run watching/listing/viewing, GitHub Actions concurrency/cache/artifact guidance and Playwright CI guidance; the pre-rebase candidate passed CI #1751, CodeQL #870 and secret-history scan #870; final exact-head evidence must refresh after rebase onto `main@4b48626935aa0ed3ddd0058bb0561ae1c2d17335`
- Remaining: pass all classifier-selected exact-head gates on the rebased head, then squash merge using the expected head SHA
- Production/provider evidence: none; no deployment, provider setting, database, migration, authentication, RLS, production data or production smoke was performed
- Snapshot update: applies after PR #302 is merged; the runbook becomes the preferred low-noise local and agent monitoring procedure
- Superseded issue, roadmap or claim: replaces ad hoc repeated polling as the preferred local monitoring procedure; it does not supersede CI required checks, the risk classifier, Phase 4 evidence or any product roadmap item
