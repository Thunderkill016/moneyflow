# PR #304 — harden and measure the MoneyFlow CI pipeline

- Date: 2026-08-06
- Change class: Class 2 delivery-system workflow and security hardening; no product/runtime behavior change
- Affected capability or project boundary: GitHub Actions checkout runtime, repository credential persistence, database gate topology, CI contracts, measurement and durable CI memory
- Status impact: candidate — the branch contains the audited first hardening slice and is not current project truth until exact-head verification passes and the owner separately authorizes merge
- Changed: upgrade all workflow checkouts to verified `actions/checkout@v7.0.1` full SHA; disable persisted checkout credentials; split conditional `database_checks` execution from stable required `database` summary; add topology/credential contracts; record measured source/run/log audit and prioritized decisions
- Verified: source and historical-log audit completed; the first exact-head run proved GitHub accepts the full-risk graph, selects `Database executor`, runs checkout v7 with `persist-credentials: false`, and preserves all-ref Gitleaks; final exact-head CI/database/browser/CodeQL evidence remains pending after packet-schema repair
- Remaining: repair required packet/memory schema, pass complete exact-head policy/static/test/build/database/browser/security gates, then run a documentation-only head to measure the unselected database path and update durable project memory
- Production/provider evidence: none; no deployment, provider setting, database schema, Auth/RLS, production data or production smoke is part of this PR
- Snapshot update: not applicable while PR #304 remains a candidate and exact-head evaluation is incomplete
- Superseded issue, roadmap or claim: supersedes only the old checkout runtime and unconditional no-op database runner topology; it does not supersede risk-proportional verification, exact-head policy, full CodeQL upload, all-ref Gitleaks, browser coverage or any product roadmap
- Baseline: `main@429eb6777a63b3172a04ce164a512420e31085c8`
- Branch: `agent/ci-system-hardening`
- Current PR: #304
- Preserved: lint, TypeScript, complete unit/static RLS tests, production build, fresh local database reset/pgTAP, Chromium smoke, Chromium/WebKit audit, real CodeQL upload, all-ref Gitleaks, stable `verify`/`database`/`e2e` checks and selective same-SHA reruns
- Baseline evidence: PR #301 CI #1746, CodeQL #866 and secret-history #866 provide the principal full-risk timing sample; PR #303 CI #1760 provides the documentation-only no-database comparison
- Known measurement limit: the available connector does not expose a complete paginated workflow-run census, so no repository-wide first-attempt failure percentage is claimed
- Merge boundary: do not merge without a separate explicit owner instruction
