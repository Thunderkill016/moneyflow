# PR #461 — feat(import): remember Direct CSV column mappings

- Date: 2026-08-25
- Change class: Class 3 — financial-domain UI boundary; the change is browser-local and never performs a ledger/source action itself.
- Affected capability or project boundary: Direct CSV repeated column mapping on one device for an exact normalized ordered header shape.
- Lifecycle impact: candidate only until merge. Current slice #460 remains active; same-PR lifecycle convergence is pending exact-head checks and owner merge.
- Status impact: adds an explicit opt-in remember action and a later opt-in apply offer. It stores only preset version, normalized header shape and column indexes; it never stores CSV rows, filenames, snippets, money, account/category choices, source data or credentials.
- Changed: adds a pure mapping-preset helper and regression tests; Direct CSV reads optional browser storage after upload, falls back to normal auto-map for malformed/mismatched storage, and keeps existing dry-run/duplicate/transfer/commit paths intact.
- Verified: focused unit/UI contract 18/18; migration identity, knowledge, CI policy (190), CSS ownership, typecheck and formatter/lint passed. `NEXT_PUBLIC_APP_MODE=demo npm run verify:prepush` passed: 1,113 tests and production build. Manual Chromium demo exercised upload → remember → reload → matching re-upload offer at desktop and 390×568; no commit action was invoked.
- Remaining: full local E2E reported 120 pass and four auth CAPTCHA failures because the local demo environment has no provider configuration. Full local UI audit reported 503 pass, 141 skipped and 54 WebKit iPhone/Tablet failures at `page.goto` with the engine's internal error across unrelated routes before app assertions. Exact-head provider checks must establish the required browser evidence.
- Production/provider evidence: no provider read/write, production data action or deployment occurred.
- Merge evidence: draft PR #461; merge remains owner decision after exact-head provider checks and review.
- Snapshot update: active board/current memory select #460 while this candidate is open; no lifecycle projection is claimed.
- Superseded issue, roadmap or claim: addresses #460 only; does not expand Direct CSV into a provider-specific import contract or cross-device mapping sync.
- Rollback: revert PR #461 and optionally remove the browser-local preference key; ledger and source data are unaffected.
- Untrusted external instructions copied into project memory: none.
