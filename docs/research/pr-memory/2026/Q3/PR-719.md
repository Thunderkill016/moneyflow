# PR #719 — Two-up secondary actions on phones instead of full-width stack

- Date: 2026-09-25
- Change class: bounded UI/CSS (Class 2-presentational). No JS/TSX,
  no schema, no server action.
- Affected capability or project boundary: phone-width layout density
  of secondary action groups across 8 CSS modules; desktop (≥761px)
  behavior unchanged.
- Superseded issue, roadmap or claim: closes the "mobile secondary
  actions stack one full-width row each" finding from the whole-app
  design comparison (2026-09-25).
- Status impact: at ≤760px shared `SecondaryHeader` actions and
  per-page action groups rendered `flex: 1 1 180px` or `1fr` — on a
  390px phone one ~50px full-width row per button (~250px before
  content on /inbox). Leftover lower-priority design-comparison
  findings remain open: desktop `/capture` narrow column, whole-app
  dark-mode visual sweep, `/reports` 96×16px "Tùy chọn xuất"
  inline-link tap target (P3, pre-existing).
- Changed: at the existing mobile breakpoints every `.actions`-style
  group keeps `display:flex; flex-wrap:wrap` with children
  `flex: 1 1 45%; min-width: min-content` — short labels go two-up,
  a long nowrap label exceeds 45% and takes a full row instead of
  overflowing ("Xem lại ghi 1 giao dịch" ~167px vs ~144px cell).
  Single-column at ≤340px via `display:grid; 1fr`. Files:
  `secondary-layout` (`.actions`/`.sectionAction`),
  `transactions-workspace` (`.headingActions`), `planning-workspace`,
  `rules-page` (incl. `.dialogFooter`), `imports-page`,
  `import-preview-page`, `direct-csv-import-page`, `inbox-bulk-bar`
  (`.buttons` only — `.actions` there is a radio fieldset and stays
  stacked). Round-2 director fixes: `min-width: min-content` for long
  labels; `SecondarySummary` reverted to 1-col ≤480px because
  `MoneyValue` must not wrap (~154px at 17px min).
- Verified: browser sweep 9 routes × {390,360,320,1440}px — 0 console
  errors, 0 horizontal scroll, per-element clip check
  (scrollWidth ≤ clientWidth on every a/button), 44px targets;
  interactions: "Chia khoản chi" dialog open/close, kind chip
  aria-pressed, bulk bar two-up/one-col boundary. Independent design
  evaluator PASS (8/7/8/9, all gates, `design:check` pass on the
  evaluation JSON). Gates: lint, typecheck, build 52 routes,
  css-ownership, dead-css, code-css-ownership (0 new/0 stale, 211
  baseline), ci-policy 191/191, knowledge. Test suite unchanged
  (CSS-only). Director round-2 verdict: approve on code review at
  `730f9842`.
- Remaining: none blocking. P3 notes — odd-count groups leave the
  last cell stretched/half-width (inherent); pre-existing /reports
  inline-link target.
- Production/provider evidence: none — CSS-only client rendering
  change; no production verification applicable.
