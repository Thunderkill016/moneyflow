# PR #704 — Restore owned route-loading skeletons across 29 routes

- Date: 2026-09-23
- Change class: Class 1 (presentation-only: one shared CSS Module + mechanical className swap in 29 loading boundaries + baseline shrink; no schema/RPC/provider change)
- Affected capability or project boundary: App Router loading boundaries under `src/app/**/loading.tsx`; presentation-ownership baseline `docs/research/presentation-ownership-baseline.json`.
- Status impact: candidate → implemented. Route skeletons render owned again; dead global skeleton class family is contract-banned from returning.
- Changed: new `src/app/route-loading.module.css` owns the skeleton vocabulary (route, line variants, card, block, row, grid, panel) with a reduced-motion guard; 29 `loading.tsx` files resolve through it; `activity/` keeps its own module, `dashboard/` keeps live Tailwind. New `src/lib/route-loading-style-contract.test.ts` pins the module and bans the dead classes. Baseline 285 → 220 (65 stale entries removed).
- Verified: `npm run test` 1790/1790; `npm run typecheck` clean; `npm run lint` 0 errors (known `_reviewStatus` warning); `npm run build` + `check:code-css-ownership` green; `check:knowledge`, `test:ci-policy` 191/191 pass; `dead-css-scanner` zero-unreachable contract still holds.
- Remaining: per-route skeleton shapes were simplified to a generic vocabulary — visual fidelity is intentionally approximate; no production skeleton evidence exists beyond the build bundle.
- Production/provider evidence: none (presentation layer only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: any new loading boundary must import the shared module or a co-located module — bare global skeleton classes fail the contract test; baseline may only shrink further.
