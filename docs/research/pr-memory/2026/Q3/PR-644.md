# PR #644 — Capture V2 scripted TTLT baseline + bench driver

- Date: 2026-09-21
- Change class: Class 0 (docs + dev tooling; no product code)
- Affected capability or project boundary: `scripts/capture-bench-driver.mjs`, `docs/research/CAPTURE_V2_BENCH_BASELINE_2026-09.md`. The harness `public/capture-bench.html` (#636) is exercised, unchanged.
- Status impact: C1 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — the benchmark ran end-to-end; repeatable scripted baseline recorded.
- Changed: Playwright driver performs the real UI work for all 7 tasks (amount fill, kind/category chips incl. the #596 learned-pattern chip, transfer form, paste→"Vào Inbox"→per-candidate Duyệt→"Duyệt vào sổ") while the harness tab measures storage-event timing. First run: amount-first median 1.0s, description 1.8s (candidate ~0.9s + approve ~0.9s), 7/7 `đúng` matches. Doc records the machine-floor caveat, the demo-sample warm-up requirement, and that human device runs remain the gating evidence tier.
- Verified: driver run on local production build (7/7 recorded, report generated); check:knowledge, check:architecture, eslint clean. Not run: npm test/build — no runtime code changed (script + docs only).
- Remaining: CI static lanes; owner merge decision. Human phone run still required before any H1/H4-gated product change.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes C1's runnable part; the packet's owner-run task stays open. Next in plan order: P1 budget-pace line, then R1/R2, S1, X1/X2.
