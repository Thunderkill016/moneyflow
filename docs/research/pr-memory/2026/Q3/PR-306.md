# PR #306 — Phase 5 Transactions and Capture ownership

- **PR:** #306
- **Branch:** `feat/ui-phase-5-transactions-capture`
- **Base:** `main@ca7fa855f0e9278f2c33e4d1aba0272a24d53fb0`
- **Status:** draft / implementation in progress
- **Status impact:** candidate only until merge and owner acceptance
- **Packet:** `docs/plans/active/ui-phase-5-transactions-capture.md`
- **Owner authorization:** explicit **“tiếp tục p5”** on 2026-08-06
- **Merge authorization:** not granted

## Intended outcome

Move the highest-frequency Transactions and Capture flow behind local route/component ownership and the Phase 2 primitive contracts while preserving integer-VND, transfer exclusion, split exactness, current mutation payloads and soft-delete/undo behavior.

## Current branch evidence

At draft head `48dc710ba9613277ce64afd3cee6c434af852e36`:

- active Phase 5 packet exists with research, contracts, tasks, rollback and permission boundary;
- `/transactions` and `/timeline` select the new locally owned `TransactionsWorkspace`;
- summary, filters, ranges, selection/bulk controls, day groups, rows, money and row actions compose Phase 2 feedback/action/money primitives;
- stable `data-slot` evidence hooks replace new dependence on legacy manager class names;
- the local module includes 320/360 phone reflow and forced-colors behavior without `:global(...)` or `!important`;
- current financial hooks, URL filter serialization, transfer/split/edit orchestration and eight-second delete undo remain present;
- a source contract locks route adoption, primitive composition, transfer-excluded totals, deletion recovery and local responsive ownership.

## Work still required

- exact-head CI has not yet produced results for this record;
- add/edit/transfer/split dialogs and embedded quick capture still use the legacy dialog/form presentation boundary;
- browser selectors and keyboard/virtual-viewport evidence need repair against stable slots/roles;
- global manager/dialog CSS and the final transaction amount-field compatibility repair have not been removed;
- complete browser/security verification, independent diff evaluation and owner review remain pending.

## Safety boundary

No database schema, migration, Auth, RLS, provider setting, deployment, production data or financial-domain formula is changed. This branch must remain unmerged until exact-head verification is green and the owner separately authorizes merge.
