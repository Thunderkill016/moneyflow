# PR #306 — Phase 5 Transactions and Capture ownership

- Date: 2026-08-06
- Change class: Class 3 — cross-cutting authenticated UI ownership migration
- Affected capability or project boundary: `/transactions`, `/timeline`, `/capture/quick`, add/edit/transfer/split transaction dialogs, responsive audit evidence and transaction presentation ownership
- Status impact: candidate — verified-unmerged only; PR #306 is open and is not current product behavior until owner-authorized merge
- Changed: introduced a locally owned ledger workspace; moved summary, filters, ranges, bulk correction, day groups, rows and actions onto Phase 2 primitives; migrated add/edit/transfer/split and embedded quick capture onto shared Dialog/field/action contracts; fixed `Lưu & thêm tiếp`; split Timeline into a reviewed-only read-only owner and hook; preserved mutation payloads, integer-VND, transfer exclusion, split exactness and eight-second soft-delete undo; replaced retired class-based browser dependencies with stable slots; removed the retired transactions component/module and final MobileShellContract repair; added deterministic phone bottom-sheet ownership and complete large-VND narrow-screen rendering
- Verified: implementation head `81d9cf0cc289a8676e9150278789b4d9957a3c32` passed CI #1827 (`31070085106`), CodeQL #945 (`31070085098`) and secret-history scan #945 (`31070085113`); policy, CSS/architecture, lint, typecheck, complete unit/static RLS, production build, browser smoke, final e2e aggregation and the Chromium/WebKit cross-device matrix all passed
- Browser evidence: `browser-smoke-evidence-31070085106-1` with SHA-256 `65ccb37db1437903da99395a081f91d508b02f9472a5675d61cba310ce165a1d`
- UI-audit evidence: `ui-audit-evidence-31070085106-1` with SHA-256 `e8df3389fcdda6e2e987e73a7863548967d0057cb0f284bd597c07ae0b46b069`; covers phone 320/360/390, tablet, desktop, dark mode, Chromium/WebKit, 200% text and keyboard
- Remaining: exact-head checks for evidence-only documentation commits, explicit owner PR review/merge decision, later deployment verification if merged, and a separately bounded cleanup for historical root transaction selectors interleaved with active account-dialog styles
- Production/provider evidence: not applicable; no deployment, provider setting, production data, database, Auth or RLS operation was attempted or authorized
- Snapshot update: not applicable — current project memory records merged truth and must not describe this branch as current product behavior
- Superseded issue, roadmap or claim: supersedes the parent packet claim that Phase 5 is unauthorized; it does not supersede the owner merge/deployment boundary or physical-device migration acceptance

## Authorization and safety

- Owner authorization: explicit `tiếp tục p5` on 2026-08-06.
- Branch: `feat/ui-phase-5-transactions-capture` from `main@ca7fa855f0e9278f2c33e4d1aba0272a24d53fb0`.
- Packet: `docs/plans/active/ui-phase-5-transactions-capture.md`.
- Merge authorization: not granted.
- No financial-domain formula, schema, migration, Auth, RLS, provider or production-data write is included.

## Evaluation record

The first heavy browser runs were intentionally treated as evaluative evidence, not as a box-checking exercise. They exposed and led to fixes for:

1. Timeline retaining review/filter mutation controls instead of being read-only.
2. Stale `.manager-row`, `.empty-state-actions`, `.transaction-dialog` and SAFE-09 selectors.
3. Parent-controlled closing defeating `Lưu & thêm tiếp`.
4. Transaction dialogs remaining inset by 18px on phone widths because shared utility and local owner rules had equal specificity.
5. Large integer-VND values overflowing narrow ledger boxes at 320 and 390 CSS pixels.

The final implementation-head matrix passed without weakening placement, overflow, focus, target-size or accessibility assertions. Database checks were correctly not selected because no database, migration or RLS boundary changed.

## Evidence interpretation

PR #306 was marked ready-for-review only to activate the heavy risk-proportional gates. Earlier draft CI shells are not implementation evidence because heavy shards were skipped. The green implementation head is verified-unmerged evidence; it is not authorization to merge or deploy and it does not make branch behavior current product truth.
