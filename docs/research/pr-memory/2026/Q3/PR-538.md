# PR #538 — replacement merge vehicle for #527 closure

- Date: 2026-09-02
- Change class: Class 2 replacement delivery/provenance for the already-verified #527 performance closure
- Affected capability or project boundary: delivery provenance only on top of the exact-green #532 runtime/lifecycle tree
- Status impact: replaces draft PR #532 as the non-draft merge vehicle; if merged, completes #527 to zero current executable slices and selects no follow-on work
- Changed: branch starts at exact-green #532 head `6ae948e553309d28bcb3ee6132324241cfa0e2ac`; adds only this PR-specific memory and reconciles CURRENT_PROJECT_MEMORY so the replacement PR number is truthful
- Verified: predecessor #532 exact head `6ae948e553309d28bcb3ee6132324241cfa0e2ac` passed CI #3209 including policy/static/unit/build/fresh Supabase reset+pgTAP/archive round-trip, Browser smoke, authenticated ownership, Cross-device UI audit and `e2e`; CodeQL #2244 and Secret history scan #2244 also passed. PR #538 must pass its own exact-head required checks after this provenance-only delta before merge.
- Measured: runtime/performance tree is unchanged from #532. Canonical same-methodology `/dashboard` evidence remains score 86->87, LCP 4009.7->3793.9 ms, TBT 140.0->77.9 ms, script transfer 319,931->303,886 B, main-thread 1735->1585 ms and bootup 805->630 ms; `/` remained effectively flat.
- Replacement reason: GitHub REST correctly refuses merging draft #532, while the connected `markPullRequestReadyForReview` mutation fails upstream because its response requests nonexistent `Repository.fullDatabaseId`; repository auto-merge is also disabled. A non-draft replacement preserves the verified tree without bypassing branch protection.
- Production/provider evidence: no deploy, provider configuration or Supabase write is added by this replacement
- Snapshot update: CURRENT_PROJECT_MEMORY names #538 as the canonical merge vehicle and retains #532 as detailed historical evidence
- Superseded issue, roadmap or claim: #532 remains the detailed implementation/evidence predecessor but is superseded as the merge vehicle only; #536/#537 remain unselected security planning
- Lifecycle impact: completes current slice #527 to `current: null`, archives its packet, refreshes current project memory, and selects no follow-on work

## Provenance

PR #538 was created only because the connected GitHub Ready-for-review mutation is broken upstream. It starts from the exact commit that fully passed #532's final checks and makes no runtime, financial, database, Auth, provider or performance behavior change.

The detailed mechanism, analyzer attribution, Lighthouse methodology, focus-regression correction and remaining-bottleneck evidence remain in `docs/research/pr-memory/2026/Q3/PR-532.md`. This record exists so repository policy has an explicit durable memory for the actual merge PR number.

Owner explicitly approved merge on 2026-09-02. Merge is still conditional on #538's own exact-head required checks; no check or branch protection rule is bypassed.
