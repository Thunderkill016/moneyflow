# PR #314

- Date: 2026-08-07
- Change class: Class 3 — CI operations and required-check recovery tooling
- Affected capability or project boundary: GitHub Actions exact-head monitoring, cancellation, manual dispatch and operator recovery
- Status impact: candidate — no current product capability, financial behavior or production behavior changes before merge
- Changed: added `scripts/recover-pr-ci.mjs`; added safe stale-run planning, normal/force cancellation, `workflow_dispatch` replacement discovery, exact-head/moved-head guards, dry-run/cancel-only/no-watch modes; added focused unit contracts; exposed `npm run ci:recover`; extended the CI observability runbook and active work packet
- Verified: 7/7 focused recovery helper tests passed in the implementation environment; exact-head project-policy, static, build, CodeQL and secret-history evidence remains pending
- Remaining: run selected exact-head checks on the final PR head; review the operational write boundary and rollback; perform a dry-run against an open PR; merge only after explicit owner decision
- Production/provider evidence: no production deployment, branch protection, required-check, ruleset, provider configuration or production-data write; the new tool delegates future Actions writes to an already authenticated GitHub CLI identity
- Snapshot update: not applicable — this candidate adds repository operations tooling and does not change merged product truth
- Superseded issue, roadmap or claim: extends PR #302 exact-head observability with bounded recovery; does not supersede required checks, CodeQL, secret-history scanning or human merge authority

## Evidence boundary

The implementation follows current official GitHub CLI and Actions interfaces for force cancellation, manual workflow dispatch by ref, exact-SHA run listing and exit-status watching. Repository tooling cannot repair a GitHub service outage or guarantee runner capacity; it only provides a repeatable, auditable recovery path without no-op commits or stale-head claims.
