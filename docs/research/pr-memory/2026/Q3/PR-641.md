# PR #641 — Dashboard per-account balance strip

- Date: 2026-09-21
- Change class: Class 2 (bounded UI + read-model extension on the existing dashboard bundle path)
- Affected capability or project boundary: `src/server/dashboard.ts`, `src/server/accounts.ts`, `src/lib/dashboard-accounts.ts`, `src/app/dashboard/page.tsx`, `src/components/moneyflow-dashboard.tsx`, `src/components/dashboard/{dashboard-overview-sections,statement}.tsx`, `statement.module.css`, `dashboard-phase4-contract.test.ts`.
- Status impact: D1 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — the statement now answers "tiền nằm ví nào" under `Bạn đang có` using data the loaders already return.
- Changed: `DashboardPageWorkspace.accountBalances` populated on all three read paths (RPC join of `bundle.accounts`×`bundle.balances`, `getAccountsWorkspace` fallback, `demoAccountRows`); archived accounts excluded and accounts missing from the balance projection get no fabricated row. New `reconcileAccountBalances` replays the snapshot→live transaction delta per account so the strip stays consistent with `reconcileBalanceSnapshot` after in-page saves. Statement renders ≤4 rows (`STATEMENT_ACCOUNT_LIMIT`) with per-row `currencyCode`; overflow links to `/accounts`.
- Verified: TDD — `dashboard-accounts.test.ts` failed on missing module, passes 4/4 after implementation (expense/income deltas, transfer netting, no-op, unknown-account ignore). `npm test` 1440/1440 including a new phase-4 contract test pinning per-row currency and forbidding client-side re-derivation. `tsc --noEmit`, `lint`, `build`, `check:knowledge`, `check:architecture`, `check:css-ownership`, `check:deployment-env` clean. Browser-verified on the production build in demo mode at 390px and 1280px: four wallets render, `USD du lịch` shows `200,00 US$`, and the VND standing total equals only the VND rows.
- Remaining: CI on exact head; owner merge decision. Authenticated-path coverage depends on the existing `account_balances` projection inside `get_dashboard_bundle`; no schema change.
- Production/provider evidence: none — no migration, no provider change.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes D1; next in plan order is Transactions T1/T2 (needs_review KPI/filter, date presets) then dashboard D2/D4.
