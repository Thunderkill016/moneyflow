# PR #652 — Batch account picker on statement preview

- Date: 2026-09-21
- Change class: Class 2 (bounded UI flow; no schema/action change — candidate `account_id`/`account_name` columns, `commit_import_batch_candidates` RPC and demo store already carried the fields end to end)
- Affected capability or project boundary: `src/lib/inbox/parse-csv.ts` (`toCsvCandidateInputs` optional `{ account }`), `src/lib/inbox/parse-csv.test.ts`, `src/components/inbox/import-preview-page.tsx`, `src/app/imports/[batchId]/preview/page.tsx` (`getFinanceWorkspace` for accounts).
- Status impact: upstream half of the account-resolution gap from the 2026-09 audit — statement imports landed every row "Tài khoản chưa xác định" because nothing in the acquire path asked which account a file belongs to. Downstream half is PR #650 (Inbox bulk "Gán tài khoản").
- Changed:
  - Preview page gains an opt-in "Sao kê này thuộc tài khoản" `SelectField` — default "Chọn sau trong Inbox"; the parser's no-bank-inference stance is preserved (user chooses, parser never guesses).
  - Chosen account stamps `accountId`/`account` on every candidate; for transfer candidates it is the source leg (the account the export describes).
  - Commit review dialog shows the chosen account.
  - Pre-set candidates pre-select the account in the per-candidate approval dialog via existing `resolveAccountId`.
- Verified: TDD (`toCsvCandidateInputs` RED → GREEN); `npm test` 1486/1486; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:css-ownership, check:architecture, check:deployment-env, test:ci-policy 191/191. Browser verify on dev build (390px, demo): CSV upload → select lists 4 demo accounts with `(USD)` suffix → dialog shows "Tài khoản: MB Bank" → commit → 3 new candidates carry MB Bank; seeded unresolved flags unchanged.
- Remaining: CI lanes on exact head; authenticated commit path rides the existing tested RPC/schema (no new code path).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place; same-session `.devin/agents/` tooling shipped separately as PR #651.
