# PR #555 — MON-63 measurement and closeout continuation

Lifecycle impact: none while this PR is Draft; MON-63 remains the sole current executable slice.

## Purpose

Continue MON-63 from fresh post-#554 `main@73fa6d6ac306f0b56367da2be80a7c5f42fb59b9` and close the remaining acceptance gaps without opening a new product subsystem.

## Current implementation evidence

- Adds first-party privacy-safe maintenance counters to existing tenant-owned `import_batches` rather than introducing an analytics vendor.
- Records generic authenticated preview→Inbox commit attempts and server-confirmed exact replays.
- Measurement never receives raw statement content, descriptions, amounts, source IDs, account identifiers or credentials.
- Measurement is best-effort and must never change the result of the financial candidate commit.
- Database RPC is `SECURITY INVOKER`, RLS-owned, event-allowlisted and prevents replay count from exceeding observed attempts.
- pgTAP covers own-row increments, invalid event rejection, privilege posture and cross-tenant denial.

## Research/evidence boundary

The existing `src/lib/safe-analytics.ts` contract is privacy-safe but its sink is intentionally a no-op. Current Vercel Hobby does not provide the custom-event path needed to treat those calls as durable product measurement. This PR therefore uses the existing first-party batch model for minimum operational evidence and does not add a third-party analytics dependency.

## Remaining before Ready

- reconcile migration identity for the new migration;
- run exact-head policy/static/unit/build/database/browser/security gates;
- finish remaining MON-63 durable preset/history/review evidence that is still unproven;
- independently evaluate privacy, replay and failure cases;
- only if the complete MON-63 acceptance contract is met, archive/close the packet, reconcile current memory and return `PLAN_AUTHORITY.current` to null in this same PR.

No provider, production configuration, live-bank integration, raw-statement retention or automatic ledger posting is authorized by this PR.
