# PR #703 — Restore /capture styling via route-owned CSS module

- Date: 2026-09-23
- Change class: Class 2 (single-route UI restoration; new CSS Module; contract test; no schema/RPC/provider change)
- Affected capability or project boundary: `src/components/inbox/capture-page.tsx` + new `capture-page.module.css`; `src/lib/capture-page-style-contract.test.ts`.
- Status impact: defect → implemented. `/capture` was fully unstyled since the Phase-10 stylesheet replay deleted the global classes the page referenced; the route now renders the restored layout inside the CSS-ownership model.
- Changed: page markup migrated from bare global classes (`capture-workspace`, `capture-menu`, `panel`, `eyebrow`, `secondary-button`, `transactions-title-row`, `page-heading-actions`, `dashboard`) to module classes; `Về Inbox` link → `LinkButton intent="secondary"`; option links → `LinkButton unstyled` + `.option`. Styles re-expressed in `--mf-*` tokens (brand tile tint, border, surface, radii); 640px workspace cap kept; mobile density at ≤640px. Contract test pins module import, no bare globals, all used selectors defined, no `!important`.
- Verified: `npm run test` 1775/1775; `npm run typecheck` clean; `npm run lint` 0 errors; `check:css-ownership` 0 `!important`/single owner; `check:architecture`, `check:knowledge`, `test:ci-policy` 191/191 pass. `DESIGN_ROUTES=/capture npm run design:capture` produces restored phone+desktop screenshots (before: bare text).
- Remaining: route `loading.tsx` skeletons share the same dead-global problem across all routes (`route-loading`, `loading-line`, `capture-skeleton-*` are undefined) — handled as a separate sweep, not bundled here.
- Production/provider evidence: none (client styling only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: do not reintroduce bare `capture-*`/`panel`/`eyebrow` global classnames on this route — the contract test fails on them; any new element gets its styles from `capture-page.module.css` only.
