# PR #666 — Instrument trust-boundary actions

- Date: 2026-09-22
- Change class: Class 1 (bounded analytics wiring, three components + union extension)
- Affected capability or project boundary: `src/components/account-reconciliation-page.tsx`, `src/components/export-settings-page.tsx`, `src/components/reports-page.tsx`, `src/lib/safe-analytics.ts`.
- Status impact: closes the last dead/missing analytics signals found in the post-canon audit — `reconcile_completed` (new) is the only event feeding Stage-0 "trusted periods retained"; `export_downloaded` was declared in the union with zero call sites.
- Changed: `reconcile_completed` fires on successful period completion (demo + authenticated) with `cleared_count`; `export_downloaded` fires from `/settings/export` (`kind`, `format`, `count`) and the `/reports` CSV primary action (`surface`).
- Verified: `typecheck` clean; eslint clean on touched files; `check:knowledge` green.
- Remaining: maintenance minutes / session-effort still uninstrumented — needs a designed signal, not ad-hoc events; Stage-0 physical-device and hosted-auth evidence remain owner-only actions.
- Production/provider evidence: none — events are inert without the Web Analytics runtime.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: every declared product event now has a call site; Stage-0/1 measurable subset is complete pending real usage data.
