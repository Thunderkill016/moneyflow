# PR #522 — exception-first Inbox grouped review

- Date: 2026-08-28
- Change class: Class 3 — changes which existing pending Inbox candidates may enter grouped ledger-posting UX, while leaving ledger/RPC/schema/RLS/provider contracts unchanged.
- Affected capability or project boundary: Inbox candidate review only. One derived deterministic readiness classifier separates `ready` from `needs_attention`; classification itself never writes a financial fact.
- Lifecycle impact: completes current slice #511 through same-PR post-merge convergence; no follow-on slice is selected by this PR.
- Status impact: candidate until the final lifecycle-converged head passes required checks and the owner squash-merges PR #522.
- Baseline: implementation branch started from exact merged authority `main@133fa462d3cd5f90b1f70cccb179547815c2ba2d` (PR #521).
- Research correction: the corpus supports capture/maintenance friction as the stronger product problem. #511/#522 is review-safety infrastructure, **not** the primary answer to manual-entry friction. Follow-on acquisition work requires a separate evidence-backed authority slice after merge.
- Changed: `src/lib/inbox/readiness.ts` owns one pure classifier and machine-readable attention reasons. Ready requires a pending income/expense candidate, non-low confidence, no duplicate/transfer signal, valid positive safe-integer VND amount/date, and explicit account/category evidence resolving exactly to current compatible options. It reuses `buildLedgerPost` as a final posting-invariant check and never treats `draftFromCandidate` first-option fallbacks as readiness proof.
- Changed UI: Inbox reports textual `Sẵn sàng` / `Cần xem lại` counts and row states, includes one-action `Chọn Sẵn sàng`, and keeps explicit grouped confirmation. The bulk dialog and page handler both fail closed to the Ready subset; the page reclassifies from current facts immediately before posting.
- Keyboard safety: the former desktop `A` shortcut could reach grouped apply without opening the confirmation dialog. #522 removes `A` from the shortcut map and pins that absence in unit tests.
- Preserved: manual single review, reject/category actions, duplicate/transfer review, candidate approval idempotency/recovery, demo/auth classifier semantics, VND integer rules, transfer neutrality and explicit confirmation before ledger writes.
- Explicit non-scope: no automatic approval/posting, behavior learning, fuzzy/semantic matching expansion, transfer matching change, OCR/AI, bank/provider/native integration, schema/RLS/Auth/deployment or production/provider writes.
- Tests added/updated: readiness unit coverage for valid high/medium candidates and every specified exclusion; browser mixed-batch fixture with three Ready plus low-confidence/possible-duplicate/transfer exceptions; legacy Phase 8 safety test now asserts the zero-Ready fail-closed state instead of attempting a disabled grouped approval.
- Corrected interaction metric: the pre-#511 Inbox already had one-click `Chọn tất cả`. There is **no general 40% click-reduction claim**. Raw minimum grouped activations remain three (`select → review → confirm`). #511 improves safety/decision burden only; no manual-entry reduction is claimed.
- Verified: code/evaluation head `20896ba3c7a9bee71893994fbf199bfd9ffc77eb` passed CI #3142 including policy/knowledge, static quality, unit/static-RLS, production build, cross-device UI audit, browser smoke, authenticated ownership browser smoke and E2E aggregate; CodeQL and secret-history scan also passed. This same-PR lifecycle mutation creates a newer head, so final exact-head required checks must pass again before owner handoff; no stale-head claim is used as final acceptance.
- Browser diagnosis: run #3140 exposed two test-contract defects, not waived product failures — an ambiguous text locator after Ready selection and a legacy test trying to open grouped approval with zero Ready candidates. Both were corrected and #3142 passed desktop/mobile browser coverage.
- Local limitation: the execution sandbox previously could not resolve `github.com`, so no local npm run is claimed. GitHub exact-head CI is the machine evidence.
- Remaining: final lifecycle-converged exact-head required checks, final diff/spec review, and owner merge decision. No follow-on slice may be selected in PR #522.
- Production/provider evidence: none; no production/provider action is expected or authorized.
- Snapshot update: same-PR post-merge projection updates the Current Work Board and `CURRENT_PROJECT_MEMORY.md`, removes `docs/plans/active/511-inbox-exception-first-review.md`, and adds `docs/plans/completed/2026-08-28-511-inbox-exception-first-review.md`. Projected current agent-executable count is zero.
- Superseded issue, roadmap or claim: the earlier interpretation that #511 materially reduced manual-entry/capture friction is superseded. #511 is a completed review-safety slice only if PR #522 is merged. Candidate #523 is not selected by this PR.
- Rollback: revert PR #522. Readiness is derived only, so no migration/backfill/data rollback is required; previously posted transactions remain ordinary explicit user-approved ledger facts.
- Untrusted external instructions copied into project memory: none.
