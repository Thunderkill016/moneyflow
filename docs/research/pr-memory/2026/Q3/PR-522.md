# PR #522 — exception-first Inbox grouped review

- Date: 2026-08-28
- Change class: Class 3 — changes which existing pending Inbox candidates may enter grouped ledger-posting UX, while leaving ledger/RPC/schema/RLS/provider contracts unchanged.
- Affected capability or project boundary: Inbox candidate review only. One derived deterministic readiness classifier separates `ready` from `needs_attention`; classification itself never writes a financial fact.
- Lifecycle impact: none while implementation/evaluation is still in progress; same-PR completion projection is required before owner handoff.
- Status impact: candidate until exact-head required checks, independent review, unchanged base/main and owner squash merge.
- Baseline: implementation branch started from exact merged authority `main@133fa462d3cd5f90b1f70cccb179547815c2ba2d` (PR #521).
- Research correction: the corpus supports capture/maintenance friction as the stronger product problem. #511/#522 is review-safety infrastructure, **not** the primary answer to manual-entry friction. Research PR #4 was corrected accordingly; follow-on acquisition work must use a separate evidence-backed authority slice.
- Changed: `src/lib/inbox/readiness.ts` owns one pure classifier and machine-readable attention reasons. Ready requires a pending income/expense candidate, non-low confidence, no duplicate/transfer signal, valid positive safe-integer VND amount/date, and explicit account/category evidence resolving exactly to current compatible options. It reuses `buildLedgerPost` as a final posting-invariant check and never treats `draftFromCandidate` first-option fallbacks as readiness proof.
- Changed UI: Inbox reports textual `Sẵn sàng` / `Cần xem lại` counts and row states, includes a one-action `Chọn Sẵn sàng` affordance, and keeps the existing explicit bulk confirmation. The bulk dialog and page handler both fail closed to the Ready subset; the page reclassifies from current detected facts immediately before posting.
- Keyboard safety: the old desktop `A` shortcut could reach grouped apply without opening the confirmation dialog. #522 removes `A` from the shortcut map and pins the absence in unit tests; grouped ledger writes still require visible confirmation.
- Preserved: manual single review, reject/category actions, duplicate/transfer review, candidate approval idempotency/recovery, demo/auth shared classifier semantics, VND integer rules, transfer neutrality and explicit confirmation before ledger writes.
- Explicit non-scope: no automatic approval/posting, no behavior learning, fuzzy/semantic matching expansion, transfer matching change, OCR/AI, bank/provider/native integration, schema/RLS/Auth/deployment or production/provider writes.
- Tests added: readiness unit coverage for valid high/medium candidates and every specified exclusion class; browser mixed-batch fixture with three Ready plus low-confidence/possible-duplicate/transfer exceptions, asserting only Ready candidates post and exceptions remain pending.
- Corrected interaction metric: the pre-#511 Inbox already had one-click `Chọn tất cả`. Therefore there is **no general 40% click-reduction claim**. The raw minimum grouped path remains three activations (`select → review → confirm`). #511 changes safety/decision burden by selecting only deterministic Ready rows and leaving attention-required rows pending. Any row-by-row safe-selection comparison is not presented as overall click reduction or manual-entry reduction.
- Verified: superseded head `412888c8fcd9d9267a873c3c2ef5e76c451f05f0` passed classify, static quality/lint/typecheck, production build, policy contracts, unit/static-RLS, aggregate verify, database/not-required, cross-device UI audit, CodeQL and secret-history scan. Browser smoke then exposed two test-evidence defects rather than a waived failure: invented demo account evidence in the new mixed-batch fixture and an ambiguous legacy `Xem lại` selector. Both are corrected on the current branch; exact-head browser acceptance remains pending until CI reruns after this documentation-contract repair.
- Browser diagnosis: the new fixture incorrectly used `Vietcombank` for supposed Ready rows although demo accounts are `MB Bank`, `Tiền mặt`, `MoMo`, `USD du lịch`; strict classifier correctly rejected them. The fixture now uses explicit demo account/category IDs. Existing Phase 8 Inbox safety test also used an ambiguous global `Xem lại` locator; it is now scoped to the bulk bar and asserts Ready/Attention fail-closed semantics.
- Local limitation: the execution sandbox cannot resolve `github.com`, so repository cloning/local npm execution was unavailable. No local test command is claimed; GitHub exact-head CI is the machine evidence.
- Remaining: latest exact-head policy/unit/static/build/browser/UI checks, CodeQL/Gitleaks, independent diff/spec review and lifecycle convergence must pass before owner handoff. A final branch mutation invalidates prior-head verification.
- Production/provider evidence: none; no production/provider action is expected or authorized.
- Snapshot update: not applicable while Status impact remains candidate; completing #511 requires same-PR board, current-memory and completed-packet projection before owner handoff.
- Superseded issue, roadmap or claim: the earlier interpretation that #511 materially reduced manual-entry/capture friction is superseded. #511 remains the current agent-executable safety slice until same-PR completion projection and owner squash merge activate the post-merge state.
- Rollback: revert PR #522. Readiness is derived only, so no migration/backfill/data rollback is required; previously posted transactions remain ordinary explicit user-approved ledger facts.
- Untrusted external instructions copied into project memory: none.
