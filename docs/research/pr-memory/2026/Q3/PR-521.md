# PR #521 — select #511 exception-first Inbox review

- Date: 2026-08-28
- Change class: Class 0 planning/authority change that selects a future Class 3 financial-posting-UX slice; no runtime behavior changes in this PR.
- Affected capability or project boundary: #432 P2 task selection, Inbox review specification and active packet registry.
- Lifecycle impact: selects current slice. If merged, #511 becomes the single `current agent-executable` slice; this PR does not implement or complete it.
- Status impact: candidate selection; runtime/financial truth remains unchanged until later implementation merges.
- Research: current MoneyFlow code/tests; merged community-corpus decision intelligence; candidate `moneyflow-research#4`; current official YNAB approval/matching and Actual Budget import documentation. Candidate/external research is evidence only and cannot widen #511.
- Changed: added `docs/plans/active/511-inbox-exception-first-review.md`; advanced the board baseline to exact merged `main@6298e6c52cfff6f3a972cf72cf79022e341ce638`; registered #511 as the sole current agent-executable candidate after owner merge. No runtime, schema, RLS, Auth, provider, deployment, native or AI file changed.
- Verified: issue #511 handoff requires planning before runtime; current `candidate-store.ts` confirms `needs_review` is only low-confidence; current `review.ts` confirms bulk eligibility is confidence-based while account/category resolution can fall back, and `buildLedgerPost` owns strict amount/date/account/category constraints. Packet therefore specifies one strict shared classifier that never uses fallback as Ready evidence. Exact-head repository CI remains pending.
- Remaining: GitHub exact-head knowledge/CI-policy checks and independent owner review; owner merge of this planning PR; only then may implementation start from fresh main with `plan:resolve` and `agent:doctor`. Runtime acceptance, mixed-batch E2E, interaction delta and Class 3 gates remain future implementation evidence.
- Production/provider evidence: none; not applicable to a planning-only PR and no production/provider write is authorized.
- Snapshot update: not applicable — current implementation/runtime truth has not changed, so `CURRENT_PROJECT_MEMORY.md` is intentionally untouched.
- Superseded issue, roadmap or claim: on this unmerged branch only, supersedes the board's “no agent-executable slice selected” projection with candidate #511 selection. It becomes execution authority only if the owner merges PR #521; #432 remains master authority and release gates stay independent.
