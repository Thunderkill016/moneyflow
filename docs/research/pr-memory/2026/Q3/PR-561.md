# PR #561 — recover stalled Turnstile script loads

- Date: 2026-09-10
- Status: implementation/evaluation and same-PR lifecycle closeout complete; Draft only until final exact-head governance checks are terminal green, then hand off Ready for explicit owner merge.
- Base: `main@60fdb1846e8cd8f9e6b48729ce1ff033778112e0` (owner-merged selector PR #558).
- Change class: Class 3 auth/security user-flow slice with bounded client runtime and browser-test changes; no database, provider-console, financial or production-data mutation.
- Affected capability or project boundary: email-auth CAPTCHA availability/recovery, Turnstile client lifecycle, browser acceptance, executable packet #557.
- Changed: adds a 15-second MoneyFlow UX watchdog for a top-level Turnstile script that remains unresolved; exposes a native full-page retry action; preserves the existing token gate; adds deterministic Playwright coverage for no-callback stall, stale-timer race safety and keyboard retry.
- Security decision: timeout/error clears any stale token and never synthesizes a token. `AuthForm.captchaBlocked` remains unchanged and stays the client submit gate for email-auth flows.
- Race decision: script state is mirrored synchronously before React state; watchdog failure runs only while the authoritative ref is still `loading`; a late Script error after `ready` is ignored rather than clobbering successful readiness.
- Recovery decision: full-page reload is the only recovery action, avoiding a second custom script-loader/widget lifecycle.
- Research limit: 15 seconds is a MoneyFlow UX deadline, not a Cloudflare SLA. No evidence supported CAPTCHA bypass, provider-key changes, Supabase Auth policy changes, redirect/rate-limit/WAF changes or schema work.
- Lifecycle impact: completes current slice #557 to null; active packet removed and canonical completed packet added in the same PR.

## Evaluation and evidence

Evaluator read #557, the active packet and exact PR diff rather than relying on implementer summary.

Result: **PASS**.

Accepted implementation head: `e4a5dfd49c9529ed19f1338a8a24630b57a2ebb8`.

- CI run #3475 / `34388492430`: terminal **success**.
- Browser smoke: terminal **success**; both `Expense and Auth CAPTCHA browser smoke` and `Authenticated ownership browser smoke` passed.
- Cross-device UI audit: production audit **success**; CI completed successfully after evidence upload/cleanup.
- CodeQL #2493 / `34388492453`: terminal **success**.
- Secret History #2493 / `34388492454`: terminal **success**.
- Production build, static quality, unit/static-RLS and policy-contract shards: **success**.
- Database mutation was outside scope; detailed DB checks were correctly not selected on the accepted implementation head.

An earlier browser run failed because generic `getByRole("status")` matched both the demo notice and CAPTCHA live status. Its runtime snapshots already showed the correct finite-failure and verified states. The test locator was narrowed to the CAPTCHA `small[role="status"][aria-live="polite"]` element without weakening assertions or changing production behavior; the next exact implementation head passed cleanly.

Evaluator answers:

1. No failure path enables email auth without a real token.
2. A stale watchdog cannot overwrite successful readiness.
3. Retry cannot duplicate a custom script/widget lifecycle because it reloads the page.
4. Failure copy exposes no provider secret/configuration.
5. Browser evidence holds the top-level Turnstile script request itself rather than merely triggering existing `onError`.
6. The deadline is explicitly represented as a product UX constant, not an unsupported provider SLA.
7. Diff remains bounded to auth Turnstile client resilience, styles/shared constant, affected browser tests and required lifecycle/memory artifacts.

## Lifecycle closeout

- `docs/plans/active/557-turnstile-load-recovery.md` removed from active plans.
- Canonical completed packet added at `docs/plans/completed/557-turnstile-load-recovery.md` as required by the lifecycle contract.
- `docs/plans/PLAN_AUTHORITY.json` now has `current: null`.
- `docs/research/CURRENT_PROJECT_MEMORY.md` reconciled to #557 completion and null executable authority.
- Parent #174 remains open and unselected; provider-console work is not implicitly authorized.
- Production/provider writes: none.
- Merge: still requires explicit owner authorization.

The first lifecycle-head CI correctly rejected a noncanonical `docs/plans/archived/` destination and a missing machine-readable `Lifecycle impact: completes current slice` declaration. The closeout was corrected to the repository contract rather than bypassing the gate.

Because lifecycle closeout changes the PR head after implementation evidence, the final corrected closeout head must itself pass the repository's exact-head governance checks before the PR is marked Ready.

## Focused source record

1. MoneyFlow `src/components/auth-turnstile.tsx` — bounded client watchdog/recovery implementation.
2. MoneyFlow `src/components/auth-form.tsx` — unchanged fail-closed `captchaBlocked` authority.
3. MoneyFlow `e2e/auth-captcha.spec.ts` — deterministic stalled-script, race-safety, retry and successful-token browser evidence.
4. MoneyFlow `src/lib/auth-captcha.ts` — shared 15-second MoneyFlow UX watchdog constant.
5. `docs/plans/completed/557-turnstile-load-recovery.md` — final evaluator and lifecycle record.
6. Cloudflare Turnstile client-side errors/widget configuration/challenge troubleshooting and Playwright Clock docs — external supporting evidence, not MoneyFlow authority.

## Permission boundary after closeout

#557 grants no further implementation permission. `PLAN_AUTHORITY.current` is null. PR #561 may only receive corrections necessary to make its own exact-head governance/evidence consistent; it may not select or perform provider-console work, another roadmap slice, schema/RLS changes, financial changes or production-data mutations.
