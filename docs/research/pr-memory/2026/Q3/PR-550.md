# PR #550 — select MON-62 source adapters and provenance-safe dedupe

- Date: 2026-09-05
- Status: evaluating
- Base: `main@06c3a0cc804c3764e0b7e1a94b6c556009b2b46e`
- Change class: Class 0 authority/planning change selecting a Class 3 acquisition/data-integrity packet; no runtime, schema, provider or production mutation in this PR.
- Affected capability or project boundary: executable plan authority, M1 Vietnam Acquisition Depth, source-adapter/provenance contract, current project memory.
- Status impact: if owner-merged, this PR changes repository executable authority from `current: null` to MON-62. It does not implement MON-62 and does not select MON-63.
- Changed: adds `docs/plans/active/mon-62-source-adapters-provenance-dedupe.md`, sets candidate `PLAN_AUTHORITY.current` to that packet with `selectedByPr: 550`, reconciles `CURRENT_PROJECT_MEMORY.md`, and records this PR memory.
- Verified: selector branch starts from exact fresh `main@06c3a0cc804c3764e0b7e1a94b6c556009b2b46e`; merged main authority was re-read as `current: null`. PR #542 had just merged on clean first-run CI #3353, CodeQL #2380 and Secret history #2380, including authenticated ownership browser smoke. The packet's repository reconnaissance was rechecked against current `provenance.ts`, `inbox-map.ts`, `client-inbox.ts`, `app/actions/inbox.ts` and `import-preview-page.tsx`.
- Repository reconnaissance: existing provenance types and `candidateProvenanceInsertPatch()` already support source lifecycle and predecessor evidence, but `prepareCandidateForServer`, the client facade, the server create schema and the generic preview projection currently drop some or all richer adapter evidence. The database already contains exact-source/lifecycle/replacement primitives, so MON-62 defaults to no new migration. Direct CSV remains a generic path and must not become a provider-adapter bypass.
- Research refresh: OFX 2.2 says FITID duplicate identity is account-scoped and not globally unique across financial institutions; UK Open Banking v4 describes optional transaction IDs unique/immutable within a servicing institution; Plaid Core Exchange uses persistent account-scoped IDs and separate pending/posted lineage; SheetJS documents Excel date serial/date-system/timezone interpretation limits.
- Research limits: those external contracts are architecture references only. They do not prove VCB/ACB/VietinBank consumer Excel headers, transaction IDs, identity scope, status, timezone or lifecycle semantics. Existing first-party bank evidence still supports Excel-family retrieval only at the scoped level already recorded by #523/MON-61.
- Architecture decision: identity scope is explicit source evidence. A canonical `sourceExternalId` may exist only when the source-stable transaction ID and every required namespace component are available deterministically and safely. Provider-only prefixing is insufficient for an account-scoped ID; mutable MoneyFlow account mapping, row number, filename, parser version, file-local sequence, generated hash and MoneyFlow fingerprint are forbidden identity substitutes.
- Financial/date decision: adapter mode must fail closed for missing or ambiguous required dates, amount direction and unsupported financial semantics. It must not inherit the generic parser's fallback-to-today behavior. Source lifecycle/predecessor evidence remains evidence and cannot silently overwrite ledger/reconciliation truth.
- Privacy decision: fixtures remain synthetic/structural only. No real bank statement, account number, transaction reference, bank credential, customer data or sensitive namespace material is committed without an explicitly safe representation contract.
- Measured: no runtime/product performance measurement is claimed by this selector. No production behavior change is claimed.
- Remaining: exact-head selector CI/policy, CodeQL and Secret-history gates; final mergeability/review/thread recheck; explicit owner merge. After merge, fresh `plan:resolve` and `agent:doctor` are required before any implementation branch work. Implementation must then prove replay, collision, strict-date, changed-observation, pending→posted predecessor and no-ID behavior.
- Production/provider evidence: none added by this PR. No Supabase DB/Auth/provider write, Vercel production mutation, external bank access or production financial-data write is authorized or performed.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` now separates current repository baseline `06c3a0cc...` from the last explicitly verified production runtime baseline `0bf9335c...`, records #550 as a candidate selector only, and preserves `main` authority truth as null until merge.
- Superseded issue, roadmap or claim: supersedes the stale memory claim that MON-62 has no selector candidate. It does not supersede #523/MON-61 history, does not promote MON-63, and does not claim target-bank auto-map/live sync.
- Lifecycle impact: authority transition from `PLAN_AUTHORITY.current: null` to the MON-62 packet if #550 is owner-merged; this Draft selector does not complete the slice. A later completing MON-62 implementation PR must archive the packet, return `current → null`, reconcile memory and leave MON-63 unselected.
- Production writes: none.
- Merge: explicit owner authorization required; this PR was created after prior legacy-PR merge authorization and must not be auto-merged from that earlier instruction.

## Focused source record

1. Financial Data Exchange / OFX 2.2 — `https://www.financialdataexchange.org/common/Uploaded%20files/OFX%20files/OFX%202.2.pdf` — account-scoped FITID duplicate identity; not proof of Vietnamese bank-export fields.
2. UK Open Banking v4 Transactions — `https://openbankinguk.github.io/read-write-api-site3/v4.0/resources-and-data-models/aisp/Transactions.html` — optional transaction ID uniqueness/immutability within servicing institution; architecture reference only.
3. Plaid Core Exchange 6.3 — `https://plaid.com/core-exchange/docs/reference/6.3/` — persistent account-scoped transaction identity and pending/posted linkage; provider API semantics only.
4. SheetJS Dates and Times — `https://docs.sheetjs.com/docs/csf/features/dates/` — spreadsheet date serial/date-system/timezone parsing limits; parsing mechanics only.

## Permission boundary

#550 is a selector only. It may change planning authority if owner-merged. It authorizes no runtime code, migration/RPC, Supabase Auth/provider mutation, bank credential flow, screen scraping, provider API integration, Vercel production mutation, production-data write or bank-specific auto-map claim. Implementation remains blocked until selector owner merge plus fresh repository authority resolution/doctor.
