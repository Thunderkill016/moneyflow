# PR #327 — Provider Sync production database alignment evidence

- Date: 2026-08-08
- Change class: Class 3 provider-evidence reconciliation; documentation only in this PR, with previously owner-approved production DB execution recorded as evidence
- Affected capability or project boundary: MoneyFlow Trust Provider Sync / Supabase production database alignment before the separate destructive Edge recent-auth rollout
- Status impact: production database truth advances from ten missing MoneyFlow migrations to the exact ten-file set aligned and structurally verified; Provider Sync remains active because `delete-account` Edge v5 and safe recent-auth provider acceptance are still pending
- Changed: updates the active Provider Sync packet and current project memory with the 2026-08-08 DB rollout method, exact-history preservation, post-write catalog/invariant/advisor evidence, accepted dry-run limitation, service-role legacy-default ACL finding, and the remaining Edge permission boundary
- Verified: all ten original MoneyFlow migration versions exist exactly once; seven legitimate Atoryn history rows remain; no temporary rollout version remains; 27 checked target indexes are valid/ready; 10 checked constraints are validated; affected baseline row counts and new-column non-null invariants are preserved; RLS/security-invoker/RPC boundaries and provider advisors/logs were inspected
- Remaining: actual linked-production CLI dry-run was not executed and remains an accepted limitation for the completed DB checkpoint; production `delete-account` remains v5; separate owner approval is required before Edge deployment; safe stale/fresh password + Google/OAuth acceptance and a post-Edge runtime observation window remain open; legacy broad `service_role` table ACL should be addressed only by a new reviewed hardening migration/spec
- Production/provider evidence: owner said “Go” after the DB boundary and free-dry-run limitation were explicit; PR #326 run `31259696558` selected exactly the ten MoneyFlow migrations in a local union-history Supabase CLI dry-run; production then received 10/10 exact source migrations from `main@8d0070b3d039fc80647e888aa1bd89f18b4de0b4`; exact remote history/catalog/invariant/advisor/log read-back completed; no Edge deployment and no destructive real-user deletion occurred
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` now records production DB alignment as current truth, removes the obsolete claim that ten MoneyFlow migrations remain missing, preserves the actual-linked-dry-run limitation, and keeps the stale Edge/provider-acceptance blocker active
- Superseded issue, roadmap or claim: supersedes the current-truth claim that production Supabase still lacks the ten reviewed review/reconciliation/rules/audit migrations; does not supersede the requirement for separate Edge approval or safe recent-auth provider acceptance

## Evidence boundary

The zero-cost PR #326 dry-run was a local union-history simulation using the Supabase CLI. It is intentionally **not** described as an actual linked-production dry-run. The owner chose to proceed with the production DB checkpoint after that distinction was explicit.

The Supabase Management/MCP migration endpoint generated current-time history versions during execution. Each source SQL migration was first applied successfully and tracked, then only that unique successful history row's `version` was guarded-normalized to the original repository timestamp. No unapplied or failed SQL was marked applied, and no shared Atoryn history was rewritten.

## Safety

- this PR changes documentation only;
- DB authorization from the owner's “Go” has been consumed/completed;
- current provider scope returns to read-only inspection;
- this PR does not authorize Edge deployment, new production DDL/ACL changes, provider configuration writes, production-data mutation or destructive account deletion;
- the `service_role` ACL finding is recorded for a future bounded migration rather than changed ad hoc.