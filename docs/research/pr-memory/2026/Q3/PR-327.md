# PR #327 — Provider Sync production database evidence

- Date: 2026-08-08
- Change class: Class 3 provider-evidence reconciliation; documentation only in this PR
- Affected boundary: MoneyFlow Trust / Supabase production database evidence before later provider-write checkpoints
- Status impact: the exact ten-file migration-history/schema drift is closed, but Provider Sync remains `evaluating` because effective audit-table `service_role` DML contradicts the merged pgTAP least-privilege contract and production `delete-account` remains v5
- Changed: synchronizes the active Provider Sync packet, MoneyFlow Trust parent and current project memory with the ten-file production execution, accepted dry-run limitation, post-write catalog/data evidence, the live audit ACL blocker and the remaining Edge boundary
- Verified: all ten original MoneyFlow migration versions exist exactly once; seven legitimate Atoryn history rows remain; no temporary rollout version remains; 27 checked target indexes are valid/ready; 10 checked constraints are validated; affected baseline counts/defaulted-column invariants are preserved; RLS/security-invoker boundaries and provider advisors/logs were inspected
- Blocking finding: merged `supabase/tests/database/financial_audit_atomicity_and_service_role.test.sql` requires `service_role` SELECT but denies INSERT/UPDATE/DELETE on `financial_mutation_audit_events`; live production effective privilege inspection shows those DML privileges are present, so audit/provider DB alignment must remain partial
- Remaining: actual linked-production CLI dry-run was not executed and remains an accepted limitation for the consumed ten-file DB checkpoint; audit least-privilege requires a new reviewed forward migration/spec + explicit owner provider-write approval; production `delete-account` remains v5; safe stale/fresh password + supported OAuth/Google acceptance and post-Edge observation remain open
- Production/provider evidence: owner said “Go” after the exact ten-file DB boundary and dry-run limitation were explicit; PR #326 run `31259696558` selected exactly ten MoneyFlow migrations in a local union-history Supabase CLI dry-run; production then received 10/10 reviewed source migrations from `main@8d0070b3d039fc80647e888aa1bd89f18b4de0b4`; exact remote history/catalog/data/advisor/log read-back completed; no Edge deployment or destructive real-user deletion occurred
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` removes the obsolete “ten migrations missing” claim but explicitly does **not** call the audit/database provider fully aligned
- Parent update: `docs/plans/active/public-beta-trust.md` now advances PS-T6/PS-T7 to complete, records the accepted linked-dry-run limitation, inserts the audit least-privilege forward-hardening gate, and keeps Edge/Recover blocked

## Evidence boundary

The zero-cost PR #326 dry-run was a local union-history simulation using the Supabase CLI. It is intentionally **not** described as an actual linked-production dry-run. The owner chose to proceed with the exact ten-file production DB checkpoint after that distinction was explicit.

The connected Supabase migration endpoint generated current-time history versions during execution. Each source SQL migration was first applied successfully and tracked; only that unique successful history row's `version` was then guarded-normalized to the original repository timestamp. No unapplied/failed SQL was marked applied and no shared Atoryn history was rewritten.

The historical ten migrations being present is not sufficient to claim full provider alignment. Repository tests outrank prose: the live audit-table effective privilege mismatch remains a blocker until corrected through a new reviewed forward migration.

## Review reconciliation

Review findings on #327 required:

1. do not call audit/provider DB fully aligned while live `service_role` DML contradicts pgTAP;
2. synchronize the active MoneyFlow Trust parent so later handoffs do not repeat the already-consumed ten-file rollout;
3. preserve required active-packet headings for `check:knowledge`;
4. use canonical delivery-state values.

The branch was updated to address all four before merge. Exact-head CI/CodeQL/Secret evidence must be re-read after the final review-fix head and before the owner-authorized merge.

## Safety

- this PR changes documentation only;
- the prior ten-file DB authorization has been consumed/completed;
- current provider scope is read-only inspection;
- this PR does not authorize an audit ACL migration, Edge deployment, provider configuration write, production-data mutation or destructive account deletion;
- the next production provider write requires a new explicit owner checkpoint.