# PR #350 — docs: record migration history alignment and close Mission 17B

- Date: 2026-08-12
- Change class: Class 0 (current-authority documentation and one code comment)
- Affected capability or project boundary: migration history truth and Mission 17B status
- Status impact: Mission 17B pending → complete; P2 Recover unchanged and still not accepted
- Changed: corrected `check:migrations` wording from "normalized content hash" to raw-byte SHA-256 in the script header and project memory; refreshed the stale audited-main SHA; recorded that the history repair for `20260715001400` and `20260715001500` is complete and touched history only; recorded the clean linked dry-run; marked Mission 17B complete in the Recover packet
- Verified: `supabase migration list --linked` full output inspected and parsed — 39 aligned, zero remote-only, exactly two local-only; `supabase db push --linked --dry-run` proposes exactly the two Recover migrations; `check:migrations`, `check:knowledge`, `test:ci-policy`, `typecheck`, `lint` and `check:architecture` pass
- Remaining: production application of the two Recover migrations, then hosted backup and restore acceptance and the P2 decision
- Production/provider evidence: **no production write was performed in this change**. The migration repair was completed earlier under separate owner approval; it changed the history table only and did not execute the migrations' SQL, with post-write checks confirming table counts and the live `create_split_expense`, `create_financial_account` and `transaction_feed` definitions unchanged
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` — hash wording, audited main SHA, history-alignment truth, dry-run result and Mission 17B completion
- Superseded issue, roadmap or claim: supersedes the wording that `check:migrations` hashes a normalized form, and the Recover packet's statement that the history repair was an outstanding checkpoint. Does **not** supersede PR #349's memory, which correctly recorded the repair as pending at that time

## Deployment detail recorded for the next checkpoint

The cleanup migration `20260812043219` carries a later timestamp than both Recover
migrations, so a plain `supabase db push` reports them as "to be inserted before
the last migration on remote database". The real deployment will need
`--include-all`.

## Not claimed

- The Recover migrations are not deployed.
- P2 Recover is not accepted; no hosted acceptance evidence exists.

## Related

- Contract packet: `docs/plans/active/moneyflow-trust-recover.md`
- Predecessor: `docs/research/pr-memory/2026/Q3/PR-349.md`
