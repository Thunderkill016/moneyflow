# PR #572 — select #570 production Supabase migration reconciliation

- Date: 2026-09-11
- Base: owner-merged `main@3c9794926effd3d0f0f0786ac286a8619d0518ca` after PR #571.
- Change class: Class 3 selector / authority transition; documentation only.
- Affected capability or project boundary: hosted Supabase migration lifecycle, production schema truth, import maintenance evidence, and #567 production security verification.
- Status impact: candidate selection of GitHub #570; no hosted migration is executable until this selector is explicitly owner-merged and fresh main resolves the packet as current.
- Changed: adds `docs/plans/active/570-production-migration-reconciliation.md`, projects `PLAN_AUTHORITY.current` to that packet through PR #572, refreshes project memory, and records the exact four-migration production gap.
- Verified: Draft-state exact head `da5a8265205a4e4bf4113bdfc5d2e192b9c17393` passed CI #3549, CodeQL #2554 and Secret History #2554, including fresh local reset/full pgTAP, archive round trips, static/unit/build, browser smoke and cross-device audit. Independent selector evaluation found the four-migration chain contiguous and the selector diff limited to planning/authority/project-memory artifacts.
- Ready-state correction: moving PR #572 to Ready triggered CI #3550, where lifecycle projection correctly failed because the packet recorded selector PR #572 inside `**Issue/PR:**`, causing the selector to be interpreted as owner of executable work. The packet now follows the canonical selector pattern used by #568: executable-work ownership remains pending in `**Issue/PR:**`, while selector identity is recorded separately in `**Selector:** GitHub PR #572`. Exact-head CI is required after this correction.
- Production/provider evidence: read-only hosted inspection after PR #571 merge shows migration history still ends at `20260825090000`; all four later repo migrations are absent; measurement objects are not live; no global postgres default-function ACL override exists; reconciliation snapshot remains SECURITY DEFINER. No production/provider write occurred.
- Research: current Supabase CLI/workflow guidance uses `migration list` for history reconciliation, `db push --dry-run` for preview, and migration-file `db push` for deployment; remote reset is destructive and forbidden for production. Existing timestamped files require a version-preserving deployment path rather than an execution primitive that synthesizes new migration versions.
- Safety: this PR contains no migration SQL change, runtime change, provider setting change, user-data write, migration-history repair or production DDL.
- Remaining: exact-head CI/CodeQL/Secret History after the Ready-state metadata/memory correction, then explicit owner merge, fresh authority resolution, and only then the bounded four-migration production preflight/rollout/verification described by #570.
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` is reconciled in this selector to post-#571 merged truth, Ready selector state, the #3550 lifecycle finding, and the candidate #570 authority state.
- Superseded issue, roadmap or claim: the previous two-migration wording for #570 is superseded; the current contiguous hosted gap is four migrations after PR #571.
- Lifecycle impact: authority transition from no current executable packet to candidate #570 through PR #572; merged `main` remains `current = null` until owner merge.

## Permission boundary

PR #572 may change only planning, authority and project-memory artifacts needed to select #570. It may not apply or alter database migrations, mutate production, repair migration history, change provider/Auth/WAF settings, touch user data or start UI work. Merge remains an explicit owner decision.
