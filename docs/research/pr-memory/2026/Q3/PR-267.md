# PR #267 — selective CI retry gates

- Date: 2026-08-04
- Change class: Class 2 CI workflow topology and repository verification contract
- Affected capability or project boundary: GitHub Actions verification, browser audit orchestration and stable required-check summaries
- Status impact: candidate — `main` and production remain unchanged until exact-head workflow evaluation and owner merge decision
- Changed: split the monolithic `verify` job into policy, static, test and build shards; retain `verify` as the stable required summary; split browser smoke and cross-device UI audit; retain `e2e` as the stable browser summary; make retry artifacts unique per run attempt; add a permanent retry-graph contract test
- Verified: authored workflow parses as YAML and the new Node retry-graph contract passes locally against the authored file; branch was reset and reapplied on current `main@cdbc0579`
- Remaining: run exact-head CI, CodeQL and secret-history scan; confirm stable required checks; perform one controlled same-SHA failed-job rerun and verify successful/skipped shards are not repeated; obtain owner merge decision
- Production/provider evidence: no product, financial, database, provider, deployment, production configuration or production-data write is part of this PR
- Snapshot update: not applicable — this PR changes candidate CI mechanics only and does not change current product truth
- Superseded issue, roadmap or claim: none
