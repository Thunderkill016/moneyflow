# PR #451 — feat(acquisition): persist PWA Share Target atomically

- Date: 2026-08-24
- Change class: Class 3 — financial acquisition and authenticated persistence boundary.
- Affected capability or project boundary: installed-PWA Share Target text/CSV ingestion, authenticated Inbox batch/candidate persistence, source provenance, tenant isolation and #432 P1 delivery lifecycle.
- Lifecycle impact: completes current slice — #451 archives #450 and leaves zero projected agent-executable product/governance slices in the same-PR completion projection.
- Status impact: candidate P1 acquisition hardening. If merged, one authenticated Share Target action persists its pending source evidence atomically without creating a ledger-write path.
- Changed: promotes #450 as the current child; adds an authenticated server action and a transactional ingestion RPC for prepared text/CSV batches and candidates; routes authenticated `/capture/share` through that one boundary while preserving browser-local demo behavior; adds database proof for atomic rollback, tenant isolation, server-owned provenance and no ledger writes.
- Verified: local `check:migrations`, `check:knowledge`, `check:rls`, `test:ci-policy`, lint (five pre-existing warnings), `typecheck`, and 1,106 unit tests pass. A demo-local explicit configuration passes the deployment contract and production build. Independent server/RPC/RLS review found no new blocker. Docker is unavailable locally, so pgTAP and browser/UI/e2e remain exact-head CI evidence. The prior non-draft CI run failed only because this mandatory PR-memory record was absent; its database/static/unit/build evidence is diagnostic until a new exact-head run completes.
- Remaining: run the non-draft exact-head Class 3 policy/static/unit/build/database/browser/UI/e2e, CodeQL and Secret History gates. Merge remains owner-authorized only.
- Production/provider evidence: not applicable. No provider connection, credential, deployment, production-data write or provider configuration is in scope.
- Snapshot update: board, completed packet and `CURRENT_PROJECT_MEMORY.md` carry the same PR #451 completion projection; it remains candidate evidence until the exact matching squash merge.
- Superseded issue, roadmap or claim: supersedes no shipped behavior. It replaces only the authenticated browser-orchestrated Share Target persistence sequence with one atomic source-ingestion boundary if merged.
- Rollback: revert #451. The existing Share Target bridge, parser and Inbox review remain available; authenticated persistence returns to the prior multi-mutation client orchestration.
- Untrusted external instructions copied into project memory: none.
