# PR #657 — Honest caps + forward actions in goals/commitments

- Date: 2026-09-22
- Change class: Class 2 (bounded UI flow; no schema/action change — `fundableIntoGoals` and `noticeAction` already existed)
- Affected capability or project boundary: `src/components/planning/goal-dialogs.tsx` (`GoalAllocationDialog` optional `fundable` cap), `src/components/planning/goals-page.tsx` (wire `fundableIntoGoals(reserve)`; `notify()` + `noticeAction` for the archive refusal), `src/components/planning/commitment-dialog.tsx` (paid-edit divergence warning), `src/lib/planning-pages.test.ts` (contract pins).
- Status impact: three audit findings closed — the allocation dialog previously let users enter amounts the `adjust_savings_goal` RPC was guaranteed to refuse (it enforces `balance − unpaid_bills`, not just the goal remainder); the archive refusal was a transient toast with no path forward; editing a paid commitment silently diverged the card amount from the posted ledger row.
- Changed:
  - Dialog cap is `min(remaining, fundable)`; context line states "hiện chỉ dành thêm được X sau khi trừ hoá đơn chưa trả" when the reserve is the tighter bound; validation distinguishes goal-remainder vs reserve rejections and names the figure.
  - Archive refusal toast carries "Giảm số đánh dấu" → opens the release dialog for that goal.
  - CommitmentDialog warns on editing a paid commitment that the posted expense is unchanged and to undo payment first.
- Verified: `npm test` 1486/1486; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:css-ownership, check:architecture, check:deployment-env, test:ci-policy. Browser (dev build, demo 390px): refusal toast → action → release dialog opens ("Đang đánh dấu 2.400.000 ₫"); allocate dialog unchanged when `fundable=null`.
- Remaining: auth-mode reserve cap exercised via contract-tested domain + code path only (`reserve=null` in demo by design); audit leftovers — `paid_at` evidence on paid cards, overdue-goal pace wording, `goalProgress` rounding edge.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
