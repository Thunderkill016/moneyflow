# PR #419 — test: add the #403 FCP attribution experiment

- Date: 2026-08-18
- Change class: Class 3 — `agent:doctor` reports `ci-security-policy, repository-governance` because a `.github/workflows/**` file is added, so a full packet and every gate are required even though no application code changes. Nothing financial, schema, RLS, Auth, provider or production is touched.
- Affected capability or project boundary: measurement instruments for canonical `/dashboard` load, plus one dispatch-only CI workflow. `src/app/dashboard/loading.tsx` is the subject under test and is **not** modified; the control arm removes it inside the runner's checkout and restores it. Routine CI cost is unchanged.
- Status impact: candidate. Owner-directed first step on #403 after PR #415 merged with no demonstrated improvement. **#403 stays open in `NOW`** and this branch neither claims nor closes it; RRB-08 is untouched.
- Changed: made the Lighthouse sample count and an arm label configurable (`PERF_SAMPLES` defaults to 3, `PERF_ARM` scopes output filenames and records `loadingBoundaryPresent`); added `e2e/auth/performance-attribution.mobile.auth.spec.ts`, a Playwright probe gated behind `PERF_ATTRIBUTION` that records per navigation whether the loading boundary's text rendered and when first paint occurred; added `scripts/perf-attribution-report.mjs`, which reports a mode split and exits non-zero when an arm's artifact is missing; added `.github/workflows/perf-attribution.yml`, `workflow_dispatch` only, running both arms on one runner with `cancel-in-progress: false`; added the required full packet `docs/plans/active/403-fcp-attribution.md` and registered it; recorded the method and its limits in `docs/performance-budgets.md`.
- Why two instruments: `npx lighthouse@13.4.1 --list-all-audits` returns 165 audits and **none names a paint node**, so Lighthouse can show the bimodal FCP split but can never say which element painted — more sampling cannot settle the question. And a feature flag cannot produce the control arm, because the Suspense boundary exists *because* `loading.tsx` exists; rendering `null` would leave the boundary in place and measure nothing. A real control needs a tree without the file, hence two production builds, hence a dispatch-only workflow rather than a per-PR cost.
- Verified: locally `check:migrations`, `check:knowledge`, `test:ci-policy`, `lint`, `typecheck`, `test` (1073 pass), `verify:prepush` including `build`, the active-packet registry validator, and `git diff --check`. Both attribution arms were executed end to end, plus a run confirming the probe skips without `PERF_ATTRIBUTION`. Dry run at n=6 per arm on a developer machine, unthrottled: with the boundary its text was seen in 1 of 6 navigations at 96 ms FCP against 196–220 ms on the five where it was not seen; without the boundary it was seen in 0 of 6 and never reached the low mode. That establishes the instrument works and the mechanism is visible; it is explicitly **not** the result, and its unthrottled milliseconds must never be quoted against the 1.8 s FCP target.
- Remaining: the CI-hardware run has not been collected — the owner triggers `Performance attribution (#403)` with `lighthouse_samples` and `navigations`, and only that result gets recorded as the finding. Per the owner's decision, a confirmed signal is recorded without any code change following from it; if the signal does not survive, #403 moves to the measured hydration/client-JS/main-thread bottleneck (766 → 814 ms JS bootup against 311.6 KB transferred script, server response only 5–15 ms). An earlier revision of this branch missed its own memory record and CI's project-knowledge contract correctly rejected it; the checker was not weakened.
- Production/provider evidence: not applicable. No provider write, deployment, production-data access or production verification. The workflow declares `permissions: contents: read`, adds no secret, writes nothing back to the repository, and reaches only the loopback Supabase double with synthetic fixtures. `test:db` is **not runnable in this environment** — no `supabase` CLI and no usable Docker — so it is recorded as a limitation rather than claimed; the diff contains no migration, SQL, RLS or schema change and CI runs the database shard on the exact head.
- Retry-pass disclosure, not treated as a clean pass: on head `2ad37a8f` the CodeQL workflow reached `success` only on **`run_attempt=2`**. Root cause was investigated rather than assumed. Both first-attempt failures — on `c7820541` and again six minutes later on `2ad37a8f` — failed at `Initialize CodeQL` with the identical message `Encountered an error while trying to determine feature enablement: HttpError: No server is currently available`, i.e. before CodeQL read any source, so the diff cannot be implicated. GitHub's own status page showed an open incident from 2026-08-17T13:40Z at `major` severity, and the same 503 blocked pull-request creation (three retries) and the rerun endpoint itself. No commit exists between the failing and passing attempts. The passing attempt did real work — `Initialize CodeQL: success` and `Analyze: success`, not a skip — which is what `AGENTS.md` requires of this provider gate. Recorded because a green reached by retry during a provider outage is weaker evidence about run conditions than a first-attempt green, even when it is valid evidence about the code.
- Snapshot update: not applicable while Status impact remains candidate. `CURRENT_PROJECT_MEMORY.md` should change only once the experiment produces a CI result that alters durable verification truth.
- Superseded issue, roadmap or claim: supersedes the open question left by PR #416, which named the attribution experiment as the next step without an instrument for it, and records that Lighthouse cannot supply that instrument. Supersedes nothing about #403 itself, which stays open with acceptance 3 unmet. Does not touch #417 (draft-to-ready CI false-green race) or #418 (Dependabot alerts held open by our own `postcss` override); both remain separate.
