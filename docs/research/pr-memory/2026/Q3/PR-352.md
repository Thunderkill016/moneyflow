# PR #352 — docs: record hosted backup acceptance for P2 Recover

- Date: 2026-08-12
- Change class: Class 0 (acceptance evidence for current authority docs)
- Affected capability or project boundary: MoneyFlow Trust P2 Recover hosted backup acceptance
- Status impact: hosted backup acceptance BLOCKED → PASS; P2 Recover remains OPEN pending hosted restore acceptance
- Changed: recorded hosted backup acceptance, the artifact identity note and the remaining restore gap in `docs/research/CURRENT_PROJECT_MEMORY.md` and the Recover packet
- Verified: the owner-downloaded production artifact's exact raw bytes passed the shipped `ingestArchiveBytes` (R8), which ran the shipped `validateMoneyFlowArchive` (R5) — R8 PASS, R5 PASS with zero contract violations, no pre-parse and no normalization; 19 dispositions (18 restorable + 1 history), 189 archived rows, no credential or tenant-authority key, profile carrying no source id; archive suites 180/180; `check:knowledge`, `check:migrations`, `check:architecture`, `test:ci-policy` pass
- Review findings: recorded in the PR
- Remaining: hosted restore acceptance under a separate approval with a designated bootstrap-only throwaway account, then the P2 decision
- Production/provider evidence: **no production write occurred**. No restore RPC was invoked, no ledger, Auth, provider, Edge, schema or migration-history mutation. The backup itself was produced by the owner through the hosted authenticated route
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` — hosted backup acceptance result, artifact identity note and the P2 phase row
- Superseded issue, roadmap or claim: supersedes the statement that hosted backup acceptance is blocked on an owner session. Does **not** supersede the statement that P2 Recover is unaccepted

## Artifact identity

The mission brief quoted an earlier download (`c339dc2b…`). The supplied file
hashed `f2fb8228…` at the identical 63216 bytes. Acceptance was **stopped** rather
than silently substituted, then diagnosed: `produced_at` was 2026-08-12T07:40:43Z,
after the R6 deployment and after the brief, and all seven row counts from the
brief's independent sanity check matched. Identical length with different bytes is
the expected signature of a second export, because `archive_id` is a fixed-width
uuid and `produced_at` a fixed-format timestamp. The owner then confirmed the
newer artifact supersedes.

## Privacy handling

The archive arrived inside the working tree and was moved out before anything was
staged, then handled only under a temp path outside the repository. It is not
committed, not a fixture, and never entered git history. No financial row content
appears in any evidence, PR text or log.

## Not claimed

- No browser automation was performed; the download was owner-driven.
- Hosted restore acceptance did not run.
- P2 Recover is not accepted.

## Related

- Contract packet: `docs/plans/active/moneyflow-trust-recover.md`
- Predecessor: `docs/research/pr-memory/2026/Q3/PR-351.md`
