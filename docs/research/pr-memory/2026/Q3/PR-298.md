# PR #298 — UI migration no-new-debt guardrails

- Date: 2026-08-05
- Change class: Class 1 repository policy/tooling change for a future cross-cutting interface migration
- Affected capability or project boundary: CI classification, UI/CSS ownership policy, token-reference validation, canonical route enforcement and legacy-class registration control
- Status impact: candidate only — `main`, production UI and current presentation behavior remain unchanged until owner-approved merge
- Selected direction: diff-based no-new-debt enforcement that checks added lines while allowing current legacy debt to migrate incrementally
- Changed: add `scripts/check-ui-migration-diff.mjs`; add fixture coverage; run the guardrail before CI classification; classify guardrail changes as full-risk; expose `npm run check:ui-migration`; document Phase 1 and defer Storybook until primitive APIs exist
- Blocking rules: no new route/global stylesheet, no new global CSS import or CSS import chain, no new unreviewed `!important`, no undefined CSS-token reference, no new `/insights` current reference, and no new known legacy global class registration
- Exception boundary: inline reason required for important, external runtime token or legacy-class exceptions; exceptions remain reviewable debt, not automatic approval
- Rejected alternatives: raising only the global `!important` budget; making current debt instantly blocking; installing Storybook before primitive ownership; adding a new runtime styling framework; modifying product CSS in this phase
- Product boundary: no component, route, layout, copy, identity, financial state or responsive behavior change
- Financial boundary: no amount, balance, transaction, transfer, budget, report, import/export or mutation behavior change
- Security and privacy boundary: no auth, RLS, provider, secret, user-data or hosted visual-review change
- Verified: classifier self-check, diff hygiene, static quality, unit/static RLS and production build were exercised on PR heads; exact-head policy/browser/UI-audit evidence must be green before owner acceptance, while the independently tracked all-ref Penpot finding remains outside this diff
- Remaining: owner reviews exact-head evidence and approves/revises Phase 2; no merge, deployment or primitive implementation is authorized automatically
- Production/provider evidence: none applicable; no runtime or provider write
- Snapshot update: not applicable; no rendered UI change
- Superseded issue, roadmap or claim: none — parent PR #296, Phase 0 PR #297, issue #72 and existing UI candidate PRs remain unchanged
