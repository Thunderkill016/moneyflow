# PR #462 — feat(agent): owner-opt-in exact-head delivery

- Date: 2026-08-25
- Change class: Class 3 — local delivery/security boundary; it changes no MoneyFlow product, schema, RLS, provider configuration or production data.
- Affected capability or project boundary: the local GitHub/Codex harness may optionally deliver only the PR created by one owner-authored run.
- Lifecycle impact: completes current slice through the same-PR post-merge projection; no agent-executable product/governance slice is selected after an exact matching squash merge.
- Status impact: `/agent codex --automerge <task>` is candidate behavior only. The guarded worker retains the existing draft-PR-only/credential-scrubbed command boundary. A host-only provider finds one PR on its isolated branch, marks it ready, waits required checks, rereads head/base/merge state and review threads, confirms remote `main` still matches the run base, then requests a direct squash merge with that observed head SHA. It never schedules GitHub auto-merge, retries an ambiguous state, bypasses repository protection/queue, creates approval/review, deploys or mutates provider/production data.
- Changed: owner opt-in parsing and durable identity; host delivery provider; append-only delivery journal event; focused runtime/provider regression tests; workflow/board/memory contracts.
- Verified: local lint; typecheck; migration identity; knowledge; agent harness 45/45; demo production build. Local full E2E began on an isolated port but was stopped inconclusive after unrelated auth/CAPTCHA specs; it is not claimed as browser acceptance. Docker and Gitleaks are unavailable locally. Exact-head provider checks must establish database/browser/secret-history evidence.
- Remaining: exact-head provider checks, review-thread inspection and mergeability are unverified until PR #462 is ready and GitHub completes them. A real auto-delivery run is not exercised against a live PR in this feature PR.
- Production/provider evidence: no deployment, provider configuration/data action, financial data action, branch protection or repository settings change occurred.
- Merge evidence: PR #462 is draft while its final projection is prepared. Mark it ready only after the final exact head is pushed; merge requires exact-head provider checks and fresh merge prechecks.
- Snapshot update: board, completed packet and project memory carry only the PR #462 post-merge projection.
- Superseded issue, roadmap or claim: replaces the prior blanket claim that the host cannot ever merge with a narrow owner-opt-in host-only delivery policy; child agents remain unable to merge.
- Rollback: revert PR #462. Existing non-auto command identities and draft-only worker behavior remain available; no product/provider/financial state requires repair.
- Untrusted external instructions copied into project memory: none.
