# PR #552 — provenance-safe source adapter foundation

- Date: 2026-09-06
- Change class: Class 3 — acquisition/data-integrity implementation under the selected MON-62 packet.
- Affected capability or project boundary: file-source adapter contracts, source identity canonicalization, import draft/preview provenance transport, Inbox candidate provenance persistence and reload; no ledger mutation authority expansion.
- Status impact: candidate implementation only while PR #552 remains open. Merged `PLAN_AUTHORITY.current` is MON-62 via PR #550; MON-63 remains unselected.
- Changed: adds a pure versioned source-adapter contract; explicit institution/account identity scoping; non-truncating fail-closed source identity persistence; strict adapter-only date and VND amount/direction normalization; evidence-aware draft/preview projection; lifecycle/predecessor preservation through client/server insert and reload; focused and cross-boundary counterexample tests. Generic CSV/XLSX fallback behavior and Direct CSV ownership remain unchanged. VCB/ACB/VietinBank bank-specific auto-map remains disabled.
- Verified: implementation branch started from exact `main@388549f99a288d99249e26f4116539e6705cb3ff` with no base drift at draft-PR creation; merged manifest/Git history make MON-62 active via PR #550; existing database migrations already provide source lifecycle/predecessor persistence primitives so this implementation adds no migration. Focused external research was refreshed against official Plaid Core Exchange and SheetJS date-system documentation; those sources support account-scoped identity, separate pending/posted lineage, explicit 1900/1904 spreadsheet date handling and fail-closed ambiguity, but do not prove Vietnamese bank export layouts or stable IDs.
- Remaining: run exact-head CI-selected static/unit/build/browser/security gates; inspect any first-pass failures and fix on a new head rather than accepting retry-only success; independently evaluate the final diff against every MON-62 counterexample; then perform same-PR lifecycle convergence by archiving the active MON-62 packet, setting `PLAN_AUTHORITY.current` to `null`, reconciling `CURRENT_PROJECT_MEMORY.md`, and leaving MON-63 unselected before owner handoff.
- Production/provider evidence: none required or claimed. No production database, Auth, provider, bank, customer-data or deployment write is performed by this PR.
- Snapshot update: required before completion because current authority/implementation truth will change when MON-62 closes. Current main memory still contains stale pre-merge #550 wording; this PR must reconcile it during lifecycle closeout.
- Superseded issue, roadmap or claim: no follow-on roadmap item is promoted. This PR specifically supersedes the old assumptions that a raw provider-stable reference can be persisted without namespace evidence, that source IDs may be safely truncated to the database length bound, or that lifecycle/predecessor evidence can be dropped between preview and reload.
- Lifecycle impact: completes MON-62 only after independent evaluation, clean exact-head acceptance and same-PR convergence. Until then the selected packet remains active and this PR remains draft/candidate evidence.
- Production writes: none.
