# PR #696 — Point the delete-account door at the restorable backup

- Date: 2026-09-23
- Change class: Class 1 (single-component copy/link correction + contract-test pin update; no migration, schema, RPC or provider change)
- Affected capability or project boundary: `src/components/delete-account-page.tsx` pre-delete guidance; pinned by `src/lib/settings-trust-copy.test.ts`.
- Status impact: the account-deletion page now sends users to `/settings/backup` — the full, versioned, restorable archive flow — instead of `/settings/export`, which produces a partial transactions/Inbox file that cannot rebuild an account.
- Changed: the header description now instructs users to download the full restorable backup before continuing and states that the data export only covers transactions + Inbox and cannot rebuild the account. The action button moved from `href="/settings/export"` ("Xuất dữ liệu") to `href="/settings/backup"` ("Sao lưu đầy đủ"). The trust-copy contract test now pins the backup href.
- Verified: `settings-trust-copy.test.ts` 6/6 pass; `npm run typecheck` clean; `npm run lint` clean (1 pre-existing warning in `quick-add-defaults.test.ts`, untouched); `check:knowledge`; `test:ci-policy` 191/191. Browser smoke and visual audit delegated to CI lanes.
- Remaining: the settings hub still lists the partial export separately under its own honest label ("chưa phải bản sao lưu đầy đủ") — unchanged and correct there. Hosted/managed-Supabase restore rehearsal remains an owner-authorized operations task.
- Production/provider evidence: none (copy/link only).
- Superseded issue, roadmap or claim: the pre-#694 copy claimed the export was the thing to keep before deletion; that framing is replaced by backup-first guidance.
- Lifecycle impact: if `/settings/backup` is ever renamed or split, update this link and the contract-test pin together.
