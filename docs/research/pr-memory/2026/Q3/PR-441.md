# PR #441 — feat(acquisition): preserve changed source observations

- Date: 2026-08-21
- Change class: Class 3 — financial import/provenance boundary.
- Affected capability or project boundary: #432 P1 Acquisition Foundation; live stable-source-ID observations whose source evidence changes after the canonical transaction already exists.
- Status impact: candidate until merge. If accepted, authenticated Inbox planning distinguishes unchanged same-ID replay from changed/unknown same-ID evidence and lets a user explicitly preserve the later observation without overwriting the ledger. No provider/native capability, deployment state or public-beta gate changes.
- Changed: closed/archived #438 lifecycle; promoted #440 packet/board authority; reconciled current project memory to merged #439; reused `inbox_candidates` as durable observation history; added `source_external_id_changed` planning, reviewed observation-only linkage RPC, approved-candidate evidence immutability with batch-FK cleanup compatibility, browser-role candidate DELETE removal, exact-source server override hardening, server action, Inbox **Ghi nhận cập nhật nguồn** review path, migration identity and pgTAP/unit/static/UI counterexamples.
- Design evidence: Plaid official transaction-state/sync documentation shows transactions can be added/modified/removed and pending→posted can use a new ID; Actual Budget official API separates raw add from reconciliation/import and reports updated transactions. These sources justify explicit update lifecycle but do not select a MoneyFlow provider or grant imported-data precedence over user corrections. PostgreSQL official trigger documentation confirms `ON DELETE SET NULL` referential actions run as ordinary updates and fire referencing-table triggers, so the evidence guard deliberately allows only `import_batch_id` non-null→null cleanup plus trigger-managed `updated_at`.
- Verified so far: source-observation pgTAP counterexamples were written before the migration; static review found and fixed the pgTAP declared plan (23→24 assertions), a server-side escape where `p_allow_heuristic_duplicate=true` could otherwise bypass some hard exact-source decisions, and a UI stale-error path where clearing a failed hard server plan could expose ordinary approval. Migration identity is pinned from exact raw bytes: Git blob verification matched the no-trailing-newline file and SHA-256 prefix `7d83da3f844e97cd28a77b3beae16518`. Full Class 3 CI is not yet claimed.
- Remaining: complete draft checks, independently inspect the PR diff, mark ready only after candidate blockers are fixed, then require a post-ready synchronize head with real policy/knowledge, migration identity, lint/typecheck/build, unit/static RLS, fresh Supabase + pgTAP, archive/batch compatibility, browser/auth ownership/e2e, CodeQL and secret-history checks green. Update this record with exact-head evidence before owner merge handoff.
- Production/provider evidence: not applicable. This PR performs no provider write, deployment, production-data access or physical-device claim.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records #439 as merged current truth and #440 as active candidate work; it must not claim #440 runtime behavior as shipped until merge.
- Superseded issue, roadmap or claim: #438/#439 are completed input. If merged, #441 supersedes the claim that a live same stable source ID is always an undifferentiated duplicate. Different-ID pending→posted/provider predecessor identity and any source-to-ledger mutation policy remain explicitly deferred.
- Rollback: revert PR #441. Merged #435/#437/#439 acquisition semantics remain intact.
- Untrusted external instructions copied into project memory: none.
