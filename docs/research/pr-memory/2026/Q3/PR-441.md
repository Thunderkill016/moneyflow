# PR #441 — feat(acquisition): preserve changed source observations

- Date: 2026-08-21
- Change class: Class 3 — financial import/provenance boundary.
- Affected capability or project boundary: #432 P1 Acquisition Foundation; live stable-source-ID observations whose source evidence changes after the canonical transaction already exists.
- Status impact: candidate until merge. If accepted, authenticated Inbox planning distinguishes unchanged same-ID replay from changed/unknown same-ID evidence and lets a user explicitly preserve the later observation without overwriting the ledger. No provider/native capability, deployment state or public-beta gate changes.
- Changed: closed/archived #438 lifecycle; promoted #440 packet/board authority; reconciled current project memory to merged #439; reused `inbox_candidates` as durable observation history; added `source_external_id_changed` planning, reviewed observation-only linkage RPC, approved-candidate evidence immutability with batch-FK/archive-restore compatibility, browser-role candidate DELETE removal, browser fake-approval protection for both direct UPDATE and direct INSERT, exact-source server override hardening, server action, Inbox **Ghi nhận cập nhật nguồn** review path, stale-hard-plan safety, migration identity and pgTAP/unit/static/UI counterexamples.
- Design evidence: Plaid official transaction-state/sync documentation shows transactions can be added/modified/removed and pending→posted can use a new ID; Actual Budget official API separates raw add from reconciliation/import and reports updated transactions. These sources justify explicit update lifecycle but do not select a MoneyFlow provider or grant imported-data precedence over user corrections. PostgreSQL official trigger documentation confirms `ON DELETE SET NULL` referential actions run as ordinary updates and fire referencing-table triggers; official SECURITY DEFINER/current-user behavior supports a narrow archive-reconstruction exception tied to the actual `restore_user_archive(jsonb)` function owner rather than any broad privileged role.
- Pre-ready evaluation: source-observation pgTAP counterexamples were written before implementation. Static review found and fixed (1) a declared pgTAP plan mismatch, 23→24 assertions; (2) direct RPC escape via `p_allow_heuristic_duplicate=true` for hard exact-source decisions; (3) a UI stale-error path where clearing a failed hard server plan could expose ordinary approval; (4) initial approved-evidence immutability blocking archive restore's phase-two `transfer_pair_id` repair; and (5) browser-role ability to fabricate an `approved` transition through direct candidate UPDATE. A post-ready evaluator then found (6) the corresponding direct-INSERT path: authenticated still legitimately has candidate INSERT permission, so an `UPDATE`-only guard allowed a caller to insert an already-`approved` owned row. The guard now fires on INSERT+UPDATE and rejects browser-created approved rows while preserving pending/rejected ingestion and the SECURITY DEFINER archive path. The compatibility pgTAP suite now has seven assertions including the direct-INSERT counterexample.
- Migration identity: primary #440 migration `20260821184500_source_observation_precedence.sql` remains pinned from exact no-trailing-newline raw bytes at SHA-256 prefix `7d83da3f844e97cd28a77b3beae16518`. Review-fix migration `20260821190000_source_observation_guard_compat.sql` is now pinned from exact raw bytes at `1d073507d7d0963aed7c1be5acd6ea1f` after the INSERT+UPDATE guard hardening.
- Draft verification: CI #2772 ran classify only; unit/build/policy/database/browser/e2e jobs were skipped because PR #441 was draft. It is explicitly **not** Class 3 verification evidence.
- Ready transition: PR #441 became non-draft on head `188d6dd2f435de0bb24e158bb3d39400e82c84db`. That transition head is also not accepted as final evidence.
- Verified: CI #2786, CodeQL #1845 and Secret history #1845 all passed on head `577370b3f4325462d2afeb54a865646ac86f496c`, including real policy/knowledge, build/static/unit, fresh Supabase + pgTAP/database, archive restore, browser/auth ownership and cross-device UI shards. That evidence was later invalidated as final acceptance when the post-ready evaluator found the direct approved-INSERT bypass and the branch changed to fix it. The old green run remains historical evidence only; exact-head verification after the guard/test/migration-identity fix is pending.
- Remaining: require real policy/knowledge, migration identity, lint/typecheck/build, unit/static RLS, fresh Supabase + pgTAP, archive/batch compatibility, browser/auth ownership/e2e, CodeQL and secret-history checks green on the final post-fix head. Independently inspect exact-head diff/logs before owner merge handoff; no older green head may substitute.
- Production/provider evidence: not applicable. This PR performs no provider write, deployment, production-data access or physical-device claim.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records #439 as merged current truth and #440 as active candidate work; it must not claim #440 runtime behavior as shipped until merge.
- Superseded issue, roadmap or claim: #438/#439 are completed input. If merged, #441 supersedes the claim that a live same stable source ID is always an undifferentiated duplicate. Different-ID pending→posted/provider predecessor identity and any source-to-ledger mutation policy remain explicitly deferred.
- Rollback: revert PR #441. Merged #435/#437/#439 acquisition semantics remain intact.
- Untrusted external instructions copied into project memory: none.
