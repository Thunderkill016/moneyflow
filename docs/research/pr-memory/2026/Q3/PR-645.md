# PR #645 — Budget pace line on budget cards (P1)

- Date: 2026-09-21
- Change class: Class 2 (bounded UI on one surface; descriptive text only)
- Affected capability or project boundary: `src/lib/planning/budgets.ts`, `src/server/budgets.ts`, `src/components/planning/budgets-page.tsx`, `src/lib/planning/budgets.test.ts`, `src/server/capabilities/capability-test-helpers.test.ts` (fixture gained `today`).
- Status impact: P1 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — implemented.
- Changed: budget cards show "Đã dùng X% hạn mức · tháng đã qua Y%" under the progress bar. `monthElapsedPercent(monthStart, today)` returns the calendar share of the viewed month and null when `today` falls outside it, so finished months (100%) and future months (0%) stay silent instead of printing a tautology. `budgetPaceLine` reuses `budgetProgress` for the usage side. `BudgetsWorkspace.today` is server-resolved (`todayInVietnam`) inside `workspaceMetadata`, so every return path (auth/demo/error) shares it and a client clock cannot skew elapsed time. Descriptive only — no verdict, no safe-to-spend, no advice (still banned per #635).
- Verified: TDD — 4 new tests pin first-day 3%, mid-month 50%, leap-Feb last day 100%, out-of-month null (past + future); `npm test` 1456/1456; lint/typecheck/build/css-ownership/architecture/deployment-env/knowledge/ci-policy all pass. Browser-verified demo `/budgets` at 390px and 1366px: three cards show pace lines (21/30 → 70%), `?month=2026-08` renders zero, no `data-money` overflow.
- Remaining: exact-head CI; owner merge decision.
- Production/provider evidence: none.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: completes P1; next in plan order R1/R2, then S1, then X1/X2.
