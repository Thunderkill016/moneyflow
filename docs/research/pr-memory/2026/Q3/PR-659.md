# PR #659 — Stop inventing pace for overdue goals + honest 100%

- Date: 2026-09-22
- Change class: Class 1 (bounded domain + card copy; integer VND untouched)
- Affected capability or project boundary: `src/lib/planning/goals.ts` (`goalProgress` floor, `dailyGoalSaving` overdue → 0, new `goalIsOverdue`), `src/lib/planning/goals.test.ts`, `src/components/planning/goals-page.tsx` (overdue context copy, summary meta), `src/components/planning/commitments-page.tsx` (filter-empty label truthfulness + "Xem tất cả" secondary).
- Status impact: final two honesty findings from the 2026-09-22 planning audit. `Math.round` let 99.6% claim "100% hoàn thành" and disabled the allocate action while "Còn thiếu" was still positive; `daysUntil`'s ≥1 clamp turned any overdue goal's whole remainder into a fake "per day" pace and inflated the summary figure.
- Changed:
  - `goalProgress` floors (allocated ≥ target still yields exactly 100); card can no longer disable "Đánh dấu thêm" on a goal that is not actually funded.
  - `dailyGoalSaving` returns 0 once the deadline passed; new `goalIsOverdue` (not archived, has deadline, remaining > 0). Card context: pace → achieved → "Đã quá hạn — còn thiếu X" → no-deadline fallback.
  - Commitments filtered-empty: button label is "Thêm khoản định kỳ" unless truly the first; secondary "Xem tất cả" resets the filter when items exist outside it.
- Verified: `npm test` 1488/1488 (new pins: floor 99.67%→99, overdue→0, overdue edges); lint (1 pre-existing warning), typecheck, build, check:knowledge, check:architecture, check:css-ownership, check:deployment-env, test:ci-policy. Browser (dev build, demo 390px): created overdue goal renders "Quá hạn 52 ngày" + "Đã quá hạn — còn thiếu 1.000.000 ₫."; in-window goals keep their per-day pace.
- Remaining: audit is now exhausted for /goals + /commitments except cosmetic items (identical `commitmentDueLabel` branches, dead `safe-ux-planning.css` selectors — pinned, leave).
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
