# PR #631 — RRB-08 self-serve phone-smoke evidence page

- Date: 2026-09-21
- Change class: Class 1 (additive static tool + docs pointer; no runtime product path touched)
- Affected capability or project boundary: `public/rrb-08.html` (new), `src/lib/rrb-08-tool-contract.test.ts` (new), `docs/plans/active/rrb-08-physical-device-proof.md` (Next action pointer).
- Status impact: removes friction from the only remaining RRB-08 gap — owner-observed physical-device evidence. The 2026-08-27 observation left OS/browser/network/commit unstated; the tool auto-fills all of them on the phone under test (UA + Client Hints + `navigator.connection` + same-origin `/api/health` commit).
- Changed: a self-contained Vietnamese evidence form at `/rrb-08.html` walks checkpoints T1–T6 with PASS/FAIL/N-A + notes, carries the P0 stop-condition list, and emits the runbook's exact evidence template for pasting into #398. No external requests beyond same-origin `/api/health`; no analytics; `noindex`; unlinked.
- Verified: contract test 3/3 (six checkpoints, full template fields, noindex, no exfiltration); Playwright render check on 390px viewport including report generation and graceful fallback without `/api/health`; lint/typecheck/knowledge/architecture/diff-hygiene clean.
- Remaining: owner runs the smoke on a real phone after deploy and pastes the record into #398; iOS/Safari observation still the highest-value missing cell.
- Production/provider evidence: page is static; `/api/health` same-origin fetch verified against the route source (`build`, `commit` fields), not yet exercised on production.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: adds a public static file served with the app; no schema, contract, CSS-ownership or capability change.

## Boundaries

- Tool produces evidence only; it cannot mark RRB-08 complete — that verdict remains the owner's.
- Not linked from product navigation; direct URL only.

## Evidence limits

- Autodetect verified in headless Chromium; real-device UA strings (especially Android model via Client Hints, iOS version from UA) are covered by parse paths but not yet observed on physical hardware.
