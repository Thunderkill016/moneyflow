# PR #371 — docs: reconcile lifecycle after the UI refresh Slice 1 merge

- Date: 2026-08-14
- Change class: Class 0 (lifecycle reconciliation only; no runtime, UI, CSS or test change)
- Affected capability or project boundary: packet lifecycle and current-status authority after PR #370 merged
- Status impact: UI evolutionary refresh Slice 1 moves from **active** to **accepted/completed and archived**; the active registry returns to the Trust parent alone. Phase E exploration stays paused with every territory rejected and none selected; Phase F not started; PBT-AC15 open
- Changed: archived the Slice 1 packet to `docs/plans/completed/2026-08-14-current-ui-evolutionary-refresh-slice-1.md` with its final record; removed it from the active registry; updated current memory and its superseded-status register; added merged status and the merge SHA to the PR-370 record
- Verified: `agent:doctor` Class 0, `check:knowledge`, `check:migrations`, `test:ci-policy` 119/119, registry guard, `git diff --check`
- Review findings: CI rejected the first push because this PR had no own memory record; this file is that fix
- Remaining: nothing for Slice 1. A later UI slice needs its own packet and authorisation
- Production/provider evidence: none, and none claimed. No provider, production, database, Auth, CI, Design Harness or runtime change
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` — baseline moved to `75ade3bc4ce9d17a099a38df704c8c82542447ee`, Slice 1 recorded as merged and accepted rather than active, and two stale register entries corrected
- Superseded issue, roadmap or claim: supersedes "the UI evolutionary refresh is active" and "Phase E Creative Territories is the immediate next phase". Does **not** supersede PR-370's historical findings, which are left exactly as written

## Final record carried into the archive

Owner-accepted for Slice 1; merged as PR #370 at
`main@75ade3bc4ce9d17a099a38df704c8c82542447ee`; materially better than the previous main:
yes; identity preserved: yes; no further redesign of Slice 1; the desktop-filtered +36px
vertical regression is known and accepted; **no physical-device validation was claimed**.

## The guard that mattered here

The squash-merge commit body carries intermediate metric claims from earlier commits on
the branch — including my own selectively quoted figures, which a later commit corrected.
Both the archived packet and the PR-370 record now state that explicitly, so the only
numbers that read as current authority are the four-state table measured on the final
implementation head:

| Viewport and state | previous main | shipped | delta |
|---|---|---|---|
| mobile 390 — no secondary filter | 791px | 271px | −520px |
| mobile 390 — secondary filter open | 851px | 641px | −210px |
| desktop 1440 — no secondary filter | 273px | 155px | −118px |
| desktop 1440 — secondary filter open | 333px | 369px | **+36px** |

Repository docs outrank historical commit-message prose; this record exists so a future
reader does not have to reconstruct that from the merge commit.

## Not claimed

- No physical-device validation for Slice 1.
- No Slice 2, no Phase E restart, no Phase F.
- Memory remains one line over its soft compaction threshold — 186 on main, 185 here — so
  that is pre-existing debt for a separate compaction task, not something this PR added.

## Related

- Archived packet: `docs/plans/completed/2026-08-14-current-ui-evolutionary-refresh-slice-1.md`
- Implementation record: `docs/research/pr-memory/2026/Q3/PR-370.md`
