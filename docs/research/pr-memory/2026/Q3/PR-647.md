# PR #647 — S1 backup reminder chip + bundle range-bound fix

- Date: 2026-09-21
- Change class: Class 3 (schema migration + SECURITY INVOKER RPC replace + server-action write to user-owned data)
- Affected capability or project boundary: `supabase/migrations/20260921120000_backup_reminder_state.sql` (new `profiles.last_backup_at`, `get_dashboard_bundle` `create or replace`), `src/app/actions/archive.ts`, `src/server/dashboard.ts`, `src/lib/attention.ts`, `src/app/dashboard/page.tsx`, `src/components/moneyflow-dashboard.tsx`, new pgTAP `backup_reminder_state.test.sql`, updated `dashboard_read_bundle.test.sql`. Work packet: `docs/plans/active/backup-reminder-chip.md`.
- Status impact: S1 of `ALL_SURFACES_UPGRADE_PLAN_2026.md` — implemented, pending CI database lane + owner review.
- Changed:
  - Home attention strip gains a calm chip: `Chưa có bản sao lưu nào` (never backed up, account > 30 days) or `Bản sao lưu gần nhất đã N ngày trước`; links to `/settings/backup`; `BACKUP_REMINDER_DAYS = 30`; appended before the existing 4-item cap; demo/fallback/skew `backupState: null` ⇒ silent.
  - `createArchiveBackupAction` stamps `profiles.last_backup_at` only after the archive validates; stamp failure logs and still returns the archive. `export_user_archive` stays read-only (pgTAP-proven invariant).
  - Bundle gains additive `backup_state { last_backup_at::date, created_at::date }` under the `ledger_trust` skew contract (`nullish` read; `::date` emission avoids the DateStyle JSON hazard).
  - **Bound fix (latent #643 defect):** bundle `p_transaction_start` bound widened 45→62. The app's month-to-date compare legitimately sends up to 2d−1 = 61 days back (day 31); from the 24th of each month the old bound threw `invalid_dashboard_transaction_range` and silently degraded every authenticated render to the multi-query fallback — which would also have hidden this chip. pgTAP now pins both edges (`-62` accepted, `-63` rejected).
- Verified: TDD — attention tests RED→GREEN (10/10 incl. never-backed-up, boundary days, demo-null silence); `npm test` 1465/1465; lint/typecheck/build/css-ownership/architecture/deployment-env/knowledge/ci-policy pass. Demo render confirmed chip absent (backupState null). Migration diffed against reviewed RPC body — additive `backup_state` + bound comment only.
- Remaining: pgTAP **not executed locally** (no Docker/Podman — `agent:doctor` reports `ready:false` on docker capability); CI `database` lane is the first executor and must pass on exact head. Browser smoke + UI audit lanes pending. Owner review + merge decision required (Class 3). Post-merge owner verification: an auth backup stamps `last_backup_at` and the chip clears on next Home render.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: implements S1. Next in plan order: X1/X2.
