# PR #656 — Honest error state on /goals + prerequisite guidance on /commitments

- Date: 2026-09-22
- Change class: Class 1 (bounded conditional-render/copy fixes; shared `EmptyState` contract preserved — one primary action per region)
- Affected capability or project boundary: `src/components/planning/goals-page.tsx`, `src/components/planning/commitments-page.tsx`, `src/lib/planning-pages.test.ts`.
- Status impact: fixes two dead-ends from the planning-surface audit (explore-subagent, 2026-09-22). `/goals` accepted `dataError` but never rendered it — a failed load showed "Chưa có mục tiêu tiết kiệm" with no action and no explanation. `/commitments` with zero accounts or zero active categories disabled its only action while the empty copy still invited adding an item.
- Changed:
  - `/goals` now mirrors `/commitments`: error `Alert` + dedicated `EmptyState` ("Không tải được mục tiêu" → "Về Tổng quan"); summary and section render only when the workspace loaded.
  - `/commitments` empty state names the missing prerequisite and routes to `/accounts` ("Tạo tài khoản") or `/categories` ("Tạo danh mục") instead of a dead end.
  - `planning-pages.test.ts` pins both behaviors so the dead-ends cannot silently return.
- Verified: `npm test` 1487/1487; lint (1 pre-existing warning), typecheck, build, check:knowledge, check:css-ownership, check:architecture, check:deployment-env, test:ci-policy. The error/prerequisite branches are auth-mode-only — demo seeds accounts and a workspace, so the branches are covered by contract tests and code review rather than browser evidence (same constraint as PR #655).
- Remaining: audit findings deliberately not bundled — `fundableIntoGoals` cap on the allocation dialog (the contract-tested RPC boundary that UI never consults — shipped separately as PR #657), paid-commitment edit divergence notice (PR #657), `paid_at` evidence on paid cards (PR #658), overdue-goal pace wording.
- Production/provider evidence: none — not deployed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: continues upgrade-in-place direction; opened during an unattended-overnight work session — merge awaits owner approval.
