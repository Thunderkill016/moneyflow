# PR #558 — select #557 Turnstile load recovery

- Date: 2026-09-09
- Status: selector candidate; executable only after owner merge
- Base: `main@ad0461514acaec1c9a8a100ba92592c83ece46b2`
- Change class: Class 0 authority/planning change selecting a Class 3 auth/security user-flow packet; no runtime, schema, provider or production mutation in this PR.
- Affected capability or project boundary: executable plan authority, public-beta auth resilience, CAPTCHA failure UX, current project memory.
- Status impact: authority transition candidate from `PLAN_AUTHORITY.current: null` to bounded issue #557 through `selectedByPr: 558`; runtime implementation remains blocked before owner merge plus fresh authority/doctor resolution.
- Changed: adds the #557 packet, projects `PLAN_AUTHORITY.current` to it, reconciles current project memory and records selector provenance/evaluation.
- Verified: selector starts from exact post-MON-63 `main@ad0461514acaec1c9a8a100ba92592c83ece46b2`; #556 is merged and production READY with `/api/health` 200 for that exact commit; merged-main authority before this selector is `current: null`.
- Repository reconnaissance: `AuthForm` already keeps CAPTCHA-enabled email auth fail-closed whenever config is not ready or the token is empty. `AuthTurnstile` handles Script `onReady`/`onError` and widget callback/expired/error callbacks but has no app-side deadline for a top-level script that never becomes ready. Existing `e2e/auth-captcha.spec.ts` covers only successful token acquisition and phone-width containment.
- Queue correction 1: #511 is not new executable work; merged commit `d6ff88b...` / PR #522 explicitly completed #511 as deterministic exception-first grouped review, and current code/E2E still prove that behavior.
- Queue correction 2: #426 is not executable as originally written; PR #480 failed cross-device audit because removing the desktop capture entry removes paste/upload access, and later planning direction superseded the dashboard-column deletion. A fresh owner design decision is required before any replacement slice.
- Parent-lane decision: #174 remains open because its remaining provider-console controls are not equivalent to a source-code PR. #557 isolates one code-testable reliability defect and authorizes no provider-console write.
- External research: current Cloudflare Turnstile docs recommend explicit client error/timeout handling and document network/browser-extension interference. Current Next.js Script docs define third-party script lifecycle hooks. Cloudflare widget `timeout-callback` is treated as widget/challenge lifecycle evidence, not proof that a top-level script stall always resolves.
- Research limits: no external reference establishes a provider SLA, no evidence supports CAPTCHA bypass, and no evidence supports modifying Supabase Auth/provider/WAF settings inside #557.
- Architecture decision: preserve the existing real-token gate. Add only a finite application-side top-level script watchdog/recovery state if #557 becomes executable.
- Recovery decision: timeout/error clears stale token, reports a non-sensitive finite failure and offers keyboard-accessible retry/reload. A full-page retry is preferred over inventing a second script-loader lifecycle unless implementation evidence proves otherwise.
- Race decision: implementation must prevent a stale deadline callback from overwriting later success and must preserve current widget reset/error/expiration lifecycle.
- Browser decision: implementation must deterministically simulate a stalled top-level script, prove finite failure UX and disabled submit, and keep the existing successful Turnstile E2E green.
- Security decision: timeout never synthesizes a token or enables login/register/forgot-password email submit; no site key/secret/provider identifier or sensitive auth payload is logged.
- Remaining: latest exact selector head must pass project knowledge/lifecycle, CI, CodeQL, Secret History, mergeability and thread checks. Only explicit owner merge may activate #557. After merge, implementation requires fresh main, `plan:resolve`, `agent:doctor`, a bounded runtime branch, evaluator pass and same-PR closeout to `current: null`.
- Production/provider evidence: read-only evidence only. PR #558 performs no Supabase DB/Auth/provider write, Turnstile provider mutation, Vercel/WAF mutation, environment-secret change or production-data write.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` is updated in this PR with merge-stable #558 candidate/active projection semantics, post-#556 production baseline, and reconciled #511/#426/#174 status so later sessions do not repeat stale work.
- Superseded issue, roadmap or claim: supersedes the planning assumption that #511 is still unimplemented, that #426 can safely execute its original deletion plan, and that #174 should be treated as one monolithic code slice. It does not close those tracking issues or provider-console work.
- Lifecycle impact: authority transition from `PLAN_AUTHORITY.current: null` to `docs/plans/active/557-turnstile-load-recovery.md` if #558 is owner-merged; this selector does not complete #557.
- Production writes: none.
- Merge: explicit owner authorization required.

## Focused source record

1. MoneyFlow `src/components/auth-turnstile.tsx` — current Script/widget lifecycle and missing top-level load deadline.
2. MoneyFlow `src/components/auth-form.tsx` — existing fail-closed `captchaBlocked` token gate.
3. MoneyFlow `e2e/auth-captcha.spec.ts` — current successful-token browser coverage.
4. MoneyFlow PR #522 / commit `d6ff88b...` — #511 already completed as exception-first grouped review.
5. MoneyFlow #426 / PR #480 evidence — original simplification slice is stale and unsafe as written.
6. MoneyFlow #174 production notes — observed indefinite Turnstile loading and remaining provider-console boundary.
7. Cloudflare Turnstile client-side errors / widget configuration / challenge troubleshooting / server validation — current external platform guidance.
8. Next.js Script documentation — current third-party script lifecycle reference.

## Permission boundary

#558 is a selector only. It may change planning authority if owner-merged. It authorizes no runtime/auth code change, Supabase Auth/provider mutation, Turnstile site/secret configuration, redirect allowlist/rate-limit change, Vercel/WAF mutation, environment secret, schema/RLS change, financial behavior, production-data write or CAPTCHA bypass.

If owner merges #558, implementation is limited to the bounded client auth-resilience and browser-test surface in #557. Provider-console controls in #174 remain separately authorized operational work.
