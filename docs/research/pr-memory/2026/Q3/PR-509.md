# PR #509 — keep an activated authority projection usable by child branches

- Date: 2026-08-28
- Change class: Class 1 — bounded plan-authority contract repair plus the recovery projection it requires. No product, schema, RPC, RLS, Auth, provider, financial calculation or transaction-behavior change.
- Affected capability or project boundary: `plan:resolve` and `check:knowledge` authority freshness on a branch based on a merged same-PR post-merge projection.
- Lifecycle impact: authority transition. This recovery resumes the exact post-merge projection pattern after #508 and fixes its child-branch validation; it neither selects nor completes an agent-executable slice.
- Status impact: candidate until merged. After merge, an unchanged child branch of an activated projection can resolve plan authority while an older copied projection on a later board commit remains rejected.
- Research: not applicable. The defect is fully reproducible from repository contracts: #505's projection resolves on its exact merged `main`, but the pre-fix resolver compares the child `HEAD` with that base commit and fails. The existing later-main negative test establishes the safety boundary; the added regression distinguishes an unchanged child branch from a later board edit.
- Changed: `plan-authority.mjs` identifies an activated projection from the expected base commit that last changed the board, instead of requiring a child branch's unrelated `HEAD` to equal that base. The board and current memory return to #508's merged baseline and carry #509's same-PR projection.
- Verified: regression test was observed red before the implementation with the board reported stale, then green. `node --test scripts/plan-authority.test.mjs scripts/plan-selection.test.mjs` passed 20/20. `npx eslint --fix scripts/plan-authority.mjs scripts/plan-authority.test.mjs`, `npx tsc --noEmit --pretty false`, and `git diff --check` passed before the projection/documentation update. Full project knowledge is rerun with this exact PR record below.
- Remaining: GitHub exact-head checks and required review remain outstanding. No merge, deployment, provider operation or financial mutation is performed by this PR.
- Production/provider evidence: none; this is a local deterministic contract repair with no provider or production operation.
- Snapshot update: board and current-memory baseline/projection metadata move from #508's completed closeout to #509's candidate same-PR projection; this is candidate evidence until merge.
- Superseded issue, roadmap or claim: supersedes the false assumption that an activated projection must execute at a child branch's identical `HEAD`; no product roadmap item is promoted.
