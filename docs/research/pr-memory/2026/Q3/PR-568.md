# PR #568 — select #567 RPC least-privilege proof

- Date: 2026-09-10
- Base: merged `main@1ea23a1e9600807e0b3edf10e02f7414bcdf1cf0`.
- Change class: Class 3 database/security selector, but this PR is documentation/authority only; no runtime, migration, schema, provider, deployment or production-data mutation.
- Affected capability or project boundary: authenticated privileged RPC exposure, default function EXECUTE policy, and reconciliation snapshot read authority.
- Status impact: projects `PLAN_AUTHORITY.current` to `docs/plans/active/567-rpc-least-privilege-proof.md` with `selectedByPr: 568`; the projection remains candidate until owner merge enters first-parent history.
- Changed: adds the bounded #567 selector packet and authority projection. The implementation scope is limited to direct pgTAP proof of deny-by-default postgres/public function ACL and an evidence-gated reduction of `reconciliation_snapshot_for_user` from SECURITY DEFINER to SECURITY INVOKER.
- Verified: production read-only audit found 62 public SECURITY DEFINER functions, 43 authenticated-callable, 19 internal-only, and zero anon/PUBLIC-callable; current repo contracts already enforce explicit grants, empty search paths and authenticated identity guards. Cross-tenant reconciliation snapshot rejection is already covered by pgTAP.
- Remaining: exact-head selector CI/CodeQL/evaluation and explicit owner merge; then fresh `plan:resolve`/`agent:doctor` before any database/test implementation.
- Production/provider evidence: production catalog/grant/RLS state was read only; no production or provider write occurred.
- Snapshot update: #567 is a selector candidate only. Merged-main runtime/product truth remains unchanged until a later implementation PR is separately validated and owner-merged.
- Superseded issue, roadmap or claim: does not supersede #432 or #174 and does not reopen #559 UI work. It rejects the interpretation that all 43 Supabase Advisor findings are 43 confirmed vulnerabilities.
- Lifecycle impact: selects one bounded security hardening candidate; no runtime/database implementation starts before PR #568 owner merge.

## Permission boundary

PR #568 may change only planning authority and lifecycle/project-memory artifacts needed to select #567. It authorizes no migration/test/runtime database change, Supabase production write, provider-console change, UI work or financial-semantics change before owner merge and fresh authority resolution.
