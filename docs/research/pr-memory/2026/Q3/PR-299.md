# PR #299 — UI migration Phase 2 token and primitive ownership

- Date: 2026-08-05
- Change class: Class 2 presentation architecture and shared-component migration, with no financial/domain behavior change authorized
- Affected capability or project boundary: semantic token vocabulary, shared UI primitives, accessibility states, keyboard/focus behavior, target-size ownership, status announcements, financial/stored-data UI safeguards, component evidence and the temporary `MinimumTargetSizeContract` compatibility boundary
- Status impact: candidate — Phase 2 is owner-authorized and active on draft PR #299, but `main`, production UI and current runtime behavior remain unchanged until separately reviewed and merged
- Changed: create the dedicated Phase 2 branch and work packet; define the primitive API/state matrix; classify external standards, migration practice and MoneyFlow policy separately; correct target sizing to WCAG AA 24×24-or-valid-exception versus MoneyFlow 44×44 important-action policy; add native-first and WAI-ARIA APG keyboard contracts for Button, Select/combobox, Checkbox, Radio, Dialog and Sheet; remove generic `invalid` from the future Button contract; make dialog descriptions optional for complex content; distinguish modal and non-modal sheets; define polite-by-default toast/status behavior; require text error identification and correction suggestion when known; require reversible, checked or confirmed protection for affected financial/stored-data mutations; separate 200% text, equivalent 320 CSS-pixel reflow and actual 320px mobile evidence; require focus not entirely obscured; preserve `document-theme.css` as token authority; defer Storybook behind an explicitly internal adoption heuristic
- Verified: official W3C/WCAG and WAI-ARIA APG material supports the corrected target-size, keyboard, dialog, status-message, error-assistance, financial-error-prevention, resize/reflow and focus boundaries; official Next.js material supports both global CSS and CSS Modules, confirming the no-new-global-CSS gate is MoneyFlow-specific; repository inspection confirms Button uses Base UI/CVA with 24–36px boxes while `MinimumTargetSizeContract` globally repairs broad control families to 44px; no runtime code, dependency, token value, financial behavior or product style has changed in the documentation correction
- Remaining: complete current primitive/token/consumer inventory; classify every target-size consumer as MoneyFlow-important, AA-baseline, valid exception or route-specific debt; classify existing Select/Checkbox/Radio/Dialog/Sheet/Toast implementations against their keyboard, focus and live-region contracts; inventory affected financial/stored-data consumers and their existing reversible/checked/confirmed safeguard; implement bounded primitive slices with exact-head static/build/browser evidence; migrate a low-risk consumer set; measure remaining global target-size consumers; obtain owner approval before Phase 3
- Production/provider evidence: none applicable at Phase 2 start; no deployment, provider write, database/auth/RLS change, production-data access or production behavior change
- Snapshot update: not applicable — this is an open candidate PR and does not change current product truth on `main`
- Superseded issue, roadmap or claim: supersedes wording that treats 44×44 as the universal WCAG AA minimum, treats MoneyFlow's global-CSS/route/class gates as universal framework rules, treats the 1,200 `!important` budget as an external standard, treats the five-state Storybook gate as a Storybook requirement, assigns generic invalid state to Button, requires descriptions for every dialog, treats every sheet as modal, treats every toast as an assertive alert or treats a 320px phone viewport as complete reflow proof; Phase 3 remains unauthorized
