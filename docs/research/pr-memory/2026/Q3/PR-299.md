# PR #299 — UI migration Phase 2 token and primitive ownership

- Date: 2026-08-05
- Change class: Class 2 presentation architecture and shared-component migration; no financial-domain behavior change
- Affected boundary: semantic tokens, action/form/overlay/feedback/empty-state/money primitives, keyboard/focus behavior, target ownership, status announcements, financial/stored-data UI safeguards and the temporary `MinimumTargetSizeContract`
- Status impact: evaluating — the owner instructed “hoàn tất p2 đi”; implementation and inventories are complete on PR #299, while current product truth remains unchanged until exact-head gates pass and the PR merges
- Changed: preserve Button compatibility while adding semantic intent/density/target/pending inputs, LinkButton and accessible IconButton; add TextField, native-first SelectField, CheckboxField and RadioGroup; add native modal Dialog and explicit modal/non-modal Sheet; establish Badge, Alert, Toast/ToastRegion, EmptyState and MoneyValue contracts; preserve existing noninteractive Card; add primitive source-contract tests; reconcile TSX token references to canonical `--mf-*-subtle` roles; complete the target-contract selector inventory; reassess and continue to defer Storybook
- Verified so far: Phase 1 no-new-debt classifier reports zero violations across the PR diff; selector inventory confirms the global target contract still mixes important targets, AA/exception cases, discoverability and responsive route debt; new primitives change no token values, database/auth/RLS/provider code or financial mutation logic; draft-mode classifier runs selected full static/build/browser verification but intentionally skipped those shards until ready-for-review
- Bounded proof consumers: Dialog consumes IconButton; modal Sheet consumes Dialog; non-modal Sheet consumes IconButton; Badge/Alert/Toast consume canonical semantic tokens; MoneyValue consumes existing money formatting helpers
- Remaining before closure: move PR #299 from draft to ready-for-review; pass exact-head policy, lint, typecheck, complete tests, production build, browser smoke, Chromium/WebKit cross-device audit, CodeQL and all-ref secret scan; fix any failures; update this record and the Phase 2 packet with final evidence; merge under the owner's completion instruction
- Production/provider evidence: not applicable; no deployment, provider write, database/auth/RLS change, production-data access or production operation is authorized or required
- Snapshot update: pending merge — update current project truth only after PR #299 is merged
- Superseded claim: 44×44 is not universal WCAG AA; MoneyFlow global-CSS, route/class, `!important` budget and Storybook gates are project controls; Button does not own generic invalid state; descriptions are not mandatory for every dialog; sheets may be modal or non-modal; ordinary toasts are not assertive; a 320px phone viewport alone is not reflow proof
- Phase boundary: Phase 3 remains unauthorized
