# PR #299 — UI migration Phase 2 token and primitive ownership

- Date: 2026-08-05
- Change class: Class 2 presentation architecture and shared-component migration, with no financial/domain behavior change authorized
- Affected capability or project boundary: semantic token vocabulary, shared UI primitives, accessibility states, target-size ownership, component evidence and the temporary `MinimumTargetSizeContract` compatibility boundary
- Status impact: candidate — Phase 2 is owner-authorized and active on draft PR #299, but `main`, production UI and current runtime behavior remain unchanged until separately reviewed and merged
- Changed: create the dedicated Phase 2 branch and work packet; define the MoneyFlow primitive API/state matrix; preserve `document-theme.css` as token authority; record the staged order for action, form, overlay, feedback, empty-state and finance-value primitives; defer Storybook until its separate adoption gate is met
- Verified: repository inspection confirms the existing Button uses Base UI/CVA with 24–36px size variants, while `MinimumTargetSizeContract` globally repairs broad control families to the 44px MoneyFlow product target; no runtime code, dependency, token value or product style has changed in the initial specification commit
- Remaining: complete current primitive/token/consumer inventory; review P2-T1 API and state matrix; implement bounded primitive slices with exact-head static/build/browser evidence; migrate a low-risk consumer set; measure remaining global target-size consumers; obtain owner approval before Phase 3
- Production/provider evidence: none applicable at Phase 2 start; no deployment, provider write, database/auth/RLS change, production-data access or production behavior change
- Snapshot update: not applicable — this is an open candidate PR and does not change current product truth on `main`
- Superseded issue, roadmap or claim: Phase 2 is no longer unauthorized as a workstream; Phase 3 App Shell work, unbounded Storybook adoption and any new visual direction remain unauthorized
