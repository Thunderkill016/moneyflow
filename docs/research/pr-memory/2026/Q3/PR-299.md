# PR #299 — UI migration Phase 2 token and primitive ownership

- Date: 2026-08-05
- Change class: Class 2 presentation architecture and shared-component migration; no financial-domain behavior change
- Affected capability or project boundary: semantic tokens, action/form/overlay/feedback/empty-state/money primitives, keyboard and focus behavior, target ownership, status announcements, financial/stored-data UI safeguards, current project memory and the temporary `MinimumTargetSizeContract`
- Status impact: evaluating — the owner instructed “hoàn tất p2 đi”; implementation and inventories are complete on PR #299, while current product truth remains unchanged until exact-head gates pass and the PR merges
- Changed: preserve Button compatibility while adding semantic intent/density/target/pending inputs, LinkButton and accessible IconButton; add TextField, native-first SelectField, CheckboxField and RadioGroup; add native modal Dialog and explicit modal/non-modal Sheet; establish Badge, Alert, Toast/ToastRegion, EmptyState and MoneyValue contracts; preserve existing noninteractive Card; add primitive source-contract tests including canonical semantic-token validation; reconcile new TSX token references to canonical `--mf-*-subtle` roles; classify the remaining global target-contract selectors; reassess and continue to defer Storybook; synchronize `CURRENT_PROJECT_MEMORY.md` to the post-merge Phase 2 state
- Verified: the Phase 1 no-new-debt classifier reports zero violations across the PR diff; the first ready-for-review run passed the complete unit/static test shard and correctly exposed a project-knowledge schema failure instead of silently accepting stale records; the required packet headings, exact memory fields and current-project snapshot were then restored; selector inventory confirms the global target contract still mixes important targets, AA/exception cases, discoverability and responsive route debt; new primitives change no token values, database/auth/RLS/provider code or financial mutation logic
- Remaining: rerun the full exact-head policy, static, test, production-build, browser-smoke, Chromium/WebKit cross-device, CodeQL and all-ref secret-history gates; fix any failure; record the final exact-head evidence; merge PR #299 under the owner's completion instruction
- Production/provider evidence: not applicable — this PR performs no deployment, provider write, database/auth/RLS change, production-data access or production operation
- Snapshot update: updates `docs/research/CURRENT_PROJECT_MEMORY.md` to the post-merge Phase 2 primitive-ownership truth while preserving Phase 3 as unauthorized
- Superseded issue, roadmap or claim: supersedes claims that 44×44 is the universal WCAG AA minimum; that MoneyFlow's global-CSS, route/class, `!important` budget or Storybook gates are universal standards; that Button owns a generic invalid state; that every dialog needs a description; that every sheet is modal; that ordinary toasts are assertive; that a 320px phone viewport alone proves reflow; or that Phase 2 remains unauthorized or documentation-only
