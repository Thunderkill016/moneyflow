# PR #360 — Repository Reset 1 authority/config routing

- Date: 2026-08-12
- Change class: Class 1 — bounded executable documentation/knowledge guard
- Affected capability or project boundary: repository authority, current-memory routing, active-packet lifecycle, agent adapters and configuration documentation
- Status impact: current project state and immediate execution route clarified; Repository Reset 1 is active
- Changed: compacted default authority route; moved 37 stale packets to `docs/plans/archived/`; added an exact active-packet registry/current-reference guard; reclassified stale roadmap/research/UI documents without deleting historical evidence; clarified configuration/deployment ownership and retained agent tooling adapters
- Verified: `git diff --check`; `npm run check:migrations`; `npm run check:knowledge`; `npm run test:ci-policy`; `npm run typecheck`; direct active-registry validation; fresh-context evaluator clean
- Remaining: exact-head provider checks are pending. The first exact-head test shard caught a missing README link to the released MVP reference; the link was restored in a follow-up commit. Local `npm run build` and `npm run verify:prepush` did not reach a completion result after Next began the optimized build in this runner; no build pass is claimed.
- Production/provider evidence: not applicable — no provider, deployment, database or production behavior changed
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` replaced its historical chronology with current truth and the ordered next state
- Superseded issue, roadmap or claim: generic P4 Improve as next work; current-roadmap claims in capability/competitive research; stale active packet discovery; old P3/7-day wording as current

## Boundaries

No Reset 2 source/assets deletion, UI/brand implementation, financial behavior,
schema/auth/provider/production change, workflow/permission change or merge. `.agents`,
`.claude` and `.specify` remain because their current hooks/adapter functions were
verified; no skills or configuration files were deleted.

## Evaluator result

Fresh-context evaluation found stale Trust sequencing, research-roadmap authority,
adapter precedence and memory-budget drift. The final re-audit was clean.
