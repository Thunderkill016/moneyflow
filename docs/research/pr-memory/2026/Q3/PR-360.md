# PR #360 — Repository Reset 1 authority/config routing

- Date: 2026-08-12
- Change class: Class 1 — bounded executable documentation/knowledge guard
- Affected capability or project boundary: repository authority, current-memory routing, active-packet lifecycle, agent adapters and configuration documentation
- Status impact: Repository Reset 1 is accepted/completed post-merge; Repository Reset 2 is immediate next but not started
- Changed: compacted default authority route; moved 37 stale packets to `docs/plans/archived/`; added an exact active-packet registry/current-reference guard; reclassified stale roadmap/research/UI documents without deleting historical evidence; clarified configuration/deployment ownership and retained agent tooling adapters; completed and moved the Reset 1 packet to `docs/plans/completed/`
- Verified: `git diff --check`; `npm run check:migrations`; `npm run check:knowledge`; `npm run test:ci-policy`; `npm run typecheck`; direct active-registry validation; fresh-context evaluator clean; exact final lifecycle-head CI `verify`, `database`, `e2e`, Gitleaks and CodeQL green. Browser smoke and cross-device audit were correctly risk-classified as skipped.
- Remaining: the first exact-head test shard caught a missing README link to the released MVP reference; the link was restored and the rerun passed. Local `npm run build` and `npm run verify:prepush` did not reach a completion result after Next began the optimized build in this runner; exact-head production-build CI passed instead.
- Production/provider evidence: not applicable — no provider, deployment, database or production behavior changed
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` records Reset 1 accepted/completed and Reset 2 as immediate next/not started
- Superseded issue, roadmap or claim: generic P4 Improve as next work; current-roadmap claims in capability/competitive research; stale active packet discovery; old P3/7-day wording as current; Reset 1 as an active/outstanding task

## Boundaries

No Reset 2 source/assets deletion, UI/brand implementation, financial behavior,
schema/auth/provider/production change, workflow/permission change or merge. `.agents`,
`.claude` and `.specify` remain because their current hooks/adapter functions were
verified; no skills or configuration files were deleted.

## Evaluator result

Initial fresh-context evaluation found stale Trust sequencing, research-roadmap
authority, adapter precedence and memory-budget drift; the initial re-audit was clean.
The final lifecycle amendment then received a separate fresh-context, assumed-post-merge
audit: clean. It verified that the registry contains only the Trust parent, Reset 1 is
completed with owner merge as the acceptance boundary, and Reset 2 is next/not started
with no packet. The exact-head rerun initially exposed the required PR-memory field
label (`- Verified:`), which was restored; its Gitleaks download also had a transient
GitHub network failure. The corrected exact-head rerun is green.
