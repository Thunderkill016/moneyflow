# PR #252 — close the MVP empty-state release gate

- Date: 2026-08-03
- Change class: test-only release acceptance
- Affected capability or project boundary: locked MVP exit criterion 6, core empty-state action hierarchy and Báo cáo export discoverability
- Status impact: candidate — the PR adds the focused browser assertion identified by PR #251 but does not claim the criterion closed until exact-head CI and browser checks pass
- Changed: add a Playwright pass across the nine locked core routes; assert every rendered empty-state action region has exactly one visible primary action; record per-route JSON evidence; verify direct period CSV and one-click advanced export; add a static source contract for the shared `EmptyState` and Reports empty branch
- Verified: pending exact-head CI, CodeQL and secret-history results for the final head
- Remaining: fix any exact-head failure; update the release acceptance ledger and canonical project memory only after the browser gate is green; owner review and merge decision
- Production/provider evidence: no provider read/write, deployment, production probe, physical-device claim or production-data access is part of this test-only change
- Snapshot update: not applicable — canonical release truth remains PR #251 until the new browser evidence passes and is recorded
- Superseded issue, roadmap or claim: none; this implements the single focused release-candidate gate retained by `docs/release/MVP_RELEASE_ACCEPTANCE_2026-08-03.md`
