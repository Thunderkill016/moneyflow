# PR #464 — feat(import): preview explicit rules in Direct CSV

- Date: 2026-08-26
- Change class: Class 3 — Direct CSV source preparation, a server action and a Supabase RPC over user-owned import data.
- Affected capability or project boundary: existing explicit Inbox rules can normalize eligible Direct CSV dry-run rows, but only before the existing explicit all-or-nothing ledger review.
- Lifecycle impact: continues current slice while this draft PR is candidate evidence.
- Status impact: candidate. The branch carries exact rule id/version as evidence, prepares all source candidates with an authenticated atomic RPC, reuses the existing rule ownership/version/match validator, then reads the server-normalized category before the unchanged approval RPC. It adds no auto-post, rule inference, account/category memory, transfer normalization, provider/native/AI behavior or production write.
- Changed: rule-aware Direct CSV planning and preview/review text; initial rule workspace loading; paired server-action evidence; a security-definer source-preparation RPC with pgTAP coverage; focused unit/action/browser tests; current board and Class 3 packet.
- Verified: `NEXT_PUBLIC_APP_MODE=demo npm run verify:prepush` (knowledge, architecture, lint, typecheck, 1,115 tests and build); migration identity; RLS checks; CI-policy tests; focused Direct CSV Playwright desktop/mobile flow.
- Remaining: local `npm run test:db` is blocked by absent PostgreSQL (`LegacyDbConnectError`); exact-head provider database/browser/CodeQL/secret-history checks, review threads and mergeability cannot be claimed from this draft.
- Local test limitation: full E2E had 120 passes and four CAPTCHA-readiness failures because `input[name="captchaToken"]` was absent on desktop/mobile; auth/CAPTCHA is outside this diff. The full UI audit was intentionally stopped after targeted Direct CSV responsive evidence and is not claimed as green.
- Production/provider evidence: no deployment, schema application, provider configuration/data action, financial data action, protection change or `main` write occurred.
- Merge evidence: draft PR #464 must become ready only after its final lifecycle projection and exact head are pushed. Merge then requires fresh required provider checks, no unresolved review threads, unchanged base/main and the observed head SHA.
- Snapshot update: not applicable while PR #464 is open; current board and active packet truth remain unmerged candidate state.
- Superseded issue, roadmap or claim: none; addresses #463 as the selected P2 slice without changing the P2 program boundary.
- Rollback: revert PR #464. Direct CSV returns to default-category dry-run behavior; no backfill or source/ledger rewrite is required.
- Untrusted external instructions copied into project memory: none.
