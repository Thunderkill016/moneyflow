# PR #628 — Activity deep links to exact transaction / Inbox candidate (#602)

- Date: 2026-09-21
- Change class: Class 1 (bounded UI wiring on existing surfaces; no new loader, mutation or data path)
- Affected capability or project boundary: `src/lib/activity.ts`, `src/lib/transactions/open-target.ts` (new), `src/lib/inbox/review.ts`, `src/lib/url-params.ts` (new), `src/app/transactions/page.tsx`, `src/app/inbox/page.tsx`, `src/components/transactions/transactions-workspace.tsx`, `src/components/inbox/inbox-page.tsx`, `e2e/activity.spec.ts`
- Status impact: closes the #602 gap — Activity items previously linked to bare `/transactions` and `/inbox` lists; each item now opens the exact object.
- Changed: Activity ledger items link to `/transactions?open=<id>` (attention rows keep `review=needs_review` context); candidate items link to `/inbox?candidate=<id>`. `resolveTransactionOpenTarget` classifies a target as `edit | notice | missing` — split rows resolve to the same "xóa rồi tạo lại" notice their row action shows, recurring rows to the `/commitments` notice, stale/foreign ids to "Không tìm thấy giao dịch". `findPendingCandidateTarget` resolves only `status === "pending"`. The transactions workspace waits for the settled owner-scoped list (demo swaps to device storage after mount — resolution deferred via a settled-list check + `setTimeout` callback so `react-hooks/set-state-in-effect` stays clean and a locally deleted fixture cannot open a ghost editor), strips the consumed param via `clearQueryParam`, and reuses `EditTransactionDialog`/`showNotice`. Inbox seeds `reviewId` from the param so the existing review panel opens when the pending row loads; both load paths (`load` + mount fetch) clear it and emit one honest notice when nothing pending resolves.
- Verified: focused unit tests 28/28; `npm test` 1427/1427; lint 0 errors (1 pre-existing warning); typecheck clean; `NEXT_PUBLIC_APP_MODE=demo npm run build` clean; Playwright `e2e/activity.spec.ts` 4/4 desktop + mobile (click-through to edit dialog, param consumed, stale id → notice + no dialog, candidate → review panel, no horizontal overflow).
- Remaining: CI provider lanes on the exact head.
- Production/provider evidence: none — demo-mode browser verification only; authenticated behavior reuses the same owner-scoped loaders, but no production click-through was performed.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: additive navigation wiring only; no schema, contract, CSS ownership or capability-surface change.

## Boundaries

- Deep links open existing surfaces only — no new editor, review panel or Activity redesign.
- Recurring payments still managed under `/commitments`; split expenses still recreate-to-edit.
- `?candidate` resolves pending rows only; approved/rejected candidates intentionally produce the stale-target notice.

## Evidence limits

- e2e covers demo mode against fixture ids (`sample-*` ledger rows, pending `cand-demo-*`); real-account RLS isolation is inherited from the existing loaders rather than re-proven here.
