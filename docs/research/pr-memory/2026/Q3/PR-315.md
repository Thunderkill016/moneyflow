# PR #315

- Date: 2026-08-07
- Change class: Class 1 — bounded repository tooling with a full packet because external agent-workflow research informed the decision
- Affected capability or project boundary: coding-agent task startup, project-context routing and risk-proportional verification guidance
- Status impact: none — no current product capability, financial behavior, runtime, database, provider or production truth changes
- Changed: added a dependency-free `task:brief` CLI; parses the current context router; requires explicit task boundary and risk class; preserves the mandatory `AGENTS.md` read order; inspects local branch/head/dirty state and changed files; blocks implementation on `main`; requires a repository-owned active packet for Class 3; contains packet/output paths inside the repository; emits Markdown, JSON or prompt output; adds focused policy tests and an operations runbook
- Verified: the earlier prototype passed 5/5 isolated Node contracts, synthetic feature-branch output and `main` blocking; final hardening adds current-router alias, authority-order, active-packet and path-containment contracts but has not yet received exact-head execution evidence
- Current delivery state: PR #315 was marked ready for review on head `e45ff512968126708c5e4194d42240693f4eb61d`; GitHub returned no workflow runs and no commit statuses for that head after the `ready_for_review` event
- Remaining: approve automation-created workflows if GitHub displays an approval banner; then inspect the new exact-head CI, CodeQL and secret-history runs, fix only observed failures, reconcile the packet/PR body and keep merge as a separate owner decision
- Production/provider evidence: not applicable; the command is local read-only repository tooling and performs no GitHub/provider write, deployment or production-data operation
- Snapshot update: not applicable — this candidate does not change merged product capability or architecture truth
- Superseded issue, roadmap or claim: none; the tool compiles existing policy and does not replace `AGENTS.md`, `docs/context/README.md`, work packets, `ci:status`, `ci:watch` or candidate PR #314 CI recovery

## Evidence boundary

External research supports high-signal context curation, repository instructions, durable task state and developer-productivity tooling. It does not establish MoneyFlow risk class, financial semantics, permission scope or merge authority. Those remain explicit project-owned inputs and reviewer decisions.

GitHub's official workflow-trigger guidance states that pull-request events created or updated by automation can enter an approval-required state depending on the token context. That is a plausible explanation for the absent runs, not a proven diagnosis of this connector. No blind retry, no-op commit, required-check bypass or provider-setting change was used.

The CLI intentionally does not infer risk from natural language or filenames, create a new memory/orchestration layer, run tests, recover CI, create branches/packets/PRs, access providers, merge or deploy. A successful task brief proves startup consistency only; it is not implementation or delivery evidence.
