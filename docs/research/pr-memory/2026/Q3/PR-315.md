# PR #315

- Date: 2026-08-07
- Change class: Class 1 — bounded repository tooling and policy contracts with a full packet because external agent-workflow research informed the decision
- Affected capability or project boundary: coding-agent task startup, project-context routing, work-packet reliability and risk-proportional verification guidance
- Status impact: none — no current product capability, financial behavior, runtime, database, provider or production truth changes
- Changed: added dependency-free `task:brief`; parses the context router; requires explicit task boundary/class; preserves the mandatory read order; inspects branch/head/base/dirty state and changed files; blocks `main`; requires a repository-owned Class 3 packet; contains packet/output paths inside the repository; emits Markdown/JSON/prompt output; adds State, Feedback, Removal impact and Action safety to the canonical work packet; validates changed active packets through the existing policy-test surface
- Reliability hardening: work-packet subsections must live inside the single `## Control contract`; duplicate fields and copied feedback signals fail; bare unresolved answers fail while legitimate inline syntax such as ``<button>`` remains valid; committed, staged, unstaged and untracked packet changes are all included; a missing or explicitly invalid Git base fails closed instead of producing a false zero-packet success
- Independent base-truth review: after `main` advanced, a synthetic Git repository reproduced a false task scope when local `main` lagged the fetched `origin/main`: comparison against local `main` included an already-merged active packet, while `origin/main` left only the feature change. Default task/packet comparison now prefers existing `origin/main` and falls back to local `main`; an explicit packet `--base` remains exact. A cross-tool regression locks this invariant.
- Verified: earlier bootstrap prototype passed 5/5 isolated Node contracts plus synthetic branch and `main` protection; work-packet checker passed 14/14 focused Node tests before the base-truth review; committed/staged/unstaged/untracked fixtures passed; missing scope and invalid base failed; the actual PR #315 contract passed as one changed packet and replacing semantic evidence with bare `Not applicable` failed with exit code `1`; the stale-local-main red fixture reproduced the base bug before the fix. Final exact-head repository execution is refreshing after the independent review.
- Current delivery state: PR #315 is ready for review; GitHub Actions is functioning and exact-head CI, CodeQL and secret-history runs now trigger normally. The final candidate SHA is owned by live PR metadata because repository evidence updates move the branch until the last pre-green commit.
- Remaining: inspect the final exact-head CI, CodeQL and secret-history runs, fix only observed failures, record final run IDs in PR metadata without creating a self-referential evidence commit, and keep merge as a separate owner decision
- Production/provider evidence: not applicable; tooling reads repository/Git state only and performs no GitHub Actions mutation, deployment or production-data operation
- Snapshot update: not applicable — this candidate does not change merged product capability or architecture truth
- Superseded issue, roadmap or claim: supersedes work packets that rely on implicit state, mechanism-only green evidence, stale-local-main scope or unstated action boundaries when those active packets are next changed; does not replace `AGENTS.md`, the context router, risk policy, `ci:status`, `ci:watch` or PR #314 recovery

## Evidence boundary

The user-provided `DISTILLATION.md` supports external state, deterministic feedback, semantic evidence, red-before-green, removal understanding and bounded action permissions. Focused OpenAI guidance supports repository instructions and reliable testing; Martin Fowler's TDD description supports recording a failing signal before accepting green behavior. These sources do not establish MoneyFlow financial semantics, permission scope or merge authority.

The repository adopts only machine-checkable consequences. It does not claim that every numerical or source-specific assertion in the supplied synthesis was independently reproduced. `docs/research/AI_RELIABILITY_DISTILLATION_ADOPTION_2026-08.md` records the adopted and rejected parts.

Git's official remote-branch documentation states that local branches do not automatically synchronize with remote changes and that remote-tracking refs such as `origin/main` are updated by fetch. The bootstrap remains network-free: `origin/main` represents the latest fetched merged truth, not guaranteed server-fresh truth. The emitted base ref is therefore evidence that reviewers can inspect rather than a hidden assumption.

The official `actions/checkout` documentation states that `fetch-depth: 0` fetches all history for all branches and tags. MoneyFlow's protected policy checkout already uses that setting, so `origin/main` is available and current inside that protected checkout while the work-packet gate still fails closed when required Git scope cannot be proven.

The Codex review quota notice on the PR is external to repository acceptance. It does not block repository-owned independent review or GitHub Actions evidence, and no product requirement depends on another Codex review run.

A passing work-packet contract proves required decisions are explicit and structurally owned; it does not prove the decisions are true. Reviewers must still inspect the cited evidence and real path. No blind retry, no-op commit, required-check bypass, provider-setting change, merge or deployment was used.
