# PR #406 — fix: separate mobile Ghi label from capture CTA

- Date: 2026-08-16
- Change class: Class 2 bounded mobile shell/UI polish; no financial, database, Auth, provider, deployment or production mutation.
- Affected capability or project boundary: center `Ghi` action in the five-tab mobile bottom navigation during RRB-08 physical-device follow-up.
- Status impact: candidate only. The owner-observed P2 spacing defect is being fixed; RRB-08 remains open and owner physical-phone acceptance remains pending.
- Changed: `app-shell.module.css` replaces three independent center-action offsets with one local grid geometry owner. The 42px Fresh Blue CTA and icon share the first track and move together; the `Ghi` label remains in its own normal-flow row. The existing 52px item target, 70px nav reserve, five-column IA and safe-area variables are unchanged. `mobile-nav-capture-spacing.test.ts` locks the geometry and prevents the old 5px/7px stacked margins from returning. The Current Work Board promotes #405 and keeps #403 next in the one-at-a-time queue.
- Research: no external pattern or dependency adopted. Current shipped code and the owner's real-phone screenshot define the defect; PR #370 is provenance for the accepted floating mobile-nav concept and reserve contract.
- Verified: no local/browser PASS is claimed from this ChatGPT session because the connected GitHub surface does not provide a runnable local checkout. Static regression coverage is committed; repository CI, responsive browser evidence and independent review remain required before merge.
- Remaining: verify 320/360/390px widths, light/dark and active/inactive center states; confirm no horizontal overflow, target overlap or nav-reserve drift; owner repeats the real-phone observation before RRB-08 closeout.
- Production/provider evidence: not applicable; no provider or production read/write is claimed.
- Snapshot update: not applicable — Current Work Board is updated on the candidate branch for #405/#403 queue state, while `CURRENT_PROJECT_MEMORY.md` intentionally remains unchanged until merged work changes durable project truth.
- Superseded issue, roadmap or claim: PR #402 is already closed as stale provenance; #405 is the active implementation issue. This PR does not close RRB-08 or claim physical-device PASS.
