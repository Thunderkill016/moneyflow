# PR #556 — MON-63 affected-flow evidence and lifecycle closeout

- Date: 2026-09-09
- Status: Draft closeout continuation under active MON-63 authority; browser evidence must pass before lifecycle completion is recorded.
- Base: `main@63c239aefca9b5629561808c17948e3aea39bf3e` after owner-merged PR #555 and verified production deployment.
- Change class: Class 3 acquisition/data-integrity closeout evidence; initial change is browser-test-only.
- Affected capability or project boundary: Direct CSV remembered mapping, explicit manual override evidence, MON-63 lifecycle completion.
- Status impact: none yet; merged `PLAN_AUTHORITY.current` remains MON-63 until this PR proves complete acceptance and projects the same-PR transition to `null`.
- Changed: adds an affected-flow Playwright test that remembers a Direct CSV mapping, reapplies it after reset/re-upload, verifies the review dialog reports remembered-mapping use, then changes a mapping field and verifies evidence returns to manual review.
- Verified: initial exact head `da593c044a53d93582bebc18803fc8e4d68c5f75` passed build, lint/typecheck and unit/static-RLS; CI #3447 was intentionally blocked by the Project Knowledge contract because this PR-memory record was not yet present, so browser shards did not run and no browser success is claimed yet. CI #3448 then confirmed the lifecycle validator requires a literal authority-transition marker only when `PLAN_AUTHORITY.json` changes in the same projection; no browser success is claimed from that blocked run.
- Remaining: obtain exact-head affected browser evidence; reconcile #554/#555/#556 acceptance; move the completed MON-63 packet out of `active`; set the PR projection of `PLAN_AUTHORITY.current` to `null`; reconcile `CURRENT_PROJECT_MEMORY.md`; run clean evaluator and exact-head policy/CI/CodeQL/Secret/browser gates before Ready.
- Production/provider evidence: #555 production is READY at `63c239aefca9b5629561808c17948e3aea39bf3e`; this PR has no production write and introduces no provider sync, bank-specific mapping, raw-statement retention, or new ledger mutation authority.
- Snapshot update: pending lifecycle closeout; do not treat this Draft record as merged authority.
- Superseded issue, roadmap or claim: none; this PR completes evidence and lifecycle work for the already-selected MON-63 packet only.
- Lifecycle impact: none while affected-flow evidence is still being proven. After that evidence passes, the same PR will project the actual authority transition together with `PLAN_AUTHORITY.current = null`.
- Production writes: none.
- Merge: not authorized by the current continuation instruction; merge remains an explicit owner decision.

## Focused evidence record

1. The remembered mapping remains an explicit user action and dry-run/review remains visible.
2. Manual mapping changes must invalidate the `preset_applied` operational evidence and return the review surface to `mapping_reviewed`.
3. Structural preset evidence never becomes provider/bank identity or automatic posting authority.
4. Lifecycle closeout will only be projected after browser acceptance is demonstrated on an exact PR head.
