# PR #495 — docs(board): reflect RRB-08 closed by owner declaration

- Date: 2026-08-27
- Change class: Class 0 — documentation only. No code, workflow, schema, migration, RPC, RLS, provider or dependency change.
- Affected capability or project boundary: `docs/plans/active/README.md` release-readiness statement and NOW section, and the release-proof row of `docs/research/CURRENT_PROJECT_MEMORY.md`.
- Status impact: candidate until merged. On merge it records a release-gate state change that already happened on GitHub, so the repository stops describing a gate as open after the owner closed it.
- Lifecycle impact: completes current slice. This PR carries its own post-merge convergence: the board projection names PR #495, no successor slice is promoted, and projected current memory is updated here. `plan:resolve` intentionally reports NOT READY while this is unmerged.
- What happened: the owner exercised the deployed production origin on a real phone, reported PASS, and closed `#398` themselves at 04:50 on 2026-08-27. I had prepared a closing comment and found the issue already closed — which is the correct order, because an agent declaring a physical-device gate satisfied is precisely what this program forbade for twelve days.
- Changed: RRB-08 moves from NOW to done with its device context inline; the owner-action table drops from four rows to three; the release-readiness line now states that RRB-08 is closed **and** that the product remains not public-beta ready; the capability inventory's release-proof row records the closure with its coverage.
- The closure is recorded with its limits rather than as a bare tick, in three places — the board, current memory, and a comment on `#398` itself. One Android device on Chrome, authenticated, with a single summary verdict rather than six per-checkpoint notes. OS version, browser version, network context and time were not supplied and stay marked unstated. There is no iOS or Safari observation, which matters because `webkit-iphone` was the only CI project to behave differently during this session, failing a `safety-review-states` audit twice while every other project passed.
- Why the limits are written down at closure rather than left implicit: a closed gate is read later as "this was tested", and six months from now nobody will remember it meant one Android handset. The purpose of the record is to make the next reader's inference correct without them having to ask.
- Release readiness is deliberately unchanged in substance. RRB-08 was one of several gates; RRB-02 hosted restore, RRB-04, RRB-05, RRB-06 and RRB-09 remain open. Closing one blocker does not move the release decision, and the board now says both facts in the same sentence so neither can be quoted without the other.
- Verified: `check:knowledge` passes, including the local record validation from PR #493, so this record was checked on the machine that wrote it. `test:ci-policy` and diff hygiene apply; no application, database or browser gate is selected by a documentation-only change.
- Limitation, not a pass: I did not witness the device session and hold no independent evidence of it. Everything recorded is the owner's report transcribed, and the verdict and the closure are theirs.
- Remaining: an iOS/Safari observation and the unstated fields are the smallest additions if the coverage gap is ever to be closed rather than accepted. `RRB-02` is now the largest unproven claim in the product — the archive and restore contract has only ever been executed against local Postgres as a superuser, never against a hosted project where restores actually break.
- Production/provider evidence: the owner exercised the deployed production origin on a real device. No provider write, deployment or production-data access was performed by me.
- Snapshot update: `CURRENT_PROJECT_MEMORY.md` is updated here as an explicit post-merge projection, together with the board baseline moving to `da872d92` (PR #494). The release-proof row changes because a gate genuinely closed, which is durable capability truth rather than bookkeeping.
- Superseded issue, roadmap or claim: supersedes every statement that RRB-08 is open, including the board's NOW entry and the previous release-readiness line. Does not supersede the remaining RRB gates, `#432` or `#426`.
