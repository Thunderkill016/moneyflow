# PR #408 — refactor: redesign mobile bottom navigation

- Date: 2026-08-16
- Change class: Class 2 bounded mobile shell/UI redesign; no financial, database, Auth, provider, deployment or production mutation.
- Affected capability or project boundary: five-slot mobile bottom navigation during RRB-08 physical-device follow-up.
- Status impact: candidate only. Owner real-phone review rejected the protruding center-action architecture that remained after #406; #407 now owns the whole bottom-nav redesign. RRB-08 remains open and final acceptance remains owner-observed on a physical phone.
- Changed: the mobile dock is now one five-column geometry with a shared 36px icon row and 14px label row for every slot. Active destinations use a subtle icon-container tint. `Ghi` is emphasized by a filled Fresh Blue icon container inside the same row. The previous `.mobileCapture::before`, vertical `translateY` lift and center-only protrusion model are removed. The 70px shell reserve, 10px inset, safe-area ownership, five-slot IA and route/action semantics stay unchanged.
- Research/design rationale: apply established mobile navigation principles already reviewed with the owner — stable primary destinations, visible labels, touch-safe targets and clear separation between navigation geometry and primary-action emphasis. MoneyFlow keeps its own Fresh Blue identity instead of copying a platform component verbatim.
- Verification contract: `mobile-nav-capture-spacing.test.ts` now requires five equal columns, one icon-row + label-row geometry, a shared 36px icon visual box, integrated `Ghi` emphasis, no center pseudo-FAB/vertical lift, coherent active-state hierarchy and unchanged reserve/safe-area ownership.
- Verified: the first draft-state CI selection run intentionally skipped expensive shards; PR #408 was then marked ready and a follow-up commit triggered fresh ready-state repository CI/browser/cross-device evidence on the full cumulative diff. No physical-phone PASS is claimed.
- Remaining: inspect ready-state CI artifacts at 320x568, 360x800 and 390x844 including dark mode; confirm no clipping/overlap/reserve drift; then owner repeats the real-phone bottom-nav observation before RRB-08 closeout.
- Production/provider evidence: not applicable; no provider or production read/write is claimed.
- Snapshot update: not applicable — the candidate Current Work Board records #407 execution authority, while `CURRENT_PROJECT_MEMORY.md` intentionally remains unchanged until merged work changes durable project truth.
- Superseded issue, roadmap or claim: #405/#406 remain provenance for the failed local-spacing approach; #407/#408 supersede further patching of the protruding center-action design.
