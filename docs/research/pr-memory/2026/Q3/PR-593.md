# PR #593 — Activity 2.0 unified maintenance workstream MVP

- Date: 2026-09-13
- Issue: #592
- Parent intent: #559
- Branch: `feat/592-activity-workstream`
- Change class: product UI / application read-workflow surface; no database migration.
- Affected capability or project boundary: daily transaction maintenance across posted Transactions, transaction Review state, pending Inbox candidates and truthful candidate source/provenance; mutation ownership remains `/transactions` and `/inbox`.
- Status impact: Activity R1 is additive and evaluating in draft PR #593; legacy routes remain available and primary navigation is not replaced.
- Changed: added typed Activity item model, additive `/activity` route/workspace, existing-read composition, candidate source/lifecycle presentation, knowledge-aware partial states, unit tests, demo browser coverage and strict authenticated phone/desktop coverage.
- Remaining: exact-head CI/CodeQL/Secret/browser/UI acceptance; only after first clean route proof may R1 add a discoverability affordance. R2 primary-nav promotion and R3 retirement/optimization require separate authorization.
- Production/provider evidence: none required or performed; no migration, provider configuration, production data write or deployment intervention is part of R1.
- Superseded issue, roadmap or claim: none. #559 remains parent program intent; #592 is the executable bounded slice. Activity does not supersede `/transactions` or `/inbox` in R1.
- Scope: additive Activity R1 only; no legacy route retirement, DB migration, provider write, merge or production change.
- Baseline: `main@77def2218dfb1a66134bbefe8158c993b960ebf6`.
- Decision: Activity is a read/workflow layer, not a new financial truth model. Posted transactions remain ledger facts; pending Inbox candidates remain pre-ledger evidence; `needs_review` is an attribute of a posted transaction rather than a separate Activity item.
- Data flow: compose existing `getFinanceWorkspace()` transaction/review read with the existing `loadInboxForClient()` candidate path, then build typed Activity items. Existing `/transactions` and `/inbox` remain mutation owners and rollback surfaces.
- Source-state boundary: candidate source/lifecycle/match evidence may be displayed when present; posted transactions do not fabricate provider/source state because current transaction feed does not prove it.
- Partial-failure boundary: candidate failure keeps healthy ledger rows visible; finance failure may still show pending candidate evidence but disables readiness inference and marks dependent counts unknown.
- Research: official Copilot Money review, YNAB approve/match, Actual Budget import and rules documentation support a single maintenance workstream while preserving explicit review, matching and user-owned processing semantics.
- Verified: pre-final source review plus successful no-new-debt classifier after scoped loading fix; exact-head CI/browser/build/security acceptance remains pending before owner handoff.
- Rollback: remove additive `/activity` route/model/presentation and any additive nav affordance; `/transactions`, `/inbox`, existing mutations and DB contracts remain unchanged.
