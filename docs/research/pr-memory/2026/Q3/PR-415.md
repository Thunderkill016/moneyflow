# PR #415 — perf: optimize canonical landing and dashboard load

- Date: 2026-08-17
- Change class: Class 1 measurement harness at the current head; expected Class 2 only if the final diff changes dashboard loading/presentation. No financial, schema, RLS, Auth/provider, deployment or production-data mutation is authorized.
- Affected capability or project boundary: canonical `/` public-entry performance and authenticated `/dashboard` load/interaction performance. The healthy request-private `get_dashboard_bundle` read path and financial semantics are outside the optimization boundary and must be preserved.
- Status impact: candidate performance work. #403 is promoted on the Current Work Board after #413/#414 completed; RRB-08 remains open and still requires owner physical-phone evidence independently.
- Changed: reconciled the board to merged Capture 4 and promoted #403; archived the completed Capture 4 packet; added a temporary authenticated production-build Lighthouse harness that measures canonical `/` without a session and `/dashboard` with only the synthetic loopback harness session, using one pinned mobile methodology.
- Verified: initial ready-state CI #2606 reached project-knowledge validation and correctly rejected the first candidate because PR #415 lacked its own memory record and the merged Capture 4 packet remained under `docs/plans/active/` after its registry entry was removed. The checker is not weakened; both lifecycle obligations are being corrected before baseline measurement. No Lighthouse baseline or application-performance improvement is claimed yet.
- Remaining: obtain the fresh current-main baseline for `/` and `/dashboard`; inspect bundle/critical-path evidence; implement only measured repository-controlled changes; repeat the same Lighthouse methodology; run load contracts and risk-selected exact-head browser/UI/provider checks; perform an independent evaluator pass; record honest before/after metrics and any remaining bottleneck.
- Production/provider evidence: not applicable for the current candidate. No provider write, production load test, production-data access or production deployment verification is part of this branch. Lighthouse uses the production Next build against loopback/synthetic harness state.
- Snapshot update: not applicable while Status impact remains candidate. The Current Work Board owns current execution; `CURRENT_PROJECT_MEMORY.md` should change only if merge alters durable capability/verification truth.
- Superseded issue, roadmap or claim: #413/#414 Capture 4 execution is completed and its active packet is archived; #403 is no longer held behind Capture work. Historical `/insights` performance scores remain provenance only and are not accepted as a current canonical baseline.
