# PR #422 — docs: prepare the four P1 release gates

- Date: 2026-08-18
- Change class: Class 0 — documentation only. No runtime, test, schema, RLS, Auth, provider, CI or product change.
- Affected capability or project boundary: release-readiness preparation for the P1 gates. Adds `docs/compliance/P1_GATE_PREPARATION.md`, a data inventory and provider read-back checklist derived from current migrations and runtime code.
- Status impact: candidate. **Closes no gate.** RRB-04, RRB-05, RRB-06 and RRB-09 all remain open, as does RRB-08. The owner asked for P1 to stop blocking; an agent cannot clear gates that need a lawyer, a mailbox owner and provider access, so the cost of clearing them was reduced instead. Deleting the gates was declined and the reasoning recorded: that would remove the record of the risk rather than the risk.
- Changed: added the dossier with the personal-data inventory across 20 tenant tables, the processor list (Supabase, Vercel, Vercel Analytics, Vercel Speed Insights, Cloudflare Turnstile), the privacy-protective measures already implemented, the data-subject-rights mechanisms, a single-session provider checklist for RRB-04/09, the RRB-05 questions, and an explicit list of what the repository cannot answer.
- Findings surfaced while gathering evidence, recorded rather than smoothed over. First, RRB-05 is not merely open: `src/components/privacy-policy-page.tsx` already publishes `support@moneyflow.app` and the live policy makes a commitment on it, while nobody has verified the operator controls that address — if not, the policy must change before beta. Second, erasure is complete but not for the reason the code claims: `purge_user_tenant_data` deletes 19 tables and its comment says it covers every tenant row, yet `archive_restore_batches` and `archive_restore_rows`, added later in `20260812000000_export_user_archive.sql`, are in neither the purge nor the deletion test; they are still removed because both cascade from `auth.users`, so the guarantee rests on the cascade and any flow that purged without deleting the Auth user would leave them behind. Third, `supabase/tests/database/tenant_isolation_and_deletion.test.sql` enumerates tables by hand rather than deriving them from the schema, so it cannot notice a new tenant table; five tables the purge does delete are absent from it. None of these is a live data leak; the second and third are claims that no longer match their code.
- Provider read-back actually performed, not merely planned: the Vercel half of RRB-09 was read from the connected account on 2026-08-18 and recorded in §3.0. Production project identity is now unambiguous — `moneyflow` / `prj_eAusnkm1X1HzAt4wMFbuMnRXela7` under team `team_1MZEcAVjG3nrOnklJxYIqGQs` with no SAML, latest production deployment `dpl_G1atNtCLqSTVkTqje222dZRK6Mnd` — which matters because two similarly named projects exist and that ambiguity is what RRB-09 was for. All deployment protection (password, SSO, trusted IPs) is disabled, so every preview deployment is world-readable; whether previews carry production Supabase credentials is now the sharpest open question, since if they do a public preview URL is a public front end onto real user data. No custom domain is attached, while the privacy policy publishes an address at `moneyflow.app`. Deployment region, production environment variables and whether Analytics/Speed Insights/Turnstile are enabled remain unverified because the connected tooling does not expose them, and the entire Supabase side remains unverified.
- The finding that changes the urgency, measured 2026-08-18: **MoneyFlow is already live, public and accepting registrations.** `https://mfvn.vercel.app` returns 200 with no password, SSO or IP protection; it runs authenticated mode rather than demo (`/login` 200, `/dashboard` 307 to `/login?next=%2Fdashboard`); **`/register` returns 200** and serves a working signup form; the live `/privacy` page publishes `support@moneyflow.app`; and the Vercel project was created 2026-07-13, so it has been publicly reachable for over a month. The project's documents describe the P1 gates as blocking controlled closed beta, which reads as though collection has not begun — it can begin from any browser today. Whether anyone has signed up was not investigated and should not be. RRB-06 is therefore not a pre-beta formality: a live service can collect Vietnamese personal financial data under a policy whose contact nobody has verified, in a region nobody had recorded until today. If the intent is that this is not yet open, the fastest correction is Vercel password protection or removing the public alias, which is a setting rather than a release process.
- Region answered: **`sin1`, Singapore**, read from the `x-vercel-id` header on all three hostnames. That is the cross-border fact the legal review depends on, and it means data does not stay in Vietnam.
- Correction to an earlier claim in this same branch: I raised public preview deployments carrying production credentials as the sharpest open question. That is **retracted** — all twenty most recent deployments carry `target: production`, no preview environment exists, and the three hostnames are aliases of one deployment, confirmed by identical build asset hashes served from each. Every merge to `main` deploys straight to production with no staging tier, which is itself worth the owner knowing.
- RRB-05 narrowed by public DNS read on 2026-08-18: `moneyflow.app` resolves, uses Google Cloud DNS nameservers and carries Google Workspace MX records, so a `support@moneyflow.app` mailbox is provisioned rather than imaginary. DNS cannot establish ownership and no inference about it is made here; the gate reduces to one question the operator answers in under a minute.
- Verified: `check:knowledge`, `test:ci-policy` and `lint` pass. Exact-head provider checks are authoritative. No browser, database or provider evidence applies to a documentation-only diff and none is claimed. The findings above were each read out of the migrations and specs cited in the dossier rather than inferred.
- Remaining: the gates themselves. Recommended order is recorded in the dossier — provider read-back (RRB-04/09) first, because production region and processor identity are inputs to the legal review, then RRB-05, then RRB-06. Booking the lawyer before the provider session wastes the most expensive hour. The two verification gaps in the deletion path are reported, not fixed, since changing purge coverage or its test is database work outside a documentation change.
- Production/provider evidence: not applicable, and the dossier states plainly that **no verified statement about production exists anywhere in this repository** — every current claim about it is inference from configuration files. The checklist records environment variables by name only, never values.
- Snapshot update: not applicable while Status impact remains candidate. On merge the durable change is preparation material, not capability; `CURRENT_PROJECT_MEMORY.md` should not be read as though any P1 gate moved.
- Superseded issue, roadmap or claim: supersedes nothing. It deliberately does **not** name the governing Vietnamese personal-data instrument or assert what it requires, because that judgement is the thing being bought from a qualified reviewer and asserting it here would be the overreach this project has spent the week correcting.
