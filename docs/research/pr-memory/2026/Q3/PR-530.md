# PR #530 — retire Markdown board authority

- Date: 2026-09-02
- Change class: Class 3 governance / CI-policy boundary
- Affected capability or project boundary: executable plan authority, lifecycle validation, agent task selection and project-knowledge routing
- Status impact: authority recovery — preserve merged PR #528 intent with #527 as the current executable slice while removing the duplicated Markdown board/SHA-baseline authority model
- Changed: `PLAN_AUTHORITY.json` schema v2 now owns master + zero/one current packet; resolver/doctor/lifecycle use manifest + merged first-parent history; the former Current Work Board is a retired compatibility pointer; guidance/tests/current memory are aligned
- Verified: implementation complete on branch; exact-head CI, CodeQL and secret scan pending after this record lands
- Remaining: repair only migration defects exposed by exact-head CI; no runtime #527 performance implementation belongs in this PR; owner merge is required before resuming #527 from fresh main
- Production/provider evidence: none required or claimed; repository governance only, no provider/deployment write and no production behavior change
- Snapshot update: `docs/research/CURRENT_PROJECT_MEMORY.md` updated to current #527 selection plus #529/#530 recovery truth
- Superseded issue, roadmap or claim: the Markdown Current Work Board, hand-maintained Current main baseline SHA and Post-merge projection marker are no longer executable authority; GitHub Issues/PRs track human work and `PLAN_AUTHORITY.json` selects executable work
- Lifecycle impact: authority transition — recover the already-owner-merged #528 selection by representing #527 as manifest current authority; does not complete #527 and does not select #523

## Root cause

PR #528 passed pull-request checks and was owner-merged, but push CI #3162 failed because the Markdown board still declared the pre-merge baseline `d6ff88b...`. The resolver therefore treated merged `main@dea07378...` as stale and returned no current slice. This PR removes the duplicate state rather than adding another reconciliation marker.

## Safety boundary

No application runtime, financial semantics, schema/RLS/Auth, provider configuration, production deployment or performance implementation changes. Rollback is one focused revert restoring the prior plan-authority implementation; partial rollback of only manifest or scripts is not safe.
