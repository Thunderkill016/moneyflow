# PR #698 — Disclose demo data is not carried into new accounts

- Date: 2026-09-23
- Change class: Class 1 (bounded copy/conditional-render on the auth surface + one conditional sentence on the export page; contract-test pins; no migration, schema, RPC or provider change)
- Affected capability or project boundary: `src/components/auth-form.tsx` demo notice now also renders on `mode="register"`; `src/app/(auth)/register/page.tsx` passes `demoMode={!isSupabaseConfigured()}` (same derivation as login); `src/components/export-settings-page.tsx` header description gains one `viewer.isDemo`-gated sentence; pinned by `src/lib/auth-copy.test.ts` and `src/lib/settings-trust-copy.test.ts`.
- Status impact: candidate → implemented for the Stage 1 disclosure slice only. The demo local ledger (`moneyflow-demo-transactions-v1`, Inbox candidates, etc.) is still never migrated into a new account — this PR only says so plainly before registration and points at the scoped export.
- Changed: register-mode demo notice tells the user demo data stays on this device, is not carried into the new account, and can be exported at Cài đặt → Xuất dữ liệu first, with the honest caveat that the file `chưa phải bản sao lưu đầy đủ` (transactions + Inbox only). Login notice copy unchanged. Export page tells demo viewers that creating an account does not move the device-local data.
- Verified: `npm run typecheck` clean; `npm run lint` 0 errors (1 pre-existing warning in untouched `quick-add-defaults.test.ts`); `npm test` 1773/1773 including 3 new auth-copy pins and 1 new settings-trust-copy pin; `check:knowledge`, `test:ci-policy` 191/191, `check:architecture`, `check:css-ownership` all pass. Browser smoke / visual audit delegated to CI lanes.
- Remaining: the consented demo→account import is a separate Class-3 packet and is intentionally unimplemented. A first-authenticated-landing note was evaluated and skipped — OnboardingFlow and the dashboard toast have no existing demo-ledger read, and `readStoredTransactions()` returns the seeded baseline when storage is empty, so detecting an orphaned ledger there needs new probing/state plumbing. Whether pure demo deployments should disable registration outright is a product decision left unchanged.
- Production/provider evidence: none (copy/conditional-render only).
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: if `/settings/export` is renamed or the export scope grows into a restorable backup, update the notice copy and both contract tests together.
