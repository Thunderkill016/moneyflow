# PR #459 — fix(import): guide retained Direct CSV failures to review

- Date: 2026-08-25
- Change class: Class 3 — a financial-domain recovery boundary with no financial mutation.
- Affected capability or project boundary: authenticated Direct CSV atomic approval failure recovery; existing Inbox and import-history navigation.
- Lifecycle impact: completes current slice #458 through the same-PR post-merge projection; no agent-executable product/governance slice is selected after an exact matching squash merge.
- Status impact: candidate only until merge. An explicit retained batch id adds a review-only user handoff; no retry, approval, ledger/source write, RPC/schema/RLS, provider or deployment behavior changes.
- Changed: adds a pure explicit-batch recovery descriptor and Direct CSV error actions for `/inbox` and `/imports`; generic errors remain message-only. Adds domain and UI source-contract regressions.
- Verified: red→green focused recovery test, focused 15-test unit/UI contract run, `lint -- --fix`, typecheck, knowledge/CI-policy/migration checks and diff hygiene passed. `NEXT_PUBLIC_APP_MODE=demo npm run verify:prepush` passed with 1,110 tests and production build. Chromium `/imports/direct` responsive and target-size checks passed at 320, 360, 390, tablet and desktop widths. Exact code head `de946d5dc8050fcefeb4cd4e6561e822560216bf` passed provider CI run `32766848106`: policy, static quality/typecheck, production build, unit/static RLS, browser smoke and cross-device UI audit; CodeQL and Gitleaks passed.
- Remaining: authenticated retained-batch browser failure is not locally fixtureable without backend access. Full local E2E has 120 pass plus four `auth-captcha` failures because demo omits a Turnstile site key. Full local UI audit has 502 pass, 141 skips and 55 failures: one unrelated 320px password-reveal flake and 54 WebKit `page.goto` internal errors before application assertions; provider Chromium/WebKit cross-device audit supplied the exact-head evidence. Review and merge remain owner decisions.
- Production/provider evidence: no provider read/write, deployment or production data action occurred.
- Merge evidence: pending owner decision after exact-head CI/review.
- Snapshot update: current board, archived packet and project memory carry only the PR #459 post-merge projection.
- Superseded issue, roadmap or claim: closes #458 if merged; does not supersede Direct CSV atomic server behavior.
- Rollback: revert PR #459; the existing error message and server-side retained evidence remain unchanged.
- Untrusted external instructions copied into project memory: none.
