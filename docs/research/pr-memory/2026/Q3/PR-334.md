# PR #334 — production auth and shared UI regression handoff

- Date: 2026-08-10
- Change class: Class 0 documentation/handoff only at this head
- Affected capability or project boundary: MoneyFlow Trust Secure acceptance, auth UX, onboarding, App Shell account menu, shared popup/form primitives and mobile UI regression investigation
- Status impact: candidate handoff only until product-code remediation is implemented and separately verified; current merged/runtime/provider truth remains `main`
- Changed: records production acceptance context, confirmed missing current CSS owners for onboarding and authenticated dropdown classes, password UX gaps, the unresolved transaction amount visibility defect, wider popup/mobile audit scope, acceptance criteria, stop conditions and an Opus handoff prompt
- Verified: reconciled against `main@ca23b1f74cf4b886072e81276f325d87672dcd08`, current auth actions/callback, onboarding source, App Shell/UserChip/DropdownMenu source, transaction dialog/TextField source, historical pre-retirement CSS and official Supabase PKCE/password documentation
- Remaining: Opus reproduces current failures, fixes only current owners, adds browser/regression evidence, then owner reviews; MoneyFlow Trust S2 remains paused until the UI regressions are repaired/deployed
- Production/provider evidence: read-only observations only; no provider/Auth/DB/config write and no account deletion by this PR
- Snapshot update: not applicable; the draft handoff PR is not merged/provider truth
- Superseded issue, roadmap or claim: none; this packet supplements the merged MoneyFlow Trust execution roadmap with regressions discovered while executing Secure acceptance

## Evidence boundary

At this head PR #334 contains documentation only. It does not claim the UI defects are fixed, does not claim S2/S3 recent-auth acceptance, and does not authorize provider writes or merge.

Two source/history findings are stronger than hypotheses:

1. current onboarding markup still emits `onboarding-*` globals while current `globals.css` no longer owns the onboarding family; historical pre-retirement CSS did;
2. current `DropdownMenu`/`UserChip` emits `ui-dropdown-*` / `profile-menu` globals while current CSS no longer owns those families; historical pre-retirement CSS did.

The transaction amount visibility issue remains a reproduction/debugging requirement rather than a guessed root cause.

## Safety

- no application/runtime code change at this head;
- no database/migration/RLS change;
- no Supabase Auth/config/Edge write;
- no production-data mutation;
- no destructive account deletion;
- no merge authorization;
- no reopening of the archived UI migration as a broad redesign.
