# PR #686 — Deleted-transactions browse + restore surface ("trash")

- Date: 2026-09-23
- Change class: Class 3 (financial/data — new tenant-scoped read view over the ledger plus demo persistence and restore mutations)
- Affected capability or project boundary: `supabase/migrations/20260923090000_deleted_transaction_feed.sql` (`public.deleted_transaction_feed` security-invoker view mirroring `transaction_feed` + `deleted_at`, partial index `(user_id, deleted_at desc) where deleted_at is not null`, authenticated-only SELECT), `src/server/finance.ts` (`getDeletedTransactions()` + `deletedFeedSchema`/`mapDeletedTransactionFeedRow` reusing `mapTransactionFeedRow`), `src/lib/deleted-transactions.ts` (demo tombstone store `moneyflow-demo-transactions-deleted-v1`), `src/hooks/use-transactions.ts` (demo delete/bulk-delete tombstone, demo restore releases), `src/lib/delete-account.ts` (key registered in `LOCAL_DATA_STORAGE_KEYS`), `src/app/transactions/trash/page.tsx` + `transactions-trash-page.tsx`/.module.css (read-only rows, `Đã xóa lúc …`, `Khôi phục`), quiet `Đã xóa` link in `transactions-workspace` heading actions, `refreshFinancePages` revalidates `/transactions/trash`.
- Status impact: candidate → implemented. Previously recovery ended with the 8-second undo toast; soft-deleted rows are now browsable and restorable until account deletion in both authenticated and demo modes.
- Changed: `transaction_feed` untouched (active-only); restore returns rows with entries reset to pending because deletion clears cleared-state; recurring and reconciled rows stay undeletable and are stated as absent from the trash.
- Verified: `typecheck` pass, `lint` pass (1 pre-existing warning in `quick-add-defaults.test.ts`), `npm test` 1691/1691 incl. new `src/lib/deleted-transactions.test.ts` (9 cases), `build` pass (`/transactions/trash` registered), `check:migrations` pass (66 pinned), `check:knowledge`, `test:ci-policy` (191), `check:architecture`, `check:css-ownership`, `git diff --check` clean. `test:db` not runnable locally (no Docker; `ECONNREFUSED 127.0.0.1:54322`) — `deleted_transaction_feed.test.sql` (`plan(36)`: tenant isolation, anon `42501`, active excluded, `deleted_at` populated, ordering, restore round-trip, `recurring_payment_locked`, `transaction_reconciled`, cleared→pending reset) runs in CI. Catalog tests updated: `schema_and_rls` plan 81, `browser_role_privileges` select-only list, `security_catalog` security-invoker count 6.
- Remaining: pgTAP CI result pending at open; view `ORDER BY deleted_at desc` is belt-and-suspenders over the query's explicit `.order()`; demo undo and trash-restore share `restoreTransaction` so undo also releases tombstones (intended).
- Production/provider evidence: none at open; migration applies on deploy, owner decision post-merge.
- Superseded issue, roadmap or claim: none.
- Lifecycle impact: if `transaction_feed`'s projection changes again, `deleted_transaction_feed` must be re-mirrored (same SELECT + `deleted_at`, flipped `deleted_at is not null` predicate); the work packet `docs/plans/active/trash-surface.md` records the contract.
