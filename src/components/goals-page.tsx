"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { adjustGoalAction, archiveGoalAction, saveGoalAction } from "@/app/actions/goals";
import { GoalAllocationDialog, GoalDialog } from "@/components/goal-dialogs";
import { Icon } from "@/components/icons";
import { UserChip, type ViewerSummary } from "@/components/user-chip";
import { dailyGoalSaving, goalProgress, goalTotals, type SaveGoalInput, type SavingsGoal } from "@/lib/goals";
import { formatMoney } from "@/lib/money";

const navItems = [{ label: "Tổng quan", icon: "home" as const, href: "/" }, { label: "Giao dịch", icon: "arrows" as const, href: "/transactions" }, { label: "Ngân sách", icon: "target" as const, href: "/budgets" }, { label: "Báo cáo", icon: "chart" as const, href: "/reports" }, { label: "Định kỳ", icon: "calendar" as const, href: "/commitments" }, { label: "Mục tiêu", icon: "flag" as const, href: "/goals" }, { label: "Tài khoản", icon: "wallet" as const, href: "/accounts" }];
function deadlineLabel(goal: SavingsGoal, today: string) { if (!goal.deadline) return "Không đặt thời hạn"; if (goalProgress(goal) === 100) return "Đã đạt mục tiêu"; const days = Math.ceil((Date.parse(`${goal.deadline}T00:00:00Z`) - Date.parse(`${today}T00:00:00Z`)) / 86_400_000); if (days < 0) return `Quá hạn ${Math.abs(days)} ngày`; if (days === 0) return "Hạn hôm nay"; return `Còn ${days} ngày`; }

export function GoalsPage({ viewer, initialGoals, today, dataError }: { viewer: ViewerSummary; initialGoals: SavingsGoal[]; today: string; dataError: string | null }) {
  const [goals, setGoals] = useState(initialGoals); const [editing, setEditing] = useState<SavingsGoal | null>(null); const [goalDialogOpen, setGoalDialogOpen] = useState(false); const [allocationGoal, setAllocationGoal] = useState<SavingsGoal | null>(null); const [allocationMode, setAllocationMode] = useState<"allocate" | "release">("allocate"); const [allocationOpen, setAllocationOpen] = useState(false); const [dialogVersion, setDialogVersion] = useState(0); const [notice, setNotice] = useState(""); const [busyId, setBusyId] = useState<string | null>(null); const [showArchived, setShowArchived] = useState(false);
  useEffect(() => { if (!notice) return; const timer = window.setTimeout(() => setNotice(""), 4200); return () => window.clearTimeout(timer); }, [notice]);
  const active = useMemo(() => goals.filter((goal) => !goal.isArchived), [goals]); const visible = showArchived ? goals.filter((goal) => goal.isArchived) : active; const totals = goalTotals(goals, today);
  function openGoal(goal: SavingsGoal | null) { setEditing(goal); setDialogVersion((value) => value + 1); setGoalDialogOpen(true); }
  function openAllocation(goal: SavingsGoal, mode: "allocate" | "release") { setAllocationGoal(goal); setAllocationMode(mode); setDialogVersion((value) => value + 1); setAllocationOpen(true); }
  async function save(input: SaveGoalInput) { if (viewer.isDemo) { const existing = goals.find((goal) => goal.id === input.id); const next: SavingsGoal = { ...input, id: existing?.id ?? crypto.randomUUID(), allocated: existing?.allocated ?? 0, isArchived: false }; setGoals((current) => existing ? current.map((goal) => goal.id === existing.id ? next : goal) : [...current, next]); setGoalDialogOpen(false); setNotice(existing ? "Đã cập nhật mục tiêu demo." : "Đã thêm mục tiêu demo."); return { ok: true }; } const result = await saveGoalAction(input); if (result.ok && result.goal) { setGoals((current) => current.some((goal) => goal.id === result.goal!.id) ? current.map((goal) => goal.id === result.goal!.id ? result.goal! : goal) : [...current, result.goal!]); setGoalDialogOpen(false); setNotice("Đã lưu mục tiêu."); } return result; }
  async function adjust(amount: number) { if (!allocationGoal) return { ok: false, message: "Không tìm thấy mục tiêu." }; const signedAmount = allocationMode === "allocate" ? amount : -amount; if (viewer.isDemo) { const nextAllocated = allocationGoal.allocated + signedAmount; setGoals((current) => current.map((goal) => goal.id === allocationGoal.id ? { ...goal, allocated: nextAllocated } : goal)); setAllocationOpen(false); setNotice(allocationMode === "allocate" ? `Đã dành ${formatMoney(amount)} cho mục tiêu.` : `Đã mở khóa ${formatMoney(amount)}.`); return { ok: true }; } const result = await adjustGoalAction(allocationGoal.id, signedAmount); if (result.ok && result.allocated !== undefined) { setGoals((current) => current.map((goal) => goal.id === allocationGoal.id ? { ...goal, allocated: result.allocated! } : goal)); setAllocationOpen(false); setNotice(allocationMode === "allocate" ? `Đã dành ${formatMoney(amount)} cho mục tiêu.` : `Đã mở khóa ${formatMoney(amount)}.`); } return result; }
  async function archive(goal: SavingsGoal) { if (!goal.isArchived && goal.allocated > 0) { setNotice("Hãy rút hết tiền đã dành trước khi lưu trữ mục tiêu."); return; } if (!window.confirm(`${goal.isArchived ? "Khôi phục" : "Lưu trữ"} “${goal.name}”?`)) return; setBusyId(goal.id); const result = viewer.isDemo ? { ok: true as const } : await archiveGoalAction(goal.id, !goal.isArchived); setBusyId(null); if (!result.ok) { setNotice(result.message); return; } setGoals((current) => current.map((item) => item.id === goal.id ? { ...item, isArchived: !item.isArchived } : item)); setNotice(goal.isArchived ? "Đã khôi phục mục tiêu." : "Đã lưu trữ mục tiêu."); }
  return <div className="app-shell"><aside className="sidebar" aria-label="Điều hướng chính"><Link className="brand" href="/"><span className="brand-mark"><span /></span><span>MoneyFlow</span></Link><nav>{navItems.map((item) => <Link href={item.href} className={item.href === "/goals" ? "active" : ""} key={item.label}><Icon name={item.icon} /><span>{item.label}</span></Link>)}</nav><div className="sidebar-bottom"><a href="#cai-dat"><Icon name="settings" /><span>Cài đặt</span></a><UserChip viewer={viewer} /></div></aside><div className="page-column"><header className="topbar"><Link className="mobile-brand brand" href="/"><span className="brand-mark"><span /></span><span>MoneyFlow</span></Link><div /><div className="topbar-actions"><button className="icon-button" aria-label="Thông báo"><Icon name="bell" /></button><button className="primary-button desktop-add" onClick={() => openGoal(null)} disabled={Boolean(dataError)}><Icon name="plus" />Thêm mục tiêu</button><span className="mobile-avatar avatar">M</span></div></header><main className="dashboard goals-workspace">{dataError && <div className="data-alert" role="alert"><Icon name="bell" /><span>{dataError}</span></div>}<section className="budgets-heading"><div><p className="eyebrow">Tiền cho tương lai</p><h1>Mục tiêu tiết kiệm</h1><p>Khóa một phần số dư cho điều quan trọng mà không làm sai tổng tài sản.</p></div><button className="primary-button" onClick={() => openGoal(null)} disabled={Boolean(dataError)}><Icon name="plus" />Thêm mục tiêu</button></section><section className="goal-hero"><div><span className="round-icon purple"><Icon name="lock" /></span><p>Đã dành riêng</p><strong>{formatMoney(totals.allocated)}</strong><small>Không tính vào tiền có thể chi</small></div><div><p>Đích của các mục tiêu</p><strong>{formatMoney(totals.target)}</strong></div><div><p>Nên dành mỗi ngày</p><strong>{formatMoney(totals.plannedDaily)}</strong><small>Dựa trên các mục tiêu có thời hạn</small></div></section><div className="commitment-list-heading"><div><h2>{showArchived ? "Mục tiêu đã lưu trữ" : "Mục tiêu đang theo đuổi"}</h2><p>Mỗi khoản được dành vẫn nằm trong tổng tài sản của bạn.</p></div><button onClick={() => setShowArchived((value) => !value)}>{showArchived ? "Xem đang hoạt động" : `Đã lưu trữ (${goals.filter((goal) => goal.isArchived).length})`}</button></div>{visible.length ? <section className="goal-card-grid">{visible.map((goal) => { const progress = goalProgress(goal); const daily = dailyGoalSaving(goal, today); return <article className={`goal-card${progress === 100 ? " achieved" : ""}`} key={goal.id}><div className="goal-card-top"><span className="round-icon purple"><Icon name={progress === 100 ? "check" : "flag"} /></span><div><h3>{goal.name}</h3><p>{deadlineLabel(goal, today)}</p></div><strong>{progress}%</strong></div><div className="goal-amount"><strong>{formatMoney(goal.allocated)}</strong><span>/ {formatMoney(goal.target)}</span></div><div className="budget-track" aria-label={`Đã hoàn thành ${progress} phần trăm`}><span style={{ width: `${progress}%` }} /></div><div className="goal-card-foot"><span>{daily > 0 ? `Nên dành ${formatMoney(daily)}/ngày` : progress === 100 ? "Đã đủ tiền mục tiêu" : "Tự do theo tiến độ của bạn"}</span><strong>Còn {formatMoney(Math.max(0, goal.target - goal.allocated))}</strong></div><div className="goal-actions">{!goal.isArchived && <><button className="goal-allocate" onClick={() => openAllocation(goal, "allocate")} disabled={progress === 100 || busyId === goal.id}><Icon name="lock" />Dành thêm</button><button onClick={() => openAllocation(goal, "release")} disabled={goal.allocated === 0 || busyId === goal.id}><Icon name="restore" />Rút ra</button><button onClick={() => openGoal(goal)} disabled={busyId === goal.id}><Icon name="edit" />Sửa</button></>}<button onClick={() => archive(goal)} disabled={busyId === goal.id}><Icon name={goal.isArchived ? "restore" : "archive"} />{goal.isArchived ? "Khôi phục" : "Lưu trữ"}</button></div></article>; })}</section> : <section className="budget-page-empty"><span><Icon name="flag" /></span><h2>{showArchived ? "Không có mục tiêu đã lưu trữ" : "Chưa có mục tiêu tiết kiệm"}</h2><p>{showArchived ? "Mục tiêu được lưu trữ sẽ xuất hiện tại đây." : "Bắt đầu với một mục tiêu đủ gần để bạn thấy tiến bộ mỗi tuần."}</p>{!showArchived && <button className="primary-button" onClick={() => openGoal(null)}><Icon name="plus" />Tạo mục tiêu đầu tiên</button>}</section>}</main></div><nav className="mobile-nav" aria-label="Điều hướng di động">{navItems.map((item) => <Link href={item.href} className={item.href === "/goals" ? "active" : ""} key={item.label}><Icon name={item.icon} /><span>{item.label}</span></Link>)}</nav><button className="mobile-fab" onClick={() => openGoal(null)} disabled={Boolean(dataError)} aria-label="Thêm mục tiêu"><Icon name="plus" /></button><GoalDialog key={`goal-${editing?.id ?? "new"}-${dialogVersion}`} open={goalDialogOpen} goal={editing} today={today} onClose={() => setGoalDialogOpen(false)} onSave={save} /><GoalAllocationDialog key={`allocation-${allocationGoal?.id ?? "none"}-${allocationMode}-${dialogVersion}`} open={allocationOpen} goal={allocationGoal} mode={allocationMode} onClose={() => setAllocationOpen(false)} onSubmit={adjust} /><div className={notice ? "toast visible" : "toast"} role="status" aria-live="polite"><span><Icon name="check" /></span>{notice}</div></div>;
}
